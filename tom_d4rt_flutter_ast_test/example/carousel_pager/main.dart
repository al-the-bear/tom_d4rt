// Entry point for the carousel_pager sample (example #23).
//
// The in-tester harness (`SourceFlutterD4rt.buildMultiFile`) calls a
// top-level `Widget build(BuildContext)` directly, so this file
// exposes that contract.
import 'package:flutter/material.dart';

import 'home.dart';

Widget build(BuildContext context) {
  return const CarouselPagerApp();
}
