# Only the forked native_semaphores remains as an override: the fork carries a
# fix not present in the hosted pub.dev release (0.0.5). All d4rt-family
# siblings (tom_d4rt etc.) now resolve from their hosted analyzer-10 releases;
# the analyzer-10-migration path overrides were dropped once those siblings
# published.
dependency_overrides:
  runtime_native_semaphores:
    git:
      url: https://github.com/al-the-bear/native_semaphores.git
      ref: fork-fix
