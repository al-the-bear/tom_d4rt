dependency_overrides:
  runtime_native_semaphores:
    git:
      url: https://github.com/al-the-bear/native_semaphores.git
      ref: fork-fix
  tom_d4rt:
    path: ../tom_d4rt
  # tom_d4rt_generator override removed for analyzer-10 migration (generator
  # stays on analyzer ^8.4.1; deferred to the cli_tools-quest migration).
