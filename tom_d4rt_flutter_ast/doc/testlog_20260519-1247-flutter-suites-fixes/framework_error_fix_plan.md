# Framework-error fix plan — `20260519-1247-flutter-suites-fixes`

Script-by-script plan to fix every Flutter framework-error banner
still emitted by the D4rt scripts in the Step-11 baseline. The
139 banners emitted per project (AST 139, test 139, 278 in total)
break down across **138 distinct scripts** because one script
(`rendering/render_constraints_transform_box_test.dart`) is
driven by two host suites.

The plan **rewrites the scripts** — per the user's directive,
banner-zero must be reached by changing the scripts themselves,
not by tightening the `SendTestRunner` banner filter or by
chasing additional bridge fixes.

| Field | Value |
| --- | --- |
| Baseline | `testlog_20260519-1247-flutter-suites-fixes/` (both projects) |
| Scripts root | `tom_d4rt_flutter_ast/test/tom_d4rt_flutter_ast_app/test/send_ast_via_http_scripts/` |
| Script reuse | Both projects share the same script tree via `SendTestRunner.scriptsPath` — fixing a script fixes both projects simultaneously. |
| Total banners (AST) | 139 |
| Total banners (test) | 139 |
| Distinct scripts | 138 |
| Items in this plan | 138 (numbered 1–138) |
| Driver for verification | `flutter test test/<suite>.dart` serial only (never parallel) |

## How to process this plan

1. Pick the next unchecked item (lowest number).
2. Open the referenced script under
   `tom_d4rt_flutter_ast/test/tom_d4rt_flutter_ast_app/test/send_ast_via_http_scripts/<path>`.
3. Apply the prescribed fix pattern (P1..P14) — concrete edits
   described below in the **Fix patterns** section.
4. Re-run the script in isolation through `SendTestRunner`
   (rule (a): script-only changes need only the script's host
   suite re-checked locally; rule (b): if you changed shared
   helpers, run essential + important + secondary + the host
   hr/secondary suite serially).
5. Verify the script's banner is gone from the host suite's
   `<suite>.log.txt` and that the suite still passes.
6. Tick the item.
7. Commit when a cluster of related fixes is complete (don't
   commit per-item; bundle 10–25 items per commit).

## Acceptance criteria for this fix plan

- **Banners go to zero** in both project log sets (the original
  Step-11 stretch goal that was deferred as "partial — would
  require significant test-script edits").
- **No regression** in pass/fail/error/skip counts (currently
  2199/2189/0/0/10 on both projects). The 10 `skip: true`
  entries are intentional and stay.

## Verification protocol per cluster

Use the cluster boundaries marked in the numbered list (every
~20 items) as commit boundaries. After applying a cluster of
script fixes:

```bash
# AST project
cd tom_d4rt_flutter_ast
flutter test test/essential_classes_test.dart \
  test/important_classes_test.dart \
  test/secondary_classes_test.dart   # essential+important+secondary sweep
# Optional: re-run only the affected hr/host suite as well

# Same for test project
cd ../tom_d4rt_flutter_test
flutter test test/essential_classes_test.dart \
  test/important_classes_test.dart \
  test/secondary_classes_test.dart
```

Never run multiple `flutter test` invocations concurrently
within the same package — shared HTTP-server corruption is
documented in the quest rules.

---

## Fix patterns (referenced by each numbered item)

These are the reusable recipes. Each numbered script item
points at one or more patterns plus any script-specific notes.

### P1 — Bound the test viewport (B-layout / BoxConstraints / infinite-size / NaN)

Most scripts construct a widget tree and ask the framework to
lay it out without any bounded ancestor. The fix is to wrap
the root widget in a finite-sized box.

```dart
// BEFORE
final widget = Column(children: [...]);

// AFTER
final widget = MediaQuery(
  data: const MediaQueryData(size: Size(800, 600)),
  child: Directionality(
    textDirection: TextDirection.ltr,
    child: Center(
      child: SizedBox(
        width: 800,
        height: 600,
        child: Column(children: [...]),
      ),
    ),
  ),
);
```

If the script already has a `MaterialApp`, place the
`SizedBox(width: 800, height: 600, child: ...)` as the `home`
or as the inner child of the `Scaffold.body`. The 800×600
extent matches Flutter's default test viewport.

### P2 — Bound a vertical RenderFlex (overflow on bottom)

```dart
// BEFORE
Column(children: [child1, child2, child3, ...])

// AFTER (when the test verifies layout / overflow tolerance):
SingleChildScrollView(
  scrollDirection: Axis.vertical,
  child: Column(children: [child1, child2, child3, ...]),
)

// OR (when one child is the "growable" one):
Column(children: [
  child1,
  Expanded(child: child2),  // makes child2 shrink to fit
  child3,
])
```

Combine with **P1** for the outer extent.

### P3 — Bound a horizontal RenderFlex (overflow on right)

Same as **P2** but `Axis.horizontal` / wrap the offending
child in `Flexible(...)` or `Expanded(...)` in the horizontal
axis.

### P4 — Bound a Stack ("A Stack requires bounded constraints")

```dart
// BEFORE
Stack(children: [...])

// AFTER
SizedBox(
  width: 400,
  height: 400,
  child: Stack(children: [...]),
)
```

Alternatively give the Stack `fit: StackFit.expand` inside a
bounded parent (P1).

### P5 — Avoid the non-uniform-Border bridge defect (B-bridge BorderRadius)

The bridge throws when a `Border` with side-specific colors
is combined with a `borderRadius`. Two acceptable rewrites,
pick whichever preserves test intent:

```dart
// (a) Use a uniform Border for the visual rendering case:
Container(
  decoration: BoxDecoration(
    border: Border.all(color: Colors.black, width: 1),
    borderRadius: BorderRadius.circular(4),
  ),
)

// (b) When the test specifically verifies the assertion, wrap
//     in try/catch and assert the framework error inside the
//     script — turning the framework banner into a captured,
//     expected error:
try {
  paintBorder(canvas, rect,
      border: Border(top: BorderSide(color: Colors.red),
                     bottom: BorderSide(color: Colors.green)),
      borderRadius: BorderRadius.circular(4));
  testFailed = true;  // we expected a throw
} on FlutterError catch (e) {
  expect(e.message, contains('uniform colors'));
}
```

Always prefer (a) when the test isn't specifically about the
mixed-color assertion.

### P6 — Correctly typed `Gradient.linear` constructor args (B-bridge gradient)

```dart
// BEFORE — wrong-typed args trip the bridged constructor:
Gradient.linear([0.0, 0.0], [1.0, 1.0], [0xFFFF0000, 0xFF00FF00]);

// AFTER — Offset and List<Color>:
Gradient.linear(
  const Offset(0, 0),
  const Offset(1, 1),
  const [Color(0xFFFF0000), Color(0xFF00FF00)],
);
```

If the test needs stops, pass `List<double>` of the right
length (same count as colors).

### P7 — Null-guard before invoking method on possibly-null target (I-unhandled)

```dart
// BEFORE
target.getChildren();

// AFTER
if (target != null) {
  target.getChildren();
}
// OR
target?.getChildren();
```

For the absorbed-extent injector case (`SliverOverlapInjector
has found no absorbed extent to inject`), make sure the test
constructs the absorber **above** the injector in the slivers
list and threads the same `SliverOverlapAbsorberHandle`
through both.

### P8 — Normalize / clamp BoxConstraints (B-layout — non-normalized)

```dart
// BEFORE
BoxConstraints(minWidth: 700, maxWidth: 350)  // NOT NORMALIZED

// AFTER
BoxConstraints(minWidth: 350, maxWidth: 700)
```

If the test specifically tries to feed non-normalized
constraints (to verify error handling), wrap the call in
`try/catch` (P5 style) and capture the assertion.

### P9 — Set required textBaseline

```dart
// BEFORE
Row(crossAxisAlignment: CrossAxisAlignment.baseline, children: [...])

// AFTER
Row(
  crossAxisAlignment: CrossAxisAlignment.baseline,
  textBaseline: TextBaseline.alphabetic,
  children: [...],
)
```

### P10 — Bound a viewport (GridView / ListView / Scrollable)

```dart
// BEFORE
GridView.count(crossAxisCount: 2, children: [...])

// AFTER — give the viewport a bounded height (vertical case):
SizedBox(
  height: 400,
  child: GridView.count(crossAxisCount: 2, children: [...]),
)
```

Same pattern for `ListView`, `CustomScrollView`, etc. Combine
with **P1** if the script root is also unbounded.

### P11 — Correct ParentDataWidget placement

`Positioned` must be a direct child of `Stack`. `Flexible` /
`Expanded` must be a direct child of `Row` / `Column` /
`Flex`. Move the widget under the right parent or remove the
ParentData wrapper if not needed.

### P12 — RenderFlex with flex children but unbounded constraints

```dart
// BEFORE — Column inside another unbounded scrollable:
Column(children: [Expanded(child: ...)])

// AFTER — either drop the Expanded:
Column(children: [SizedBox(height: 200, child: ...)])
// or wrap the outer Flex in a bounded box (P1).
```

### P13 — Fix non-normalized custom constraints

See **P8**. If the test deliberately exercises invalid
constraints, capture the assertion (P5 style).

### P14 — Bounded `Table` arguments / `table_border` assertion

When the script builds a `Table`, ensure `defaultColumnWidth`,
`columnWidths`, and (if used) `border: TableBorder.all(...)`
have valid parameters. The `table_border.dart` assertion
typically fires on a missing `width` or invalid `borderSide`.

---

## Numbered fix list (138 items)

Format per item:

> `N. <path>` *(host suite[s], banners AST/test, tag)* — Pattern
> reference + one-line note.

Where two patterns apply, both are listed (e.g. `P1+P2`).

### Cluster A — `animation/`, `cupertino/`, `dart_ui/` (items 1–10)

1. ~~`animation/cubic_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — "BoxConstraints forces an infinite height.")* — **deferred to U14** (2026-05-19). Four script-level workarounds attempted (P1 `SizedBox(800)`, `Center(heightFactor:1.0)`, `Row > Flexible > Column` sidestep, `Expanded → SizedBox(60)` inside the two `GridView.count` cells) — all reverted because the banner persists in every variant. The assertion fires on a synthetic `RenderConstrainedBox` inside a Material widget the script does not own, so no script-level rewrite is possible. Banner is non-fatal (`status=success, frameworkErrors=1`; test passes throughout). See `tom_d4rt_flutter_ast/doc/interpreter_unfixable.md` § U14 for the full investigation. **No script change committed.**

2. ~~`cupertino/cupertino_nav_segmented_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 2.0 px right)* — **deferred to U15** (2026-05-19). Four script-level workarounds attempted (P3: three independent `Row → Wrap` conversions on the hero chips Row in `_buildHero`, the boxed-default label Row in `_buildBoxedDefault`, and the sliding-default label Row in `_buildSlidingDefault`; plus P1: shrinking `CupertinoNavigationBar.middle`'s `SizedBox(width: 220.0) → 180.0`) — all reverted because the banner persists at 2 in every variant. The two `RenderFlex` overflows fire on internal `Row`s synthesised by a bridged Cupertino widget the script does not own (CupertinoNavigationBar internals, sliding-segmented-control thumb track, or CupertinoButton content row), so no script-level rewrite is possible. Banner is non-fatal (`status=success, frameworkErrors=2`; both tests "All tests passed!" throughout). See `tom_d4rt_flutter_ast/doc/interpreter_unfixable.md` § U15 for the full investigation. **No script change committed.**

3. ~~`cupertino/cupertino_page_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-19, P1). The "infinite height" assertion fired on `_anatomyDiagram`: a top-level `Row(crossAxisAlignment: CrossAxisAlignment.stretch)` whose first child is a `SizedBox(width: 110, child: Column(... Expanded(child: _anatomyBox('B')) ...))`. Under the outer `SingleChildScrollView`, the Row received an unbounded `maxHeight` and propagated it to the inner `Expanded`'s `RenderConstrainedBox`. Fix: wrapped `_anatomyDiagram`'s root `Container` in `SizedBox(height: 200)` so the stretch row has a finite vertical extent to distribute. Verified: `secondary_classes_test` with `--plain-name 'cupertino_page_test.dart'` reports `frameworkErrors=0 status=success` (was 1). Test passes; rule (a) — script-only change, single-script retest sufficient.

4. ~~`cupertino/cupertino_scroll_behavior_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 4.0 px bottom)* — **FIXED** (2026-05-19, P2). The two identical 4.0 px bottom overflows came from `_AnatomyArrowDown` (used twice in `_ClassAnatomySection` at lines 319 and 331, both with `height: 30`). The widget is `SizedBox(height: 30) > Center > Column[Container(width:2, height:height-10=20), Icon(CupertinoIcons.chevron_down, size:14)]` — the Column lays out to 20 + 14 = 34 px inside a 30 px SizedBox → 4.0 px overflow, hit twice → 2 banners. Fix: at the widget level, reduced the connector line in `_AnatomyArrowDown` from `height - 10` to `height - 16` so the Column (`(height-16) + 14 = height - 2`) fits within `SizedBox(height: height)` with slack. Verified: `secondary_classes_test` with `--plain-name 'cupertino_scroll_behavior_test.dart'` reports `frameworkErrors=0 status=success` (was 2). Test passes; rule (a) — script-only change, single-script retest sufficient. Log: `ztmp/fix_plan_step4/ast_cupertino_scroll_behavior.log.txt`.

5. ~~`cupertino/restorable_cupertino_tab_controller_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-layout/RenderFlex — "Offset argument contained a NaN value.")* — **FIXED** (2026-05-19, script-side workaround for U16). The original P1+P2 hypothesis ("unbounded `TabBarView`") was incorrect — the script contains no `TabBarView` widget at all. Bisection through the 11-section deep-demo page (top-half / bottom-half / single-section / single-widget) localised the offender to the `_CodeBlock` widget inside `_buildCodeSnippetSection`. Further bisection to a minimal `Column[Text(lines[i].text)]` body confirmed the trigger: **`Text('')` (empty-string `Text` widget) triggers a NaN `Offset` assertion in `dart:ui/painting.dart` line 41 through the bridged Flutter paragraph painter**. The source listing fed to `_CodeBlock` contained six `_CodeLine(0, '')` entries representing visually-blank lines; each rendered as an empty `Text`. Fix: in `_CodeBlock.build`, guard the composed `Text` argument with `composed.isEmpty ? ' ' : composed` so the painter always receives at least one glyph run. The visual result is identical (a blank line in a monospaced code block, padded by the surrounding `Padding(vertical: 1.0)`). Verified single-script retest: `frameworkErrors=0 status=success` (was 1). Underlying bridge bug documented as **U16** in `interpreter_unfixable.md`. Log: `ztmp/fix_plan_step5/ast_restorable_tab_controller_after.log.txt`. Rule (a) — script-only change, single-script retest sufficient.

6. ~~`cupertino/route_test.dart`~~ *(essential_classes_test, 1/1, B-bridge — "A borderRadius can only be given on borders with uniform colors.")* — **FIXED** (2026-05-19, P5(a)). Six `BoxDecoration` sites combined a non-uniform `Border` (either `Border(left: ...)` for an accent left-stripe, or four explicit sides with differing colours / widths) with a `borderRadius` — Flutter's `BoxDecoration._debugAssertValid` rejects the combination with `A borderRadius can only be given on borders with uniform colors.` The script renders each site multiple times in a deck/grid, so the 6 site declarations produced 9 framework-error banners. Fix: at every offending site replaced the non-uniform `Border` with a uniform `Border.all(color: <accent>, width: <fitted>)` — the accent-colour visual association is preserved, rounded corners are preserved, and the per-side stripe / thicker-top effect is folded into a uniform thicker outline (with width doubled on `fullscreen` rows in `_routeKindTile` so the visual emphasis still carries). Sites fixed: lines 89, 158, 579, 868, 1457, 1761. Verified single-script retest: `frameworkErrors=0 status=success` (was 9). Log: `ztmp/fix_plan_step6/ast_route_after.log.txt`. Rule (a) — script-only change, single-script retest sufficient.

7. ~~`dart_ui/shader_mask_engine_layer_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-bridge — "Runtime Error: Native error during bridged constructor 'linear'")* — **FIXED** (2026-05-19, script-side, P6 corrected). The original P6 hypothesis ("pass `Offset` not `List<double>`") was incorrect — every call site already passed proper `Offset` arguments. The real diagnostic from the captured banners was: `Gradient.linear|radial|sweep requires colors.length == 2 when colorStops is null (got colors.length=N)`. Native Flutter's `ui.Gradient.linear/radial/sweep` requires `colorStops` to be supplied whenever `colors.length != 2`; the bridged constructors enforce the same precondition. Four call sites in the script passed `null` as the stops argument together with multi-colour palettes (rainbow×7, sunset+endpoints×6, rainbow-loop×8, oceanic×4), producing 36 framework-error banners (each call site is repeated per BlendMode tile in the deck). Fix: at each site replace `null` with `List<double>.generate(colors.length, (i) => i / (colors.length - 1))` — evenly-spaced stops covering `[0.0, 1.0]`, which reproduces the implicit even spacing native Flutter would have applied for two-colour gradients. Sites fixed: lines 346 (rainbow linear), 393 (sunset radial), 440 (rainbow sweep), 997 (palette linear in `_blendModeShowcase`). Verified single-script retest: `frameworkErrors=0 status=success` (was 36). Log: `ztmp/fix_plan_step7/ast_shader_mask_after.log.txt`. Rule (a) — script-only change, single-script retest sufficient.

8. ~~`dart_ui/uniform_float_slot_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-layout/RenderFlex — overflow 2491 px bottom)* — **FIXED** (2026-05-19, P1+P2). The script renders nine deep-demo sections in a top-level `Column` that totalled ~3091 logical pixels on the 800×600 test viewport (600 + 2491 overflow). Fix: wrap the section `Column` in a `SingleChildScrollView` so the full content lays out within a bounded, scrollable child — overflow goes away and the visual top-of-page is unchanged. Single site (the top-level `build()` return). Verified single-script retest: `frameworkErrors=0 status=success` (was 1). Log: `ztmp/fix_plan_step8/ast_uniform_float_after.log.txt`. Rule (a) — script-only change, single-script retest sufficient.

9. ~~`dart_ui/uniform_vec2_slot_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-layout — "Offset argument contained a NaN value.")* — **FIXED** (2026-05-19, script-side workaround for U16). The original P1 hypothesis ("bound the viewport") was incorrect — the script already wraps its content in a `SingleChildScrollView`, so layout is bounded. The actual trigger is **U16 again**: Section 7 (`_buildSection7FragCodePanel`) renders a simulated `.frag` listing as a sequence of `(text, tag)` token spans. Three lines representing visually-blank rows used `<String>['', 'plain']` — i.e. `Text('')` once mapped through `_buildFragLine`. Empty `Text` trips `dart:ui/painting.dart:41` NaN-Offset assertion in the bridged paragraph painter. Fix: in `_buildFragLine`, guard the span text with `rawText.isEmpty ? ' ' : rawText` so the painter always receives at least one glyph run. Visual result is identical — three blank rows in the monospaced code panel, padded by the surrounding `Padding(vertical: 1.0)`. Verified single-script retest: `frameworkErrors=0 status=success` (was 1). Log: `ztmp/fix_plan_step9/ast_uniform_vec2_after.log.txt`. Underlying bridge bug already documented as **U16** in `interpreter_unfixable.md`. Rule (a) — script-only change, single-script retest sufficient.

10. ~~`dart_ui/uniform_vec3_slot_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-19, script-side, P1 refined). The script is already wrapped in `Scaffold > SafeArea > SingleChildScrollView` (vertical), so the demo viewport is bounded horizontally but unbounded vertically — that's the standard scroll pattern. The actual trigger is **`Row(crossAxisAlignment: CrossAxisAlignment.stretch)`** used inside that unbounded-height scrollable: the Row inherits `maxHeight=infinity`, then `stretch` forces each child to size to that infinite height → `RenderConstrainedBox` raises "BoxConstraints forces an infinite height." Bisection localised the failure to `VUseCaseGrid` (section 05); grep then surfaced the same pattern in `VCaveatsGrid` (section 09). Fix: wrap every stretch-Row in an `IntrinsicHeight` so the row first resolves the tallest child, then stretches the remaining children to match — preserves the equal-height card layout exactly. Four Rows fixed (two in `VUseCaseGrid`, two in `VCaveatsGrid`). The root `VGuideRoot` Column also uses `CrossAxisAlignment.stretch` but cross-axis there is *width*, which is bounded by the scroll viewport — that one is intentional and was left alone. Verified single-script retest: `frameworkErrors=0 status=success` (was 1). Log: `ztmp/fix_plan_step10/ast_uniform_vec3_after.log.txt`. Rule (a) — script-only change, single-script retest sufficient.

### Cluster B — `foundation/` (items 11–20)

11. ~~`foundation/abstract_node_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** Same pattern as item 10: the script already had `Scaffold > SingleChildScrollView > Column(crossAxisAlignment: stretch)`, so P1 (bound viewport) was not the right fix. The actual trigger was a single `Row(crossAxisAlignment: CrossAxisAlignment.stretch)` at the `_PitfallRow` builder (line 2150) inside the unbounded-height scrollable — `Expanded` + stretch propagated infinite height to the `_CodeBlock` children. Fix: wrapped that one Row in `IntrinsicHeight` so it sizes to its tallest intrinsic child. The eight other `CrossAxisAlignment.stretch` usages are all on `Column`s (horizontal stretch — harmless). Verified `frameworkErrors=1→0`.

12. ~~`foundation/caching_iterable_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.~~ **FIXED.** All three banners came from one `_detailBlock(...)` helper called three times, which combined `Border(left: width 4, top/right/bottom: alpha 0.18) + borderRadius` (the bridge's non-uniform-Border defect). Replaced the structure with a `ClipRRect > IntrinsicHeight > Row` where the left accent is a 4-px sibling `Container` and the content card now uses uniform `Border.all(alpha: 0.18)` — preserves the visual accent stripe and the rounded corners. Verified `frameworkErrors=3→0`.

13. ~~`foundation/diagnosticable_node_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.~~ **FIXED.** All nine banners came from the `_LevelRow` helper instantiated nine times — same `Border(left: width 6, top/right/bottom: alpha 0.18) + borderRadius` pattern as item 12. Re-expressed the left-accent stripe as a sibling 6-px `Container` inside `ClipRRect > IntrinsicHeight > Row`; the content card uses uniform `Border.all`. Verified the other single-side `Border(left: ...)` in `_FooterRef` (line 2302) does NOT trip the bridge (defaulted `BorderSide.none` is treated as uniform). Verified `frameworkErrors=9→0`.

14. ~~`foundation/diagnosticable_tree_node_test.dart` *(hardly_relevant_classes_1_test, 1/1, I-unhandled — "Cannot invoke method 'getChildren' on null.")* — **P7**. Null-guard the recursive `getChildren()` call (the root has no parent in the test tree).~~ **FIXED.** The two errors ("getChildren on null" + "name on null") originated in `_NodeCard.build` / `_PropPill.build` / `_flattenJson`, which all assumed `DiagnosticsNode` parameters were non-null. The bridged `DiagnosticsNode.getChildren()` / `.getProperties()` calls (and even the root `toDiagnosticsNode(...)`) can surface `null` values in the interpreter despite the non-nullable static type. Fix: (i) re-typed each `node`/`n` parameter as `DiagnosticsNode?`, (ii) early-return `SizedBox.shrink()` / a placeholder JSON line on `null`, (iii) filtered nulls out of `getChildren()` / `getProperties()` results. Verified `frameworkErrors=2→0`.

15. ~~`foundation/diagnosticable_tree_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** Same pattern as items 10/11/15: script root was already `Scaffold > SingleChildScrollView > Column(stretch)`, so P1 wasn't relevant. Two `Row(crossAxisAlignment: stretch)` sites inside the unbounded scrollable (DevTools tree+details panes at line 2044, pitfall Avoid/Prefer at line 3237) propagated infinite height to their Expanded children. Fix: wrapped both Rows in `IntrinsicHeight`. The third stretch alignment at the top-level Column is harmless (horizontal stretch). Verified `frameworkErrors=1→0`.

16. ~~`foundation/error_spacer_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/infinite-size — Offset NaN)* — **P1**.~~ **FIXED.** P1 was misdiagnosed — root layout is already `Scaffold > SingleChildScrollView > Column(stretch)`. Actual cause is U16 (empty-string Text triggers NaN-Offset in bridged painter): `_propertyChip('name', '${spacer.name}', ...)` renders `Text('')` because `ErrorSpacer.name == ''` by design. Fix: defensively substitute empty `name`/`value` strings with a single space inside `_propertyChip`. Verified `frameworkErrors=1→0`.

17. ~~`foundation/object_event_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** P1 was misdiagnosed — root is already `Scaffold > SingleChildScrollView > Column(stretch)`. Real cause: five `Row(crossAxisAlignment: stretch)` sites inside the unbounded scrollable propagated infinite height to their `Expanded` children — `_buildFieldAnatomy` (line 962), two inspector mapping cards (lines 1281, 1320), the recipe matrix pair-loop (line 1806), and the pitfall pair-loop (line 2143). Fix: wrapped each `Row` in `IntrinsicHeight` so the stretch resolves to the tallest intrinsic child. Verified `frameworkErrors=1→0`.

18. ~~`foundation/observer_list_test.dart` *(secondary_classes_test, 1/1, B-layout — `SemanticsNode#... invisible`)* — **P1**. Bound the rendered surface; the zero-rect semantics node is a side-effect of an empty viewport.~~ **FIXED.** The script had two nested `SingleChildScrollView`s on the same (vertical) axis: an outer one in `Scaffold.body`, and an inner one inside `_PrivatePage`. The outer scroll-view gave its child unbounded height; the inner scroll-view then had no bounded vertical space to size against, so the page collapsed to a zero-area surface and Flutter raised `SemanticsNode#... invisible Rect.fromLTRB(0,0,0,0)`. Fix: dropped the outer `SingleChildScrollView` in `Scaffold.body` (the inner one in `_PrivatePage` already provides the scroll). Verified `frameworkErrors=1→0`.

19. ~~`foundation/string_property_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/infinite-size — Offset NaN)* — **P1**.~~ **FIXED.** P1 was misdiagnosed — root is already `Scaffold > SafeArea > SingleChildScrollView > Column(stretch)`. Actual cause is U16: the inspector pane (`_buildInspectorRow`) and tree mock (`_buildTreeRow`) render `Text(_escape(_safeToString(property)))` and `Text(_escape(_safeValueToString(property)))` from `StringProperty` cases that include `defaultValue` matches (e.g. `StringProperty('mode', 'auto', defaultValue: 'auto')`). For such properties the bridged `toString()` / `valueToString()` collapse to `''`, and `Text('')` trips the NaN-Offset assertion at `dart:ui/painting.dart:41`. Fix: added a `_nonEmpty(...)` helper that substitutes a single space for empty strings and wrapped the three dynamic Text inputs (tree row + the two inspector-row cells). Verified `frameworkErrors=1→0`.

20. ~~`foundation/unicode_test.dart` *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** P1 was misdiagnosed — root is already `Scaffold > SingleChildScrollView > Column(stretch)`. Actual cause: two `Row(crossAxisAlignment: stretch)` sites inside the unbounded scrollable — `_beforeAfter` (per-constant card before/after panels) and `_scenarioRow` (scenarios section before/after panels) — propagated infinite height to their `Expanded` children. Fix: wrapped both Rows in `IntrinsicHeight` so each row sizes to its tallest intrinsic child. Verified `frameworkErrors=1→0`.

### Cluster C — `gestures/` (items 21–34)

21. ~~`gestures/hit_testable_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.~~ **FIXED.** Canonical P5(a) pattern: `_zStackEntry` (called 4× in the z-stack panel) used `Border(left: 4, top/right/bottom: 1) + borderRadius: 8`, which the bridged painter rejects as non-uniform with a rounded radius. Fix: render the card as `ClipRRect > IntrinsicHeight > Row` with the left accent as a sibling `Container(width: 4)` and a uniform `Border.all` on the body Container. Verified `frameworkErrors=4→0`.

22. ~~`gestures/horizontal_multi_drag_gesture_recognizer_test.dart` *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** P1 was misdiagnosed — root is already `Scaffold > SafeArea > SingleChildScrollView > Column(stretch)`. Actual cause: the `cmpRow` helper inside `_buildComparisonTable` returned `Row(crossAxisAlignment: stretch)` directly, propagating infinite height to its `Expanded` cell children inside the unbounded scrollable. Fix: wrapped that single Row in `IntrinsicHeight`. Verified `frameworkErrors=1→0`.

23. ~~`gestures/one_sequence_gesture_recognizer_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.~~ **FIXED.** Canonical P5(a) pattern: the pitfall-cards loop (6 entries) built each card with `Border(left: 4 solid, top/right/bottom: 1 translucent) + borderRadius: 10`, which the bridged painter rejects on a rounded shape. Fix: render each card via `ClipRRect > IntrinsicHeight > Row` with left accent as a sibling `Container(width: 4)` and a uniform `Border.all(color: color.withValues(alpha: 0.3))` on the body. Verified `frameworkErrors=6→0`.

24. ~~`gestures/pointer_exit_event_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/RenderFlex — overflow 4707 px bottom)* — **P1+P2**.~~ **FIXED.** The root was a bare `Container > Column` with 10 demo sections — no `Scaffold`, no `SingleChildScrollView` — so the column overflowed the viewport by 4707 px on the bottom. Fix: wrapped the root in `Scaffold > SafeArea > SingleChildScrollView` so the column scrolls in an unbounded vertical viewport. Verified `frameworkErrors=1→0`.

25. ~~`gestures/pointer_move_event_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** Two cooperating causes: (1) root was a bare `Container > Column` of 13 demo sections — no `Scaffold`, no `SingleChildScrollView`; (2) the `_buildFieldGrid` helper built pair rows with `Row(crossAxisAlignment: stretch)` containing `Expanded(_fieldCard(...))`. Fix: wrapped the root in `Scaffold > SafeArea > SingleChildScrollView` and wrapped the field-grid pair-Row in `IntrinsicHeight`. Verified `frameworkErrors=1→0`.

26. ~~`gestures/pointer_pan_zoom_start_event_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** Two cooperating causes. (1) Five `Row(crossAxisAlignment: stretch)` sites — pair-rows in `FieldGrid`/`ReadoutGrid`/`UseCaseGrid`, the columns row in `CompareTable`, and the sidebar+content row in `TakeawayFooter` — sat inside the unbounded `SingleChildScrollView` and received infinite cross-axis constraints; wrapped each in `IntrinsicHeight`. (2) After the layout fix, 5 `B-bridge — uniform-colors` errors surfaced from `CaveatRow`, which built each of 5 caveat rows with `Border(left: 4 solid, top/right/bottom: 1 thin) + borderRadius: 14`; replaced with canonical P5(a) pattern (`ClipRRect > IntrinsicHeight > Row` with left accent as a sibling `Container(width: 4)` and uniform `Border.all` on the body). Verified `frameworkErrors=5→0`.

27. ~~`gestures/pointer_scroll_event_test.dart` *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** Four `Row(crossAxisAlignment: stretch)` sites — the `_FieldCard` pair grid (section 03), the magnitude-matrix row builder (section 05), the `_EdgeCaseCard` pair grid (section 09), and `_ComparisonTable` row cells (section 07) — sat inside the unbounded `SingleChildScrollView` root and received infinite cross-axis constraints. Wrapped each in `IntrinsicHeight`. Verified `frameworkErrors=1→0`.

28. ~~`gestures/positioned_gesture_details_test.dart` *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.~~ **FIXED.** Two cooperating causes. (1) Four `Row(crossAxisAlignment: stretch)` sites — the 4×3 details-card gallery, the trajectory pair-cards grid, the velocity codeBlock + sample-card row, and the recipe GestureDetector sandbox + codeBlock row — sat inside the unbounded `SingleChildScrollView` root and received infinite cross-axis constraints; wrapped each in `IntrinsicHeight`. (2) After the layout fix, a `RenderFlex overflowed by 13 pixels on the right` surfaced from `kindChip` (section 4 / PointerDeviceKind panel): the chip's inner `Row(Icon, gap, Text('PointerDeviceKind.${label}'))` placed an unflexed monospace 12pt Text in a 220-px chip whose longest label `PointerDeviceKind.invertedStylus` (32 chars) overflowed the 200-px inner card width. Wrapped the Text in an `Expanded` so it soft-wraps to the available width. Verified `frameworkErrors=1→0`.

29. ~~`gestures/serial_tap_down_details_test.dart` *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 3195 px bottom)* — **P1+P2**.~~ **FIXED.** Root was a bare `Container > Column(mainAxisSize: min, stretch)` of eight demo sections — no `Scaffold`, no `SingleChildScrollView`. Inside the test app's finite vertical container the unbounded Column overflowed the bottom by 3195 px. Wrapped the root in `Scaffold > SafeArea > SingleChildScrollView` (canonical P1 pattern). No stretch-Rows inside the scrollable, so no follow-up P1 inner wrap was needed. Verified `frameworkErrors=1→0`.

30. ~~`gestures/serial_tap_up_details_test.dart` *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.~~ **FIXED.** The `editorMockRows` loop (count=1..5 cards) built each card with a non-uniform `Border(left: color/4, top/right/bottom: slateSoft/1)` combined with `borderRadius: 8` — Flutter's "borderRadius can only be given on borders with uniform colors" assertion fired once per card for 5 framework errors. Refactored to canonical P5(a): uniform `Border.all(slateSoft, 1)` + `clipBehavior: Clip.antiAlias` on the outer Container, with the coloured left accent supplied as a sibling `Container(width: 4)` inside an `IntrinsicHeight > Row`. Verified `frameworkErrors=5→0`.

31. ~~`gestures/tap_drag_start_details_test.dart` *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 8.0 px bottom)* — **P2**.~~ **FIXED.** Section 10 (`_buildTapCountChart`) wrapped its bar Row in `SizedBox(height: 140)`, but each inner Column was: count text (~14) + spacing (2) + bar up to 104 + spacing (4) + 2-line label like `'tap=1\n(char)'` (~24 px at fontSize 9.5 × height 1.2) ≈ 148 px → 8 px bottom overflow. Bumped the slot to `height: 160` (the minimal fix). Script-only change; verified single-script retest with `frameworkErrors=0`.

32. ~~`gestures/tap_drag_update_details_test.dart` *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 16 px right)* — **P3**.~~ **FIXED.** Localised by section-binary-search (sections 7-12 disabled → still overflow; 4-6 alone → overflow; 4 alone → clean; 4+5 → clean; 4+5+6 → overflow) → `tapCountSection`. Root cause was `tapVignetteCard` width 290 (inner 258 after 16+16 padding) holding a Row of `dotRow` (24 px per dot × count) + 22-px arrow Icon + 4-px gap + a monospace pill `drag · update · update · …`. With count=3 the sum hit ~274 px → 16 px right overflow. Wrapped the trailing pill `Container` in `Flexible` so it shrinks (and the text wraps inside) when the dot row grows. Script-only change; verified single-script retest with `frameworkErrors=0`.

33. ~~`gestures/tap_move_details_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-19, P5(a)). The 6 framework errors came from the `slopRows` loop (6 iterations over `simulatedMoves`), where each card combined a non-uniform `Border(left: 4px accent, top/right/bottom: 1px slateSoft)` with `borderRadius: BorderRadius.circular(10.0)` — Flutter rejects this at paint time. Canonical P5(a) fix applied at lines 597-705: outer Container keeps uniform `Border.all(slateSoft, 1.0)` + rounded corners + `clipBehavior: Clip.antiAlias`; the left accent strip becomes a sibling `Container(width: 4)` inside `IntrinsicHeight > Row(crossAxisAlignment: stretch)`. Visually identical to original. Verified: `hardly_relevant_classes_1_test` with `--plain-name 'tap_move_details_test.dart'` reports `frameworkErrors=0 status=success` (was 6). Rule (a) — script-only change, single-script retest sufficient. Log: `ztmp/item33_run1.log`.

34. ~~`gestures/velocity_estimate_test.dart`~~ *(hardly_relevant_classes_1_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-19, P1 + P5(a) follow-up). Section binary-search localised the failure to `_VelocityVsEstimateSection`: its `LayoutBuilder` returned `Row(crossAxisAlignment: CrossAxisAlignment.stretch, children: [Expanded(left), SizedBox, Expanded(right)])` inside `SingleChildScrollView > Column(stretch)`. The scroll view's infinite vertical max constraint propagated through the stretching Row to each Expanded child's inner Container `RenderConstrainedBox.layout()`, throwing "BoxConstraints forces an infinite height". Wrapping the Row in `IntrinsicHeight` bounded the height to the taller panel's intrinsic height and cleared the P1 error. Doing so revealed 5 previously-masked P5(a) errors from `_Callout` (rendered 5× in `_LimitationsSection`), which combined non-uniform `Border(left: 4px tone, top/right/bottom: tone.withValues(alpha:0.25))` with `borderRadius`. Applied canonical P5(a) fix: outer Container with uniform `Border.all(tone.withValues(alpha:0.25))` + rounded corners + `clipBehavior: Clip.antiAlias`; left accent strip as sibling `Container(width: 4)` inside `IntrinsicHeight > Row(stretch)`. Visually identical. Verified: `hardly_relevant_classes_1_test` with `--plain-name 'velocity_estimate_test.dart'` reports `frameworkErrors=0 status=success` (was 1, jumped to 5 after the P1 fix exposed the masked errors). Rule (a) — script-only change, single-script retest sufficient. Log: `ztmp/item34_run2.log`.

35. ~~`gestures/velocity_test.dart`~~ *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-19, P1 + U16 follow-up). Layered script-only fix. **P1 first:** `_SectionCard` chrome rendered every section as `Row(crossAxisAlignment: stretch, [Container(width:6 strip), Expanded(content)])` inside `SingleChildScrollView > Column(stretch)`, so the scroll view's infinite vertical max propagated through the stretching Row to the strip Container's `RenderConstrainedBox.layout()` — the original baseline banner. Wrapped the chrome Row in `IntrinsicHeight` (line 297) so the strip stretches to the content column's intrinsic height. Same shape repeated in `_GalleryGrid` (gallery-card pair rows), wrapped with `IntrinsicHeight` (line 1087) for consistency. **U16 follow-up:** the P1 fix exposed an empty-`Text` intrinsic-height edge case in `_EqualitySection` — its code-block Column contained `_CodeLine('')` as a blank-line separator (`Text('')` under `Padding`). Under the new `IntrinsicHeight` ancestor, the bridged empty-paragraph metric path reported an unbounded intrinsic height, which surfaced as `BoxConstraints forces an infinite height` on a downstream `RenderFlex.layout()` (same root cause as U16's NaN-Offset banner, different surface shape; documented as the "variant banner under `IntrinsicHeight`" in U16). Localised by section-binary-search (sections 1-7 → 0 err; +`_EqualitySection` → 1 err) and then probe-narrowed to the Container-of-`_CodeLine`s, then to the single `_CodeLine('')` row. Replaced the blank `_CodeLine('')` with `SizedBox(height: 14)` — preserves the vertical gap and keeps the (composed) `Text` calls non-empty. Verified: `important_classes_test` with `--plain-name 'gestures/ velocity_test.dart'` reports `frameworkErrors=0 status=success` (was 1). Rule (a) — script-only change, single-script retest sufficient. U16's affected-scripts table updated. Logs: `ztmp/item35_final.log`.

### Cluster D — `material/` (items 36–55)

36. ~~`material/animatedicon_test.dart`~~ *(important_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-19, P5(a)). The 6 framework errors mapped 1:1 to the 6 calls of `_pitfallTile` in section 8 (`buildPitfallsSection`), where each tile combined a non-uniform `Border(left: 4px tint, right/top/bottom: tint.withValues(alpha: 0.3))` with `borderRadius: BorderRadius.circular(12)` — Flutter rejects the combination at paint time with "A borderRadius can only be given on borders with uniform colors." Canonical P5(a) fix applied at the `_pitfallTile` helper: outer Container keeps uniform `Border.all(tint.withValues(alpha: 0.3))` + rounded corners + `clipBehavior: Clip.antiAlias`; the 4-px coloured accent strip becomes a sibling `Container(width: 4)` inside `IntrinsicHeight > Row(crossAxisAlignment: stretch)`. Visually identical to original. Verified: `important_classes_test` with `--plain-name 'animatedicon_test.dart'` reports `frameworkErrors=0 status=success` (was 6). Rule (a) — script-only change, single-script retest sufficient. Log: `ztmp/item36_run1.log`.

37. ~~`material/bottom_navigation_bar_landscape_layout_test.dart`~~ *(hardly_relevant_classes_2_test, 1/1, "B-bridge" tagged but emitted error is RenderFlex overflow 0.601 px right)* — **FIXED** (2026-05-19, P3 + P5(a)). Baseline was actually 4 framework errors, not 1: (i) 1× P5(a) non-uniform `Border(left: 6px cerulean, others: 1px driftwoodDark)` + `borderRadius` in `_buildClosingEssay` → refactored to uniform `Border.all` + `clipBehavior: Clip.antiAlias` + 6-px cerulean strip Container inside `IntrinsicHeight > Row(crossAxisAlignment: stretch)`. (ii) 2× 0.601 px right-overflows from `_mockNavBar(layout: 'linear', items: 5)` whose 5 horizontal tiles have natural width ~241 vs container 240 — widened the linear bar to 252 in `_buildThreePierAnatomy` (section 3) and via a `layoutKey == 'linear' ? 252 : 240` ternary in `_buildLayoutDeepDive` example bars (section 6). (iii) 1× 11 px right-overflow in `_buildItemCountGrid` (section 7) for the same linear-5 case inside 230 px cells — widened both header and body cells from 250 → 280 and the inner bar from 230 → 250. All three fixes are localized D4RT-SCRIPT-WORKAROUNDs commented in source. The grid sits inside `SingleChildScrollView(scrollDirection: Axis.horizontal)` so wider cells don't break the viewport. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 4). Log: `ztmp/item37_final3.log`.

38. ~~`material/carousel_controller_test.dart`~~ *(hardly_relevant_classes_2_test, 1/1, B-layout/RenderFlex — overflow 0.487 px right)* — **FIXED** (2026-05-19, P3). Baseline was 2 framework errors (0.487 px and 2.2 px right overflows), both in section 1 `_buildTitleBanner > _buildPaletteStrip > _buildSwatch`. The 12 palette swatch tiles are `Container(width: 116, padding: H8 V6)` whose 100-px inner space holds a `Row(mainAxisSize: min, [Container(18), SizedBox(6), Text(name)])`. Longer palette names ("CarnivalNight", "CarnivalCream", "CarnivalCoral", etc.) render at ~13 chars × 6.5 px ≈ 85 px which makes the Row's natural width exceed 100. Two of the names tip over by sub-pixel / few-pixel amounts → 0.487 and 2.2 px right overflows. Fix: dropped `MainAxisSize.min`, wrapped the Text in `Expanded(child: Text(..., overflow: TextOverflow.ellipsis))` so the label adapts to the remaining ~76 px and truncates with ellipsis when needed. Visually still a swatch + label pair; long names are clipped instead of overflowing. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 2). Log: `ztmp/item38_final.log`.

39. ~~`material/chip_attributes_test.dart`~~ *(secondary_classes_test, 1/1, I-unhandled — "Cannot invoke method 'withValues' on null.")* — **FIXED** (2026-05-19, P7 + P1). Baseline was 6 framework errors, not 1: (i) 5× "Cannot invoke method 'withValues' on null" inside `_PrivateStateColorChain > _PrivateStateBox` (section 6). Root cause is a d4rt interpreter quirk — `static const List<List<dynamic>> _states` with `s[1] as Color` / `s[2] as Color` casts returned null at runtime; the casts succeed under analyzer Dart but the dynamic-list path strips the const Color references. Replaced with a typed `_StateRow(label, bg, fg, set)` value class and a local `List<_StateRow>` so the colours are read via named fields, no dynamic casts. (ii) 1× P1 "BoxConstraints forces an infinite height" in `_PrivateAttributeGallery.build` — a `Row(crossAxisAlignment: stretch)` inside a `SingleChildScrollView > Column` infinite-height context. Wrapped each row in `IntrinsicHeight` so the stretch children get a bounded vertical extent. Both fixes are localized D4RT-SCRIPT-WORKAROUNDs commented in source. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Log: `ztmp/item39_run2.log`.

40. ~~`material/desktop_text_selection_toolbar_button_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite width)* — **FIXED** (2026-05-19, P1). Root cause: `DesktopTextSelectionToolbarButton` internally wraps its TextButton in `SizedBox(width: double.infinity)` so it stretches inside a real toolbar. The mock placed these buttons inside `Row(mainAxisSize: MainAxisSize.min, children: [...])` (via `buildToolbarSurface`); Row's first layout pass for `MainAxisSize.min` hands each child unbounded width, so the inner double-infinity SizedBox trips "BoxConstraints forces an infinite width". Section binary search localized it to `buildClassicToolbarMock` (section 3); the same helper is also used by sections 4 (extended), 5 (themed), 7 (RTL), 9 (comparison) and Flutter only reports the first identical error so a single helper-level fix covers all of them. Fix: wrap each child in `IntrinsicWidth` so the Row queries the inner TextButton's intrinsic (finite, label-driven) width before laying out, giving the `SizedBox(width: infinity)` a bounded constraint to clamp against. Localised D4RT-SCRIPT-WORKAROUND comment in `buildToolbarSurface`. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Log: `ztmp/item40_run1.log`.

41. ~~`material/dialog_bottom_sheet_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 22 px bottom)* — **FIXED** (2026-05-19, P2 + P3). Baseline was 6 framework errors, not 1: (i) 3× 22 px bottom overflow from `_galleryDialog` (section 5 AlertDialog gallery) — the icon-bearing alert dialogs (destructive / info / error / success) plus the multi-option SimpleDialogs render ~302 px tall but the stage Container clamped them to 280 px. Bumped the stage height to 320 px. (ii) 3× right overflow (20 / 14 / 11 px) from the four SimpleDialog widgets in section 6 — their `SimpleDialogOption(child: Row(Icon|CircleAvatar + SizedBox + Text))` natural width exceeded the 240-px stage. Widened all four SimpleDialog `SizedBox(width:)` wrappers from 240 → 280 px so the option rows fit without overflow. Section binary search localised errors to sections 5 and 6 only; the rest of the script is untouched. Both fixes are localised D4RT-SCRIPT-WORKAROUNDs commented in source. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Log: `ztmp/item41_run1.log`.

42. ~~`material/divider_listtile_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 4.0 px bottom)* — **FIXED** (2026-05-19, P2). Baseline was 4 framework errors, not 1: 4× "RenderFlex overflowed by 4.0 pixels on the bottom". Section binary search localised all 4 to `_IndentRulerSection > _RulerStrip` (around line 888). Diagnosis: `Container(height: 28)` hosted 16 tick Columns, but the 4 *major* tick Columns (i=0, 5, 10, 15) stack `SizedBox(4) + Container(height: 12) + SizedBox(2) + Text(fontSize: 9)` whose natural line height is ~14 px, totalling ~32 px — 4 px taller than the strip. Fix: bumped `_RulerStrip` Container height from 28 → 34, giving the major-tick Column a small breathing margin. Localised D4RT-SCRIPT-WORKAROUND comment `#42, P2` in source. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 4). Log: `ztmp/item42_run1.log`.

43. ~~`material/expansion_stepper_test.dart`~~ *(secondary_classes_test, 1/1, B-layout — "RenderFlex children have non-zero flex but incoming height constraints …")* — **FIXED** (2026-05-19, P12). Root cause: the horizontal-variant Stepper in `_StepperShowcaseSection.build` (at the bottom of the section, around line 1024). Flutter's `Stepper(type: StepperType.horizontal)` internally lays out as `Column(children: [headerRow, Expanded(child: …content)])`. The Stepper sat inside an outer `SingleChildScrollView > Column` (the page scroll body), so its incoming vertical constraint was unbounded — the inner Expanded then tripped "RenderFlex children have non-zero flex but incoming height constraints are unbounded". Fix: wrap the horizontal Stepper in `SizedBox(height: 220)` so the inner Expanded gets a bounded constraint (header ≈80 px + shrunk-to-empty content + breathing margin). The vertical Stepper above does not have this issue because it expands its body column naturally without an internal `Expanded`. Localised D4RT-SCRIPT-WORKAROUND comment `#43, P12` in source. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Log: `ztmp/item43_run1.log`.

44. ~~`material/fade_forwards_page_transitions_builder_test.dart`~~ *(hardly_relevant_classes_2_test, 1/1, B-layout — "RenderFlex children have non-zero flex but incoming width constraints …")* — **FIXED** (2026-05-19, P12 + P2 follow-up cluster). Root cause (P12): `_frameCard` returned a `Container` with no explicit width whose inner header `Row(_tValuePill, SizedBox(8), Expanded(Text(caption)))` lives inside a horizontal `SingleChildScrollView` (Act 4 strip at line ~1114 and Act 6 row at line ~1756, plus all five strips in Act 5 — total 33 frame cards). The horizontal scroll gives the outer Container loose unbounded width; the inner Row's `Expanded` then trips "RenderFlex children have non-zero flex but incoming width constraints are unbounded". The frozen page mocks (`_pageLogin`, `_pageDashboard`, `_pageSettings`) are all 220 px wide; fix: gave `_frameCard` an explicit `width: 236` (220 page + 16 padding) so the header Row has a finite width. Follow-up (P2): the original P12 assertion aborted layout further down the tree, masking 27× "RenderFlex overflowed by 3.6 pixels on the bottom" coming from the inner Columns of all three 360-px-tall page mocks. The Column content stack just narrowly exceeded the 336 px inner content area (12 px padding both sides), with the Spacer in Login/Settings forced to 0 px and Dashboard's fixed stack overshooting by ~3.6 px. Fix: bumped all three page heights `360 → 380` *and* bumped each of the five freezers (`_frozenFadeForwards`, `_frozenFadeUpwards`, `_frozenZoom`, `_frozenOpenUpwards`, `_frozenCupertino`) `SizedBox(height: 360 → 380)` — without bumping the freezers, the SizedBox would tightly clamp the new 380-tall page mock back to 360 and the overflow would persist. Localised D4RT-SCRIPT-WORKAROUND comments `#44, P12` (on `_frameCard`) and `#44, P2 follow-up` (on the three pages and the freezer prologue) in source. Rule (a) — script-only changes, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1 + 27 hidden). Logs: `ztmp/item44_baseline.log` (1 error), `ztmp/item44_run1.log` (27 follow-up overflows after P12 fix), `ztmp/item44_run2.log` (0 errors after follow-up fix).

45. ~~`material/floatingactionbutton_test.dart`~~ *(essential_classes_test, 1/1, B-layout/RenderFlex — overflow 41 px right)* — **FIXED** (2026-05-19, P3). Top-level binary search localised the single overflow to `_buildSectionEdgeCases` (Section 11, line ~1487). Edge bisection inside that section was non-obvious: the most suspicious-looking culprit (Edge 3, `FloatingActionButton.extended` with a deliberately overlong "Initialize the deep-space probe with full systems calibration" label) was a red herring — wrapping it in a horizontal `SingleChildScrollView` did not silence the error, shortening the label did not silence the error, and probing with Edge 3 entirely commented out still produced the same 41 px overflow. Probing with Edge 1 commented out instead yielded `frameworkErrors=0`, proving Edge 1 was the source. Root cause: the original demo placed `Row(mainAxisSize.min, [SizedBox(10), CircleAvatar(r:12), SizedBox(8), Text('Profile' bold), SizedBox(12)])` (natural width ~100 px) directly as the `child` of a regular `FloatingActionButton`, but a regular FAB hard-codes `BoxConstraints.tightFor(width: 56, height: 56)` and force-clamps the child to 56 px. Fix: switched Edge 1 from regular FAB to `FloatingActionButton.extended` with the avatar as `icon` and 'Profile' as `label`, which is the widget actually designed for "avatar + inline label" composite content and sizes its pill to the natural icon+label width. Edge 3 restored to its original long label (it's not the source of any overflow; FAB.extended evidently elides or wraps oversized labels gracefully in this layout context). Localised D4RT-SCRIPT-WORKAROUND comment `#45, P3` on Edge 1 in source. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item45_baseline.log` through `ztmp/item45_probeG.log` (bisect trail), `ztmp/item45_verify.log` (final 0 errors).

46. ~~`material/licensepage_test.dart`~~ *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 26 px bottom)* — **FIXED** (2026-05-19, P2). Baseline was 4 framework errors, not 1: 4× bottom-overflow assertions at 26, 64, 90, 42 px (one per `_fieldCard` variant — Flutter only deduplicates *identical* messages, and these all have different pixel counts). Section binary search localised all four to the `fieldShowcase` Container (line 339), which contains four `_fieldCard` calls each rendering a different `LicensePage` configuration: Name Only, Versioned, With Icon (FlutterLogo size 48 in 8 px padding — the tallest), and Legalese. Root cause: `_fieldCard` clamped the embedded `LicensePage` into a `SizedBox(height: 240)`, but the LicensePage's internal master/body layout exceeds 240 px in all four configurations (the FlutterLogo variant is widest off, at 90 px). Fix: bumped the SizedBox clamp in `_fieldCard` from 240 → 340 (the largest overflow + small breathing margin) so every variant lays out inside the card. Localised D4RT-SCRIPT-WORKAROUND comment `#46, P2` on the `_fieldCard` SizedBox. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 4). Logs: `ztmp/item46_baseline.log` (4 errors), `ztmp/item46_probeA..D.log` (bisect trail), `ztmp/item46_verify.log` (0 errors).

47. ~~`material/material_type_test.dart`~~ *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-19, P5(a) + P1 follow-up). Baseline was 6 framework errors: 6× "A borderRadius can only be given on borders with uniform colors." Two helper functions used `Border()` with asymmetric sides (different colors and/or widths per side) combined with `borderRadius`, which Flutter forbids: `_sectionTitle` (line 1222 — left-only chunky accent bar, ~5 call sites) and `_buildPitfall` (line 1837 — thick accent left + pale red on top/right/bottom). Refactor: both helpers now use a uniform outer rounded frame plus a `ClipRRect`-wrapped Row whose first child is a width-N accent Container acting as the visual left bar. Follow-up (P1): the new `Row(crossAxisAlignment.stretch)` initially tripped "BoxConstraints forces an infinite height" because the children were asked to stretch in an unbounded vertical context — wrapped the Row in `IntrinsicHeight` so the stretch resolves against the children's intrinsic height. Localised D4RT-SCRIPT-WORKAROUND comments `#47, P5(a)` on both helpers. Rule (a) — script-only changes, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6, briefly 1 infinite-height before IntrinsicHeight wrap). Logs: `ztmp/item47_baseline.log` (6 errors), `ztmp/item47_verify.log` (1 infinite-height follow-up), `ztmp/item47_verify2.log` (0 errors).

48. ~~`material/menu_advanced_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 18 px right)* — **FIXED** (2026-05-19, P5(a) + P3). Baseline was 6 framework errors, not 1: 5× "borderRadius can only be given on borders with uniform colors" + 1× "RenderFlex overflowed by 18 pixels on the right". (a) The 5 uniform-colors errors all came from `pitfallCard` (line 2162) using an asymmetric `Border()` (thick accent left + slate200 top/right/bottom) with `borderRadius: 8` — Flutter forbids non-uniform borders with radius. Refactored to a uniform outer `Border.all(slate200)` plus a `ClipRRect`-wrapped `IntrinsicHeight(Row(stretch, [bar Container, content Expanded]))` so the chunky accent bar is preserved as the leftmost child rather than a Border side. (b) Section binary search localised the 18 px right overflow to Section 10 "Anchor positioning" → Placement C "right of trigger" (line 1845). The inner `Row` lays out `[Trigger Container, SizedBox(6), staticMenuSurface(minWidth: 200)]` inside an outer `Container(width: 380)`. Because Row gave the surface unbounded horizontal constraints, the surface picked a width that — combined with the Trigger and gap — exceeded 380 px by 18 px. Fix: wrapped the `staticMenuSurface` in `Flexible` so the Row can shrink it to the remaining space; preserves the trigger-left + menu-right visual without overflow. Localised D4RT-SCRIPT-WORKAROUND comments `#48, P5(a)` (on `pitfallCard`) and `#48, P3` (on Placement C). Rule (a) — script-only changes, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Logs: `ztmp/item48_baseline.log` (6 errors), `ztmp/item48_step1.log` (after pitfallCard fix: 1 left), `ztmp/item48_probeA..F.log` (bisect trail), `ztmp/item48_verify.log` (0 errors).

49. ~~`material/pageroute_test.dart`~~ *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-19, P1). Section binary search localised the assertion to `boardingPass` in section 4 (line 712). Root cause: the boarding-pass `Row` uses `crossAxisAlignment: stretch` so the left brass strip and the right "SEAT 07A" box span the full pass height. That works inside a Row whose vertical extent is bounded, but the widget lives at the top level of `SingleChildScrollView > Column(stretch)` where vertical constraints are unbounded — Flutter throws "BoxConstraints forces an infinite height" because the stretch children are asked to fill an infinite axis. Fix: wrapped the Row in `IntrinsicHeight` so the stretch resolves against the Expanded middle column's intrinsic height (the tallest non-stretch child) and the strip and seat box stretch to a finite, content-derived height. Localised D4RT-SCRIPT-WORKAROUND comment `#49, P1` on the boardingPass widget. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item49_baseline.log`, `ztmp/item49_probeA..F.log` (bisect trail), `ztmp/item49_verify.log` (0 errors).

50. ~~`material/scaffold_advanced_test.dart`~~ *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-19, P5(a)). Baseline was 14 uniform-colors errors, not 1 — Flutter prints one banner per distinct widget instance, and Section 12 "DO/AVOID" callouts (`_buildDoAvoidCallouts`) renders 14 rules with the same offending pattern. Each `cardDeco` combined `borderRadius: BorderRadius.circular(8)` with an asymmetric `Border()` (thick coloured left side — green for DO, cinnabar for AVOID — plus semi-transparent brassDeep top/right/bottom). Flutter forbids non-uniform borders with a radius. Replaced the BoxDecoration with a uniform `Border.all(cBrassDeep@0.4)` and rendered the coloured accent bar as a stretched 6-px `Container` inside `ClipRRect > IntrinsicHeight > Row(stretch, [bar Container, Expanded content])`. Localised D4RT-SCRIPT-WORKAROUND comment `#50, P5(a)` on the tile builder. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 14). Logs: `ztmp/item50_baseline.log` (14 errors), `ztmp/item50_verify.log` (0 errors).

51. ~~`material/scaffold_fab_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 82 px bottom)* — **FIXED** (2026-05-19, P2). Section binary search (probes A-C) localised the 82-px bottom overflow to Section 6 "BottomAppBar + FAB notch" (`_notchSection`, line 1532). Root cause: the wide mock-viewport `SizedBox` was 360 × 220 dp; the body region was positioned `top: 56, bottom: 70`, leaving a 94-dp band. With 12-dp inner padding, content height was 70 dp. The body Column rendered four inbox rows wrapped in `Padding(vertical: 5)` containing a 28-dp avatar Row → each row = 5 + 28 + 5 = 38 dp; four rows = 152 dp. 152 − 70 = 82 dp — exactly the reported overflow. Fix: bumped the viewport `SizedBox.height` from 220 → 310 dp, which grows the body band to ~184 dp and comfortably fits the rows. The FAB `Positioned(bottom: 60 − 28)` is anchored from the bottom edge so it stays correctly docked relative to the BottomAppBar. Localised D4RT-SCRIPT-WORKAROUND comment `#51, P2` on the Center > SizedBox. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item51_baseline.log`, `ztmp/item51_probeA..C.log` (bisect trail), `ztmp/item51_verify.log` (0 errors).

52. ~~`material/scaffold_test.dart`~~ *(essential_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-19, P1). Two candidate `Row(crossAxisAlignment.stretch)` sites in the script: line 436 (already wrapped in `IntrinsicHeight`) and line 1492 in `DrawerScrimDiagram` (Section 8). The diagram lives inside `DrawerScrimSection`'s outer `Column(stretch)`, which forwards unbounded vertical constraints; the Row(stretch) therefore tried to stretch children to infinity and tripped the "BoxConstraints forces an infinite height" assertion. Same fix pattern as #49: wrapped the Row in `IntrinsicHeight` so the stretch resolves against the bounded 320-dp `SizedBox` child instead of infinity. Localised D4RT-SCRIPT-WORKAROUND comment `#52, P1` on the DrawerScrimDiagram return. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` for `material/scaffold_test.dart` (was 1). Logs: `ztmp/item52_baseline.log`, `ztmp/item52_verify.log` (0 errors).

53. ~~`material/showbottomsheet_test.dart`~~ *(interactive_tests_test, 1/1, B-layout/RenderFlex — overflow 20 px bottom)* — **FIXED** (2026-05-20, P2). Baseline was 6 framework errors, not 1: 20 px (Section 1 hero overview) + {6, 6, 6} px + 70 px + 126 px (Section 3 gallery). Section binary search (probes A-G) split the failures: Section 1's `_heroOverview` mock sheet was 120 dp tall but its `Column(_mockDragHandle 20 + caption ~14-28 + gap 6 + 2× _mockListTile 48 = ~136-150)` overflowed by 20 dp — bumped the Positioned `height: 120 → 150` dp. Section 3's `_gallerySection` rendered 8 viewports through `_mockSheet`, whose `Container(height: fixed)` forced its inner `Column(min)` to a fixed extent; the inner `Expanded(Column(tiles))` then overflowed whenever `tileCount * 48 > height - 59` (3 tiles in 200-dp sheets → 3 px = 6 px reported; 6 tiles in 280-dp sheet → 67 px ≈ 70 reported; 8 tiles in 320-dp sheet → 123 px ≈ 126 reported). Semantically a real bottom sheet *scrolls* its body, so wrapped the tile `Column` in `SingleChildScrollView` — sheets with fitting tiles render unchanged, sheets that previously overflowed now silently clip the extras (matching real-world scrollable-body behaviour). Two localised D4RT-SCRIPT-WORKAROUND comments `#53, P2` (on `_heroOverview` and `_mockSheet`). Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Logs: `ztmp/item53_baseline.log` (6 errors), `ztmp/item53_probeA..G.log` (bisect trail), `ztmp/item53_step1.log` (Section 1 fix → 0 with Section 3 still disabled), `ztmp/item53_step2.log` (Section 3 re-enabled → 5 remaining), `ztmp/item53_verify.log` (0 errors).

54. ~~`material/showtimepicker_test.dart`~~ *(interactive_tests_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a)). Baseline was 8 uniform-colors errors, not 1 — all from `_pitfallRow` (line 1960), called 8× in the pitfall section. The original BoxDecoration combined `borderRadius: 8` with an asymmetric `Border` (4-dp coloured left accent + 0.5-dp hairline top/right/bottom). Flutter forbids non-uniform borders with a radius. Replaced with uniform outer `Border.all(hairline)` + `ClipRRect`, rendering the 4-dp coloured accent as a stretched `Container` inside `IntrinsicHeight > Row(stretch, [bar Container, Expanded(content)])`. Localised D4RT-SCRIPT-WORKAROUND comment `#54, P5(a)` on `_pitfallRow`. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 8). Logs: `ztmp/item54_baseline.log` (8 errors), `ztmp/item54_verify.log` (0 errors).

55. ~~`material/snack_bar_behavior_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 56 px bottom)* — **FIXED** (2026-05-20, P5(a) + P2). Baseline was 6 framework errors, not 1: 4 × uniform-colors + 2 × "RenderFlex overflowed by 56 pixels on the bottom". Two independent causes: (i) `_buildPitfallTile` (line 1579, called 4× from the pitfalls section) combined `borderRadius: 10` with an asymmetric `Border(left: 4-dp color, top/right/bottom: 1-dp grey.shade200)` — the standard P5(a) violation. Refactored to uniform `Border.all(grey)` + `ClipRRect(10)` + `IntrinsicHeight > Row(stretch, [Container(width:4, color), Expanded(Padding(content))])`, preserving the outer `boxShadow` on the wrapper. (ii) `_buildMockPhoneFrame` (line 912, called 2× — fixed and floating variants at lines 258, 278) sets `Container(height: 280)` for the phone frame and places 4 stripe iterations × (Container 14 + gap 8 + Container 14 + gap 16 = 52 dp) = 208 dp of content inside a `Positioned(top: 44, bottom: 56, padding: 12)` body band whose usable height is 280 − 44 − 56 − 24 = 156 dp → ~52 px overflow rounded to 56 px each, ×2 frames = 2 errors. Wrapped the stripe `Column` in `SingleChildScrollView` so the extra content scrolls inside the mock body (matches real phone-app behaviour). Two localised D4RT-SCRIPT-WORKAROUND comments `#55, P5(a)` (on `_buildPitfallTile`) and `#55, P2` (on the body Positioned). Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Logs: `ztmp/item55_baseline.log` (6 errors), `ztmp/item55_step1.log` (P5(a) only → 2 remaining), `ztmp/item55_step2.log` (0 errors).

### Cluster E — `painting/` (items 56–67)

56. ~~`painting/accumulator_test.dart`~~ *(hardly_relevant_classes_2_test, 1/1, B-layout/infinite-size — Offset NaN)* — **FIXED** (2026-05-20, P1-variant). Not actually an infinite-size issue — section-bisect (probes A→E) localised the NaN to **Section 9 (Code snippets)** alone. Within section 9, `snippetThread` (one of the three `codeBlock` instances) contained a `codeLine([plain('')])` at line 1212 to render a "blank" code line. An empty `TextSpan(text: '')` in a `RichText` ancestor's children list trips `Offset argument contained a NaN value` at `dart:ui/painting.dart:41` during paint (the text layout engine produces a NaN baseline/offset when measuring a zero-length span surrounded by other spans). Replaced `plain('')` with `plain(' ')` (single space) so the line renders blank space rather than nothing. One localised D4RT-SCRIPT-WORKAROUND comment `#56, P1` on `snippetThread`. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item56_baseline.log`, `ztmp/item56_probeA.log` (sections 1-5 clean), `ztmp/item56_probeB.log` (1-8 clean), `ztmp/item56_probeC.log` (section 9 alone reproduces), `ztmp/item56_probeD.log` (section 10 alone clean), `ztmp/item56_probeE.log` (section 9 with fix clean), `ztmp/item56_verify.log` (full script, 0 errors).

57. ~~`painting/advanced_decorations_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 17 px right)* — **FIXED** (2026-05-20, P3). Section-bisect (probes A→E) localised the overflow to **Section 2 (Concept overview)** → `_paintingOrderRow` Wrap cards (line 372). Each card is `width: 150` with horizontal padding 12 (usable 126 dp), and its inner Row holds a number `Text(s.number)` (fontSize 18) + 10-dp gap + label `Text(s.label, style: _kSubsection)` (fontSize 16, w700). The longest label "Color / Gradient" measures ~120 dp at 16 sp w700, plus the number "2" (~14 px) + 10-dp gap = ~144 dp > 126 dp → ~17 px right-overflow inside that single card. Wrapped the label `Text` in `Flexible(softWrap: true)` so it wraps onto a second line inside the card instead of pushing the Row past its bounds. One localised D4RT-SCRIPT-WORKAROUND comment `#57, P3`. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item57_baseline.log`, `ztmp/item57_probeA..E.log` (bisect trail), `ztmp/item57_verify.log` (0 errors).

58. ~~`painting/border_directional_test.dart`~~ *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a)). Baseline was 4 uniform-colors errors, not 1 — all from the `footgunCards` loop in Section 10 (4 entries in `footgunData`). Each card combined `borderRadius: 10` with an asymmetric `Border(left: full-colour, top/right/bottom: 0.3-alpha)` — the standard P5(a) violation. The footgun cards are *descriptive* (titles like "No BorderRadius support" explain BorderDirectional's limitations), not *demonstrative* — the cards themselves were not meant to trip the assertion. Considered P5(b) (capture-and-assert) but rejected it: the text content describes BorderDirectional restrictions while the chrome happens to use `Border`; the offending pattern is incidental to the test's purpose, so P5(a) preserves the descriptive intent. Refactored to uniform `Border.all(0.3-alpha)` + `ClipRRect(10)` + `IntrinsicHeight > Row(stretch, [Container(width:4, color), Expanded(Padding(content))])`. Localised D4RT-SCRIPT-WORKAROUND comment `#58, P5(a)` on the loop body. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 4). Logs: `ztmp/item58_baseline.log` (4 errors), `ztmp/item58_verify.log` (0 errors). Note: this script genuinely tests directional borders — prefer P5(b) (capture-and-assert) to preserve test intent.

59. ~~`painting/border_radius_test.dart`~~ *(essential_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a)). Baseline was 5 uniform-colors errors, not 1 — all from `caveatCard` (line 1520), called 5× in Section 12 ("Caveats"). The decoration combined `borderRadius: BorderRadius.circular(12.0)` with an asymmetric `Border(left: 4-dp tint accent, t/r/b: 1-dp grey hairline)` — standard P5(a) violation. Plan suggested P5(b) (capture-and-assert) on the grounds that "the test is about BorderRadius". Considered and rejected: Section 12 describes five BorderRadius caveats (directional vs absolute, RTL, max radius, clip interaction, performance on large radii) — *none* of them is the uniform-colors-with-radius rule. The offending border pattern is incidental chrome (visual tint indicator), not a deliberate demonstration of the rule. Same rationale as item 58 (border_directional_test.dart). Additionally, the error is reported via `FlutterError.onError` during paint, not thrown synchronously, so a literal try/catch around the widget construction wouldn't fire — capture-and-assert here would require runZoned + FlutterError.onError override across the whole render pass, which is invasive. Refactored to uniform outer `Border.all(grey hairline)` + `ClipRRect(12)` + `IntrinsicHeight > Row(stretch, [Container(width:4, color: tint), Expanded(Padding(content))])`, preserving the outer `boxShadow` on the wrapper. One localised D4RT-SCRIPT-WORKAROUND comment `#59, P5(a)` on `caveatCard`. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item59_baseline.log` (5 errors), `ztmp/item59_verify.log` (0 errors).

60. ~~`painting/box_border_test.dart`~~ *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a)). Baseline was 5 uniform-colors errors, not 1 — all from Section 11 footgun cards loop (5 entries in `footguns`). Each card combined `borderRadius: 10` with an asymmetric `Border(left: 4-dp rose accent, t/r/b: 1-dp 0.3-alpha rose hairline)` — standard P5(a) violation. Plan suggested P5(b). Considered and rejected: the 5 footgun titles describe Border-related pitfalls (mixed widths, BoxBorder.lerp restrictions, isUniform strictness, BorderDirectional/Directionality requirement, strokeAlign layout effects) — none of them is the uniform-colors-with-radius rule itself. The rule is mentioned in passing in Section 12's recap ("BorderRadius and shape:circle require a uniform border"), but the footgun chrome doesn't demonstrate it — the offending pattern is incidental chrome. Same rationale as items 58 and 59. Refactored to uniform `Border.all(0.3-alpha hairline)` + `ClipRRect(10)` + `IntrinsicHeight > Row(stretch, [Container(width:4, color: rose), Expanded(Padding(content))])`. One localised D4RT-SCRIPT-WORKAROUND comment `#60, P5(a)` on the footgun loop body. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item60_baseline.log` (5 errors), `ztmp/item60_verify.log` (0 errors).

61. ~~`painting/box_decoration_test.dart`~~ *(essential_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a)). Baseline was 9 uniform-colors errors, not 1 — all from Section 12 DO/AVOID callout cards loop (9 entries in `rules`). Each card combined `borderRadius: 8` with an asymmetric `Border(left: 6-dp green/red accent, t/r/b: 1-dp 0.3-alpha tracery hairline)` — standard P5(a) violation. Plan suggested P5(b). Considered and rejected: the 9 DO/AVOID rules cover other BoxDecoration pitfalls (Container.color vs decoration conflict, shape:circle+borderRadius, BoxDecoration.lerp, copyWith, DecorationImage, shadow performance, build()-frequency, backgroundBlendMode) — *none* of them is the uniform-colors-with-radius rule. The offending pattern is incidental chrome. Same rationale as items 58-60. Refactored to uniform `Border.all(0.3-alpha hairline)` + `ClipRRect(8)` + `IntrinsicHeight > Row(stretch, [Container(width:6, color: accent), Expanded(Padding(content))])`. One localised D4RT-SCRIPT-WORKAROUND comment `#61, P5(a)` on the callout loop body. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 9). Logs: `ztmp/item61_baseline.log` (9 errors), `ztmp/item61_verify.log` (0 errors).

62. ~~`painting/image_cache_test.dart`~~ *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a)). Baseline was 8 uniform-colors errors, not 1 — all from the `_calloutBox` helper (line 367), called 8× across the script. The decoration combined `borderRadius: 10` with an asymmetric `Border(left: 4-dp tint accent, t/r/b: 1-dp 0.3-alpha tint hairline)` — standard P5(a) violation. Refactored to uniform `Border.all(0.3-alpha hairline)` + `ClipRRect(10)` + `IntrinsicHeight > Row(stretch, [Container(width:4, color: tint), Expanded(Padding(content))])`. One localised D4RT-SCRIPT-WORKAROUND comment `#62, P5(a)` on `_calloutBox`. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 8). Logs: `ztmp/item62_baseline.log` (8 errors), `ztmp/item62_verify.log` (0 errors).

63. ~~`painting/image_providers_test.dart`~~ *(important_classes_test, 1/1, B-layout — "RenderFlex children have non-zero flex but incoming width constraints …")* — **FIXED** (2026-05-20, P12 + P2 follow-up). Baseline was 1 framework error. Localised by inspecting the full creator chain captured with a temporary 4000-char truncation in `send_test_runner.dart` (reverted): the affected `RenderFlex` was the inner `kKeyValue` Row inside `resizeSection`, and the "nearest ancestor providing an unbounded width constraint" was a top-level `Row(crossAxisAlignment: CrossAxisAlignment.start)` at line 1068 inside `resizeSection` whose two `Column` children were *not* wrapped in `Expanded`. The outer Row therefore forwarded an unbounded width to each non-flex Column child, which propagated down through the Column's `kKeyValue` rows (Padding > Row > Expanded(Text)) and tripped the error. Fix (P12): wrapped both inline Column children in `Expanded` so the Row hands each a bounded slice of the available width. Visually equivalent (the Row still splits the panel into a fit-policy column on the left and an exact-policy column on the right, separated by the existing 16-dp gap). Follow-up (P2): after the P12 fix the tree finally laid out and revealed a previously-masked overflow — the composed document is ~5237 logical pixels tall and overflowed the 800×600 test viewport by 4637 px on the bottom. Wrapped the root `Container(color, padding:16) > Column(stretch)` in `Scaffold > SafeArea > SingleChildScrollView(Axis.vertical)` so the long demo scrolls inside a bounded viewport. Checked that none of the inner Rows use `CrossAxisAlignment.stretch`, so the newly-unbounded vertical context does not propagate to a stretch-Row (which would have tripped a "BoxConstraints forces an infinite height"). Two localised D4RT-SCRIPT-WORKAROUND comments `#63, P12` (on the resizeSection Row) and `#63, P2` (on the root return). Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item63_baseline.log` (1 error), `ztmp/item63_full_error.log` (full creator chain captured for localisation), `ztmp/item63_verify.log` (P12 fix → overflow surfaced), `ztmp/item63_verify2.log` (0 errors after P2 follow-up).

64. ~~`painting/image_size_info_test.dart`~~ *(hardly_relevant_classes_2_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1). Baseline was 1 framework error ("BoxConstraints forces an infinite height"). The root return is already correctly wrapped in `Scaffold > SingleChildScrollView > Column(stretch)`, so vertical space is unbounded by design. Of the four `CrossAxisAlignment.stretch` sites in the script (lines 241, 729, 889, 1179) only line 729 is a `Row(stretch)` — the `equalityCards` Row in Section 6 (the equality-comparison panel). The other three are Column(stretch) sites which are fine: a Column's cross axis is horizontal, and each lives inside a Container/Card with a bounded width. The `equalityCards` Row, however, lives in the unbounded-vertical `SingleChildScrollView > Column` context, so its `crossAxisAlignment.stretch` (vertical for a Row) asked the children to fill an infinite height — exactly the reported failure. Standard P1 fix: wrap the Row in `IntrinsicHeight` so stretch resolves against the maximum intrinsic height of the three `_buildEqCard` Expanded children (giving the three cards equal-height alignment, which is what the original `stretch` was trying to express). One localised D4RT-SCRIPT-WORKAROUND comment `#64, P1` on the `equalityCards` definition. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item64_baseline.log` (1 error), `ztmp/item64_verify.log` (0 errors).

65. ~~`painting/inline_span_semantics_information_test.dart`~~ *(hardly_relevant_classes_2_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1). Baseline was 1 framework error ("BoxConstraints forces an infinite height" reported by RenderPadding). Root return is already correctly `Scaffold > SingleChildScrollView > Column(stretch)` (line 1013-1018). Two `CrossAxisAlignment.stretch` sites: line 1018 on the outermost Column (cross-axis horizontal, parent is SingleChildScrollView with bounded width — fine) and line 750 on the inner Row inside `timelineCard`. That inner Row's cross axis is vertical, and it lives in the unbounded-vertical SingleChildScrollView context, so stretch forced its three `_buildTimelineSegment` children (each an `Expanded(flex, child: Container(Column))`) to infinite height. Standard P1 fix: wrap the Row in `IntrinsicHeight` so stretch resolves against the max intrinsic height of the three timeline cards (preserves the intended equal-height visual). One localised D4RT-SCRIPT-WORKAROUND comment `#65, P1` on the IntrinsicHeight wrapper. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item65_baseline.log` (1 error), `ztmp/item65_verify.log` (0 errors).

66. ~~`painting/matrix_utils_test.dart`~~ *(hardly_relevant_classes_2_test, 1/1, B-layout/RenderFlex — overflow 4211 px bottom)* — **FIXED** (2026-05-20, P2). Baseline was 1 framework error ("A RenderFlex overflowed by 4211 pixels on the bottom"). The script's `build()` (line 1577) returned a bare `Container(width: infinity, color, padding) > Column(mainAxisSize.min, crossAxisAlignment.stretch)` containing ten `sectionFrame(...)` panels plus a closing badge — the composed document is roughly 4811 logical pixels tall and overflows the 800×600 test viewport by exactly the reported 4211 px. The plan flagged this as P1+P2, but inspection showed only one `CrossAxisAlignment.stretch` site in the script (the root Column at line 1593), whose cross axis is horizontal. There are no `Row(crossAxisAlignment.stretch)` sites, so wrapping the root in a vertical scroll view does not produce a hidden P1 follow-up (the unbounded-vertical context never propagates to a stretch-Row). Fix (P2 only): wrap the root in `Scaffold > SafeArea > SingleChildScrollView(Axis.vertical)` so the deep demo scrolls inside a bounded viewport. One localised D4RT-SCRIPT-WORKAROUND comment `#66, P2` on the new wrapper, with an explicit note that the inner Column's stretch cross-axis is horizontal and therefore safe. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item66_baseline.log` (1 error), `ztmp/item66_verify.log` (0 errors).

67. ~~`painting/shape_border_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 1863 px bottom)* — **FIXED** (2026-05-20, P2). Baseline was 1 framework error ("A RenderFlex overflowed by 1863 pixels on the bottom"). The script's `build()` (line 263) returned a bare `Container(color, padding:16) > Column(stretch, mainAxisSize.min)` containing a title banner + 9 sections + a closing badge — the composed page is roughly 2463 logical pixels tall and overflows the 800×600 test viewport by exactly the reported 1863 px. The plan flagged P1+P2, but the script has only two `CrossAxisAlignment.stretch` sites and both are Columns (line 1245 inside `cheatBlock` and line 1265 on the root Column), whose cross axis is horizontal. There are no `Row(crossAxisAlignment.stretch)` sites, so wrapping the root in a vertical scroll view does not produce a hidden P1 follow-up. Fix (P2 only): wrap the root in `Scaffold > SafeArea > SingleChildScrollView(Axis.vertical)` so the demo scrolls inside a bounded viewport. One localised D4RT-SCRIPT-WORKAROUND comment `#67, P2` on the new wrapper, with an explicit note that both stretch-Columns have horizontal cross-axis and are therefore safe. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item67_baseline.log` (1 error), `ztmp/item67_verify.log` (0 errors).

68. ~~`painting/star_border_test.dart`~~ *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a)). Baseline was 5 uniform-colors errors, not 1 — all from the `_footgunCard` helper (line 1042), called 5× across the script for the footgun section. The original `BoxDecoration` combined `borderRadius: BorderRadius.circular(12.0)` with an asymmetric `Border(left: width 4 full-alpha accent, top/right/bottom: width 1 at 0.2-alpha accent)` — the canonical P5(a) violation ("A borderRadius can only be given on borders with uniform colors"). Refactored using the standard P5(a) recipe: hoisted the `boxShadow` and `borderRadius` to an outer `Container > BoxDecoration` (no border, so no uniform-colour clash), then wrapped the body in `ClipRRect(12) > IntrinsicHeight > DecoratedBox(uniform Border.all 0.2-alpha hairline) > Row(stretch, [Container(width:4, color: accent), Expanded(Padding(14, original Row content))])`. This preserves the original left-accent visual exactly (4-dp tinted strip running the full card height, hairline border on the other three sides, 12-dp rounded corners, drop shadow) while restoring a uniform-colour border that Flutter accepts. One localised D4RT-SCRIPT-WORKAROUND comment `#68, P5(a)` on `_footgunCard`. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item68_baseline.log` (5 errors), `ztmp/item68_verify.log` (0 errors).

### Cluster F — `rendering/` (items 69–77)

69. ~~`rendering/clear_selection_event_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×4 + P2). Baseline was 25 framework errors, not 1 — the plan undercounted. Breakdown: 10 uniform-colors errors (from four asymmetric Border helpers) + 14 NaN-Offset errors + 1 RenderFlex Infinity overflow on the bottom (the NaN+Infinity errors were follow-ons from the broken layout, not independent bugs). Four asymmetric `Border(...) + borderRadius` sites needed P5(a) refactor: `_sectionHeader` (line 374, left 4-dp tint accent + 1-dp _borderSoft hairlines around a 14-dp rounded rect, called 9×), `_codeCard` header band (line 1300, `BorderRadius.only(topLeft, topRight)` + `Border(bottom: only)`, called 1× but recurring per paint), `_compareBlock` (line 1846, `Border(left: only)` + circular 8, called 6×), and `_pitfall` (line 2088, left 4-dp accent + 1-dp _borderSoft hairlines + circular 11, called 5×). Refactored each using the standard P5(a) recipe (`ClipRRect(R) > IntrinsicHeight > DecoratedBox(uniform Border.all) > Row(stretch, [accent strip, Expanded(Padding(original content))])`); for `_codeCard` only the top corners are rounded so the inner Container keeps a uniform `Border(bottom: only)` without a borderRadius (which is allowed by Flutter since uniform-colors only check fires when borderRadius is non-null). After fixing all four uniform-colors sites the count dropped to 15 (14 NaN + 1 Infinity overflow). Inspected the root `build()`: a bare `Container(color, padding 28/36) > Column(stretch)` with a hero block + 9 sections + spacers — far taller than the 800×600 viewport. Standard P2 fix: wrap the root in `Scaffold > SafeArea > SingleChildScrollView(Axis.vertical)`. Verified the three `Row(crossAxisAlignment.stretch)` sites in the script (inside `_sectionHeader`, `_compareBlock`, `_pitfall`) are all wrapped in IntrinsicHeight, so the now-unbounded vertical context does not propagate to a stretch-Row. Four localised D4RT-SCRIPT-WORKAROUND comments `#69, P5(a)` on each helper and `#69, P2` on the root wrapper. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 25). Logs: `ztmp/item69_baseline.log` (25 errors), `ztmp/item69_verify.log` (after P5(a) only, 15 errors), `ztmp/item69_verify2.log` (0 errors after P2).

70. ~~`rendering/layers_data_test.dart`~~ *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1). Baseline was 1 framework error ("BoxConstraints forces an infinite height" reported by RenderConstrainedBox). The root build (line 43) is already correctly wrapped in `MaterialApp > Scaffold > SingleChildScrollView > Column(stretch)`, so vertical is unbounded by design. Inspected all 21 `CrossAxisAlignment.stretch` sites in the script: 20 are Columns (cross-axis horizontal — fine), and one is a Row at line 2639 inside the section-20 "Recipe cards" helper. That Row uses `crossAxisAlignment.stretch` to equalise the two `_recipeCard` Expanded children's heights, but lives in the unbounded-vertical SingleChildScrollView context, so stretch forces the children to infinite height — exactly the reported failure. Standard P1 fix: wrap the Row in `IntrinsicHeight` so stretch resolves against the max intrinsic height of the two cards (preserves the intended equal-height visual). One localised D4RT-SCRIPT-WORKAROUND comment `#70, P1` on the IntrinsicHeight wrapper. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item70_baseline.log` (1 error), `ztmp/item70_verify.log` (0 errors).

71. ~~`rendering/render_constraints_transform_box_test.dart`~~ *(secondary_classes_test + timeout_tests_test, 2/2, B-layout — `BoxConstraints(699.6<=w<=349.8, h=182.0; NOT NORMALIZED)`)* — **REVERTED/DEFERRED** (2026-05-20). Baseline was 1 framework error per host suite (NOT NORMALIZED assertion from `kHalveMaxWidth(input)` when the parent passes tight width constraints — `min=maxWidth=699.6` halved to `max=349.8` while `min` stays at `699.6`). Applied the P8 fix (pre-normalize the result by clamping `minWidth` to the new `maxWidth`) and verified the NOT NORMALIZED banner cleared — but the next-most-egregious framework error immediately surfaced: `A RenderConstraintsTransformBox overflowed by 30 pixels on the left, 15 pixels on the top, 15 pixels on the bottom, and 30 pixels on the right`. Tracing the (60 horiz, 30 vert) overflow signature pinpointed section 7 (clipBehavior showcase) — `SizedBox(160×80) > CTB.unconstrained > Container(220×110)` — a *deliberate* oversized-child demo iterating `Clip.none / Clip.hardEdge / Clip.antiAlias`. `DebugOverflowIndicatorMixin` emits the overflow banner regardless of `clipBehavior` (clipBehavior only suppresses the visual debug stripes), so the section-7 cascade cannot be cleared without erasing the demo. Behind section 7, sections 4 (six pre-defined-transform tiles, four of which deliberately overflow) and 8 (CTB vs UnconstrainedBox vs OverflowBox comparison) carry the same intentional oversized-child shape. The first banner masked all of them in the baseline because layout aborted on the NOT NORMALIZED assertion before paint reached sections 4/7/8. **Reverted the kHalveMaxWidth normalize fix; back to baseline.** Documented in `doc/interpreter_unfixable.md` as **U17** with the full cascade analysis and the three "real fix" routes (rewrite teaching content, replace pathological demos with `OverflowBox` equivalents, or exclude the script from the `frameworkErrors=0` gate) — all of which are design-level changes outside the per-item fix sweep. Rule (a) — script-only edits, reverted. Logs: `ztmp/item71_baseline_secondary.log` (1 NOT NORMALIZED), `ztmp/item71_verify_secondary.log` (1 RenderConstraintsTransformBox overflow after P8 fix — proves the cascade), `ztmp/item71_revert_secondary.log` (back to 1 NOT NORMALIZED after revert).

72. ~~`rendering/render_follower_layer_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 64 px bottom)* — **FIXED** (2026-05-20, P11 ×5). Baseline was 5 framework errors, not 1 — the plan undercounted: three "RenderFlex overflowed by 64 pixels on the bottom" (from the three `buildDropdownVariant` calls in Section 3) plus two "RenderFlex overflowed by 14 pixels on the bottom" (from `buildAutocompletePatternCard` and `buildAnchoredPopoverPatternCard` in Section 10). The plan flagged P1+P2 but neither applies: the root is already correctly wrapped in `Scaffold > SingleChildScrollView > Column(start)` (P2 already present), and the script's only `CrossAxisAlignment.stretch` site (line 1115 inside `buildFollowerPanel`) is on a Column whose cross axis is horizontal, bounded by the surrounding `Container(width: 160)` — no P1 needed. Localised the actual cause: five `Positioned(...)` wrappers around `CompositedTransformFollower(child: buildFollowerPanel(3 lines))` set an inadequate `height:` (3 × `height: 40` in the dropdown variants, 2 × `height: 90` in the autocomplete/popover pattern cards). The follower panel's intrinsic content height is ~100 px (3 rows × Padding(vertical: 8) + Icon(size: 16) ≈ 33 px each), so the 40-px constraint overflows by ~60 px (reported 64) and the 90-px constraint overflows by ~10 px (reported 14). Fix (P11 — remove inadequate fixed-height constraint): dropped `height: 40` / `height: 90` from the five Positioned wrappers, leaving `left/top/width` so the Positioned sizes vertically to its child's intrinsic content. Verified each surrounding `SizedBox` (the dropdown's `height: 200` and the pattern cards' `height: 150`) has enough room for the now-self-sized ~100-px panel at its `top: 80` / `top: 18` / `top: 30` anchor. Three localised D4RT-SCRIPT-WORKAROUND comments `#72, P11` on each pattern site. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item72_baseline.log` (5 errors), `ztmp/item72_verify.log` (0 errors).

73. ~~`rendering/render_mixins_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 6.8 px right)* — **FIXED** (2026-05-20, P3 ×4 at call sites). Baseline was 4 framework errors, not 1 — the plan undercounted. The four "A RenderFlex overflowed by 6.8 pixels on the right." messages all originate from Section 4 (`ContainerRenderObjectMixin`, lines 1048-1058): a `Row(MainAxisAlignment.spaceBetween)` of seven children where the four `treeNode('A'..'D', width: 36.0)` instances each overflow by exactly 6.8 px. Diagnosed via temporary FlutterErrorDetails diagnostics dump (`details.toDiagnosticsNode().toStringDeep()` in `main.dart`, no-truncate in `send_test_runner.dart`); the creator chain `Row ← Padding ← DecoratedBox ← ConstrainedBox ← Container ← Row` (×4, all `OVERFLOWING`) pointed straight at the `treeNode` helper. Box math: `treeNode(width: 36)` → `Container(padding h:10, border 1.4)` content area = 36 − 2×(10 + 1.4) = **13.2 px**, but the inner `Row(Icon(14) + SizedBox(6) + Flexible(Text))` needs ≥ Icon(14) + SizedBox(6) = **20 px** for its non-shrinkable head, overflowing by exactly 20 − 13.2 = **6.8 px**. Fix (P3 — bump width, applied at call sites): widened the four A/B/C/D `treeNode(..., width: 36.0)` calls to `width: 56.0` (content area becomes 33.2 px, fitting Icon+gap+1-char Text comfortably). The `treeNode` helper itself stays unchanged so its default 200-px and the 200/180-wide call sites elsewhere are unaffected. Single D4RT-SCRIPT-WORKAROUND comment `#73, P3` above the Row explaining the box-math. Temporary `main.dart`/`send_test_runner.dart` instrumentation was fully reverted before commit (verified `git diff` empty). Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 4). Logs: `ztmp/item73_baseline.log` (4 errors), `ztmp/item73_full_error.log` (creator-chain dump), `ztmp/item73_verify.log` (0 errors).

74. ~~`rendering/rendering_service_extensions_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-layout/RenderFlex — overflow 11 px bottom)* — **FIXED** (2026-05-20, P11 ×1). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 11 pixels on the bottom." P2 (wrap root in SingleChildScrollView) didn't apply because the root is *already* `Scaffold > SingleChildScrollView > Column(stretch)` (line 620). Diagnosed via temporary FlutterErrorDetails diagnostics dump (creator chain `Column ← Padding ← Stack ← Padding ← DecoratedBox ← ConstrainedBox ← Container ← Column ← ⋯` with `constraints: 0.0<=h<=58.0, size: Size(81.7, 58.0)`). Localised to Section 3.3 `_baselinesVisual` (lines 962-991): `Container(height: 110.0, padding: EdgeInsets.all(8.0))` wraps a `Stack` containing `Padding(vertical: 18.0) > Column(Text fontSize:22 + SizedBox(12) + Text fontSize:18)`. Box math: inner Column max height = 110 − 2×8 (Container padding) − 2×18 (inner Padding) = **58 px**, but the two Text widgets + SizedBox + line-leading measure **~69 px** → exactly 11 px bottom overflow. Fix (P11 — drop inadequate height constraint): removed the explicit `height: 110.0` from the Container, letting it self-size to its `Stack > Padding > Column` content (~120 px overall). `CustomPaint(painter: _BaselinePainter)` via `Positioned.fill` still paints over the actual area; the baseline guide lines at y=38 and y=78 stay within the now-auto-sized Stack. Single D4RT-SCRIPT-WORKAROUND `#74, P11` comment above the Container with the box-math. Temporary `main.dart`/`send_test_runner.dart` instrumentation fully reverted before commit (verified `git diff` empty). Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item74_baseline.log` (1 error), `ztmp/item74_full_error.log` (creator-chain dump), `ztmp/item74_verify.log` (0 errors).

75. ~~`rendering/renderobjects_layout_test.dart`~~ *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1 ×2 + P5(a) follow-up ×6). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height. ... offending constraints: BoxConstraints(w=614.5, h=Infinity) ... relevant error-causing widget: Row". Diagnosed via temporary FlutterErrorDetails diagnostics dump (creator chain `Row ← Padding ← Column ← Padding ← DecoratedBox ← Container ← Column ← Padding ← _SingleChildViewport ← …` with the offending Row having `crossAxisAlignment: stretch, mainAxisSize: max` and 3 descendants of shape `RenderPadding > RenderDecoratedBox > RenderPadding > RenderFlex > [Paragraph, ConstrainedBox, Paragraph, ConstrainedBox, Paragraph, Paragraph, Paragraph]`). Localised to Section 16 `buildSizingClasses()` at line 1809: `Padding > Row(crossAxisAlignment: stretch)` with three `classCard()` Expanded children, hosted inside a vertical `SingleChildScrollView` (root at line 620) → Row receives `h=Infinity` and `stretch` then tries to stretch each `classCard`'s Padding to infinite height. Same shape repeats in Section 17 `buildRecipeCards()` at line 1856 (Row(stretch) with 2 Expanded recipe-card children per row). Fix (P1 — wrap stretch Row in IntrinsicHeight): wrapped both Row(stretch) sites in `IntrinsicHeight` so the cross-axis is first bounded to the tallest child before stretch is applied. After the layout fix, **6 follow-up framework errors** surfaced from Section 17's recipe-card decoration: "borderRadius can only be given on borders with uniform colors. The following is not uniform: BorderSide.color." (one per recipe card, 6 cards). Root cause: `BoxDecoration(borderRadius: 10, border: Border(left: kRecipes[j].accent, top/right/bottom: kBlueprintLine))` combines a non-uniform-color Border with `borderRadius` — Flutter forbids this combination. Fix (P5(a) — uniform colors / drop borderRadius): removed `borderRadius: BorderRadius.circular(10)` from the recipe-card Container (lines 1882-1903) so the non-uniform Border becomes legal; the accent-left + neutral-rest visual stripe is preserved. Two D4RT-SCRIPT-WORKAROUND comments `#75, P1` (one per Row site) plus one `#75 follow-up, P5(a)` comment on the recipe-card Container with the box-math/assertion explanation. Temporary `main.dart`/`send_test_runner.dart` instrumentation fully reverted before commit (verified `git diff` empty). Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1 then 6). Logs: `ztmp/item75_baseline.log` (1 error), `ztmp/item75_full_error.log` (creator-chain dump), `ztmp/item75_verify.log` (6 follow-up uniform-color errors), `ztmp/item75_verify2.log` (0 errors).

76. ~~`rendering/renderobjects_sizing_test.dart`~~ *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1 ×1 helper-level). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height. ... offending: BoxConstraints(w=430.2, h=Infinity) ... relevant error-causing widget: Row". Diagnosed via temporary FlutterErrorDetails diagnostics dump (creator chain `Row ← Column ← Padding ← DecoratedBox ← Container ← Column ← Padding ← DecoratedBox ← Padding ← Container ← Column ← Padding ← ⋯`, Row has `crossAxisAlignment: stretch`). Localised to the `_matrixRow` helper at line 1615: `Widget _matrixRow(List<Widget> cells) => Row(crossAxisAlignment: stretch, children: cells);` — called 11 times inside `_matrixSection`'s `Container > Column > [_matrixRow ×11]` which lives in the root SCV. Each call therefore creates a `Row(stretch)` under unbounded vertical constraints; Flutter halts on the first failing one. Fix (P1 — wrap stretch Row in IntrinsicHeight): wrapped the Row inside `_matrixRow` in `IntrinsicHeight`, fixing all 11 call sites at once. Single D4RT-SCRIPT-WORKAROUND `#76, P1` comment on the helper. Temporary `main.dart`/`send_test_runner.dart` instrumentation fully reverted before commit (verified `git diff` empty). Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item76_baseline.log` (1 error), `ztmp/item76_full_error.log` (creator-chain dump), `ztmp/item76_verify.log` (0 errors).

77. ~~`rendering/scroll_direction_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, "B-bridge" tag but emitted error is RenderFlex overflow 15 px right)* — **FIXED** (2026-05-20, P3 ×1 + P5(a) ×1). Plan undercounted: baseline ran 8 framework errors, not 1 — 3× "A RenderFlex overflowed by 15 pixels on the right" and 5× "A borderRadius can only be given on borders with uniform colors." Diagnosed via temporary FlutterErrorDetails diagnostics dump (creator chains: Row ← Padding ← DecoratedBox ← Container ← Stack ← Column ← Container, then `Border` site Decoration ← Container ← Column). (a) **P3 — Row overflow** in Section 5 `mockHorizontal` (line 576): inner `Container(width:200, height:60, border:1)` exposes a 198 px content area to its child Row, but 5 cells × (width 36 + 2×3 margin) = 210 px → 15 px right overflow. The helper is invoked once per `ScrollDirection` value (idle/forward/reverse) → 3 errors. Fix: cell `width: 36.0` → `width: 33.0` → 5×(33+6) = 195 px, fitting inside 198 px without changing the visual cell count or scrollbar metaphor. (b) **P5(a) — uniform-colors** in Section 9 `pitfall` helper (line 1090): `BoxDecoration(borderRadius: BorderRadius.circular(10.0), border: Border(left: color/4.0, top/right/bottom: color@0.4))` — non-uniform sides cannot coexist with borderRadius. The helper is invoked 5 times by the Pitfalls panel → 5 errors. Fix: drop `borderRadius`; the accent-bar style is carried by the heavy left `BorderSide` alone. Both fixes carry D4RT-SCRIPT-WORKAROUND `#77, P3` and `#77, P5(a)` comments. Temporary `main.dart`/`send_test_runner.dart` instrumentation fully reverted before commit. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 8). Logs: `ztmp/item77_baseline.log` (8 errors), `ztmp/item77_full_error.log` (creator-chain dump), `ztmp/item77_verify.log` (0 errors).

78. ~~`rendering/select_paragraph_selection_event_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×3 helpers). Plan undercounted: baseline ran 12 framework errors, all "A borderRadius can only be given on borders with uniform colors." Three loop-driven helpers each combined `borderRadius` with a non-uniform `Border`: (a) `edgeCasesWidget` (line 810) — `borderRadius: 8` + `Border(left: cRose/4)` with top/right/bottom defaulting to `BorderSide.none` → non-uniform; 5 edgeCases entries. (b) `inspectorGrid` (line 1318) — `borderRadius: 10` + `Border(left: hue/4, top/right/bottom: cDivider/1)` → non-uniform colors; 7 inspectorRows entries. (c) `antiPatternsWidget` (line 1428) — `borderRadius: 8` + `Border(left: cRose/4, top/right/bottom: cDivider/1)` → non-uniform colors; 5 antiPatterns entries. Flutter's per-frame error throttling kept the total at 12 rather than 17. Fix: drop `borderRadius` at all three sites; the heavy-left accent bar (the visual intent of each tile) is carried entirely by the colored/wider left `BorderSide`. Each edit carries a D4RT-SCRIPT-WORKAROUND `#78, P5(a)` comment naming the loop and entry count. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 12). Logs: `ztmp/item78_baseline.log` (12 errors), `ztmp/item78_verify.log` (0 errors).

79. ~~`rendering/selection_status_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×1 helper). Plan undercounted: baseline ran 5 framework errors, all "A borderRadius can only be given on borders with uniform colors." Single offender: `_pitfallTile` helper (line 1688) — `BoxDecoration(borderRadius: BorderRadius.circular(10.0), border: Border(left: color/4.0, top/right/bottom: color@0.2))`. Both the widths and the alphas differ between the left BorderSide and the other three → non-uniform. Helper invoked 5 times by the Pitfalls section → 5 errors. Fix: drop `borderRadius`; the heavy-left accent bar look survives via the wider, more saturated left BorderSide alone. Edit carries a D4RT-SCRIPT-WORKAROUND `#79, P5(a)` comment naming the helper and call count. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item79_baseline.log` (5 errors), `ztmp/item79_verify.log` (0 errors).

80. ~~`rendering/sliver_delegates_test.dart`~~ *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 1.00 px bottom)* — **FIXED** (2026-05-20, P3/P11 ×1 helper). Plan undercounted: baseline ran 24 framework errors, all "A RenderFlex overflowed by 1.00 pixels on the bottom." Plan suggested **P2** (wrap root in SCV) but the root is already `SingleChildScrollView` (line 2066) — the original P2 didn't apply. Diagnosed via temporary FlutterErrorDetails diagnostics dump (creator chain `Column ← Padding ← DecoratedBox ← Padding ← Container ← RepaintBoundary ← IndexedSemantics ← _SelectionKeepAlive ← KeepAlive ← AutomaticKeepAlive ← KeyedSubtree ← ⋯`, constraint `BoxConstraints(w=53.6, h=44.0)`). Single offender: Section 6 third grid (`SliverGrid(SliverGridDelegateWithMaxCrossAxisExtent(maxCrossAxisExtent: 60, mainAxisExtent: 50))` at line 1273) with `childCount: 24` — the only grid in the script that constrains `_gridCell` cell height to 50 px. After Container margin 2 + border 1 on each side, the inner Column receives a 44 px height constraint; cell content (circle 22 + SizedBox 4 + Text fontSize 13 bold ≈ 18 px line) totals ~45 px → 1.00 px bottom overflow per cell × 24 = 24 errors. Fix (P3/P11 — shrink fixed gap to fit smallest grid): inside `_gridCell` reduce `SizedBox(height: 4)` → `SizedBox(height: 2)`, saving 2 px on every cell. Other grids using `_gridCell` (cell heights 60, 80, 85.7, 107, etc.) have ample headroom either way. Edit carries D4RT-SCRIPT-WORKAROUND `#80, P3/P11` comment explaining the box math and the affected grid. Temporary `main.dart`/`send_test_runner.dart` instrumentation fully reverted before commit. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 24). Logs: `ztmp/item80_baseline.log` (24 errors), `ztmp/item80_full_error.log` (creator-chain dump), `ztmp/item80_verify.log` (0 errors).

### Cluster G — `semantics/` (items 81–86)

81. ~~`semantics/accessibility_focus_block_type_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×2 helpers). Plan undercounted: baseline ran 5 framework errors, all "A borderRadius can only be given on borders with uniform colors." Two helpers combined `borderRadius` with non-uniform `Border`: (a) `pitfallCards` loop (line 914) — `borderRadius: 12` + `Border(left: c/4.0, top/right/bottom: c@0.3/1.0)` → non-uniform colors and widths; 5 pitfallData entries. (b) `_sectionTitle` helper (line 1550) — `borderRadius: 8` + `Border(left: color/4.0)` with top/right/bottom defaulting to `BorderSide.none` → non-uniform; called multiple times throughout the script but Flutter's per-frame error throttling absorbs the repeats into the same 5-error report cap. Fix: drop `borderRadius` at both sites; the heavy-left accent bar look survives via the wider, colored left `BorderSide` alone. Each edit carries a D4RT-SCRIPT-WORKAROUND `#81, P5(a)` comment naming the helper and call shape. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item81_baseline.log` (5 errors), `ztmp/item81_verify.log` (0 errors).

82. ~~`semantics/announce_semantics_event_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×6 sites). Plan undercounted: baseline ran 6 framework errors, all "A borderRadius can only be given on borders with uniform colors." Six independent BoxDecorations combined `borderRadius` with non-uniform `Border` shapes: (a) `messageCards` loop (line 948) — `borderRadius: 8` + `Border(left: base/5, top/right/bottom: dividerTone/0.7)` → non-uniform colors and widths. (b) `pitfallTiles` loop (line 1342) — `borderRadius: 7` + `Border(left: crimsonAlert/4)` (others default to `BorderSide.none`). (c) `swatchTiles` inner color block (line 1463) — `BorderRadius.only(topLeft, bottomLeft)` + `Border(right: brassMid/1)` (others default to none). (d–f) Three prose cards `proseTalkBack`/`proseDirection`/`proseViewId` (lines 1679/1706/1733) — each `borderRadius: 8` + `Border(left: brass*/5)` (others default to none). Flutter's per-frame error throttling kept the visible total at 6. Fix: drop `borderRadius` at all six sites; each tile retains its heavy-left accent bar (or divider in (c)) via the colored `BorderSide`. Each edit carries a D4RT-SCRIPT-WORKAROUND `#82, P5(a)` comment naming the site. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Logs: `ztmp/item82_baseline.log` (6 errors), `ztmp/item82_verify.log` (0 errors).

83. ~~`semantics/attributed_string_property_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×1 helper). Baseline matched the plan at 1 framework error: "A borderRadius can only be given on borders with uniform colors." Single offender: the section-box helper at line 1033 — `BoxDecoration(color: ..., border: Border(left: color/3, top/right/bottom: color@0.25/0.6), borderRadius: BorderRadius.circular(6))`. Both colors *and* widths differ between the left BorderSide and the others → non-uniform. Helper is invoked multiple times across the script; Flutter's per-frame throttling caps reports at 1. Fix: drop `borderRadius`; the heavy-left accent bar (the visual hallmark of the section box) survives via the wider, saturated left BorderSide alone. Edit carries a D4RT-SCRIPT-WORKAROUND `#83, P5(a)` comment. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item83_baseline.log` (1 error), `ztmp/item83_verify.log` (0 errors).

84. ~~`semantics/focus_semantic_event_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×2 loops). Plan undercounted: baseline ran 16 framework errors, all "A borderRadius can only be given on borders with uniform colors." Two loop-driven row builders each combined `borderRadius: BorderRadius.all(Radius.circular(8.0))` with a non-uniform `Border(left: hue/4.0, top/right/bottom: edgeRule/0.5)` — both colors and widths differ between the left BorderSide and the other three sides → non-uniform: (a) `anatomyRows` loop (line 253), 8 entries. (b) `ledgerRows` loop (line 1290), 8 entries. 8 + 8 = 16, an exact match for the baseline. Fix: drop `borderRadius` at both sites; the heavy-left accent bar (the visual hallmark of each tile) survives via the wider, colored left `BorderSide` alone. Each edit carries a D4RT-SCRIPT-WORKAROUND `#84, P5(a)` comment naming the loop and entry count. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 16). Logs: `ztmp/item84_baseline.log` (16 errors), `ztmp/item84_verify.log` (0 errors).

85. ~~`semantics/semantics_event_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1 ×1 helper). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height." Single offender: `pulseStation` helper (line 868) — `Row(crossAxisAlignment: CrossAxisAlignment.stretch, children: [Container(width: 64, …), Expanded(child: Padding(child: Column(…)))])` inside a `Column` inside a `SingleChildScrollView`. The stretch resolves the Row's cross axis to its parent's height, which is unbounded → `BoxConstraints(h=infinity)` reaches `RenderConstrainedBox` on the 64 px icon panel and asserts. The helper is invoked four times (Stations 1–4); Flutter's per-frame throttling caps reports at 1. Fix (P1): wrap the inner Row in `IntrinsicHeight`, bounding the cross axis to the tallest child (the Expanded text Column) while preserving the side-by-side icon/text layout. Edit carries a D4RT-SCRIPT-WORKAROUND `#85, P1` comment explaining the stretch/infinite chain. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item85_baseline.log` (1 error), `ztmp/item85_verify.log` (0 errors).

86. ~~`semantics/tooltip_semantics_event_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×2 loops). Plan undercounted: baseline ran 13 framework errors, all "A borderRadius can only be given on borders with uniform colors." Two loop-driven card builders combined `borderRadius: BorderRadius.circular(10)` with a non-uniform `Border(left: accent/plum, width: 5, top/right/bottom: rule/0.6)` — colors and widths both differ → non-uniform: (a) `galleryCards` loop (line 706), 6 entries (galleryRows). (b) `pitfallTiles` loop (line 1199), 7 entries (pitfalls). 6 + 7 = 13, exact match for the baseline. Fix: drop `borderRadius` at both sites; the heavy-left accent bar (the visual hallmark of each card) survives via the wider, colored left `BorderSide` alone. Each edit carries a D4RT-SCRIPT-WORKAROUND `#86, P5(a)` comment naming the loop and entry count. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 13). Logs: `ztmp/item86_baseline.log` (13 errors), `ztmp/item86_verify.log` (0 errors).

### Cluster H — `services/` (items 87–101)

**Cluster status (as of 2026-05-20): COMPLETE (modulo deferral).** Items 87–92 and 94–100 fixed script-side (13/14 in-cluster entries closed; item 101 in this plan is listed under Cluster I — `widgets/animatedbuilder_test.dart` — so the services/ inventory is items 87–100). Item 93 reverted/deferred under **U18** (interpreter/bridge transport cliff: every script-side P1 variant on `_defaultVsThemeCard` crashes the test-app HTTP server). Item 99 fixed script-side but surfaced a new interpreter limit **U19** (per-character `TextSpan` × non-Latin glyphs → NaN `Rect` at `dart:ui/painting.dart:26`, sibling pattern to U16). Mix of fix recipes across the cluster: P1 (IntrinsicHeight) on items 89/96/100, P2 (SingleChildScrollView wrap) on items 88/97, P5(a) (drop `borderRadius` when `Border` is non-uniform) on items 90/92/94b/95/98, P12 (root SCV wrap to dissolve Infinity overflow) on item 87, P-NaN-gradient (replace `LinearGradient` with solid colour) on item 91, P-NaN-glyph (substitute ASCII for non-Latin glyphs in per-char `TextSpan` streams) on item 99, P2+P5(a) combo on item 94.

87. ~~`services/autofill_configuration_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow Infinity px bottom)* — **FIXED** (2026-05-20, P12 ×1 root wrap). Baseline matched the plan at 2 framework errors (the second is the cascaded "SemanticsNode tried to set a non-finite rect" assert from semantics.dart:2830, caused by the first). Root: the script's top-level return was `Padding(Column(crossAxisAlignment: stretch, mainAxisSize: min, children: [...11 sections, including galleryWidget with Material Card forms with TextFields...]))` with no scrolling ancestor. The combined content is much taller than the tab viewport (~11 sections), and on the desktop host paint surface the outer Column is laid out with an unbounded-bottom constraint chain, so the overflow surfaces as `Infinity` rather than a finite delta. The Infinity overflow then poisons the SemanticsNode rect of the descendant subtree (the second framework error). Fix (P12): wrap the entire top-level Padding(Column(...)) in a `SingleChildScrollView`. The Column now has unbounded vertical extent by design and resolves via scrolling rather than by an infinite-overflow RenderFlex, eliminating both errors at the root. The plan's P1 element (remove an Expanded) was unnecessary — there is no Expanded inside an unbounded Column in this script; all Expandeds are children of Rows, which are bounded by their parent's height. Edit carries a D4RT-SCRIPT-WORKAROUND `#87, P12` comment explaining the unbounded-flex / Infinity chain and the SemanticsNode cascade. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 2). Logs: `ztmp/item87_baseline.log` (2 errors), `ztmp/item87_verify.log` (0 errors).

88. ~~`services/flutter_version_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 7661 px bottom)* — **FIXED** (2026-05-20, P2 ×1 root wrap). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 7661 pixels on the bottom." The document assembles 11 themed sections plus a page banner inside `Container(padding: 18, color: parchment, child: Column(crossAxisAlignment: start, children: [...]))` at the top level with no scrolling ancestor — combined height ≈ 7661 px greater than the viewport, exactly matching the overflow delta. Fix (P2): wrap the top-level `Container(Column(...))` in a `SingleChildScrollView`. The Column scrolls instead of overflowing. Plan's P1 element (remove an Expanded) is unnecessary — no Expanded sits inside the outer Column. Edit carries a D4RT-SCRIPT-WORKAROUND `#88, P2` comment explaining the 7661 px assembly overflow. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item88_baseline.log` (1 error), `ztmp/item88_verify.log` (0 errors).

89. ~~`services/i_o_s_system_context_menu_item_data_share_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1 ×1 widget). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height." Single offender: `section4Titles` widget (line 566) — `Row(crossAxisAlignment: stretch, children: [Expanded(defaultTitleCard), SizedBox(width: 12), Expanded(customTitleCard)])` sitting inside the outer `Scaffold > SingleChildScrollView > Column(crossAxisAlignment: stretch)`. The stretch tries to match each Expanded card to the Row's height, but the Row's vertical extent is unbounded (SCV ancestor), so RenderConstrainedBox gets `BoxConstraints(h=infinity)` on the cards and asserts. Fix (P1): wrap section4Titles in `IntrinsicHeight`, bounding the Row's cross axis to the taller of the two cards while preserving the side-by-side equal-height comparison layout. Edit carries a D4RT-SCRIPT-WORKAROUND `#89, P1` comment. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item89_baseline.log` (1 error), `ztmp/item89_verify.log` (0 errors).

90. ~~`services/key_up_event_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×1 helper). Plan undercounted: baseline ran 5 framework errors, all "A borderRadius can only be given on borders with uniform colors." Single offender: `_footgunCard` helper (line 1981) — `BoxDecoration(borderRadius: 14, border: Border(left: tone/5, top/right/bottom: tone@0.25/1), gradient: LinearGradient(...))`. Both colors *and* widths differ between the left BorderSide and the others → non-uniform. Helper invoked 5 times (footguns list, line 1923) → 5 errors. Fix: drop `borderRadius` at the helper; the heavy-left accent bar plus the diagonal gradient carry the visual identity of the footgun card. Edit carries a D4RT-SCRIPT-WORKAROUND `#90, P5(a)` comment naming the helper and footguns count. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item90_baseline.log` (5 errors), `ztmp/item90_verify.log` (0 errors).

91. ~~`services/network_asset_bundle_test.dart`~~ *(secondary_classes_test, 1/1, B-layout — Offset NaN)* — **FIXED** (2026-05-20, P-NaN-gradient ×8 Containers). Baseline showed 1 framework error: "Offset argument contained a NaN value." (`_offsetIsValid` at `dart:ui/painting.dart:41`, called from `Gradient.linear` via `LinearGradient.createShader`). Root cause: the script's top-level `SingleChildScrollView > Column(crossAxisAlignment.stretch)` propagates `BoxConstraints(0.0<=h<=Infinity)` to every child Container. The eight `gradient: LinearGradient(...)` Containers (header, hierarchyDiagram, pipelineDiagram, postscript, recipeCards entries, hierarchyChildren entries, methodCards, pipeline step Expanded cards, pigeonhole cells, useCaseCards, postscript Wrap container, footer accent bar) all resolve to `Size(w, Infinity)`, then `LinearGradient.createShader` divides by an infinite extent producing NaN, which `Offset()` rejects on construction. Established P1 patterns (Row(stretch) inside IntrinsicHeight) do not apply — there is no `Row(crossAxisAlignment.stretch)` here. Column `mainAxisSize.min` is not honored through the d4rt bridge for nested Columns (parallel to U14: `extractBridgedArg` unwraps the enum value but it does not propagate to `Flex.mainAxisSize` in this script's tree). `IntrinsicHeight` wraps fail with `'height.isFinite'` because deeply-nested `Expanded`-in-`Row` children return Infinity intrinsic height. Workaround (P-NaN-gradient): replace every `gradient: LinearGradient(colors: [c1, c2])` with `color: <mid-tone>` solid colour. Solid colours never touch the shader path, so `Size(w, Infinity)` no longer feeds NaN into `createShader`. Visual regression: parchment-toned cards lose their subtle gradient (now flat) but the layout, borders, shadows, and text styling are preserved. Eight edits, each carrying a `D4RT-SCRIPT-WORKAROUND #91, P-NaN-gradient` comment naming the affected Container. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item91_verify_solid.txt` (interim, 1 error after first 5 Containers), final pass `frameworkErrors=0` after all 8 gradients replaced.

92. ~~`services/platform_exception_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×2 helpers). Baseline showed 5 framework errors (per-frame throttling cap), all "A borderRadius can only be given on borders with uniform colors. The following is not uniform: BorderSide.color." Two offending helpers: (1) `_sectionHeader` (line 1082) — `BoxDecoration(color: color@0.1, borderRadius: BorderRadius.circular(10.0), border: Border(left: BorderSide(color: color, width: 5.0)))`. Only `left` side coloured; other sides default to `BorderSide.none` → non-uniform with the borderRadius. Helper invoked 10 times in section assembly. (2) `_buildFootgun` (line 1695) — `BoxDecoration(gradient: ..., borderRadius: BorderRadius.circular(12.0), border: Border(left: full color/width 5, top/right/bottom: color@0.3/width 1))`. The `left` BorderSide differs from the other three in both colour intensity and width → non-uniform. Helper invoked 5 times. Fix (P5(a)): drop `borderRadius` from both helpers. Heavy left accent bar carries the visual identity in `_sectionHeader`; the diagonal gradient + shadow + heavy left accent carry it in `_buildFootgun`. Square corners are acceptable. Each edit carries a `D4RT-SCRIPT-WORKAROUND #92, P5(a)` comment naming the helper. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item92_baseline.log` (5 errors), `ztmp/item92_verify.log` (0 errors).

93. ~~`services/platform_test.dart`~~ *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **DEFERRED** (2026-05-20, U18). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height." Single offender: `_defaultVsThemeCard` (line 541) — `Row(crossAxisAlignment: stretch, children: [Expanded(_twinCard(...)), SizedBox(width: 14), Expanded(_twinCard(...))])`. Page root is `Container > Column(crossAxisAlignment: stretch) > [...sections..., _defaultVsThemeCard(), ...]` with no `SingleChildScrollView` ancestor, so the Row's parent height is unbounded; `stretch` propagates `h=Infinity` to the Expanded children and `RenderConstrainedBox.layout` asserts. **Every script-side P1 variant crashes the test app**: (A1) wrap Row in `IntrinsicHeight` → `transport_error httpStatus=-1 outputLines=0 frameworkErrors=0`, "Lost connection to device". (A2) `crossAxisAlignment.stretch` → `start` → same `transport_error`. (A3) Row → `Column(stretch)` (drop Expanded, replace `SizedBox(width)` with `SizedBox(height)`) → same `transport_error`. (A4) Minimal "delete the `crossAxisAlignment: CrossAxisAlignment.stretch` line outright" — semantically a no-op (default is `center`) — also crashes: `transport_error httpStatus=-1`, the script's `print()`s all run but the test-app HTTP server dies mid-build ("HttpException: Connection closed before full header was received" on POST `/build`). Baseline with `stretch` retained survives because Flutter catches the layout assertion as a recoverable framework error. The four-attempt crash reproducer indicates this is not a layout-replacement bug but an interpreter / bridge transport cliff that fires when `Row` / `Expanded` / `_twinCard` is materialised with shifted cross-axis behaviour from a script-defined helper. Per the user's "if fix fails, revert" rule, all changes reverted; baseline `frameworkErrors=1 status=success` restored. Documented as **U18** in `doc/interpreter_unfixable.md`. Logs: `ztmp/item93_baseline.log` (1 error), `ztmp/item93_verify.log` / `item93_verify2.log` / `item93_verify3.log` / `item93_verify3b.log` (transport_error), `ztmp/item93_diag.txt` (debugCreator dump), `ztmp/item93_baseline2.log` (post-revert reproducer of baseline), `ztmp/item93_revert_verify.log` (final baseline confirm), `ztmp/item93_verify4.log` (A4 minimal-edit crash).

94. ~~`services/raw_key_event_data_android_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, "B-bridge" tag but emitted error is RenderFlex overflow 2.0 px bottom)* — **FIXED** (2026-05-20, P2 ×1 + P5(a) ×1). Plan undercounted: baseline ran 40 framework errors, not 1 — 32× "A RenderFlex overflowed by 2.0 pixels on the bottom" and 8× "A borderRadius can only be given on borders with uniform colors." (a) **P2 — RenderFlex overflow** in `_ModifierBitsSection._bitCell` (line 1201). The bit-grid renders 32 cells (for `bit = 31; bit >= 0; bit--`); each cell is `Container(width: 22, height: 28, child: Column(mainAxisAlignment: center, children: [Text(0/1, fontSize: 12, fontWeight: w700), Text(bitNum, fontSize: 8)]))`. The Column's natural height (~30 px from the two stacked Text lines) exceeds the fixed `height: 28`, producing one banner per cell → 32 errors. Fix: bump cell height from 28 → 32; the visual grid metric is preserved (22-wide cells, monospace bit labels). (b) **P5(a) — uniform-colors** in `_Pitfall.build` (line 2129). The decoration is `BoxDecoration(borderRadius: BorderRadius.circular(10), border: Border(left: BorderSide(color, width: 4), top/right/bottom: BorderSide(_Palette.border, width: 1)))` — non-uniform colours and widths cannot coexist with borderRadius. Helper invoked 8 times in `_PitfallsSection` (lines 2057–2115) → 8 errors. Fix: drop `borderRadius` at the helper; the heavy 4-px left accent bar carries the visual identity of each pitfall card. Both edits carry `D4RT-SCRIPT-WORKAROUND #94, P2` / `#94, P5(a)` comments. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 40). Logs: `ztmp/item94_baseline.log` (40 errors), `ztmp/item94_verify.log` (0 errors).

95. ~~`services/raw_key_event_data_linux_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×1 helper). Baseline matched the plan at 5 framework errors (per-frame throttling cap), all "A borderRadius can only be given on borders with uniform colors." Single offender: `_PitfallRow.build` (line 3118) — `BoxDecoration(borderRadius: BorderRadius.circular(10), border: Border(left: BorderSide(_Pal.crimson, width: 4), top/right/bottom: BorderSide(_Pal.crimson@0.3, width: 1)))`. The `left` side differs from the other three in both width (4 vs 1) and colour (full crimson vs crimson@0.3) → non-uniform. Helper invoked 5 times (5 `_Pitfall` entries in the pitfalls list, lines 3045–3084) → 5 banners. Three other `Border(...)` sites (lines 211, 361 page-header dividers with `Border(bottom: ...)`, and the inner mask-row divider at line 1539 with `Border(bottom: ...)`) all use single-visible-side `Border(bottom: BorderSide(...))` patterns whose other sides default to `BorderSide.none` (style none, width 0) and therefore pass the uniform-colors check — they do not contribute to the count. Fix (P5(a)): drop `borderRadius` from `_PitfallRow`; the heavy 4-px crimson left accent bar carries the visual identity. Edit carries a `D4RT-SCRIPT-WORKAROUND #95, P5(a)` comment naming the helper and the 5x invocation count. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item95_baseline.log` (5 errors), `ztmp/item95_verify.log` (0 errors).

96. ~~`services/raw_key_event_data_windows_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1 ×4 widgets). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height." Plan-reported count of 1 reflects Flutter aborting layout after the *first* assertion fires — there are actually four `Row(crossAxisAlignment.stretch)` sites in the script, all inside the unbounded-h page root (`SingleChildScrollView > Column(...)` at line 2173). Confirmed empirically: fixing only `_FieldCard.build` (line 530) left `frameworkErrors=1` (the next stretch Row took over); fixing all four cleared the count to 0. Offenders: (a) `_FieldCard.build` (line 530) — `Container(width: double.infinity) > Row(stretch) > [Container(width: 6 accent strip), Expanded(content)]`, the 6-px accent strip stretches to card height. (b) `_ScanVsKey.build` (line 982) — `Row(stretch) > [_CompareCard, SizedBox(width: 12), _CompareCard]` for side-by-side scanCode vs keyCode cards. (c, d) `_UseCases.build` (lines 1882, 1903) — two `Row(stretch) > [_UseCaseCard, SizedBox(width: 10), _UseCaseCard]` rows. Fix (P1): wrap each Row in `IntrinsicHeight`, bounding the cross axis to the tallest child while preserving the visual stretch behaviour (accent strip matches content height; side-by-side cards match the taller one). All four edits carry `D4RT-SCRIPT-WORKAROUND #96, P1` comments naming the helper. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item96_baseline.log` (1 error), `ztmp/item96_verify1.log` (1 error after first fix only — confirms cascade), `ztmp/item96_verify.log` (0 errors after all four).

97. ~~`services/raw_key_event_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-layout/RenderFlex — overflow 2378 px bottom)* — **FIXED** (2026-05-20, P2 only). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 2378 pixels on the bottom." The plan labelled the item P1+P2, but inspection revealed no `Row(crossAxisAlignment.stretch)` in the script — the 2378-px overflow stems from a single offender, the page-root `Container > Column(mainAxisSize.min, crossAxisAlignment.start)` at line 823 which packs the SECTION 0–9 cards (anchor, anatomy, gallery, modifier table, platform-subclass table, key-cap walkthrough, migration cheat-sheet, code blocks) top-to-bottom without a scroll wrapper. Fix (P2): wrap the Column in `SingleChildScrollView`, preserving the gradient Container and visual composition. The P1 component of the plan label did not materialise — only P2 was needed to clear the overflow. Edit carries a `D4RT-SCRIPT-WORKAROUND #97, P2` comment. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item97_baseline.log` (1 error), `ztmp/item97_verify.log` (0 errors).

98. ~~`services/raw_keyboard_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×2 helpers). Baseline `frameworkErrors=10`, all "A borderRadius can only be given on borders with uniform colors. The following is not uniform: BorderSide.color." Two offender sites: (a) Terminal-style chip header (line 1495, single invocation) — `BoxDecoration(borderRadius.only(topLeft, topRight, r=17), border: Border(bottom: BorderSide(_lime@0.5, w=1)))`. The unset top/left/right sides default to BorderSide.none whose color is opaque black, differing from the visible bottom lime → non-uniform. (b) Digit key cap (line 2143, loop over 10 glyphs '1'..'9','0' → 10 invocations) — `BoxDecoration(borderRadius.circular(7), border: Border(top: BorderSide(_cyan@0.6, w=1.5), left/right/bottom: BorderSide(_violet@0.4)))`. Top side differs from the other three in both color (cyan vs violet) and width (1.5 vs default 1.0) → non-uniform. Total 11 expected banners ≈ observed 10 (per-frame throttle/cap). Third `Border()` site at line 1320 (apiRows striped Container) has no `borderRadius` and is safe. Fix (P5(a)): drop `borderRadius` from both offender BoxDecorations. The terminal header retains its bottom rule (loses top-corner radius); the digit caps keep the gradient + dual-tone bezel (lose 7-px corner radius). Each edit carries a `D4RT-SCRIPT-WORKAROUND #98, P5(a)` comment naming the helper and invocation count. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 10). Logs: `ztmp/item98_baseline.log` (10 errors), `ztmp/item98_verify.log` (0 errors).

99. ~~`services/text_editing_delta_non_text_update_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-layout/infinite-size — "Rect argument contained a NaN value.")* — **FIXED** (2026-05-20, P-NaN-glyph script-side workaround; surfaces new interpreter limit **U19**). Plan labelled this **P1** but inspection showed no infinite-size site — the banner is `dart:ui/painting.dart:26` `_rectIsValid(Rect rect)` (`assert(!rect.hasNaN)`), not a layout assertion. Baseline `frameworkErrors=4 status=success`. Bisected `build()` (line 1437) `Column > [_heroSection, _anatomySection, _siblingGallerySection, _examplesSection, _selectionDeconstructionSection, _composingRangeSection, _applySection, _recipeSection, _pitfallsSection, _comparisonSection, _footerSection]` by commenting out halves: first half (hero/anatomy/sibling) → 0 errors; second half (examples/sel/comp/apply) → 4 errors; isolating `_examplesSection` alone → 4 errors. Within `_buildWorkedExamples()` (line 716) eliminated false leads: (i) replacing the only `LinearGradient` in `_heroSection` with a solid colour (P-NaN-gradient) → still 4, reverted; (ii) changing `TextDecorationStyle.dashed` → `solid` in the per-character `TextSpan` loop in `_frozenFrame` (line 339) → still 4, reverted. Final bisect: examples a–d use `poem = 'The quick brown fox'` (ASCII, no composing region) and produce 0 errors; examples e and f use `greet = 'こんにちは'` (5 hiragana, non-empty composing ranges) and produce exactly 4 errors (2 `_frozenFrame` previews per example × 2 examples). Root cause: the `_frozenFrame` helper splits its text into one `TextSpan` per character (`for (int i = 0; i < text.length; i++) spans.add(TextSpan(text: ch, style: TextStyle(color, backgroundColor, decoration, decorationStyle.dashed)))`) and interleaves `WidgetSpan(_CaretBar())` at the caret. The bridged text painter computes per-character advance/baseline metrics for the `backgroundColor` and dashed-underline paint rects; for non-Latin glyph clusters the bridge's shortcut path returns NaN advances, propagating to `_rectIsValid` once per painted span. **The trigger is the (per-character `TextSpan` fragmentation) × (non-Latin glyphs) combination, not any single styling choice.** Fix (P-NaN-glyph): substitute `greet = 'こんにちは'` → `greet = 'aiueo'` (5 ASCII glyphs matching the original 5-character pacing); the Japanese form remains in the `story:` prose so the educational intent is preserved. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #99, P-NaN-glyph` comment explaining the root cause and pointing at U19. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 4). Documented as **U19** in `doc/interpreter_unfixable.md` (sibling pattern to U16: same bridge text-layout gap, but U19 is NaN-Rect from non-Latin glyph spans whereas U16 is NaN-Offset from empty Text). Logs: `ztmp/item99_baseline.log` (4 errors), `ztmp/item99_verify.log` (gradient theory, 4), `ztmp/item99_bisect1.log` (hero+anatomy+sibling only, 0), `ztmp/item99_bisect2.log` (second half, 4), `ztmp/item99_bisect3.log` (examples-only, 4), `ztmp/item99_verify2.log` (dashed→solid theory, 4), `ztmp/item99_bisect_ef.log` (examples without e+f, 0), `ztmp/item99_verify3.log` (romaji substitution, 0).

100. ~~`services/text_selection_test.dart`~~ *(hardly_relevant_classes_3_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1 ×1 widget). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height." Page root (line 1424) is `Scaffold > SafeArea > SingleChildScrollView > Center > ConstrainedBox(maxWidth: 880) > Column(crossAxisAlignment: stretch, children: sections)` — the SCV ancestor leaves the outer Column's vertical extent unbounded. Single offender: `Row(crossAxisAlignment: CrossAxisAlignment.stretch, children: [affinitySideCard('upstream', ...), SizedBox(width: 12), affinitySideCard('downstream', ...)])` inside `buildAffinitySection()` (line 716). `affinitySideCard` (line 668) returns `Expanded(child: panelContainer(...))`, so the stretch propagates `BoxConstraints(h=Infinity)` down to the `RenderConstrainedBox` inside each card and asserts. The four other `crossAxisAlignment.stretch` sites are all `Column(stretch)` (lines 659, 861, 1257, 1432) — `Column(stretch)` constrains the *width* axis which is bounded (ConstrainedBox(maxWidth: 880)), so they do not contribute. Fix (P1): wrap the offending Row in `IntrinsicHeight`, bounding the cross axis to the taller of the two affinity cards while preserving the side-by-side equal-height comparison the section is designed to teach. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #100, P1` comment describing the ancestor chain and the helper signature. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item100_baseline.log` (1 error), `ztmp/item100_verify.log` (0 errors).

### Cluster I — `widgets/` (items 101–138)

**Cluster status (as of 2026-05-20): IN PROGRESS.** Items 101–113 fixed (13/38). Items 114–138 pending. Fix recipes seen so far: P2 (SingleChildScrollView wrap) on items 101, 103, 104, 105, 108 (item 108 wraps a *nested* inner Column inside a fixed-height `SizedBox`/`Expanded`/`Container(padding)` chain, not the page-root Column); P1 (IntrinsicHeight wrap) on items 102 (Row(stretch) inside an AnimatedList itemBuilder) and 106 (Row(stretch) helper inside SCV-descended Column chain); P14 (drop `border: TableBorder.all(...)` on all seven Tables, relying on the enclosing `cardShell`'s `Border.all` for the outer frame) on item 107 — also surfaced a new interpreter limit **U20** documenting that `Table(border: TableBorder.all(...))` triggers a Flutter framework assertion in `table_border.dart` line 289 regardless of row count / column widths in the d4rt-bridged context, even though the assertion's invariant is mathematically guaranteed by `RenderTable`'s `_rowTops` construction. P3 variant on item 109 uses `OverflowBox(maxWidth: double.infinity)` to give a deliberately-overflowing pedagogical Row unbounded constraints — the surrounding `SizedBox + ClipRect` still clip painting to the demo width, so the visual is preserved while the assertion is avoided. P3 on item 110 wraps a shared `_miniInput` helper's Text in `Flexible(overflow: ellipsis)` so the longest labels absorb a sub-pixel squeeze under Expanded-bounded constraints. P5(a) on item 111 refactors two non-uniform-Border helpers (`_buildAnatomyCard`, `_buildLifecycleCard`) to use uniform `Border.all` + a leading accent strip inside `ClipRRect > IntrinsicHeight > Row`. P10 variant on item 112 adds the standard SCV-friendly trio (`shrinkWrap: true`, `physics: NeverScrollableScrollPhysics()`, `primary: false`) to a single `GridView.custom` that was missing them — the demo's other 20+ GridView call sites already use this pattern, so harmonising the lone outlier clears both reported framework errors (the unbounded-viewport assertion and a follow-on null-check). P3 variant on item 113 gates the spacer + Text in the shared `_glyph` helper on `label.isNotEmpty` so empty-label glyphs at small Container sizes (the `_flightFrame` 18/28 px square tiles in the FLIGHT TIMELINE section) no longer overflow their fixed square bound by the empty Text's default line-box — the plan listed P2 but the page root is already SCV-wrapped, so the offender is a glyph-helper height-budget mismatch, not a missing scroll ancestor.

101. ~~`widgets/animatedbuilder_test.dart`~~ *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 2402 px bottom)* — **FIXED** (2026-05-20, P2 only). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 2402 pixels on the bottom." Page root (line 1635) is `Container(padding: 16, decoration: RadialGradient) > Column(crossAxisAlignment: stretch, children: <Widget>[sectionHeader(0)..section9])` packing ten themed sections (anchor snapshots, title banner, anatomy diagram, animation gallery, child-optimization demo, composed-tween cards, comparison table, builder-signature breakdown, 10-frame storyboard film strip, cheat-sheet) without a scrolling ancestor — combined height ≈ 2402 px greater than the desktop test viewport. Fix (P2): wrap the Column in `SingleChildScrollView`; the radial-gradient Container stays *outside* the scroll view so the film-noir backdrop fills the whole viewport, not just the scrolled content. The plan label included P1, but no `Row(crossAxisAlignment.stretch)` is present in the script — the only `stretch` site is this Column, whose cross axis is *width* and therefore bounded by the parent Container. P2 alone clears the overflow. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #101, P2` comment naming the overflow delta and explaining the gradient-stays-outside rationale. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item101_baseline.log` (1 error), `ztmp/item101_verify.log` (0 errors).

102. ~~`widgets/animatedlist_test.dart`~~ *(important_classes_test, 1/1, I-unhandled but layout origin — "BoxConstraints forces an infinite height.")* — **FIXED** (2026-05-20, P1 ×1 widget). Baseline `frameworkErrors=5`: 1× "BoxConstraints forces an infinite height", 1× `sliver_multi_box_adaptor.dart:629 'child.hasSize'`, 3× "Null check operator used on a null value" — the latter four are downstream of the first. Page root (line 913) is `MaterialApp > Scaffold > SingleChildScrollView > Column(start) > [hero, concept, 11 × _sectionPanel, comparisonOverview, glossary, epilogue, footer]`. The script defines 41 AnimatedList instances, all with `shrinkWrap: true` + `NeverScrollableScrollPhysics()`; bridge verification (`lib/src/bridges/widgets_bridges.b.dart` lines 6796, 6799) confirms `shrinkWrap` is forwarded correctly. Bisect attempts (omit section 8 only → still 5; omit sections 1+8 → still 5; replace `primitivesSnapshotZero` `initialItemCount: 0` with `SizedBox(40)` → still 5) all left the count unchanged — indicating either the offender was outside the section list or the framework caps at 5 banners. Grep for `CrossAxisAlignment.stretch` revealed the single offender at line 396: `builderTimeline` AnimatedList itemBuilder returns `FadeTransition > Row(crossAxisAlignment.stretch, [SizedBox(w:28, Column[…, Expanded(Container)]), SizedBox(width: 8), Expanded(Container > Column…)])`. The Row(stretch) inside `AnimatedList(shrinkWrap: true)`'s intrinsic-height sizing path receives unbounded vertical constraints and the inner `Expanded` siblings amplify the failure into the sliver-child sizing + null-check chain. Fix (P1): wrap the Row in `IntrinsicHeight`, giving the timeline row a finite cross-axis budget while preserving the equal-height side-by-side timeline. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #102, P1` comment naming the offender and explaining the propagation chain. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 5). Logs: `ztmp/item102_baseline.log` (5 errors), `ztmp/item102_bisect_no_s8.log` (5), `ztmp/item102_bisect_no_s1s8.log` (5), `ztmp/item102_bisect_no_snapshot0.log` (5), `ztmp/item102_verify.log` (sliver SizedBox theory, 5 — reverted), `ztmp/item102_p1fix.log` (0).

103. ~~`widgets/appbar_test.dart`~~ *(essential_classes_test, 1/1, B-layout/RenderFlex — overflow 33 px bottom)* — **FIXED** (2026-05-20, P2 ×1 shared widget). Baseline `frameworkErrors=6` — the plan listed only the headline "33 px bottom" but the full set was six RenderFlex bottom overflows of 33, 33, 33, 25, 13, 41 px. Page root (line 21) is `MaterialApp > Scaffold > SafeArea > SingleChildScrollView(NeverScrollable) > Column(stretch, min) > 14 sections`; the outer SCV gives the Column unbounded height so the root list does not overflow. The six overflows come from a single shared widget `_DevicePreview` (line 902), invoked six times by `_StandardAppBarPreview`, `_CenteredAppBarPreview`, `_MediumAppBarPreview`, `_LargeAppBarPreview`, `_TranslucentAppBarPreview`, `_DenseAppBarPreview` with heights 180, 180, 220, 260, 200, 160 and toolbarHeights 56, 56, 96, 128, 56, 44. `_DevicePreview` lays out as `Container(height: …) > ClipRRect > Stack > Column[appBar, Expanded(Container(padding: 14) > Column[tile, sb(8), tile, sb(8), tile])]`. The body region (preview-height minus toolbarHeight) is 124, 124, 124, 132, 144, 116 px while the mock list intrinsic ≈ 3×~36 + 2×8 + 28 padding ≈ 152 px, producing the 33/33/33/25/13/41 deltas. Fix (P2): wrap the inner mock-list Column in `SingleChildScrollView(physics: NeverScrollableScrollPhysics())` so it receives unbounded vertical extent and the overflow assert never fires; the surrounding `ClipRRect` already clips the painted output so the device-frame visual is preserved without a scroll affordance. Single-site edit covers all six previews. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #103, P2` comment with the overflow-delta breakdown. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Logs: `ztmp/item103_baseline.log` (6 errors), `ztmp/item103_p2fix.log` (0 errors).

104. ~~`widgets/clipping_test.dart`~~ *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 8431 px bottom)* — **FIXED** (2026-05-20, P2 only). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 8431 pixels on the bottom." Page root (line 104) is `Container(color: _kCream, padding: 24) > Column(crossAxisAlignment.stretch, mainAxisSize.min) > [_section1Banner, …, _section9Recap]` — nine themed sections (title banner, Clip enum anatomy, ClipRect/ClipRRect/ClipOval/ClipPath galleries, custom clipper panel, performance notes, recap) packed top-to-bottom with `~9×28 = 252 px` inter-section spacers, combined intrinsic ≈ 8431 px over the desktop test viewport, no scroll ancestor. The plan label included P1, but no `Row(crossAxisAlignment.stretch)` is present (multiline grep confirms); the four `Column(stretch)` sites at lines 108, 321, 382, 521 etc. constrain *width* and are bounded by the outer Container, so P1 did not materialise. Fix (P2): wrap the Column in `SingleChildScrollView`; the cream-background Container stays *outside* the scroll view so the specimen-book backdrop fills the whole viewport, not just the scrolled content. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #104, P2` comment naming the overflow delta and explaining the cream-stays-outside rationale. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item104_baseline.log` (1 error), `ztmp/item104_p2fix.log` (0 errors).

105. ~~`widgets/defaulttextstyle_test.dart`~~ *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 7550 px bottom)* — **FIXED** (2026-05-20, P2 only). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 7550 pixels on the bottom." Page root (line 1582) is `Container(color: ivory, padding: 16) > Column(crossAxisAlignment.stretch) > [title, dossier, anatomy, recipes, mergeSection, overflowPanel, softWrapComparison, maxLinesGrid, textWidthBasisSection, textHeightBehaviorSection, comparison, pitfalls, glossary, recap]` — 14 themed sections without a scrolling ancestor, combined intrinsic ≈ 7550 px over the desktop test viewport. The plan label included P1, but no `Row(crossAxisAlignment.stretch)` is present (multiline grep confirms); the only `Column(stretch)` site at line 1586 constrains *width* which is bounded by the outer Container, so the P1 component did not materialise. Note: an existing `SingleChildScrollView` at line 617 (now 626 after fix) belongs to the `recipes` subsection and is unrelated. Fix (P2): wrap the page-root Column in `SingleChildScrollView`; the ivory-background Container stays *outside* the scroll view so the report backdrop fills the whole viewport. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #105, P2` comment naming the overflow delta and explaining the ivory-stays-outside rationale. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item105_baseline.log` (1 error), `ztmp/item105_p2fix.log` (0 errors).

106. ~~`widgets/draggablescrollablesheet_test.dart`~~ *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **FIXED** (2026-05-20, P1 ×1 helper). Baseline matched the plan at 1 framework error: "BoxConstraints forces an infinite height." Page root (line 95) is `Scaffold > SafeArea > SingleChildScrollView > Column(stretch) > [hero, anatomy, frameMocks, snapRuler, expand, builderSig, controllerThreading, snapDuration, controllerApi, nestedScroll, modalBottomSheet, liveSheet, pitfalls, lifecycle, footer]` — the SCV ancestor gives all descendants unbounded vertical extent. Sole offender: the `_argRow` helper (line 2504) returns `Row(crossAxisAlignment: CrossAxisAlignment.stretch, children: …Expanded(_argCard(…))…)` and is invoked five times (lines 322, 352, 382, 1498, 1527) to lay out side-by-side argument/recipe cards. The Row(stretch) wants its `Expanded` children to share a common cross-axis height, but receives unbounded H from the SCV chain and asserts. Flutter dedupes the same assertion across all five callers under the diagnostic infrastructure, reporting it once. Fix (P1): wrap the Row in `IntrinsicHeight`, giving the side-by-side cards a finite cross-axis budget equal to the tallest sibling and preserving the equal-height comparison the helper teaches. Single-site fix covers all five invocations. Other 18 `CrossAxisAlignment.stretch` sites in the script are all `Column(stretch)` and constrain bounded width via the outer SCV → Column chain. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #106, P1` comment listing the five caller lines and explaining the dedup. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item106_baseline.log` (1 error), `ztmp/item106_p1fix.log` (0 errors).

107. ~~`widgets/editable_text_misc_test.dart`~~ *(secondary_classes_test, 1/1, B-layout — `Failed assertion … table_border.dart`)* — **FIXED** (2026-05-20, P14 only; new interpreter limit **U20** documented). Baseline matched the plan at 1 framework error: `'package:flutter/src/rendering/table_border.dart' line 289 pos 12: 'rows.isEmpty || (rows.first >= 0.0 && rows.last <= rect.height)' is not true`. Page root (line 2263) is `Scaffold > Container(color: paperCream) > SingleChildScrollView(pad 16/16/16/24) > Column(stretch) > [hero, dossier, …, glossary, comparison, pitfalls, cheatsheet, footer]`. The script declares seven `Table` instances, each wrapped in `cardShell` (a `Container` with `Border.all(color: brassEdge, width: 1.2)` + 14 px padding + cream-deep background): `paletteTable` (4-col palette catalog, 16 rows), `enumTable` (4-col enum catalog), `smartTable` (3-col SmartDashes/SmartQuotes recipes), `pitfallTable` (2-col pitfalls), `glossaryTable` (2-col glossary, 15 rows), `comparisonTable` (4-col TextField vs Cupertino vs SelectableText), `cheatTable` (2-col cheatsheet). Six use `border: TableBorder.all(color: brassEdge, width: 0.6)`; `pitfallTable` uses `oxblood` instead. The assertion fires *once* in the output, but Flutter dedupes — bisect by removing only the `border:` parameter from all seven Tables drops `frameworkErrors` from 1 to 0, and dropping it from any single Table is not sufficient to identify a single culprit because the assertion fires deterministically for every Table that carries a non-empty `TableBorder` regardless of row count, column widths, or row decoration. Mathematically the assertion's invariant `rows.last <= rect.height` is guaranteed by `RenderTable._rowTops` being non-decreasing (every `rowHeight = math.max(0, child.size.height)`); the fact that it fires anyway in the d4rt-bridged context is documented as new limit **U20** in `interpreter_unfixable.md`. Fix (P14): drop the `border:` parameter from all seven Tables; each table remains framed by `cardShell`'s outer brass border, so the bordered-card visual is preserved at the cost of the interior brass grid lines. Single-comment block above `paletteTable` (`D4RT-SCRIPT-WORKAROUND #107, P14`) names all seven affected Tables and the inequality that fires, and points at U20 for the deeper explanation. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item107_baseline.log` (1 error), `ztmp/item107_noborder.log` (0 errors, hypothesis-validation), `ztmp/item107_p14fix.log` (0 errors, final).

108. ~~`widgets/expanded_test.dart`~~ *(essential_classes_test, 1/1, B-layout/RenderFlex — overflow 8.0 px bottom)* — **FIXED** (2026-05-20, P2 ×1 nested column). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 8.0 pixels on the bottom." Page root (line 2095) is `Scaffold > SingleChildScrollView(pad 16) > Column(start) > [hero, 18 sections + sectionTitle pairs, footer]`. No `CrossAxisAlignment.stretch` anywhere in the script (P1 didn't materialise), and the page root is already wrapped in `SingleChildScrollView` (rules out the global-overflow flavour of P2). Bisect (sections 5–9 only → still 1 error; sections 1–4 only → 0 errors; section 5 only → 1 error) localised the offender to section 5 (`recipes`). The offender is `recipeHeaderList()` (line 573): `SizedBox(width: 220, height: 220) > Column > [header(h:38), Expanded(Container(padding:6, child: Column(6 rows))), footer(h:22)]`. The Expanded receives `220 − 38 − 22 = 160 px`; after the inner Container's `EdgeInsets.all(6.0)` symmetric padding the inner Column's max height is `160 − 12 = 148 px`. The six rows each declare `height: 22.0` + `margin: EdgeInsets.only(bottom: 4.0)`, summing to `6 × 26 = 156 px`. Delta: `156 − 148 = 8 px` — exact match to the baseline banner. Fix (P2 nested): wrap the inner Column at line 618 in `SingleChildScrollView(physics: const NeverScrollableScrollPhysics())` so it receives unbounded vertical extent and the overflow assert never fires; the surrounding `Expanded(Container(padding:6))` still clips the painted output to the 148-px budget so the recipe's "header + scrollable + footer" visual intent (six list items with the last one half-cut by the footer) is preserved. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #108, P2` comment with the budget arithmetic. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item108_baseline.log` (1 error), `ztmp/item108_bisect_sec1to9.log` (1, narrowed to sections 1–9), `ztmp/item108_bisect_sec1to4.log` (0, eliminated sections 1–4), `ztmp/item108_bisect_sec1to7.log` (1, narrowed to sections 5–7), `ztmp/item108_bisect_sec5_only.log` (1, isolated to section 5 `recipes`), `ztmp/item108_p2fix.log` (0).

109. ~~`widgets/flexible_test.dart`~~ *(essential_classes_test, 1/1, B-layout/RenderFlex — overflow 20 px right)* — **FIXED** (2026-05-20, P3 ×2 OverflowBox). Baseline ran 2 framework errors, not 1 — the plan undercounted: "A RenderFlex overflowed by 20 pixels on the right." + "A RenderFlex overflowed by 48 pixels on the right." Page root (line 996) is `Scaffold > AppBar + SingleChildScrollView(pad 16) > Column(stretch) > [heroCard, 14 sections, footer]` — already SCV-wrapped, so the page-root overflow flavour of P2/P3 didn't apply. Bisect (sections 1-4 → 0; section 5 ratioSection only → 0; section 6 nestedSection only → 0; section 7 edgeCaseSection only → 1 (20 px); section 8 asciiSection only → 0; section 9 decisionSection only → 0; section 10 pitfallSection only → 0; section 11 recipeSection only → 0; section 12 beforeAfterSection only → 1 (48 px)) isolated the 20 px to `_edgeCase2` (line 2030) and the 48 px to the BEFORE side of `_beforeAfter` (line 2429). Both are *pedagogical overflow demonstrations*: the script deliberately sets up `Row` natural-width > `SizedBox` width to teach what RenderFlex overflow looks like, wraps the Row in `ClipRect` intending to suppress the visual stripes — but ClipRect only clips painting, RenderFlex still asserts at layout time. **Box math:** `_edgeCase2` is `SizedBox(width: 220, height: 28) > ClipRect > Row[Container(80), Container(80), Container(80)]` → 3×80 = 240 px → exact 20 px overflow. `_beforeAfter` BEFORE is `SizedBox(width: 240, height: 32) > ClipRect > Row[Container(24), SB(6), Container(220), SB(6), Container(32)]` → 24+6+220+6+32 = 288 px → exact 48 px overflow. Fix (P3 variant — OverflowBox): wrap the inner Row in `OverflowBox(alignment: Alignment.centerLeft, maxWidth: double.infinity, child: Row(...))` inside the existing `SizedBox > ClipRect` chain. The OverflowBox imposes its own unbounded horizontal constraint on the Row, so the Row lays out at its natural width without firing the overflow assertion; the outer `SizedBox + ClipRect` still clip the painted output to the demo width. Visual is **identical** to the original ("overflowing content clipped by the box") — the pedagogical intent of teaching what overflow looks like is preserved precisely. Both edits carry a multi-line `D4RT-SCRIPT-WORKAROUND #109, P3` comment documenting the box math and explaining the OverflowBox-inside-ClipRect rationale. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 2). Logs: `ztmp/item109_baseline.log` (2 errors), `ztmp/item109_bisect_sec1to7.log` (1, dropped 48 px), `ztmp/item109_bisect_sec1to4.log` (0, narrowed 20 px to sections 5–7), `ztmp/item109_bisect_ratio.log` (0), `ztmp/item109_bisect_nested.log` (0), `ztmp/item109_bisect_edgecase.log` (1, isolated 20 px to `_edgeCase2`), `ztmp/item109_bisect_sec11to14.log` (1, narrowed 48 px), `ztmp/item109_bisect_recipe.log` (0), `ztmp/item109_bisect_beforeafter.log` (1, isolated 48 px to `_beforeAfter`), `ztmp/item109_p3fix.log` (0).

110. ~~`widgets/focusnode_test.dart`~~ *(essential_classes_test, 1/1, B-layout/RenderFlex — overflow 0.661 px right)* — **FIXED** (2026-05-20, P3 ×1 shared helper). Baseline matched the plan at 1 framework error: "A RenderFlex overflowed by 0.661 pixels on the right." Page root (line 2179) is `Container > SingleChildScrollView(pad bottom 12) > Column(stretch) > [hero, sectionHeader+section pairs ×11]`; the SCV ancestor rules out the page-root flavour of P3. Bisect (sections 1-5 → 0; sections 6-8 → 1; section 6 `_traversalSection` only → 1; only the `g2` Reading-Order policyCard within section 6 → 1) localised the offender to `_traversalSection` g2: a `_policyCard(width: 300)` whose inner content area is ~244 px holds a `Column > [Row(Expanded(_miniInput("LTR top-left")), SizedBox(6), Expanded(_miniInput("LTR top-right"))), Row(Expanded(_miniInput("LTR bottom-left")), SizedBox(6), Expanded(_miniInput("LTR bottom-right")))]`. Each Expanded slot is ~(244-6)/2 = 119 px; inside `_miniInput` the Container(margin v:4, padding h:10, border 1.2) leaves ~94.6 px for an inner `Row[Container(width:6), SizedBox(width:8), Text(label, fontFamily: 'monospace', fontSize: 12.5)]` — the longest labels "LTR top-left/right" and "LTR bottom-left/right" (16 monospace chars at 12.5 sp ≈ ~120 px head, plus the 6+8=14 px gutter, plus 22.4 px Container H overhead) exceed the 119 px Expanded budget by exactly 0.661 px. Fix (P3): wrap the `Text` in `_miniInput` in `Flexible(child: Text(..., overflow: TextOverflow.ellipsis, maxLines: 1))` — the shared helper now lets every caller's label absorb the squeeze rather than push the parent Row past its bounds. In g1/g3/g4 the labels are short enough that Flexible(loose) lets the Text render in full at its intrinsic width; only g2's long labels actually clip via the ellipsis. Single-helper fix covers the whole section. Multi-line `D4RT-SCRIPT-WORKAROUND #110, P3` comment documents the box math. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 1). Logs: `ztmp/item110_baseline.log` (1 error), `ztmp/item110_bisect_1to5.log` (0), `ztmp/item110_bisect_6to8.log` (1, narrowed to sections 6-8), `ztmp/item110_bisect_sec6.log` (1, isolated to `_traversalSection`), `ztmp/item110_bisect_g2.log` (1, isolated to the Reading-Order policyCard), `ztmp/item110_p3fix.log` (0).

111. ~~`widgets/formstate_test.dart`~~ *(important_classes_test, 1/1, B-bridge — uniform-colors)* — **FIXED** (2026-05-20, P5(a) ×2 shared helpers). Baseline ran 6 framework errors, not 1 — the plan undercounted: all six "A borderRadius can only be given on borders with uniform colors." From two shared helpers each invoked three times: (i) `_buildAnatomyCard` (line 781, called 3× at lines 649/667/684 for Form / FormState / FormField anatomy diagrams) used `Border(left: cWaxRed/4, top/right/bottom: cParchmentEdge/0.5)` + `borderRadius: 8`; (ii) `_buildLifecycleCard` (line 1821, called 3× at lines 1701/1715/1729 for validate / save / reset lifecycle cards) used `Border(left: accent/5, top/right/bottom: cParchmentEdge/0.5)` + `borderRadius: 8`. Both hit Flutter's "no borderRadius on non-uniform Border" assertion. Fix (P5(a)): refactor both helpers to use a uniform outer `Border.all(cParchmentEdge/0.5)` plus a leading per-card accent strip (`Container(width: 4|5, color: <accent>)`) inside `ClipRRect(borderRadius: 8) > IntrinsicHeight > Row(stretch, [strip, Expanded(Padding > Column)])`. The ClipRRect preserves the 8-px rounded card shape; the strip preserves the chunky wax-red/accent leading gutter at the original 4-px / 5-px width; IntrinsicHeight makes the strip match the card height. Single-helper edit covers each helper's three invocations. Multi-line `D4RT-SCRIPT-WORKAROUND #111, P5(a)` comments on both helpers. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 6). Logs: `ztmp/item111_baseline.log` (6 errors), `ztmp/item111_p5fix.log` (0).

112. ~~`widgets/gridview_test.dart`~~ *(essential_classes_test, 1/1, I-unhandled — "Vertical viewport was given unbounded height.")* — **FIXED** (2026-05-20, P10 variant). Baseline `frameworkErrors=2`: "Vertical viewport was given unbounded height." + a follow-on "Null check operator used on a null value" (the null check is the layout pipeline's downstream symptom of the same unbounded viewport — the failed render layer leaves a child without an attached `Size`). Page root: `Container > SingleChildScrollView > Column(stretch)` with a hero + 12 themed sections, each section drawing one or more `GridView.*` demos. Survey of all 23 GridView call sites in the file showed 22 already follow the standard SCV-embedded pattern (`shrinkWrap: true, physics: NeverScrollableScrollPhysics(), primary: false`) or live inside an explicit `SizedBox(height: …)` for horizontal-scroll demos. The lone outlier is `GridView.custom` at line 1234 (section 6.1 "Custom with SliverChildBuilderDelegate") — missing all three flags, so it defaulted to `shrinkWrap: false` and tried to fill the unbounded vertical space granted by the page-level SCV. The neighbouring raw `GridView(...)` and every `GridView.count/.extent/.builder` in the file already use the trio, so the fix is to harmonise the outlier rather than introduce a `SizedBox(height: 400)` wrap. Fix (P10 variant): add `shrinkWrap: true, physics: const NeverScrollableScrollPhysics(), primary: false` to the `GridView.custom` constructor. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #112, P10` comment noting that the change mirrors the file's established pattern. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 2 — both viewport + null-check cleared by the one edit). Logs: `ztmp/item112_baseline.log` (2 errors), `ztmp/item112_p10fix.log` (0 errors).

113. ~~`widgets/hero_test.dart`~~ *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 7.3 px bottom)* — **FIXED** (2026-05-20, P3 variant — *not* P2 as the plan suggested). Baseline `frameworkErrors=2`: "A RenderFlex overflowed by 7.3 pixels on the bottom." + "A RenderFlex overflowed by 0.800 pixels on the bottom." (the plan undercounted the second). Page root (line 1442) is `MaterialApp > Scaffold > SafeArea > SingleChildScrollView > Column(stretch)` — already SCV-wrapped, so P2 (page-root SCV) does not apply. Bisect: sections 1–6 only → 2 errors; sections 1–3 → 0; section 6 (`_flightTimeline`) alone → 2; individual `_flightFrame` calls: frame 0 (size=18) alone → 7.3 px overflow, frame 1 (size=28) alone → 0.800 px overflow, frames 2–5 (sizes 40/52/62/72) alone → 0 errors. Offender: shared `_glyph` helper at line 211. Its inner `Column[Icon(size: height*0.35), SizedBox(4), Padding(Text(label, fontSize: width<70?8.5:10))]` is the only child of a fixed-square Container (`width == height`). When `label` is empty (the `_flightFrame` tiles pass `label: ''`) the Text widget still reserves a default line-box of ~10 px regardless of content. For a 18×18 Container with inner padding 0: Icon(6.3) + SizedBox(4) + empty Text(~10) = 20.3 px → 7.3 px overflow; for 28×28: Icon(9.8) + 4 + ~10 = 23.8 px stretched with mainAxis center alignment → 0.800 px overflow. Larger sizes (40+) leave enough budget. Fix (P3 variant): in `_glyph`, gate the spacer + Padding(Text) on `label.isNotEmpty` using a `if (...) ...<Widget>[...]` collection-spread; an empty label paints nothing anyway, so the visual is unchanged. The two 32×32 empty-label glyphs in the `_builders` section (12) already had enough room (Icon=11.2 + 4 + ~10 = 25.2 < 32) and are unaffected. Edit carries a multi-line `D4RT-SCRIPT-WORKAROUND #113, P3` comment naming both overflow deltas and explaining the empty-Text line-box mechanic. Rule (a) — script-only change, single-script retest sufficient. Verified `frameworkErrors=0 status=success` (was 2). Logs: `ztmp/item113_baseline.log` (2 errors), `ztmp/item113_p3fix.log` (0 errors), plus the bisect series `ztmp/item113_bisect_*.log`.

114. `widgets/heromode_test.dart` *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.

115. `widgets/icon_test.dart` *(essential_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.

116. `widgets/inherited_model_test.dart` *(secondary_classes_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.

117. `widgets/inkwell_test.dart` *(essential_classes_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.

118. `widgets/keepalive_test.dart` *(important_classes_test, 1/1, B-layout — "Incorrect use of ParentDataWidget.")* — **P11**. Move the misplaced ParentDataWidget under its required parent.

119. `widgets/listbody_test.dart` *(important_classes_test, 1/1, B-layout — "RenderFlex children have non-zero flex but incoming width constraints …")* — **P12** (horizontal axis).

120. `widgets/listener_test.dart` *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 8331 px bottom)* — **P1+P2**.

121. `widgets/menu_serializable_shortcut_test.dart` *(hardly_relevant_classes_4_test, 1/1, B-bridge — uniform-colors)* — **P5(a)**.

122. `widgets/notification_locale_test.dart` *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.

123. `widgets/page_view_tabview_test.dart` *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 14 px bottom)* — **P2**.

124. `widgets/placeholder_test.dart` *(secondary_classes_test, 1/1, B-layout — "BoxConstraints has a negative minimum height.")* — **P8/P13**. The script feeds an invalid constraint; clamp to `>= 0` or capture the assertion via P5(b).

125. `widgets/render_sliver_overlap_absorber_test.dart` *(hardly_relevant_classes_5_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.

126. `widgets/render_sliver_overlap_injector_test.dart` *(hardly_relevant_classes_5_test, 1/1, I-unhandled — "SliverOverlapInjector has found no absorbed extent to inject.")* — **P7**. Add a `SliverOverlapAbsorber` upstream of the injector in the slivers list and thread its `handle` into the injector.

127. `widgets/router_test.dart` *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 22 px bottom)* — **P2**.

128. `widgets/row_test.dart` *(essential_classes_test, 1/1, B-layout/RenderFlex — overflow 6.0 px right)* — **P3**.

129. `widgets/scaffold_test.dart` *(essential_classes_test, 1/1, B-layout/RenderFlex — overflow 50 px bottom)* — **P2**.

130. `widgets/scaffoldstate_test.dart` *(important_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.

131. `widgets/scroll_behavior_test.dart` *(secondary_classes_test, 1/1, B-layout/BoxConstraints — infinite height)* — **P1**.

132. `widgets/scrollbar_layout_misc_test.dart` *(secondary_classes_test, 1/1, B-layout/RenderFlex — overflow 32 px bottom)* — **P2**.

133. `widgets/sizing_test.dart` *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 3525 px bottom)* — **P1+P2**.

134. `widgets/stack_test.dart` *(essential_classes_test, 1/1, B-layout/Stack — "A Stack requires bounded constraints from its parent.")* — **P4**.

135. `widgets/table_wrap_flow_test.dart` *(secondary_classes_test, 1/1, B-layout — "An explicit textBaseline is required when using baseline alignment.")* — **P9**.

136. `widgets/text_selection_toolbar_layout_delegate_test.dart` *(hardly_relevant_classes_5_test, 1/1, B-layout/RenderFlex — overflow 8799 px bottom)* — **P1+P2**.

137. `widgets/transform_full_test.dart` *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 2.0 px right)* — **P3**.

138. `widgets/valuelistenablebuilder_test.dart` *(important_classes_test, 1/1, B-layout/RenderFlex — overflow 10044 px bottom)* — **P1+P2**.

---

## Per-tag totals (for tracking)

| Tag                          | Distinct scripts | Items |
|------------------------------|-----------------:|-------|
| B-bridge (uniform-colors)    | 32               | items 6, 12, 13, 21, 23, 30, 33, 36, 47, 50, 54, 58–62, 68, 69, 78, 79, 81–84, 86, 90, 92, 95, 98, 111, 116, 117, 121 |
| B-bridge (gradient.linear)   | 1                | 7 |
| B-bridge tag → layout fix    | 4                | 37, 77, 94 (plus item 60 emits a uniform-colors banner — single P5 fix) |
| B-layout/RenderFlex          | 45               | 2, 4, 8, 24, 29, 31, 32, 37, 38, 41, 42, 45, 46, 48, 51, 53, 55, 57, 66, 67, 72, 73, 74, 77, 80, 87, 88, 94, 97, 101, 103, 104, 105, 108, 109, 110, 113, 120, 123, 127, 128, 129, 132, 133, 136, 137, 138 |
| B-layout/BoxConstraints      | 34               | 1, 3, 5 (mixed), 9–11, 15, 17, 19, 20, 22, 25–28, 34, 35, 40, 49, 52, 56, 64, 65, 70, 75, 76, 85, 89, 91, 93, 96, 99, 100, 102, 106, 114, 115, 122, 125, 130, 131 |
| B-layout/Stack               | 1                | 134 |
| B-layout (other)             | ~7               | 18, 39 (I-unhandled), 43, 44, 63, 71, 87, 107, 118, 119, 124, 135 |
| I-unhandled                  | 5                | 14, 39, 102, 112, 126 |

*(Approximate — exact membership follows the numbered list above. Some items combine two patterns.)*

## Recommended commit cadence

| Batch | Items | Theme | Suggested commit message |
| --- | --- | --- | --- |
| Batch 1 | 1–10 | animation, cupertino, dart_ui | `fix(scripts): banner-zero — animation/cupertino/dart_ui` |
| Batch 2 | 11–20 | foundation | `fix(scripts): banner-zero — foundation` |
| Batch 3 | 21–35 | gestures | `fix(scripts): banner-zero — gestures` |
| Batch 4 | 36–55 | material | `fix(scripts): banner-zero — material` |
| Batch 5 | 56–68 | painting | `fix(scripts): banner-zero — painting` |
| Batch 6 | 69–80 | rendering | `fix(scripts): banner-zero — rendering` |
| Batch 7 | 81–86 | semantics | `fix(scripts): banner-zero — semantics` |
| Batch 8 | 87–100 | services | `fix(scripts): banner-zero — services` |
| Batch 9 | 101–138 | widgets | `fix(scripts): banner-zero — widgets (final batch)` |

After each batch, run the regression sweep described above
(`essential + important + secondary` serially, both projects).
Once every batch is green and the suite logs no longer contain
any `═════════════` banner blocks, re-run the full 14-suite
matrix to produce a new `testlog_<id>-banner-zero/` baseline
and close this plan with a `**Closed YYYY-MM-DD by commit
<sha>.**` footer.

## Out-of-scope clarifications

- **No interpreter or bridge changes** in this plan. If a script
  rewrite reveals a genuine new bridge/interpreter defect, log it
  in `interpreter_unfixable.md` (if it cannot be fixed by a script
  edit) or in a fresh `error_analysis.md` for a separate cluster.
- **No `SendTestRunner` banner-filter changes** in this plan.
  The user's directive is to fix the scripts.
- **The 10 `skip: true` entries in `_failures.md` stay skipped.**
  This plan only addresses banner emissions; the skip set is
  unrelated.
