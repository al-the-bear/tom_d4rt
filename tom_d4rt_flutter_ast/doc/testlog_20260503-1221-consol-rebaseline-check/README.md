# Rebaseline-check test log — `20260503-1221-consol-rebaseline-check`

Re-run of the **non-flutter** d4rt-repository test suites
(`tom_d4rt`, `tom_d4rt_ast`, `tom_d4rt_exec`, `tom_d4rt_generator`,
`tom_d4rt_dcli`, `tom_dcli_exec`, `tom_ast_generator`) captured to
verify that the recent fixes to `tom_d4rt`, `tom_d4rt_ast` and the
generator (clusters 1..11 + GEN-094 + suspicious-rewrite cleanup)
have not regressed the component-level test suites of those projects.
Result totals are diffed against
[`testlog_20260502-1010-consol-rebaseline`](../testlog_20260502-1010-consol-rebaseline/README.md).

The flutter-material suites (`essential` / `important` / `secondary`
…) are explicitly **not** part of this rebaseline-check — those are
covered by the parallel `testlog_20260503-0948-issue-analysis` runs
in both `tom_d4rt_flutter_ast/doc/` and `tom_d4rt_flutter_test/doc/`.

| Field | Value |
| --- | --- |
| Baseline ID | `20260503-1221-consol-rebaseline-check` |
| Date | 2026-05-03 (Sun, ~12:21 CEST) |
| Git revision | `53da6410` (`53da64105de44072d4581213f6b248b6a5b17145`) |
| Branch | `main` |
| Bridges | regenerated via `tom_d4rt_flutter_ast/tool/regenerate_bridges.dart` (log: `_regen_bridges.log.txt`, success=true, 27 s) |
| Run protocol | dart tests in parallel (one suite per project, no shared HTTP server) |
| Comparison | non-flutter projects only |

## Result totals

| Project | Total | Passed | Failed | Skipped | Errors |
|---|---:|---:|---:|---:|---:|
| `tom_ast_generator` | 510 | 510 | 0 | 0 | 0 |
| `tom_d4rt` | 1747 | 1745 | 1 | 1 | 0 |
| `tom_d4rt_ast` | 117 | 117 | 0 | 0 | 0 |
| `tom_d4rt_dcli` | 706 | 706 | 0 | 0 | 0 |
| `tom_d4rt_exec` | 2260 | 2259 | 1 | 0 | 0 |
| `tom_d4rt_generator` | 660 | 660 | 0 | 0 | 0 |
| `tom_dcli_exec` | 412 | 412 | 0 | 0 | 0 |
| **TOTAL** | **6412** | **6409** | **2** | **1** | **0** |

(See `_summary.md` and `_failures.md` for the autogenerated extracts.)

## Delta vs `testlog_20260502-1010-consol-rebaseline`

| Project | 0502 (T/P/F/S/E) | 0503 (T/P/F/S/E) | Δ tests | Δ pass | Δ fail | Δ err |
|---|---|---|---:|---:|---:|---:|
| `tom_ast_generator` | 510/503/6/0/1 | 510/510/0/0/0 | 0 | **+7** | **−6** | **−1** |
| `tom_d4rt` | 1747/1736/9/1/1 | 1747/1745/1/1/0 | 0 | **+9** | **−8** | **−1** |
| `tom_d4rt_ast` | 117/115/0/0/2 | 117/117/0/0/0 | 0 | **+2** | 0 | **−2** |
| `tom_d4rt_dcli` | 706/704/1/0/1 | 706/706/0/0/0 | 0 | **+2** | **−1** | **−1** |
| `tom_d4rt_exec` | 2260/2234/25/0/1 | 2260/2259/1/0/0 | 0 | **+25** | **−24** | **−1** |
| `tom_d4rt_generator` | 660/652/8/0/0 | 660/660/0/0/0 | 0 | **+8** | **−8** | 0 |
| `tom_dcli_exec` | 75/72/3/0/0 | 412/412/0/0/0 | **+337** | **+340** | **−3** | 0 |
| **non-flutter total** | **6075/6016/52/1/6** | **6412/6409/2/1/0** | **+337** | **+393** | **−50** | **−6** |

Headline: **−50 failures, −6 errors, +337 new tests landed** since
the 0502 rebaseline. **No regressions** in any non-flutter suite.
The single remaining failure is an *intentional* `(SHOULD FAIL)`
test (`I-BUG-14a`) that documents a known won't-fix limitation —
it appears once each in `tom_d4rt` and `tom_d4rt_exec` because both
projects mount the same `limitations_and_bugs_test.dart` source.

## Verdict — no wider regression; large net improvement

The interpreter / generator / consolidation work landed since the
0502 rebaseline has cleared every actionable failure in the
non-flutter suites and added significant new coverage:

### Improvements

- **`tom_d4rt_exec` −24 failures.** The `D4rtTester end-to-end
  dcli_scripting_guide G-DCLI-01..14` cluster (13 failures
  carried over from 0429 → 0502) now passes. Plus another 11
  failures elsewhere in the suite have closed. Only the
  intentional `I-BUG-14a` (won't-fix limitation) remains.
- **`tom_d4rt_generator` −8 failures.** The remaining
  `G-DOV3-1`, `G-CB-7/11/12`, `G-FLP-16/23/28/30` cluster from
  the 0502 rebaseline is gone — all 660 tests pass. The
  generator is now a clean reference.
- **`tom_d4rt` −8 failures, −1 error.** All four "real
  regressions" flagged on 0502 (`GEN-056d`, `I-MISC-40`,
  `I-MISC-41`, `I-MISC-212`) are gone. Pre-existing items
  `I-FILE-36`, `I-FILE-38`, `DCL-RT-OPT-02`, `I-FILE-47` and the
  `HashSet I-COLL-25` error are also gone. Only the intentional
  `I-BUG-14a` remains.
- **`tom_ast_generator` −6 failures, −1 error.** All six
  G-DCLI-related failures plus the G-DCLI-12 setUpAll error
  closed.
- **`tom_d4rt_ast` −2 errors.** The two `ast_module_loader_test`
  stdlib-loading errors that were carried as the known
  starting-point of the analyzer-free split are gone. `tom_d4rt_ast`
  is now 117/117/0/0/0 — fully clean.
- **`tom_d4rt_dcli` −1 failure, −1 error.** The two VS Code
  scripting-API live-bridge items flagged as
  environment-dependent on 0502 now pass — the live VS Code
  bridge was reachable in this run. (Or the underlying scripts
  were rewritten not to need it; either way the suite is clean.)
- **`tom_dcli_exec` +337 tests, all passing.** Suite size grew
  from 75 → 412 (5.5×). All three pre-existing advanced-DCli
  example failures are gone.

### Real regressions to triage

None. The two failures that show in the totals
(`I-BUG-14a` × 2) are **intentional**: the test description
itself contains the marker `(SHOULD FAIL)`, asserting that the
interpreter must continue to *not* fully support records with
named fields until a real fix is planned. They are a documented
won't-fix and should be re-classified as expected fails or
skipped tests at some point.

### Unchanged

- Skipped count: 1 (same single skip in `tom_d4rt`).

## Failing tests (verdict ≠ pass/skip)

See `_failures.md` for the autogenerated per-project list.

### `tom_d4rt` (1 failure, 0 errors)

- `Open Bugs - Won't Fix (SHOULD FAIL) I-BUG-14a: Records with
  named fields. [2026-02-10 06:37] (FAIL)`
  — *Intentional won't-fix marker.* The interpreter currently
  returns `InterpretedRecord:<(x: 10, y: 20)>` where the test
  expects `Instance of '({int x, int y})'`. Tracked as a
  documented limitation, not a regression.

### `tom_d4rt_exec` (1 failure, 0 errors)

- Same `I-BUG-14a` test as `tom_d4rt` — same source file,
  re-mounted in `tom_d4rt_exec`. Identical signature, identical
  intent.

### All other suites — clean

`tom_ast_generator`, `tom_d4rt_ast`, `tom_d4rt_dcli`,
`tom_d4rt_generator`, `tom_dcli_exec` — 0/0/0/0 across the
board.

## Cross-project notes

- The historical G-DCLI-01..14 cluster has now disappeared from
  all three projects that used to fail on it
  (`tom_d4rt_exec`, `tom_ast_generator`, `tom_d4rt_generator`).
  The "unify the test plumbing so all three share a single
  source" follow-up from the 0502 rebaseline is now lower
  priority — the duplication is no longer masking a divergence.
- `tom_dcli_exec` grew 75 → 412 tests (+337) — an entire new
  suite layer landed since 0502. All passing on first run.
- The 0502 rebaseline noted the `tom_d4rt_dcli` VS Code
  scripting-API bridge tests as environment-dependent. They
  pass cleanly in this run.

## Files in this directory

| File | What it is |
| --- | --- |
| `_baseline_id.txt` | Just the baseline ID string. |
| `_revision.txt` | Git SHA + branch at the time of the run. |
| `_timestamp.txt` | Wall-clock timestamp from the `date` command. |
| `_regen_bridges.log.txt` | Output from `tool/regenerate_bridges.dart`. |
| `_summarize.dart` | Result.json → totals table. Re-runnable. |
| `_summary.md` | Latest table generated by `_summarize.dart`. |
| `_failures.dart` | Result.json → per-project failure list. Re-runnable. |
| `_failures.md` | Latest list generated by `_failures.dart`. |
| `error_analysis.md` | Failure / error analysis summary. |
| `<project>.result.json` | Raw `dart test` JSON file-reporter output. |
| `<project>.log.txt` | tee'd console output for human inspection. |

## How to compare a future run

```bash
cd doc/testlog_<new-id>
dart run _summarize.dart > _summary.md
dart run _failures.dart > _failures.md
diff _summary.md ../testlog_20260503-1221-consol-rebaseline-check/_summary.md
diff _failures.md ../testlog_20260503-1221-consol-rebaseline-check/_failures.md
```
