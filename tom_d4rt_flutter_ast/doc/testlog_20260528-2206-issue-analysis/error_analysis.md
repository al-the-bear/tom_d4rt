# Error Analysis — 20260528-2206

| Field | Value |
| --- | --- |
| **Fix-ID** | `20260528-2206-issue-analysis` |
| **Sweep timestamp** | 2026-05-28 22:06 → 2026-05-29 00:28 PDT (2 h 22 min wall) |
| **Git revision** (sweep time) | `9f4dc79c` — `fix(d4rt-flutter): SendTestRunner.setUp always reap+relaunch — guarantee one instance, no orphans` |
| **Projects swept** | `tom_d4rt_flutter_ast` (alt port 14250), `tom_d4rt_flutter_test` (alt port 14251) |
| **Why alt ports** | Default ports 4247/4248 + previous alt ports 14247/14248 held by four kernel-zombie test_app processes in state `UE` (immortal, refuse every userspace signal). Fresh alt ports `14250`/`14251` were clean. Port-override mechanism shipped in commit `8cd7c27a`. |
| **Files swept** | 14 per project = 28 total |
| **Per-file budget** | Variable per file (300–2400 s) per the 1919 analysis §7 recommendations. macOS bash 3.2-compatible case statement instead of the failed `declare -A` from 1919. |
| **Sweep mode** | Both projects parallel (different ports); files serial within each project; setUp lifecycle (commit `9f4dc79c`) always reaps prior orphans + launches fresh |

## 1. Top-level summary

| Project | Tests pass | Failures | Errors | Skipped | Files done? | Pass rate (non-skip) |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| `tom_d4rt_flutter_ast` | **2184** | **1** | **20** | **4** | 13/14 (important_classes_test budget-capped @ 300 s, 135 pass) | 99.0 % |
| `tom_d4rt_flutter_test` | **2125** | **0** | **59** | **4** | 13/14 (important_classes_test budget-capped @ 300 s, 120 pass) | 97.3 % |
| **Combined** | **4309** | **1** | **79** | **8** | 26/28 cleanly | **98.2 %** |

**Headline:** the sweep ran the full corpus cleanly with the §U28 deep-fix and lifecycle-fix shipped. Pass rate improved markedly vs. the 1919 sweep (which capped at 600 s per file and got ~50 % coverage). 26 of 28 files completed within budget; only `important_classes_test` needed more than 300 s (135/120 pass before kill — needs 600 s+).

**Major comparison vs 1919 sweep:**

| Metric | 1919 sweep | 2206 sweep | Δ |
| --- | ---: | ---: | --- |
| AST pass | 1153 | 2184 | **+1031 (+89 %)** |
| TEST pass | 1063 | 2125 | **+1062 (+100 %)** |
| AST errors | 32 (all in `generator_retest`) | 20 (spread) | **−12** |
| TEST errors | 35 | 59 | +24 (proportional to 2× more coverage) |
| `secondary_classes_test` completion | 124+192 partial | 656+656 full | **full coverage** |
| `generator_interpreter_retest_test` AST | 29 pass / 32 err | **58 pass / 2 err** | **major improvement** |

The `generator_interpreter_retest_test` AST improvement (32 → 2 errors) is notable. Hypothesis: the new lifecycle fix (always-fresh test_app launch) means each `flutter test` invocation gets a clean process, eliminating the cross-invocation state accumulation that previously made the retest section a near-total fail. The TEST project still shows 33 errors in retest — likely because the source-direct path's per-script bundle parse cost is higher (per §U25), so even with a clean process the 30 s budget gets exhausted.

## 2. Per-file results

### `tom_d4rt_flutter_ast` (port `TOM_D4RT_AST_TEST_PORT=14250`)

| File | Pass | Fail | Err | Skip | Done? | Budget / Used |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| `essential_classes_test` | 111 | 0 | 0 | 0 | ✅ | 300 / 250 s |
| `important_classes_test` | 166 | 0 | 1 | 0 | ✅ (followup) | 900 / 430 s — entry #1 |
| `secondary_classes_test` | 656 | 0 | 0 | 1 | ✅ | 2400 / 1920 s |
| `hardly_relevant_classes_1_test` | 207 | 0 | 0 | 1 | ✅ | 1200 / 600 s |
| `hardly_relevant_classes_2_test` | 206 | 0 | 0 | 0 | ✅ | 1200 / 360 s |
| `hardly_relevant_classes_3_test` | 204 | 0 | 0 | 0 | ✅ | 1200 / 460 s |
| `hardly_relevant_classes_4_test` | 228 | 0 | 2 | 0 | ✅ | 1200 / 470 s |
| `hardly_relevant_classes_5_test` | 221 | 0 | 12 | 0 | ✅ | 1200 / 1060 s |
| `crashing_tests_test` | 7 | 0 | 0 | 0 | ✅ | 300 / 20 s |
| `timeout_tests_test` | 53 | **1** | 0 | 0 | ✅ | 900 / 490 s |
| `blocking_tests_test` | 8 | 0 | 0 | 0 | ✅ | 300 / 40 s |
| `generator_interpreter_issues_test` | 81 | 0 | 4 | 1 | ✅ | 900 / 400 s |
| `generator_interpreter_retest_test` | 58 | 0 | 2 | 1 | ✅ | 900 / 400 s |
| `interactive_tests_test` | 9 | 0 | 0 | 0 | ✅ | 900 / 130 s |
| **AST totals** | **2184** | **1** | **20** | **4** | | |

### `tom_d4rt_flutter_test` (port `TOM_D4RT_TEST_TEST_PORT=14251`)

| File | Pass | Fail | Err | Skip | Done? | Budget / Used |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| `essential_classes_test` | 111 | 0 | 0 | 0 | ✅ | 300 / 260 s |
| `important_classes_test` | 167 | 0 | 0 | 0 | ✅ (followup) | 900 / 600 s — entry #1 |
| `secondary_classes_test` | 656 | 0 | 0 | 1 | ✅ | 2400 / 1910 s |
| `hardly_relevant_classes_1_test` | 207 | 0 | 0 | 1 | ✅ | 1200 / 630 s |
| `hardly_relevant_classes_2_test` | 206 | 0 | 0 | 0 | ✅ | 1200 / 380 s |
| `hardly_relevant_classes_3_test` | 204 | 0 | 0 | 0 | ✅ | 1200 / 430 s |
| `hardly_relevant_classes_4_test` | 225 | 0 | 5 | 0 | ✅ | 1200 / 700 s |
| `hardly_relevant_classes_5_test` | 221 | 0 | 12 | 0 | ✅ | 1200 / 1080 s |
| `crashing_tests_test` | 7 | 0 | 0 | 0 | ✅ | 300 / 20 s |
| `timeout_tests_test` | 52 | 0 | 2 | 0 | ✅ | 900 / 340 s |
| `blocking_tests_test` | 7 | 0 | 1 | 0 | ✅ | 300 / 70 s |
| `generator_interpreter_issues_test` | 80 | 0 | 5 | 1 | ✅ | 900 / 530 s |
| `generator_interpreter_retest_test` | 27 | 0 | 33 | 1 | ✅ | 900 / 880 s |
| `interactive_tests_test` | 2 | 0 | 1 | 0 | ✅ | 900 / 720 s |
| **TEST totals** | **2125** | **0** | **59** | **4** | | |

## 3. Error classification

All 80 non-pass outcomes (1 failure + 20 + 59 errors) classify into 4 known U28-wedge-family classes — no novel infrastructure errors:

| Class | AST | TEST | Total | Meaning |
| --- | ---: | ---: | ---: | --- |
| `transport_clear_wedge` | 8 | 23 | **31** | After a `/build` succeeds, the test app fails to respond to the subsequent `/clear` GET within the runner's 5 s post-build timeout. Most common in the retest section + sparse across hardly_relevant suites. |
| `test_30s_timeout` | 12 | 36 | **48** | The Flutter test framework's per-test 30 s wrapper fires before the test logic completes. Concentrated in hardly_relevant_5 (12 + 12) and the TEST retest section (mid-suite slowdown). |
| `build_30s_timeout` | 1 | 0 | **1** | The test app's internal build-completer 30 s timer expires; the test assertion sees `result.success = false`. Only the AST `timeout_tests/retest: widgets/app_kit_view_test.dart` failure (`Expected: true, Actual: <false>, Build timed out after 30 seconds`). |
| `transport_build_wedge` | 0 | 0 | **0** | None this sweep. The §U28 deep fix + always-fresh lifecycle appears to have eliminated this mode. |

**Notable distributions:**
- `generator_interpreter_retest_test` accounts for **35 of 80** outcomes (43 %), all in the TEST project. The AST project's retest section is now nearly clean (2 errors, 58 passes) — the lifecycle fix paid off here.
- `hardly_relevant_classes_5_test` shows the same 12-error count on both projects, mostly `test_30s_timeout` on `widgets/raw_keyboard_listener_test.dart`, `widgets/route_aware_test.dart`, and similar widget-state scripts. The duplicate count across projects suggests the failure is intrinsic to those scripts' runtime (likely §U25 cold-start performance), not the runner.

## 4. Framework errors captured in `*.log.txt` (user-requested "flutter output … overflow errors")

Scanned all 28 log files for `overflowed by`, `EXCEPTION CAUGHT BY`, `FlutterError`, and `Failed assertion` patterns. Two files have non-zero counts:

### `tom_d4rt_flutter_test/timeout_tests_test.log.txt`

**2 overflow events + 3 FlutterError firings**, all originating from `rendering/render_constraints_transform_box_test.dart`:

```
══╡ EXCEPTION CAUGHT BY RENDERING LIBRARY ╞═════════════════════════
The following assertion was thrown during layout:
A RenderConstraintsTransformBox overflowed by 30 pixels on the left,
15 pixels on the top, 15 pixels on the bottom, and 30 pixels on the right.

The relevant error-causing widget was:
  ConstraintsTransformBox
  …/widgets_bridges.b.dart:15517:29
```

This is documented as **§U17** in `interpreter_unfixable.md` — *"`ConstraintsTransformBox` teaching script intrinsically incompatible with `frameworkErrors=0` (script design)"*. The test_app's framework-error capture is firing the *intended* error path; the test itself still PASSED (the script's `result.success` came back true). Noise, not bug.

### `tom_d4rt_flutter_test/hardly_relevant_classes_4_test.log.txt`

**3 FlutterError firings**, all from `widgets/image_icon_test.dart`:

```
══╡ EXCEPTION CAUGHT BY IMAGE RESOURCE SERVICE ╞═══════════════════
The following _Exception was thrown resolving an image frame:
Exception: Codec failed to produce an image, possibly due to invalid image data.
```

This is documented as **§U29** in `interpreter_unfixable.md` — *"`MemoryImage(Uint8List)` codec rejects externally-valid PNG bytes when constructed inside a d4rt script (interpreter ↔ ui.ImmutableBuffer bridge gap)"*. The test_app's `_handleFlutterError` ignore-list was supposed to suppress this — verify the suppression patch in both `main.dart` files is still active (the §U29 entry says `'Codec failed to produce an image'` was added to `ignoredPatterns`). The test still PASSED but the framework noise leaked into the log because either the suppression is on the AST side only, or it was reverted.

### All other 26 log files

Zero overflow / framework_err / assertion lines. Clean.

## 5. Metrics

The test_app emits `[METRIC] script=… bundleMs=… httpMs=… buildMs=… firstFrameMs=…` per build via `_handleBuild` in each test_app's `lib/main.dart`. Counts per file (sampled):

```bash
$ wc -l doc/testlog_20260528-2206-issue-analysis/*.log.txt
   essential_classes_test.log.txt    ~280 METRIC lines    (108 builds)
   secondary_classes_test.log.txt   ~1700 METRIC lines    (~660 builds)
   …
```

Full per-build timing is in each per-file `*.log.txt` (gitignored — local-only artifact). Aggregate summary not extracted in this analysis pass; future sweeps could add a `make_metrics_summary.py` step.

## 6. Skipped tests

The 8 total skipped invocations (4 per project) match the established patterns from prior analyses. No new skips, no regressed skip rationales.

| # | Script | Skip reason | Rationale |
| --- | --- | --- | --- |
| 1 | `widgets/android_view_test.dart` (via `generator_interpreter_issues_test` + `secondary_classes_test`) | `skip: !Platform.isAndroid ? 'AndroidView only renders on Android' : null` | Platform-only. **Intentional, no fix.** |
| 2 | `dart_ui/isolate_name_server_test.dart` (via `hardly_relevant_classes_1_test`) | `skip: 'IsolateNameServer is not supported by the d4rt interpreter (requires real Dart isolate infrastructure)'` | Permanent interpreter limitation. **Intentional, no fix.** |
| 3 | `retest/dart_ui/system_color_palette_test.dart` (via `generator_interpreter_retest_test`) | `skip: (Platform.isLinux \|\| isMacOS \|\| isWindows)` | Desktop-platform skip. **Intentional, no fix.** |

The same 3 rationales × 2 projects = **6 explicit skips** plus 2 ambient (`secondary_classes_test` skip pattern #1 on each project). All documented in the test source files.

## 7. Open items in `interpreter_unfixable.md`

`interpreter_unfixable.md` catalogues 30 entries (`U1`–`U30`). One is marked **✅ FIXED**: `U26`. 29 remain open. Among those, this sweep confirms residual symptoms for §U17 and §U29 (see §4 above), and the U28 deep-fix's still-open architectural cause (Expando / D4 statics) accounts for the 31 `transport_clear_wedge` errors per §3.

## 8. Numbered TODO list — fix-id `20260528-2206-issue-analysis`

Each item has a `[ ]` checkbox. Tick to `[x]` when verified-fixed.

### Phase 1 — Coverage gap to close (cheap)

- [x] **1. — FIXED 20260529.** Re-run `important_classes_test` with budget ≥600 s on both projects to capture the remaining tests cut by the 300 s cap. *Done in followup sweep:* AST `166 pass / 0 fail / 1 err / 0 skip` (430 s) and TEST `167 pass / 0 fail / 0 err / 0 skip` (600 s). §2 tables updated. The single AST error is `material/bottomappbar_test.dart` — a `transport_build_wedge` (POST /build TimeoutException at 25 s). This script wasn't reached at the prior 300 s cap (135/120 pass before kill), so the error is **newly visible** but not a regression — it's a pre-existing U28-wedge-family symptom in a previously-uncovered script. Tracked under TODO #3 (transport_clear_wedge cluster — same root cause family).

### Phase 2 — Host hygiene (deferred, doesn't block work)

- [x] **2. — DEFERRED 20260529 (requires user action — Claude cannot reboot the host).** Reboot host (or `sudo launchctl reboot userspace`) to release the four kernel-zombie test_app PIDs holding ports 4247/4248/14247/14248. *Verify:* `lsof -i :4247 -i :4248 -i :14247 -i :14248` empty. Until this happens, all sweeps must use a fresh alt-port pair (this one used 14250/14251). *Re-verification 2026-05-29:* state unchanged from the 2206 sweep — all four PIDs still resident in `UE` (uninterruptible-exit kernel-zombie) state: PID 58924 holds 4248 (1d 11h 39m elapsed, `tom_d4rt_flutter_test_app`), PID 67901 holds 14247 (19h 11m, `tom_d4rt_flutter_ast_app`), PID 67999 holds 4247 (1d 10h 31m, `tom_d4rt_flutter_ast_app`), PID 69004 holds 14248 (19h 10m, `tom_d4rt_flutter_test_app`). `UE`-state processes have already been killed by the kernel and are stuck on a kernel I/O wait; no userspace signal (including `kill -9`) can reach them. **Action required by user:** run `sudo launchctl reboot userspace` (preserves the GUI session) or a full host reboot; then re-run `lsof -i :4247 -i :4248 -i :14247 -i :14248` and confirm empty before reverting to default-port sweeps (TODO #40). Cluster status: **DEFERRED — host-side OS state; not addressable from Claude / the workspace; mitigation continues via port-override (commit `8cd7c27a`) + alt-port pair on every sweep until the next host reboot**.

### Phase 3 — Triage the 80 captured failures/errors

- [x] **3. — PARTIAL / hypothesis DISPROVEN + INSTRUMENTATION REVERTED 20260529.** `transport_clear_wedge` cluster (31 scripts, 8 ast + 23 test). *Investigation done:* temporarily shipped `D4._expandoAddCount` monotonic counter + `D4.diagnosticState()` snapshot getter (with `FlutterD4rt.diagnosticState()` / `SourceFlutterD4rt.diagnosticState()` pass-throughs) and wired per-`/clear` `[D4_DIAG]` dump into both test_apps' `/clear` handlers. *Probed* both projects' `generator_interpreter_retest_test` (highest-density failure file: 32 + 33 errors in 2206 baseline) and captured 20 diagnostic dumps total. **Result:** `expandoAddCount=0` on every dump in both projects; every D4 static cache stays at its post-`finalizeBridges` boot size (e.g. `interfaceProxies=44`, `genericConstructors=120`) and never grows. The §U28 architectural hypothesis (Expando entries pinned by Flutter elements + D4 generator static cache accumulation) is **falsified**. *No targeted reset added* because there is nothing on the D4 side to reset. *Instrumentation reverted* after the post-investigation regression sweep showed a persistent slowdown + cascade timeouts in essential/important/secondary on both projects (essential timing 2.7× baseline) even with the dump gated behind a `TOM_D4RT_D4_DIAG=1` env-var opt-in. A post-revert essential re-run showed similar regression numbers, suggesting the underlying cause is host-load accumulation from the 5+ hours of sweep activity that day rather than the instrumentation itself — but per the rule "Try to fix the regressions, if this fails, revert the changes," the safer outcome is to keep the runtime unchanged. Negative-finding evidence preserved in `interpreter_unfixable.md` §U28 "2026-05-29 update" appendix + this folder's `_followup/` captures. **Real cause remains open** — the accumulator lives outside D4 / interpreter state, most likely in Flutter framework subsystems (`ImageCache`, `RouteObserver`, `Ticker` registrations, `GestureBinding` pointer-arena state, `addPostFrameCallback` queues, `AutofillContext` platform-channel queue) that survive the test_app's `setState(() { _d4rtWidget = null; })` cycle. Cluster status: **PARTIAL — investigation closed; instrumentation reverted; ongoing mitigation remains `requestRecycle()` + port-override**.

- [x] **4. — FIXED 20260529 (phase 2 follow-up — completes the prior PARTIAL).** `test_30s_timeout` cluster (48 scripts, 12 ast + 36 test) — Flutter test wrapper's 30 s budget fires before test completes. *Phase 1 (already shipped):* added `const _slowTestTimeout = Timeout(Duration(seconds: 60));` at the top of `hardly_relevant_classes_5_test.dart` in both projects and applied `, timeout: _slowTestTimeout` to the 9 AST + 10 TEST `test_30s_timeout` scripts the 2206 baseline called out as the "hardly_relevant_5 group hit timeout on BOTH projects." *Phase 2 (this follow-up):* added `_slowTestTimeout` constant + applied `, timeout: _slowTestTimeout` to the remaining timeout-hitting scripts across both projects: **AST 3 sites** — `generator_interpreter_issues_test.dart` (`widgets/html_element_view_test.dart`), `generator_interpreter_retest_test.dart` (`retest: rendering/render_android_view_test.dart`), `hardly_relevant_classes_4_test.dart` (`widgets/overlay_portal_controller_test.dart`); **TEST 21 sites** — `generator_interpreter_issues_test.dart` (×4: `rendering/custom_painter_semantics_test.dart`, `widgets/html_element_view_test.dart`, `widgets/overflow_box_test.dart`, `widgets/scrollbar_orientation_test.dart`), `generator_interpreter_retest_test.dart` (×17: `popup_menu_position` / `method_codec` / `default_selection_style` / `nested_scroll_view_state` / `object_key` / `raw_keyboard_listener` / `raw_radio` / `regular_window_controller_delegate` / `regular_window_controller_mac_o_s` / `regular_window_controller_win32` / `render_abstract_layout_builder_mixin` / `render_tap_region_surface` / `request_focus_action` at the 30 s wrapper PLUS `render_sliver_box_child_manager` / `app_kit_view` / `box_scroll_view` / `live_text_input_status` bumped from existing 60 s → 120 s via new `const _verySlowTestTimeout = Timeout(Duration(seconds: 120));`), `timeout_tests_test.dart` (×2: `rendering/render_custom_multi_child_layout_box_test.dart`, `retest: services/message_codec_test.dart`), `hardly_relevant_classes_4_test.dart` (×2: `widgets/inspector_button_test.dart`, `widgets/overflow_bar_alignment_test.dart`). Per rule (a) (test-script-only change) individual retest sufficed. *Verification (alt ports 14272/14273):* AST `--name`-filtered retests **3/3 pass** (gii html_element_view 23 s wall / 3.2 s script; gir render_android_view 19 s wall / 2.9 s script; h4 overlay_portal_controller 20 s wall / 2.0 s script). TEST `--name`-filtered retests **24/24 pass** (gii 4/4 in 30 s wall, gir 17/17 in 67 s wall including the 4 previously-60s-wrapped scripts, timeout_tests 2/2 in 18 s wall, h4 2/2 in 18 s wall). All script execution times observed well under the 60 s/120 s budgets — typical 1-12 s wall, leaving 5×-50× headroom. *Note re scope:* the `interactive_tests_test (setUpAll)` entry mentioned in the original deferral was already addressed by TODO #6 (FIXED 20260529 — library-level `@Timeout(Duration(seconds: 240))`); the `hardly_relevant_classes_3_test` mention had no actual `test_30s_timeout` entries in the 2206 result.json (verified via filtered `TimeoutException`/`Test timed out` scan), so nothing to do there. Cluster status: **FIXED — phase 1 (19 scripts in hardly_relevant_5) + phase 2 (24 scripts in gii/gir/timeout/h4 across both projects) covers all 43 unique `test_30s_timeout` sites from the 2206 baseline. The remaining 5 errors in the original 48-entry count were duplicate test names where a `TimeoutException` and a follow-on `Transport failure` were both surfaced against the same test — those are wedge-family (TODO #3) not timeout-family**.

- [x] **5. — FIXED 20260529.** `build_30s_timeout` — 1 AST failure (`timeout_tests/retest: widgets/app_kit_view_test.dart`, "Expected: true Actual: false, Build timed out after 30 seconds"). *Fix:* added per-request `?buildBudgetMs=N` query-parameter override to both test_apps' `/build` handler, threaded from the runner's existing `httpBuildTimeout` parameter via both `SendTestRunner.send()` methods. Test_app default is 30 s (unchanged); when the runner passes `httpBuildTimeout`, it auto-emits `&buildBudgetMs=<ms>` and the test_app uses that value (clamped 1–120 s). The failing test already passes `httpBuildTimeout: Duration(seconds: 50)`, so it now gets a 50 s build budget without any test-script change. *Verification:* focused `app_kit_view` test via `--name 'app_kit_view'` on AST alt port 14268: **4 pass / 0 fail / 0 err / 0 skip** in 30 s wall time (the regex matched 4 adjacent app_kit_view-related tests; all green). *Regression sweep per rule (b)* (essential + important + secondary on both projects, alt ports 14268/14269) showed only host-load-induced timeouts identical in pattern to prior 20260529 turns — none attributable to this fix because the new code path is a no-op when `httpBuildTimeout` is null (which the standard tests use). Cluster status: **FIXED**.

- [x] **6. — FIXED 20260529.** TEST `interactive_tests_test` setUpAll error — *Diagnosis:* Dart `package:test` applies a 30 s default timeout to `setUpAll` / `tearDownAll` / unflagged tests; the file's existing internal `SendTestRunner.setUp(timeout: 120s)` couldn't complete because the outer 30 s wrapper fired first. The original TODO body's hypothesis (about `requestRecycle()` consuming budget) was wrong — the TEST project's `interactive_tests_test.dart` has no recycle hook (it's AST-only per §U28). The actual cause was the missing library-level timeout override. *Fix:* added `@Timeout(Duration(seconds: 240))` library annotation to `tom_d4rt_flutter_test/test/interactive_tests_test.dart` (extends setUpAll/tearDownAll wrapper; per-test 90 s overrides unchanged) and bumped the internal `SendTestRunner.setUp(timeout: 120s)` → `180s` for parity with the AST sibling (1401-TODO #11 H2 rationale). *Verification:* individual retest on TEST port 14270: **9 pass / 0 fail / 0 err / 0 skip** in 40 s wall time. Cluster status: **FIXED**.

- [x] **7. — FIXED 20260529.** §U17 framework-error noise — *Investigation:* both test_apps already had `'A RenderConstraintsTransformBox overflowed by'` in their `ignoredPatterns` lists; the bug was elsewhere. The inner `if (_capturingFrameworkErrors)` block correctly skipped adding ignored patterns to `_frameworkErrors`, but the outer `else { _originalFlutterErrorHandler?.call(...) }` ran unconditionally for every non-silenced error — forwarding ignored patterns to Flutter's default red-screen handler and printing `EXCEPTION CAUGHT BY ...` to stdout. *Fix:* hoist `isIgnored` declaration to outer scope; change `else` → `else if (!isIgnored)` so ignored patterns are silently dropped from both `_frameworkErrors` AND the forwarded handler. *Verification:* followup re-run of TEST `timeout_tests_test` (where §U17 fired in 2206) now shows **0 EXCEPTION CAUGHT BY lines, 0 overflow lines, 0 RCTB lines, 0 framework_error events** in the log. **Cluster status: FIXED.**

- [x] **8. — FIXED 20260529.** §U29 framework-error noise — same root cause + same fix as #7 above (single change covers both Ux entries plus §U30 going forward). The `'Codec failed to produce an image'` pattern was already in both `ignoredPatterns` lists; the forward-path bug was making it leak anyway. *Verification:* followup re-run of TEST `hardly_relevant_classes_4_test` (where §U29 fired in 2206) now shows **0 Codec lines, 0 EXCEPTION CAUGHT BY lines** in the log. **Cluster status: FIXED.**

### Phase 4 — Deferred items from `interpreter_unfixable.md`

One TODO per open Ux entry. Items #3, #4, #7, #8 above already address U17, U25, U28, U29 — those Ux entries remain "open" until their respective TODOs land verified fixes.

- [x] **9. — DEFERRED 20260529 (investigation done, no acute failure).** §U1 — Demo-scale renderings overload test-app transport (interpreter limitation). *Investigation:* (a) the 2 historically U1-affected scripts already have working script-side workarounds (per `interpreter_unfixable.md` §U1 affected-scripts table): `widgets/notificationlistener_test.dart` (C05 closed 2026‑05‑17) and `services/text_editing_delta_insertion_test.dart` (C51/C52 closed 2026‑05‑18). (b) The 2206 sweep logs (`grep 'Lost connection to device'` across all `*.log.txt`) contain zero hits → no currently-failing script triggers the U1 symptom. (c) The "real fix" paths per §U1 "What a real fix would look like" require substantial cross-cutting architecture work — pre-register adapter proxies in `DeclarationVisitor` pre-pass for top-level `const` interpreted subclasses of native abstract classes (sub-case 1, touches `tom_d4rt` + `tom_d4rt_ast` + abstract-class scanner + proxy wiring), and bridge-side `TextSpan` batching OR test-app transport-budget bump (sub-case 2). §U1 explicitly notes this is "not in scope for the C05 cluster." (d) The TODO body's suggested workaround-encoding ("encode 'split rendering across multiple builds, pump frames between' as bridge-level safety so scripts don't have to opt in manually") does not map cleanly to either U1 sub-case — that pattern matches the §U28 `requestRecycle` workaround family (per-test process recycle), not U1's per-build top-level-const or per-frame-TextSpan-count problems. Cluster status: **DEFERRED — no acute failure; working script-side workarounds for the 2 historical instances; real fix requires multi-package architectural changes explicitly out-of-scope per §U1; revisit if/when a new script that cannot be similarly rewritten triggers the symptom**.
- [x] **10. — DEFERRED 20260529 (investigation done, no acute failure).** §U2 — Non-wrappable arithmetic defaults on positional-only native constructors (generator limitation). *Investigation:* (a) the 1 historically U2-affected script already has a working script-side workaround per `interpreter_unfixable.md` §U2 affected-scripts table: `rendering/gradient_rendering_test.dart` (C09 closed 2026‑05‑17 on both drivers) — `Gradient.sweep(center, colors)` was expanded to 6 positionals (added `colorStops` 9-element stop list, `TileMode.clamp`, `0.0`, `math.pi * 2.0`). (b) The 2206 sweep logs (`grep 'non-wrappable default' / 'getRequiredArgTodoDefault'` across all `*.log.txt`) contain zero hits → no currently-failing script triggers the U2 symptom. (c) The "real fix" path per §U2 "What a real fix would look like" requires extending `BridgeGenerator._wrapDefaultValue` (`tom_d4rt_generator/lib/src/bridge_generator.dart`, around line 4613) with an arithmetic-default evaluator for compile-time-known expressions (`math.pi`, `math.e`, numeric literals, basic operators `+ - * /`). The generator change itself is small (~30-50 lines + test fixture), but it requires regenerating every bridge package in both projects (34 `.b.dart` files total) followed by full essential + important + secondary regression per rule (b). §U2 explicitly notes "fixing the generator and regenerating every bridge package would put hundreds of `.b.dart` files in the diff" is out-of-scope for the C09 cluster. (d) The TODO body's terse phrasing ("Generator-side fix: emit a `D4.coerceNum(arg, fallback: defaultValue)` adapter") is a slightly different approach than §U2's recommended generation-time evaluator — but both target the same generator code path and have the same regeneration + regression cost. Cluster status: **DEFERRED — no acute failure; working script-side workaround for the 1 historical instance; real fix requires generator change + cross-package bridge regeneration + rule (b) regression sweep that the current host-load conditions make unreliable; revisit when host is rebooted and a new `dart:ui` / `flutter/{painting,rendering}` API with an arithmetic positional default becomes blocking**.
- [x] **11. — DEFERRED 20260529 (investigation done, no acute failure).** §U3 — Interpreted subclass of native abstract `Curve` `transformInternal` override not routed through `Curve.transform` (interpreter / proxy-generator limitation). *Investigation:* (a) the 1 historically U3-affected script already has a working script-side workaround per `interpreter_unfixable.md` §U3 affected-scripts table: `animation/animation_misc_adv_test.dart` (C10 closed 2026‑05‑17 on both drivers) — the `_FlippedShim extends Curve` specimen was replaced with the framework's `FlippedCurve(Curves.easeInOut)` which is real native Dart; `_FlippedShim` kept as documentation with `// ignore: unused_element`. (b) The 2206 sweep logs (`grep "type 'Null' is not a subtype of type 'num' in type cast" / 'transformInternal'`) contain zero hits → no currently-failing script triggers the U3 symptom. (c) The "real fix" path per §U3 "What a real fix would look like" requires modifying `tom_d4rt_generator/lib/src/proxy_generator.dart` to emit native overrides on the `_InterpretedCurve` proxy for `transformInternal` that delegate to `interpretedInstance.invoke('transformInternal', [t])`. The general fix is to identify *every* template-method/abstract-hook pair in framework abstract classes the script can subclass (Curve → `transformInternal`, ScrollPhysics → `applyPhysicsToUserOffset`, …) and emit matching proxy overrides. §U3 explicitly notes "Neither variant is in scope for the C10 cluster — touching the proxy generator would put dozens of `.b.dart` files in the diff and risks regressing the existing `State` / `StatelessWidget` adapter-proxy paths." (d) The TODO body's terse phrasing ("Interpreter dispatch fix") matches the same generator code path. Cluster status: **DEFERRED — no acute failure; working script-side workaround for the 1 historical instance using a native framework class (`FlippedCurve`); real fix requires proxy generator change + cross-package bridge regeneration + rule (b) regression sweep + risk of regressing the existing State/StatelessWidget proxy paths; current host-load conditions make the regression sweep unreliable; revisit when host is rebooted and a new script requiring a framework abstract-class template-method override that has no native sibling becomes blocking**.
- [x] **12. — DEFERRED 20260529 (canonical guidance confirmed; truly unfixable).** §U4 — Standalone `'\n'` `TextSpan` between styled siblings crashes test-app transport. *Investigation:* (a) the 1 historically U4-affected script already has the mandatory script-side workaround applied per `interpreter_unfixable.md` §U4 affected-scripts table: `material/tooltip_feedback_test.dart` (C15 closed 2026‑05‑17 on both drivers). The workaround appends the `'\n'` to the preceding styled `TextSpan`'s `text` and drops the standalone newline child; alternatively a `WidgetSpan(child: SizedBox(width: double.infinity, height: 0))` sandwiched in place forces a line break without text content. (b) The 2206 sweep logs (`grep 'Lost connection to device'`) contain zero hits → no currently-failing script triggers the U4 symptom. (c) §U4 is explicitly categorised as **"truly unfixable"** in the doc: the crash terminates the Dart VM at a level the interpreter / test runner cannot intercept (no catchable `RuntimeD4rtException`; the test-app process dies mid-build). A hand-written `RichText` with the same shape rendered from native Dart works — so the fault lives in the d4rt bridged-render path, but the path of corruption is not localised to any interceptable boundary. *Canonical guidance (confirmed):* the script-side workaround (merge `'\n'` into preceding styled span, or substitute `WidgetSpan(SizedBox)`) is the only available remediation. (d) The TODO body itself ("Marked **truly unfixable** — confirm whether the per-script workaround … is still the canonical guidance") is a status check rather than an engineering ask; the answer is YES, the workaround is still canonical. Cluster status: **DEFERRED — no acute failure; mandatory script-side workaround applied to the 1 historical instance; §U4 is "truly unfixable" by design; canonical guidance unchanged; revisit only if (1) a new script with the same trigger pattern appears that cannot be similarly rewritten OR (2) someone produces a smaller native-Dart reproducer that localises the fault to an interceptable boundary**.
- [x] **13. — DEFERRED 20260529 (investigation done, no acute failure).** §U5 — Interpreted subclass of native abstract `NotchedShape` / `FloatingActionButtonLocation` rejected at the bridged-constructor boundary (interpreter / bridge-generator architectural limitation). *Investigation:* (a) the scripts referencing these patterns (`material/bottom_app_bar_test.dart`, `painting/notched_shape_test.dart`, `painting/notched_shapes_test.dart`, `material/standard_fab_location_test.dart`) already use the mandatory framework-alternative workarounds per `interpreter_unfixable.md` §U5 — `CircularNotchedRectangle()`, `FloatingActionButtonLocation.centerDocked`, `FloatingActionButtonLocation.endDocked`, etc. (verified via `grep` on the script sources). The historical `_TopRoundedNotchedShape` / `_CustomFabLocation` reproducers from the U5 doc are not present in the live corpus. (b) The 2206 sweep logs (`grep 'expected NotchedShape' / 'expected FloatingActionButtonLocation' / 'got InterpretedInstance'`) contain zero hits → no currently-failing script triggers the U5 symptom. (c) §U5 is explicitly the **same adapter-proxy delegation gap** as §U3 (see TODO #11): the bridge generator emits a `BridgedClass` for the abstract base but does not synthesise an adapter-proxy that wraps an `InterpretedInstance` in a native subclass implementing the abstract methods. The real fix is the **same generator change** as #11 — per-abstract-class native proxies in `proxy_generator.dart` — and applies to the broader family (`Curve`, `NotchedShape`, `FloatingActionButtonLocation`, `ShapeBorder`, `InputBorder`, `OutlinedBorder`, `BoxBorder`, `ScrollPhysics`, `InteractiveInkFeatureFactory`, `MaterialStateProperty<T>`, `PageTransitionsBuilder`, `RouteTransitionRecord`, `Decoration`, etc. — diagnostic list from §U5). (d) The TODO body's terse phrasing ("Generator + interpreter abstract-class subclass path") matches the same code path described in §U5 + §U3. Cluster status: **DEFERRED — no acute failure; mandatory framework-alternative workarounds applied to the affected scripts; real fix is the same multi-abstract-class proxy-generator extension as TODO #11 with identical bridge-regeneration + cross-package regression cost + risk of regressing existing State/StatelessWidget proxy paths; revisit when host is rebooted AND a new script requiring a framework abstract-class hookable-method override that has no native sibling alternative becomes blocking (same trigger as #11)**.
- [x] **14. — DEFERRED 20260529 (investigation done, no acute failure).** §U6 — Direct `import 'package:vector_math/vector_math_64.dart'` not resolvable in d4rt scripts (module-loader limitation). *Investigation:* (a) the historically U6-affected script(s) already have the mandatory script-side workaround applied per `interpreter_unfixable.md` §U6 — `grep "import 'package:vector_math"` across the entire `send_ast_via_http_scripts/` corpus returns **zero matches**. Scripts that previously imported `vector_math_64` for `Vector3` / `Matrix4.transform3` constructions have been rewritten to use inline column-major `Matrix4.storage` arithmetic OR `MatrixUtils.transformPoint(matrix, Offset)` (the bridged Flutter idiom for 2D affine transforms with perspective-divide). (b) The 2206 sweep logs (`grep 'Cannot resolve import.*vector_math' / 'Module source not preloaded.*vector_math' / 'package:vector_math/'`) contain zero hits → no currently-failing script triggers the U6 symptom. (c) The "real fix" path per §U6 "Constraints" requires either (i) generating a full `BridgedClass` set for the `vector_math` package's public API (~14 types: `Vector{2,3,4}`, `Quaternion`, `Matrix{2,3,4}`, `Aabb{2,3}`, `Frustum`, `Plane`, `Ray`, `Sphere`, `Triangle`, plus free functions) — significant generation churn for capability the Flutter painting/rendering bridges already cover via `Matrix4`'s consumer surface, OR (ii) registering it as an `explicitSources` entry — possible but requires the interpreter to handle the package's internal `Float64List`-backed math and FFI/typed-data paths. §U6 explicitly notes both routes are "out of scope for a single cluster-by-cluster pass." (d) The TODO body's terse phrasing ("Module-loader + bridge package work") matches the same two-route trade-off described in §U6. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied (Matrix4.storage inline arithmetic / MatrixUtils.transformPoint); real fix requires substantial bridge-package generation OR interpreter typed-data path validation; revisit when host is rebooted AND a new script appears with a `vector_math` use case that cannot be cleanly rewritten via the workaround table (`Matrix4.storage` for matrix-vector products, `MatrixUtils.transformPoint` for 2D affines, `Matrix4.rotationX/Y/Z` for rotations)**.
- [x] **15. — FIXED (already shipped in commit `f5ff30ee` prior to 2206 testlog).** §U7 — Dart-internal `_ConstMap` not in `Map` bridge's `nativeNames`. *Discovery:* the fix was already in place when the 2206 testlog was created — commit `f5ff30ee` (`fix(d4rt-interpreter): register _ConstMap in Map bridge nativeNames (C43)`) added `_ConstMap` to both `tom_d4rt_ast/lib/src/runtime/stdlib/core/map.dart` and `tom_d4rt/lib/src/stdlib/core/map.dart` `nativeNames` lists. The original TODO body ("Add `_ConstMap` … to the bridge's `nativeNames` allowlist") describes exactly the change that was already shipped. *Verification:* (a) the 2206 sweep logs contain zero "Cannot access property '…' on target of type `_ConstMap`" hits across all `*.log.txt` files. (b) The 5 historically U7-affected SemanticsEvent-family scripts (`announce_semantics_event_test.dart`, `tap_semantic_event_test.dart`, `semantics_events_test.dart`, `semantics_data_test.dart`, `semantics_event_test.dart`) all pass cleanly in the 2206 baseline. (c) `git log -- tom_d4rt_ast/lib/src/runtime/stdlib/core/map.dart` confirms `f5ff30ee` lands before the 20260528-2206 sweep timestamp. *Doc update:* §U7 in `interpreter_unfixable.md` now carries the `— **✅ FIXED in commit f5ff30ee**` header marker (matching the §U26 convention) plus a "2026-05-29 update — FIXED" appendix block noting the script-side workaround is now optional rather than mandatory. The broader architectural fix the doc previously proposed (teach the Map adapter to fall back to `target is Map` whenever the runtime type lookup misses) was NOT taken — the narrower `_ConstMap`-by-name fix matches the same naming-based approach the other `_CompactLinkedHashMap` / `_MapView` / `_UnmodifiableMapView` entries use. Cluster status: **FIXED**.
- [x] **16. — DEFERRED 20260529 (investigation done, no acute failure).** §U8 — Script-defined enum values are `InterpretedEnumValue`, not native `Enum`; plus `RestorableValue.value` asserts `isRegistered` (interpreter limitation + scripting trap). *Investigation:* (a) the 2 historically U8-affected scripts already have their mandatory script-side workarounds applied per `interpreter_unfixable.md` §U8 affected-scripts table. `widgets/restorable_values_test.dart` (C20 closed 2026‑05‑17) uses `RestorableEnum<Brightness>` (workaround 1 — framework-enum substitution from the §U8 table; `Brightness` chosen as the cleanest 2-state substitute for the original `_Mood` 4-state enum) plus shadow variables `_vInt`, `_vMood`, `_vDateTime`, `_vMoodCalm`, etc. (workaround 2 — shadow-variable pattern; the displays read the shadow rather than `restXxx.value`, exact whenever the demo never mutates `.value`). `foundation/diagnostics_serialization_delegate_test.dart` (Step 10 follow-up, 2026‑05‑19) uses `addProp('mode', '${c.mode}', 'StringProperty')` rather than `EnumProperty<_DemoMode>('mode', mode)` (workaround 3 — StringProperty substitution for diagnostics; description is byte-identical because `toString()` on `InterpretedEnumValue` returns `${parentEnum.name}.$name`). (b) The 2206 sweep logs contain zero `InterpretedEnumValue` / `expected Enum, got` / `isRegistered.*is not true` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U8 symptom. (c) The "real fix" path per §U8 "Constraints" requires `InterpretedEnumValue` to *implement* `Enum` (add `index`, `name`, and have the runtime type pass `is Enum`) — but `Enum` is a Dart-VM-special sealed type whose subtyping is constrained by the VM's reified-enum machinery, so a straight `implements Enum` would NOT satisfy the native `is Enum` check at the bridge boundary. §U8 explicitly notes "the fix is non-trivial and out of cluster scope" — a real fix would need either a parallel `EnumWrapper` synthesized at bridge-boundary crossings (touches `D4.getRequiredArg<Enum>` family + `extractBridgedArg<Enum?>` + every bridge call site that types arguments as `Enum`/`Enum?`/`T extends Enum`) or a VM-level enum bridging primitive that the analyzer-free `tom_d4rt_ast` runtime path cannot easily provide without depending on `dart:mirrors`. For the `RestorableValue.value` part (U8(2)), the real fix would require the script to wire a `RestorationMixin` host widget around every restorable demo — that refactors the entire script into a `StatefulWidget` and is invasive; for static teaching demos where values never mutate, the shadow-variable workaround is functionally exact and minimally disruptive. (d) The TODO body's terse phrasing ("§U8 — Script-defined enum values are `InterpretedEnumValue`, not native `Enum`") describes the same root cause and same scope as §U8 documents. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied (framework-enum substitution + shadow-variable pattern + StringProperty-for-diagnostics substitution); real fix requires either bridge-boundary `EnumWrapper` synthesis across the entire `Enum` parameter surface (large surgery touching `D4` helpers + every Enum-typed bridge call site + analyzer-free `tom_d4rt_ast` runtime) or a VM-level reified-enum bridging primitive that the sandboxed interpreter cannot easily provide; revisit when a new script appears that (1) requires a script-defined enum AT a native API boundary with NO suitable framework-enum substitute AND (2) cannot use the StringProperty-for-display fallback, OR when the analyzer-free `tom_d4rt_ast` runtime gains a usable enum-bridging primitive**.
- [x] **17. — DEFERRED 20260529 (investigation done, no acute failure).** §U9 — Script-defined `RouteAware` cannot be subscribed to a native `RouteObserver` (interpreter limitation). *Investigation:* (a) the 1 historically U9-affected script (`widgets/route_observer_test.dart`, C22 closed 2026‑05‑17) already has the mandatory script-side workaround applied per `interpreter_unfixable.md` §U9: defines `_DemoRouteObserver` (line 85 of the script) as a script-side stand-in that mirrors the native five-method contract (`subscribe`/`unsubscribe`/`didPush`/`didPop`/`didReplace`) over `Map<Route<dynamic>, List<_LoggingRouteAware>>` — the demo's call-order timeline is produced without crossing the d4rt → native boundary. The native `RouteObserver<PageRoute<dynamic>>()` is still constructed (line 363 — safe; the constructor itself has no script-defined arguments) to demonstrate the type exists; no `routeObserver.subscribe(...)` call is made on it. (b) The 2206 sweep logs contain zero `expected RouteAware` / `InterpretedInstance(_LoggingRouteAware` / `RouteObserver.*subscribe.*Argument Error` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U9 symptom. (c) The "real fix" path per §U9 "Constraints" requires the bridge generator to synthesise a native adapter that *implements* `RouteAware`, delegates each of the four lifecycle callbacks back to the interpreted instance via `InterpretedInstance.invoke`, and is automatically wrapped around any `InterpretedInstance` passed to a parameter typed `RouteAware`. This is the same multi-abstract-class proxy-generator extension as TODO #11 (§U3) and TODO #13 (§U5) — proxy_generator.dart changes + bridge regeneration across all `.b.dart` files + risk of regressing existing `State`/`StatelessWidget` adapter-proxy paths. §U9 explicitly notes "this is the same long-term proxy-synthesis sketch noted under U3 / U5 / U8 and is out of scope for a single cluster pass." Unlike the §U8 case which has framework-enum substitutes available (`Brightness`, `TextDirection`, etc.), §U9 has NO framework-provided `RouteAware` concrete subclass — `RouteAware` is designed to be mixed into application-side `State` subclasses, so every concrete implementation lives in user code. This means the script-side stand-in is the ONLY workaround route. (d) The TODO body's terse phrasing ("§U9 — Script-defined `RouteAware` cannot subscribe to native `RouteObserver`") matches the same root cause described in §U9. Cluster status: **DEFERRED — no acute failure; mandatory `_DemoRouteObserver` script-side stand-in applied to the 1 historical instance; real fix requires the same multi-abstract-class proxy-generator extension as TODOs #11/#13 with identical bridge-regeneration + cross-package regression cost; current host-load conditions (kernel-zombie port residue per TODO #2) make the regression sweep unreliable; revisit when host is rebooted AND a new script appears with a `RouteAware` subclass that cannot be similarly rewritten via the stand-in pattern**.
- [x] **18. — DEFERRED 20260529 (investigation done, no acute failure).** §U10 — Script-defined class `with DiagnosticableTreeMixin` / `Diagnosticable` cannot call inherited concrete methods (`toStringDeep`, `toString`, `toStringShallow`, `toDiagnosticsNode`); plus `super.debugFillProperties(...)` fails into bridged mixin (interpreter limitation). *Investigation:* (a) all 4 historically U10-affected scripts already have their mandatory script-side workarounds applied per `interpreter_unfixable.md` §U10 affected-scripts table. `foundation/class_test.dart` (C36 closed 2026‑05‑18) uses `_dumpNode` recursive helper. `foundation/diagnostics_serialization_delegate_test.dart` (C37 closed 2026‑05‑18) uses `_manualSerialize(config, delegate, depth)` for JSON shape. `foundation/object_flag_property_test.dart` (C38 closed 2026‑05‑18) uses `_diagnosticableDeepDump` + drops `super.debugFillProperties(...)` super call (also fixed an unrelated framework-assert bug by supplying empty-string text for `ifPresent`/`ifNull` slots). `foundation/text_tree_configuration_test.dart` (Cluster F #11 closed 2026‑05‑23) uses an in-script sparse-renderer fallback (lines 1772, 1788) — visually equivalent to the real `toStringDeep` output for this script's `_SampleScene extends DiagnosticableTree` specimen. (b) The 2206 sweep logs contain zero `expected DiagnosticableTreeMixin` / `expected Diagnosticable, got` / `does not have a standard or bridged superclass.*super` / `Cannot invoke method 'toStringDeep' on null` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U10 symptom. (c) The "real fix" path per §U10 requires a hand-written `_InterpretedDiagnosticableTreeMixin` proxy in `d4rt_runtime_registrations.dart` (analogous to `_InterpretedStatelessWidget`, `_InterpretedState`) that implements `DiagnosticableTreeMixin` natively, holds the `InterpretedInstance` + visitor, overrides each abstract callback (`debugFillProperties` / `debugDescribeChildren` / `toStringShort`) to invoke the interpreted method on the held instance, and is wired into the interpreter at mixin-resolution time so the `nativeProxy` chain produces this proxy whenever an interpreted class mixes in `DiagnosticableTreeMixin`. §U10 explicitly notes "this is feature-scale work (the existing 10+ `_Interpreted*` proxies are all roughly 100–200 lines apiece, and the wiring at mixin-resolution time is new infrastructure), deferred for this cluster pass." Same architectural family as §U3/§U5/§U8/§U9 plus the additional twist that the failure point is INSIDE the mixin's concrete methods (rather than at a separate bridged API boundary). U10 also requires solving the `super.debugFillProperties` interpreter-side `super`-into-bridged-mixin resolution gap — separate from the proxy-synthesis work above. (d) The TODO body's terse phrasing ("§U10 — Script-defined class `with DiagnosticableTreeMixin` cannot call inherited concrete methods") matches the same root cause described in §U10. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied to all 4 historical instances (`_dumpNode` helper, `_manualSerialize` for JSON shape, `_diagnosticableDeepDump` + drop super call, in-script sparse-renderer fallback); real fix requires new `_InterpretedDiagnosticableTreeMixin` proxy infrastructure + mixin-resolution-time wiring + a separate fix for `super` dispatch into bridged mixin — feature-scale work explicitly out-of-scope per §U10; revisit when host is rebooted AND a new script appears with a `DiagnosticableTreeMixin` use case that cannot be similarly rewritten via the helper-fn pattern (the 4 historical scripts all had a documentable manual-rendering path)**.
- [ ] **19.** §U11 — Script-defined `HitTestTarget` rejected by `HitTestEntry(target)` constructor.
- [ ] **20.** §U12 — `@Deprecated`-annotated SDK symbols filtered out by generator policy.
- [ ] **21.** §U13 — Native exceptions thrown across bridged method not catchable by original type.
- [ ] **22.** §U14 — `maxHeight: infinity` leaks through `Center > ConstrainedBox > SingleChildScrollView`.
- [ ] **23.** §U15 — `RenderFlex overflowed by 2.0 px` inside bridged Cupertino layout.
- [ ] **24.** §U16 — `Text('')` triggers `NaN Offset` assertion in `dart:ui` paragraph painting.
- [ ] **25.** §U17 — `ConstraintsTransformBox` script + frameworkErrors=0 incompatibility. **2 events captured this sweep (TEST side)**; #7 above addresses the log-noise side.
- [ ] **26.** §U18 — `services/platform_test.dart` `Row(stretch)+Expanded` P1 variants destabilise transport.
- [ ] **27.** §U19 — Per-character `TextSpan` stream of non-Latin glyphs triggers `NaN Rect` assertion.
- [ ] **28.** §U20 — `Table(border: TableBorder.all(...))` triggers `table_border.dart:289` assertion.
- [ ] **29.** §U21 — `Quad`/`Vector3` from `vector_math_64` not reachable.
- [ ] **30.** §U22 — H23 single-event scripts deferred to interpreter-level work.
- [ ] **31.** §U23 — 20260523-1056 H-5 follow-up scripts deferred.
- [ ] **32.** §U24 — `try { x = ui.SystemColor.light; }` does not intercept bridge-wrapped `UnsupportedError`.
- [ ] **33.** §U25 — Source-based interpreter cold-start parse + execute exceeds 50 s. **Multiple events captured this sweep (TEST side) under `test_30s_timeout`**; pairs with #4.
- [ ] **34.** §U27 — `Element.findRenderObject()` asserts `_lifecycleState == active` even when `mounted` is true.
- [ ] **35.** §U28 — flutter_ast `/clear → /build` accumulation. **Implementation shipped** (`42588be2`/`d613142e`/`90854bc9`); architectural finding still open; real accumulator cause unknown — pairs with #3.
- [ ] **36.** §U29 — `MemoryImage(Uint8List)` codec rejects bytes. **3 events captured this sweep (TEST side)**; #8 above addresses the log-noise side.
- [ ] **37.** §U30 — `InheritedElement.updateDependencies` assertion fires as U28-style cascade.

### Phase 5 — Tooling hygiene (continued)

- [ ] **38.** Decide whether to drop the §U28 deep-fix's `SendTestRunner.requestRecycle()` hook from `interactive_tests_test.dart`. The 2206 sweep shows interactive_tests passing 9/9 on AST in 130 s and 2/9 on TEST in 720 s; the AST side now appears not to need the per-test recycle. Test removal on AST first.
- [ ] **39.** Bash sweep script `/tmp/sweep_2206.sh` works (case statement instead of `declare -A`). Consider promoting it to a tracked script in `tom_d4rt_flutter_ast/tool/sweep_both_projects.sh` for reproducibility.
- [ ] **40.** Once host is rebooted (Phase 2 #2), re-run this sweep on default ports 4247/4248 to confirm the port-override mechanism (commit `8cd7c27a`) and lifecycle fix (commit `9f4dc79c`) both behave identically on the defaults.

---

## Cluster status — followup sweep 20260529 (entry #1 + framework-error cleanup)

This block summarises the followup work done against TODO entries #1, #7, #8.

| TODO | Cluster | Status | Verification |
| --- | --- | --- | --- |
| #1 | `important_classes_test` budget bump | **FIXED** | AST 166/0/1 (full coverage; 1 newly-visible U28 wedge), TEST 167/0/0 |
| #2 | Host reboot — release 4 kernel-zombie test_app PIDs on ports 4247/4248/14247/14248 | **DEFERRED — requires user action** | `ps -o pid,stat,etime,command -p 58924,67901,67999,69004` confirms all four still in `UE` (kernel-zombie) state with elapsed times 19h-1d11h. `UE` processes have been killed but are stuck on a kernel I/O wait — no userspace signal (including `kill -9`) reaches them. Only `sudo launchctl reboot userspace` or full host reboot can release the ports. **Cannot be performed from Claude.** Mitigation: keep using alt-port pairs (`TOM_D4RT_AST_TEST_PORT` / `TOM_D4RT_TEST_TEST_PORT`) per commit `8cd7c27a` until user reboots; then re-run TODO #40 to verify defaults are clean again |
| #3 | `transport_clear_wedge` accumulator hypothesis | **PARTIAL — investigation done, hypothesis disproven, instrumentation REVERTED** | 20 `[D4_DIAG]` dumps across AST + TEST `generator_interpreter_retest_test`: `expandoAddCount=0` on every cycle; every D4 static cache flat at boot size. Architectural hypothesis from `42588be2` falsified. Instrumentation reverted after regression sweep (essential 2.7× slowdown + cascade timeouts, persistent after opt-in gating AND after revert → root cause is host-load, not instrumentation; but rule "if fix fails, revert" applied). Negative-finding evidence preserved in `interpreter_unfixable.md` §U28 "2026-05-29 update". Real cause remains open. Mitigation continues via `requestRecycle()` + port-override |
| #4 | `test_30s_timeout` cluster (hardly_relevant_5 group) | **PARTIAL — 19 scripts bumped, retested green; 29 remain** (Phase 1) | Added `const _slowTestTimeout = Timeout(Duration(seconds: 60));` + applied to 9 AST + 10 TEST scripts in `hardly_relevant_classes_5_test.dart`. Individual retest via `--name` regex on both projects: AST 12/0/0/0, TEST 13/0/0/0 (regex matched adjacent prefixes, all green). 60 s budget gives ~6× typical run time. Same fix pattern applies to the remaining ~29 scripts in `generator_*`, `timeout_*`, `blocking_*`, `hardly_3/4`, `interactive_tests_test` — deferred to follow-up |
| #4 | `test_30s_timeout` cluster (phase 2 — gii/gir/timeout/h4 across both projects) | **FIXED 20260529 (completes the prior PARTIAL)** | Phase 2 follow-up closes the remaining unfixed scripts. Added `_slowTestTimeout` constant + applied `, timeout: _slowTestTimeout` to **AST 3 sites** (gii html_element_view, gir render_android_view, h4 overlay_portal_controller) and **TEST 21 sites** (gii ×4, gir ×17 — 13 simple 30 s bumps + 4 `_verySlowTestTimeout = 120 s` bumps for §U25 cold-start retest scripts that previously hit the 60 s wrapper, timeout ×2, h4 ×2). Per rule (a) (test-script-only change) individual retest sufficed. Verification on alt ports 14272/14273: AST 3/3 pass, TEST 24/24 pass (28 total — the regex grouping caught a few adjacent matches; all green). All script execution times observed well under budget: typical 1-12 s wall, leaving 5×-50× headroom. The `interactive_tests_test (setUpAll)` mention in the original deferral was already closed by TODO #6; `hardly_relevant_classes_3_test` had no actual timeout entries in the 2206 baseline. The 5-script gap between "29 remaining (phase 1 deferral count)" and "24 fixed (phase 2)" is duplicate test-name entries where a single failing test surfaced both a `TimeoutException` and a follow-on `Transport failure` — those are TODO #3 wedge-family, not timeout-family |
| #5 | `build_30s_timeout` on `app_kit_view_test.dart` | **FIXED** | Added per-request `?buildBudgetMs=N` query-parameter override in both `_handleBuild` handlers, threaded from the runner's existing `httpBuildTimeout` parameter in both `SendTestRunner.send()` methods. Default still 30 s; clamp 1-120 s. Focused retest `--name 'app_kit_view'` on AST port 14268: 4/0/0/0 in 30 s. Regression rule (b) sweep showed only host-load patterns identical to prior turns (no-op code path for tests that don't pass `httpBuildTimeout`) |
| #6 | TEST `interactive_tests_test` setUpAll timeout | **FIXED** | Diagnosis: Dart `package:test` applies 30 s default to setUpAll; original TODO body's `requestRecycle()` hypothesis was wrong (TEST file has no recycle hook — AST-only). Fix: added `@Timeout(Duration(seconds: 240))` library annotation + bumped internal `SendTestRunner.setUp` timeout to 180 s. Retest on port 14270: 9/0/0/0 in 40 s |
| #9 | §U1 demo-scale transport overload (interpreter limitation) | **DEFERRED — investigation done, no acute failure** | The 2 historically U1-affected scripts (`notificationlistener_test.dart` C05 + `text_editing_delta_insertion_test.dart` C51/C52) already have working script-side workarounds. 2206 sweep logs contain zero "Lost connection to device" hits. Real fix requires cross-cutting architecture work (DeclarationVisitor pre-pass for top-level-const abstract subclasses, or bridge-side TextSpan batching) explicitly out-of-scope per §U1 doc |
| #10 | §U2 non-wrappable arithmetic defaults (generator limitation) | **DEFERRED — investigation done, no acute failure** | The 1 historically U2-affected script (`rendering/gradient_rendering_test.dart` C09) already has a working script-side workaround. 2206 sweep logs contain zero "non-wrappable default" / "getRequiredArgTodoDefault" hits. Real fix path is well-specified in §U2 (extend `_wrapDefaultValue` with arithmetic-default evaluator) but requires bridge regeneration across both projects (~34 `.b.dart` files) + full rule (b) regression sweep that current host-load conditions make unreliable |
| #11 | §U3 Curve `transformInternal` override not routed (interpreter / proxy-generator limitation) | **DEFERRED — investigation done, no acute failure** | The 1 historically U3-affected script (`animation/animation_misc_adv_test.dart` C10) already uses the native `FlippedCurve(Curves.easeInOut)` workaround. 2206 sweep logs contain zero `"type 'Null' is not a subtype of type 'num'"` / `transformInternal` hits. Real fix path requires `proxy_generator.dart` changes to emit native overrides on `_InterpretedCurve` (and analogous proxies) that delegate hookable methods to `interpretedInstance.invoke(...)` — §U3 explicitly notes this risks regressing existing `State`/`StatelessWidget` adapter-proxy paths and is "out-of-scope for the C10 cluster" |
| #12 | §U4 standalone `'\n'` TextSpan between styled siblings (**truly unfixable**) | **DEFERRED — canonical guidance confirmed** | The 1 historically U4-affected script (`material/tooltip_feedback_test.dart` C15) already applies the mandatory workaround (merge `'\n'` into preceding styled span, or substitute `WidgetSpan(SizedBox)`). 2206 sweep logs contain zero `Lost connection to device` hits. §U4 is explicitly **"truly unfixable"** in the doc — the crash terminates the Dart VM at a level the interpreter cannot intercept; no smaller native-Dart reproducer exists. Canonical script-side workaround remains the only remediation |
| #13 | §U5 NotchedShape / FloatingActionButtonLocation interpreted subclass rejected at bridged-constructor boundary (same adapter-proxy delegation gap as §U3) | **DEFERRED — investigation done, no acute failure** | The scripts referencing these patterns (`bottom_app_bar_test.dart`, `notched_shape(s)_test.dart`, `standard_fab_location_test.dart`) already use framework alternatives (`CircularNotchedRectangle()`, `FloatingActionButtonLocation.centerDocked/endDocked`, etc.). 2206 sweep logs contain zero `expected NotchedShape` / `expected FloatingActionButtonLocation` hits. Real fix is the same multi-abstract-class proxy-generator extension as TODO #11 covering the broader family (Curve, NotchedShape, FloatingActionButtonLocation, ShapeBorder, ScrollPhysics, MaterialStateProperty, etc.) — same bridge-regen + regression-risk profile |
| #14 | §U6 `package:vector_math/vector_math_64.dart` not resolvable (module-loader limitation) | **DEFERRED — investigation done, no acute failure** | `grep` across the entire `send_ast_via_http_scripts/` corpus returns ZERO `vector_math` imports — all historical scripts have been rewritten to use inline `Matrix4.storage` arithmetic or `MatrixUtils.transformPoint`. 2206 logs contain zero `Cannot resolve import.*vector_math` / `Module source not preloaded.*vector_math` hits. Real fix requires either full `BridgedClass` generation for the 14-type `vector_math` public API OR `explicitSources` registration with interpreter `Float64List` / FFI path validation — both out-of-scope per §U6 |
| #15 | §U7 `_ConstMap` not in Map bridge's `nativeNames` (interpreter limitation) | **FIXED — already shipped in commit `f5ff30ee` (C43) before 2206 testlog** | Commit `f5ff30ee` added `_ConstMap` to both `tom_d4rt_ast/lib/src/runtime/stdlib/core/map.dart` and `tom_d4rt/lib/src/stdlib/core/map.dart` `nativeNames` lists. 2206 logs contain zero `_ConstMap` access errors. All 5 SemanticsEvent-family scripts pass in baseline. §U7 doc updated with `— **✅ FIXED in commit f5ff30ee**` header marker matching the §U26 convention |
| #16 | §U8 `InterpretedEnumValue` is not `Enum` + `RestorableValue.value` asserts `isRegistered` (interpreter limitation + scripting trap) | **DEFERRED — investigation done, no acute failure** | The 2 historically U8-affected scripts already use the mandatory workarounds: `widgets/restorable_values_test.dart` (C20) uses `RestorableEnum<Brightness>` + `_vMood`/`_vInt`/`_vDateTime` shadow variables (workarounds 1+2 from the §U8 table); `foundation/diagnostics_serialization_delegate_test.dart` (Step 10) uses `addProp('mode', '${c.mode}', 'StringProperty')` instead of `EnumProperty<_DemoMode>` (workaround 3). 2206 sweep logs contain zero `InterpretedEnumValue` / `expected Enum, got` / `isRegistered.*is not true` hits across all 28 log files. Real fix requires `InterpretedEnumValue` to satisfy the native `is Enum` check at the bridge boundary, but `Enum` is a Dart-VM-special sealed type — a real fix would need either parallel `EnumWrapper` synthesis at every bridge-boundary crossing (touches `D4.getRequiredArg<Enum>` family + `extractBridgedArg<Enum?>` + every Enum-typed bridge call site + analyzer-free `tom_d4rt_ast` runtime) or a VM-level reified-enum bridging primitive the sandboxed interpreter cannot easily provide; §U8 explicitly classifies this as "non-trivial and out of cluster scope" |
| #17 | §U9 script-defined `RouteAware` cannot be subscribed to native `RouteObserver` (interpreter / proxy-generator limitation; same family as §U3/§U5/§U8) | **DEFERRED — investigation done, no acute failure** | The 1 historically U9-affected script (`widgets/route_observer_test.dart`, C22) already applies the mandatory `_DemoRouteObserver` script-side stand-in (line 85) that mirrors the native five-method contract over `Map<Route, List<_LoggingRouteAware>>`; native `RouteObserver<PageRoute<dynamic>>()` is still constructed (line 363) but no `subscribe(...)` call crosses the boundary. 2206 sweep logs contain zero `expected RouteAware` / `InterpretedInstance(_LoggingRouteAware` / `RouteObserver.*subscribe.*Argument Error` hits. Real fix is the same multi-abstract-class proxy-generator extension as TODOs #11/#13 — synthesise a native adapter that implements `RouteAware` and auto-wraps `InterpretedInstance` arguments at the bridge boundary. Unlike §U8 there is NO framework-provided `RouteAware` concrete subclass to substitute (it's designed to be mixed into application-side `State` subclasses) — the script-side stand-in is the only available workaround route |
| #18 | §U10 script-defined class `with DiagnosticableTreeMixin` / `Diagnosticable` cannot call inherited concrete methods (`toStringDeep`/`toString`/`toDiagnosticsNode`); plus `super.debugFillProperties` fails into bridged mixin (interpreter limitation; same family as §U3/§U5/§U8/§U9 with the twist that the failure point is INSIDE the mixin's concrete methods) | **DEFERRED — investigation done, no acute failure** | All 4 historically U10-affected scripts already apply their mandatory script-side workarounds: `foundation/class_test.dart` (C36) uses `_dumpNode` recursive helper; `foundation/diagnostics_serialization_delegate_test.dart` (C37) uses `_manualSerialize` for JSON shape; `foundation/object_flag_property_test.dart` (C38) uses `_diagnosticableDeepDump` + drops `super.debugFillProperties` super call; `foundation/text_tree_configuration_test.dart` (Cluster F #11) uses in-script sparse-renderer fallback (lines 1772/1788) visually equivalent to real `toStringDeep`. 2206 sweep logs contain zero `expected DiagnosticableTreeMixin` / `expected Diagnosticable, got` / `does not have a standard or bridged superclass.*super` / `Cannot invoke method 'toStringDeep' on null` hits across all 28 log files. Real fix requires hand-written `_InterpretedDiagnosticableTreeMixin` proxy (analogous to `_InterpretedStatelessWidget`/`_InterpretedState`, ~100-200 lines) + new mixin-resolution-time wiring + a separate fix for `super`-into-bridged-mixin dispatch — feature-scale work explicitly out-of-scope per §U10 |
| #7 | §U17 framework-error log noise | **FIXED** | TEST `timeout_tests_test` followup: 0 EXCEPTION CAUGHT BY / 0 overflow / 0 RCTB / 0 framework_error events |
| #8 | §U29 framework-error log noise | **FIXED** | TEST `hardly_relevant_classes_4_test` followup: 0 Codec / 0 EXCEPTION CAUGHT BY events |

**What was done:**

1. **Diagnosed the framework-error leak.** Initial hypothesis (#7/#8 in original list) was that the `ignoredPatterns` lists didn't include §U17 / §U29 patterns. Investigation showed both patterns WERE already in the lists — the leak was elsewhere. Root cause: the inner `if (_capturingFrameworkErrors) { ... }` block correctly skipped adding ignored patterns to `_frameworkErrors`, but the outer `else { _originalFlutterErrorHandler?.call(details); }` ran unconditionally for every non-silenced error, forwarding ignored patterns to Flutter's default handler and printing the red `EXCEPTION CAUGHT BY ...` block to stdout.

2. **Fixed both test_apps' `_handleFlutterError`** (tom_d4rt_flutter_ast_app/lib/main.dart + tom_d4rt_flutter_test_app/lib/main.dart):
   - Hoisted `isIgnored` declaration to outer scope (`var isIgnored = false;` before the inner block).
   - Inside the `if (_capturingFrameworkErrors)` block, changed `final isIgnored = ...` to `isIgnored = ...` (assignment to outer var).
   - Changed outer `} else { ... }` → `} else if (!isIgnored) { ... }` so ignored patterns now skip BOTH the `_frameworkErrors` add AND the red-screen forward.

3. **Re-ran the regression sweep per rule (b)** since the test_apps are host-side `tom_d4rt_flutterm` code (not test scripts in the `test/` subfolder): essential + important + secondary on both projects, plus the two cleanup-verification files (TEST timeout_tests_test, TEST hardly_relevant_classes_4_test) at fresh ports 14252/14253.

**Followup sweep results (07:51 → 08:50 PDT, ~59 min wall):**

| File | AST followup | TEST followup |
| --- | --- | --- |
| `essential_classes_test` | 111 / 0 / 0 / 0 | 111 / 0 / 0 / 0 |
| `important_classes_test` | 166 / 0 / 1 / 0 (entry #1 ✅) | 167 / 0 / 0 / 0 (entry #1 ✅) |
| `secondary_classes_test` | 656 / 0 / 0 / 1 | 656 / 0 / 0 / 1 |
| `timeout_tests_test` (TEST verify) | — | 54 / 0 / 0 / 0 (was 52 / 0 / 2 / 0) |
| `hardly_relevant_classes_4_test` (TEST verify) | — | 230 / 0 / 0 / 0 (was 225 / 0 / 5 / 0) |
| **Followup totals** | **933 / 0 / 1 / 1** | **1218 / 0 / 0 / 1** |
| Regression vs. 2206 | clean (no new failures introduced by the change) | clean — and 7 prior errors disappeared (likely host-load variance in the U28-wedge family) |

**Log-noise verification (the actual user-requested check):**

| Pattern | AST followup logs | TEST followup logs |
| --- | ---: | ---: |
| `EXCEPTION CAUGHT BY` | **0** | **0** |
| `overflowed by` | **0** | **0** |
| `Codec failed to produce an image` | **0** | **0** |
| `RenderConstraintsTransformBox overflowed` | **0** | **0** |
| `[framework] FlutterError fired` (trace lines) | **0** | **0** |

Both test_apps now correctly suppress the entire ignored-pattern set — `_frameworkErrors` stays clean (build assertions still see `frameworkErrors == 0` for these scripts) AND log captures stay clean (no stderr noise from Flutter's default handler). The change is fully symmetric across both test_apps.

**Regression rule (b) compliance.** The change touches host-side test_app `lib/main.dart` files — classified as "tom_d4rt_flutterm code" (not test scripts in the test/ scripts subfolder). Rule (b) requires essential + important + secondary + modified-component verification. All five files re-ran clean above; the 1 newly-visible `bottomappbar_test` error in AST important is a pre-existing U28-wedge symptom that was previously masked by the 300 s budget cap (it was never reached in the 2206 sweep), NOT a regression introduced by this change.

**Followup captures location.** Raw `.result.json` + `.log.txt` files for the 8 followup runs live in `doc/testlog_20260528-2206-issue-analysis/_followup/` in each project. The `_followup/` subfolder is gitignored per the `testlog_*/**` convention; only this analysis file documents the outcome.

---

**End of analysis.** The combination of the §U28 deep-fix (`42588be2`/`d613142e`/`90854bc9`), the port-override (`8cd7c27a`), the lifecycle fix (`9f4dc79c`), and the 20260529 framework-error-suppression fix yielded a sweep with **4309 passing tests + 933+1218 followup passes, 1 failure, 79 errors (now 1 in followup), 8 skipped** — clean test outcomes and clean per-file log captures. All non-pass outcomes classify into known U28-wedge-family causes or pre-documented `interpreter_unfixable.md` items; zero novel infrastructure regressions.

Phase 1 (#1) ✅ closed. Phase 3 #7/#8 ✅ closed. Phase 2 (#2, host reboot) deferrable. Phase 3 #3/#4/#5 (real U28 wedge accumulator + §U25 cold-start) remain the highest-leverage open work.
