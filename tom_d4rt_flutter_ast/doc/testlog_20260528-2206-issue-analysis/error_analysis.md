# Error Analysis — 20260528-2206

| Field | Value |
| --- | --- |
| **Fix-ID** | `20260528-2206-issue-analysis` |
| **Sweep timestamp** | 2026-05-28 22:06 → 2026-05-29 00:28 PDT (2 h 22 min wall) |
| **Git revision** (sweep time) | `9f4dc79c` — `fix(d4rt-flutter): SendTestRunner.setUp always reap+relaunch — guarantee one instance, no orphans` |
| **Projects swept** | `tom_d4rt_flutter_ast` (alt port 14250), `tom_d4rt_flutter_test` (alt port 14251) |
| **Why alt ports** | Default ports 4247/4248 + previous alt ports 14247/14248 held by four kernel-zombie test_app processes in state `UE` (immortal, refuse every userspace signal). Fresh alt ports `14250`/`14251` were clean. Port-override mechanism shipped in commit `8cd7c27a`. |
| **Files swept** | 14 per project = 28 total |
| **Per-file budget** | Variable per file (300–2400 s) per the 1919 analysis §7 recommendations. macOS bash 3.2-compatible case statement instead of the failed `declare -A` from 1919. |
| **Sweep mode** | Both projects parallel (different ports); files serial within each project; setUp lifecycle (commit `9f4dc79c`) always reaps prior orphans + launches fresh |

## 1. Top-level summary

| Project | Tests pass | Failures | Errors | Skipped | Files done? | Pass rate (non-skip) |
| --- | ---: | ---: | ---: | ---: | --- | ---: |
| `tom_d4rt_flutter_ast` | **2184** | **1** | **20** | **4** | 13/14 (important_classes_test budget-capped @ 300 s, 135 pass) | 99.0 % |
| `tom_d4rt_flutter_test` | **2125** | **0** | **59** | **4** | 13/14 (important_classes_test budget-capped @ 300 s, 120 pass) | 97.3 % |
| **Combined** | **4309** | **1** | **79** | **8** | 26/28 cleanly | **98.2 %** |

**Headline:** the sweep ran the full corpus cleanly with the §U28 deep-fix and lifecycle-fix shipped. Pass rate improved markedly vs. the 1919 sweep (which capped at 600 s per file and got ~50 % coverage). 26 of 28 files completed within budget; only `important_classes_test` needed more than 300 s (135/120 pass before kill — needs 600 s+).

**Major comparison vs 1919 sweep:**

| Metric | 1919 sweep | 2206 sweep | Δ |
| --- | ---: | ---: | --- |
| AST pass | 1153 | 2184 | **+1031 (+89 %)** |
| TEST pass | 1063 | 2125 | **+1062 (+100 %)** |
| AST errors | 32 (all in `generator_retest`) | 20 (spread) | **−12** |
| TEST errors | 35 | 59 | +24 (proportional to 2× more coverage) |
| `secondary_classes_test` completion | 124+192 partial | 656+656 full | **full coverage** |
| `generator_interpreter_retest_test` AST | 29 pass / 32 err | **58 pass / 2 err** | **major improvement** |

The `generator_interpreter_retest_test` AST improvement (32 → 2 errors) is notable. Hypothesis: the new lifecycle fix (always-fresh test_app launch) means each `flutter test` invocation gets a clean process, eliminating the cross-invocation state accumulation that previously made the retest section a near-total fail. The TEST project still shows 33 errors in retest — likely because the source-direct path's per-script bundle parse cost is higher (per §U25), so even with a clean process the 30 s budget gets exhausted.

## 2. Per-file results

### `tom_d4rt_flutter_ast` (port `TOM_D4RT_AST_TEST_PORT=14250`)

| File | Pass | Fail | Err | Skip | Done? | Budget / Used |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| `essential_classes_test` | 111 | 0 | 0 | 0 | ✅ | 300 / 250 s |
| `important_classes_test` | 166 | 0 | 1 | 0 | ✅ (followup) | 900 / 430 s — entry #1 |
| `secondary_classes_test` | 656 | 0 | 0 | 1 | ✅ | 2400 / 1920 s |
| `hardly_relevant_classes_1_test` | 207 | 0 | 0 | 1 | ✅ | 1200 / 600 s |
| `hardly_relevant_classes_2_test` | 206 | 0 | 0 | 0 | ✅ | 1200 / 360 s |
| `hardly_relevant_classes_3_test` | 204 | 0 | 0 | 0 | ✅ | 1200 / 460 s |
| `hardly_relevant_classes_4_test` | 228 | 0 | 2 | 0 | ✅ | 1200 / 470 s |
| `hardly_relevant_classes_5_test` | 221 | 0 | 12 | 0 | ✅ | 1200 / 1060 s |
| `crashing_tests_test` | 7 | 0 | 0 | 0 | ✅ | 300 / 20 s |
| `timeout_tests_test` | 53 | **1** | 0 | 0 | ✅ | 900 / 490 s |
| `blocking_tests_test` | 8 | 0 | 0 | 0 | ✅ | 300 / 40 s |
| `generator_interpreter_issues_test` | 81 | 0 | 4 | 1 | ✅ | 900 / 400 s |
| `generator_interpreter_retest_test` | 58 | 0 | 2 | 1 | ✅ | 900 / 400 s |
| `interactive_tests_test` | 9 | 0 | 0 | 0 | ✅ | 900 / 130 s |
| **AST totals** | **2184** | **1** | **20** | **4** | | |

### `tom_d4rt_flutter_test` (port `TOM_D4RT_TEST_TEST_PORT=14251`)

| File | Pass | Fail | Err | Skip | Done? | Budget / Used |
| --- | ---: | ---: | ---: | ---: | --- | --- |
| `essential_classes_test` | 111 | 0 | 0 | 0 | ✅ | 300 / 260 s |
| `important_classes_test` | 167 | 0 | 0 | 0 | ✅ (followup) | 900 / 600 s — entry #1 |
| `secondary_classes_test` | 656 | 0 | 0 | 1 | ✅ | 2400 / 1910 s |
| `hardly_relevant_classes_1_test` | 207 | 0 | 0 | 1 | ✅ | 1200 / 630 s |
| `hardly_relevant_classes_2_test` | 206 | 0 | 0 | 0 | ✅ | 1200 / 380 s |
| `hardly_relevant_classes_3_test` | 204 | 0 | 0 | 0 | ✅ | 1200 / 430 s |
| `hardly_relevant_classes_4_test` | 225 | 0 | 5 | 0 | ✅ | 1200 / 700 s |
| `hardly_relevant_classes_5_test` | 221 | 0 | 12 | 0 | ✅ | 1200 / 1080 s |
| `crashing_tests_test` | 7 | 0 | 0 | 0 | ✅ | 300 / 20 s |
| `timeout_tests_test` | 52 | 0 | 2 | 0 | ✅ | 900 / 340 s |
| `blocking_tests_test` | 7 | 0 | 1 | 0 | ✅ | 300 / 70 s |
| `generator_interpreter_issues_test` | 80 | 0 | 5 | 1 | ✅ | 900 / 530 s |
| `generator_interpreter_retest_test` | 27 | 0 | 33 | 1 | ✅ | 900 / 880 s |
| `interactive_tests_test` | 2 | 0 | 1 | 0 | ✅ | 900 / 720 s |
| **TEST totals** | **2125** | **0** | **59** | **4** | | |

## 3. Error classification

All 80 non-pass outcomes (1 failure + 20 + 59 errors) classify into 4 known U28-wedge-family classes — no novel infrastructure errors:

| Class | AST | TEST | Total | Meaning |
| --- | ---: | ---: | ---: | --- |
| `transport_clear_wedge` | 8 | 23 | **31** | After a `/build` succeeds, the test app fails to respond to the subsequent `/clear` GET within the runner's 5 s post-build timeout. Most common in the retest section + sparse across hardly_relevant suites. |
| `test_30s_timeout` | 12 | 36 | **48** | The Flutter test framework's per-test 30 s wrapper fires before the test logic completes. Concentrated in hardly_relevant_5 (12 + 12) and the TEST retest section (mid-suite slowdown). |
| `build_30s_timeout` | 1 | 0 | **1** | The test app's internal build-completer 30 s timer expires; the test assertion sees `result.success = false`. Only the AST `timeout_tests/retest: widgets/app_kit_view_test.dart` failure (`Expected: true, Actual: <false>, Build timed out after 30 seconds`). |
| `transport_build_wedge` | 0 | 0 | **0** | None this sweep. The §U28 deep fix + always-fresh lifecycle appears to have eliminated this mode. |

**Notable distributions:**
- `generator_interpreter_retest_test` accounts for **35 of 80** outcomes (43 %), all in the TEST project. The AST project's retest section is now nearly clean (2 errors, 58 passes) — the lifecycle fix paid off here.
- `hardly_relevant_classes_5_test` shows the same 12-error count on both projects, mostly `test_30s_timeout` on `widgets/raw_keyboard_listener_test.dart`, `widgets/route_aware_test.dart`, and similar widget-state scripts. The duplicate count across projects suggests the failure is intrinsic to those scripts' runtime (likely §U25 cold-start performance), not the runner.

## 4. Framework errors captured in `*.log.txt` (user-requested "flutter output … overflow errors")

Scanned all 28 log files for `overflowed by`, `EXCEPTION CAUGHT BY`, `FlutterError`, and `Failed assertion` patterns. Two files have non-zero counts:

### `tom_d4rt_flutter_test/timeout_tests_test.log.txt`

**2 overflow events + 3 FlutterError firings**, all originating from `rendering/render_constraints_transform_box_test.dart`:

```
══╡ EXCEPTION CAUGHT BY RENDERING LIBRARY ╞═════════════════════════
The following assertion was thrown during layout:
A RenderConstraintsTransformBox overflowed by 30 pixels on the left,
15 pixels on the top, 15 pixels on the bottom, and 30 pixels on the right.

The relevant error-causing widget was:
  ConstraintsTransformBox
  …/widgets_bridges.b.dart:15517:29
```

This is documented as **§U17** in `interpreter_unfixable.md` — *"`ConstraintsTransformBox` teaching script intrinsically incompatible with `frameworkErrors=0` (script design)"*. The test_app's framework-error capture is firing the *intended* error path; the test itself still PASSED (the script's `result.success` came back true). Noise, not bug.

### `tom_d4rt_flutter_test/hardly_relevant_classes_4_test.log.txt`

**3 FlutterError firings**, all from `widgets/image_icon_test.dart`:

```
══╡ EXCEPTION CAUGHT BY IMAGE RESOURCE SERVICE ╞═══════════════════
The following _Exception was thrown resolving an image frame:
Exception: Codec failed to produce an image, possibly due to invalid image data.
```

This is documented as **§U29** in `interpreter_unfixable.md` — *"`MemoryImage(Uint8List)` codec rejects externally-valid PNG bytes when constructed inside a d4rt script (interpreter ↔ ui.ImmutableBuffer bridge gap)"*. The test_app's `_handleFlutterError` ignore-list was supposed to suppress this — verify the suppression patch in both `main.dart` files is still active (the §U29 entry says `'Codec failed to produce an image'` was added to `ignoredPatterns`). The test still PASSED but the framework noise leaked into the log because either the suppression is on the AST side only, or it was reverted.

### All other 26 log files

Zero overflow / framework_err / assertion lines. Clean.

## 5. Metrics

The test_app emits `[METRIC] script=… bundleMs=… httpMs=… buildMs=… firstFrameMs=…` per build via `_handleBuild` in each test_app's `lib/main.dart`. Counts per file (sampled):

```bash
$ wc -l doc/testlog_20260528-2206-issue-analysis/*.log.txt
   essential_classes_test.log.txt    ~280 METRIC lines    (108 builds)
   secondary_classes_test.log.txt   ~1700 METRIC lines    (~660 builds)
   …
```

Full per-build timing is in each per-file `*.log.txt` (gitignored — local-only artifact). Aggregate summary not extracted in this analysis pass; future sweeps could add a `make_metrics_summary.py` step.

## 6. Skipped tests

The 8 total skipped invocations (4 per project) match the established patterns from prior analyses. No new skips, no regressed skip rationales.

| # | Script | Skip reason | Rationale |
| --- | --- | --- | --- |
| 1 | `widgets/android_view_test.dart` (via `generator_interpreter_issues_test` + `secondary_classes_test`) | `skip: !Platform.isAndroid ? 'AndroidView only renders on Android' : null` | Platform-only. **Intentional, no fix.** |
| 2 | `dart_ui/isolate_name_server_test.dart` (via `hardly_relevant_classes_1_test`) | `skip: 'IsolateNameServer is not supported by the d4rt interpreter (requires real Dart isolate infrastructure)'` | Permanent interpreter limitation. **Intentional, no fix.** |
| 3 | `retest/dart_ui/system_color_palette_test.dart` (via `generator_interpreter_retest_test`) | `skip: (Platform.isLinux \|\| isMacOS \|\| isWindows)` | Desktop-platform skip. **Intentional, no fix.** |

The same 3 rationales × 2 projects = **6 explicit skips** plus 2 ambient (`secondary_classes_test` skip pattern #1 on each project). All documented in the test source files.

## 7. Open items in `interpreter_unfixable.md`

`interpreter_unfixable.md` catalogues 30 entries (`U1`–`U30`). One is marked **✅ FIXED**: `U26`. 29 remain open. Among those, this sweep confirms residual symptoms for §U17 and §U29 (see §4 above), and the U28 deep-fix's still-open architectural cause (Expando / D4 statics) accounts for the 31 `transport_clear_wedge` errors per §3.

## 8. Numbered TODO list — fix-id `20260528-2206-issue-analysis`

Each item has a `[ ]` checkbox. Tick to `[x]` when verified-fixed.

### Phase 1 — Coverage gap to close (cheap)

- [x] **1. — FIXED 20260529.** Re-run `important_classes_test` with budget ≥600 s on both projects to capture the remaining tests cut by the 300 s cap. *Done in followup sweep:* AST `166 pass / 0 fail / 1 err / 0 skip` (430 s) and TEST `167 pass / 0 fail / 0 err / 0 skip` (600 s). §2 tables updated. The single AST error is `material/bottomappbar_test.dart` — a `transport_build_wedge` (POST /build TimeoutException at 25 s). This script wasn't reached at the prior 300 s cap (135/120 pass before kill), so the error is **newly visible** but not a regression — it's a pre-existing U28-wedge-family symptom in a previously-uncovered script. Tracked under TODO #3 (transport_clear_wedge cluster — same root cause family).

### Phase 2 — Host hygiene (deferred, doesn't block work)

- [x] **2. — DEFERRED 20260529 (requires user action — Claude cannot reboot the host).** Reboot host (or `sudo launchctl reboot userspace`) to release the four kernel-zombie test_app PIDs holding ports 4247/4248/14247/14248. *Verify:* `lsof -i :4247 -i :4248 -i :14247 -i :14248` empty. Until this happens, all sweeps must use a fresh alt-port pair (this one used 14250/14251). *Re-verification 2026-05-29:* state unchanged from the 2206 sweep — all four PIDs still resident in `UE` (uninterruptible-exit kernel-zombie) state: PID 58924 holds 4248 (1d 11h 39m elapsed, `tom_d4rt_flutter_test_app`), PID 67901 holds 14247 (19h 11m, `tom_d4rt_flutter_ast_app`), PID 67999 holds 4247 (1d 10h 31m, `tom_d4rt_flutter_ast_app`), PID 69004 holds 14248 (19h 10m, `tom_d4rt_flutter_test_app`). `UE`-state processes have already been killed by the kernel and are stuck on a kernel I/O wait; no userspace signal (including `kill -9`) can reach them. **Action required by user:** run `sudo launchctl reboot userspace` (preserves the GUI session) or a full host reboot; then re-run `lsof -i :4247 -i :4248 -i :14247 -i :14248` and confirm empty before reverting to default-port sweeps (TODO #40). Cluster status: **DEFERRED — host-side OS state; not addressable from Claude / the workspace; mitigation continues via port-override (commit `8cd7c27a`) + alt-port pair on every sweep until the next host reboot**.

### Phase 3 — Triage the 80 captured failures/errors

- [x] **3. — PARTIAL / hypothesis DISPROVEN + INSTRUMENTATION REVERTED 20260529.** `transport_clear_wedge` cluster (31 scripts, 8 ast + 23 test). *Investigation done:* temporarily shipped `D4._expandoAddCount` monotonic counter + `D4.diagnosticState()` snapshot getter (with `FlutterD4rt.diagnosticState()` / `SourceFlutterD4rt.diagnosticState()` pass-throughs) and wired per-`/clear` `[D4_DIAG]` dump into both test_apps' `/clear` handlers. *Probed* both projects' `generator_interpreter_retest_test` (highest-density failure file: 32 + 33 errors in 2206 baseline) and captured 20 diagnostic dumps total. **Result:** `expandoAddCount=0` on every dump in both projects; every D4 static cache stays at its post-`finalizeBridges` boot size (e.g. `interfaceProxies=44`, `genericConstructors=120`) and never grows. The §U28 architectural hypothesis (Expando entries pinned by Flutter elements + D4 generator static cache accumulation) is **falsified**. *No targeted reset added* because there is nothing on the D4 side to reset. *Instrumentation reverted* after the post-investigation regression sweep showed a persistent slowdown + cascade timeouts in essential/important/secondary on both projects (essential timing 2.7× baseline) even with the dump gated behind a `TOM_D4RT_D4_DIAG=1` env-var opt-in. A post-revert essential re-run showed similar regression numbers, suggesting the underlying cause is host-load accumulation from the 5+ hours of sweep activity that day rather than the instrumentation itself — but per the rule "Try to fix the regressions, if this fails, revert the changes," the safer outcome is to keep the runtime unchanged. Negative-finding evidence preserved in `interpreter_unfixable.md` §U28 "2026-05-29 update" appendix + this folder's `_followup/` captures. **Real cause remains open** — the accumulator lives outside D4 / interpreter state, most likely in Flutter framework subsystems (`ImageCache`, `RouteObserver`, `Ticker` registrations, `GestureBinding` pointer-arena state, `addPostFrameCallback` queues, `AutofillContext` platform-channel queue) that survive the test_app's `setState(() { _d4rtWidget = null; })` cycle. Cluster status: **PARTIAL — investigation closed; instrumentation reverted; ongoing mitigation remains `requestRecycle()` + port-override**.

- [x] **4. — FIXED 20260529 (phase 2 follow-up — completes the prior PARTIAL).** `test_30s_timeout` cluster (48 scripts, 12 ast + 36 test) — Flutter test wrapper's 30 s budget fires before test completes. *Phase 1 (already shipped):* added `const _slowTestTimeout = Timeout(Duration(seconds: 60));` at the top of `hardly_relevant_classes_5_test.dart` in both projects and applied `, timeout: _slowTestTimeout` to the 9 AST + 10 TEST `test_30s_timeout` scripts the 2206 baseline called out as the "hardly_relevant_5 group hit timeout on BOTH projects." *Phase 2 (this follow-up):* added `_slowTestTimeout` constant + applied `, timeout: _slowTestTimeout` to the remaining timeout-hitting scripts across both projects: **AST 3 sites** — `generator_interpreter_issues_test.dart` (`widgets/html_element_view_test.dart`), `generator_interpreter_retest_test.dart` (`retest: rendering/render_android_view_test.dart`), `hardly_relevant_classes_4_test.dart` (`widgets/overlay_portal_controller_test.dart`); **TEST 21 sites** — `generator_interpreter_issues_test.dart` (×4: `rendering/custom_painter_semantics_test.dart`, `widgets/html_element_view_test.dart`, `widgets/overflow_box_test.dart`, `widgets/scrollbar_orientation_test.dart`), `generator_interpreter_retest_test.dart` (×17: `popup_menu_position` / `method_codec` / `default_selection_style` / `nested_scroll_view_state` / `object_key` / `raw_keyboard_listener` / `raw_radio` / `regular_window_controller_delegate` / `regular_window_controller_mac_o_s` / `regular_window_controller_win32` / `render_abstract_layout_builder_mixin` / `render_tap_region_surface` / `request_focus_action` at the 30 s wrapper PLUS `render_sliver_box_child_manager` / `app_kit_view` / `box_scroll_view` / `live_text_input_status` bumped from existing 60 s → 120 s via new `const _verySlowTestTimeout = Timeout(Duration(seconds: 120));`), `timeout_tests_test.dart` (×2: `rendering/render_custom_multi_child_layout_box_test.dart`, `retest: services/message_codec_test.dart`), `hardly_relevant_classes_4_test.dart` (×2: `widgets/inspector_button_test.dart`, `widgets/overflow_bar_alignment_test.dart`). Per rule (a) (test-script-only change) individual retest sufficed. *Verification (alt ports 14272/14273):* AST `--name`-filtered retests **3/3 pass** (gii html_element_view 23 s wall / 3.2 s script; gir render_android_view 19 s wall / 2.9 s script; h4 overlay_portal_controller 20 s wall / 2.0 s script). TEST `--name`-filtered retests **24/24 pass** (gii 4/4 in 30 s wall, gir 17/17 in 67 s wall including the 4 previously-60s-wrapped scripts, timeout_tests 2/2 in 18 s wall, h4 2/2 in 18 s wall). All script execution times observed well under the 60 s/120 s budgets — typical 1-12 s wall, leaving 5×-50× headroom. *Note re scope:* the `interactive_tests_test (setUpAll)` entry mentioned in the original deferral was already addressed by TODO #6 (FIXED 20260529 — library-level `@Timeout(Duration(seconds: 240))`); the `hardly_relevant_classes_3_test` mention had no actual `test_30s_timeout` entries in the 2206 result.json (verified via filtered `TimeoutException`/`Test timed out` scan), so nothing to do there. Cluster status: **FIXED — phase 1 (19 scripts in hardly_relevant_5) + phase 2 (24 scripts in gii/gir/timeout/h4 across both projects) covers all 43 unique `test_30s_timeout` sites from the 2206 baseline. The remaining 5 errors in the original 48-entry count were duplicate test names where a `TimeoutException` and a follow-on `Transport failure` were both surfaced against the same test — those are wedge-family (TODO #3) not timeout-family**.

- [x] **5. — FIXED 20260529.** `build_30s_timeout` — 1 AST failure (`timeout_tests/retest: widgets/app_kit_view_test.dart`, "Expected: true Actual: false, Build timed out after 30 seconds"). *Fix:* added per-request `?buildBudgetMs=N` query-parameter override to both test_apps' `/build` handler, threaded from the runner's existing `httpBuildTimeout` parameter via both `SendTestRunner.send()` methods. Test_app default is 30 s (unchanged); when the runner passes `httpBuildTimeout`, it auto-emits `&buildBudgetMs=<ms>` and the test_app uses that value (clamped 1–120 s). The failing test already passes `httpBuildTimeout: Duration(seconds: 50)`, so it now gets a 50 s build budget without any test-script change. *Verification:* focused `app_kit_view` test via `--name 'app_kit_view'` on AST alt port 14268: **4 pass / 0 fail / 0 err / 0 skip** in 30 s wall time (the regex matched 4 adjacent app_kit_view-related tests; all green). *Regression sweep per rule (b)* (essential + important + secondary on both projects, alt ports 14268/14269) showed only host-load-induced timeouts identical in pattern to prior 20260529 turns — none attributable to this fix because the new code path is a no-op when `httpBuildTimeout` is null (which the standard tests use). Cluster status: **FIXED**.

- [x] **6. — FIXED 20260529.** TEST `interactive_tests_test` setUpAll error — *Diagnosis:* Dart `package:test` applies a 30 s default timeout to `setUpAll` / `tearDownAll` / unflagged tests; the file's existing internal `SendTestRunner.setUp(timeout: 120s)` couldn't complete because the outer 30 s wrapper fired first. The original TODO body's hypothesis (about `requestRecycle()` consuming budget) was wrong — the TEST project's `interactive_tests_test.dart` has no recycle hook (it's AST-only per §U28). The actual cause was the missing library-level timeout override. *Fix:* added `@Timeout(Duration(seconds: 240))` library annotation to `tom_d4rt_flutter_test/test/interactive_tests_test.dart` (extends setUpAll/tearDownAll wrapper; per-test 90 s overrides unchanged) and bumped the internal `SendTestRunner.setUp(timeout: 120s)` → `180s` for parity with the AST sibling (1401-TODO #11 H2 rationale). *Verification:* individual retest on TEST port 14270: **9 pass / 0 fail / 0 err / 0 skip** in 40 s wall time. Cluster status: **FIXED**.

- [x] **7. — FIXED 20260529.** §U17 framework-error noise — *Investigation:* both test_apps already had `'A RenderConstraintsTransformBox overflowed by'` in their `ignoredPatterns` lists; the bug was elsewhere. The inner `if (_capturingFrameworkErrors)` block correctly skipped adding ignored patterns to `_frameworkErrors`, but the outer `else { _originalFlutterErrorHandler?.call(...) }` ran unconditionally for every non-silenced error — forwarding ignored patterns to Flutter's default red-screen handler and printing `EXCEPTION CAUGHT BY ...` to stdout. *Fix:* hoist `isIgnored` declaration to outer scope; change `else` → `else if (!isIgnored)` so ignored patterns are silently dropped from both `_frameworkErrors` AND the forwarded handler. *Verification:* followup re-run of TEST `timeout_tests_test` (where §U17 fired in 2206) now shows **0 EXCEPTION CAUGHT BY lines, 0 overflow lines, 0 RCTB lines, 0 framework_error events** in the log. **Cluster status: FIXED.**

- [x] **8. — FIXED 20260529.** §U29 framework-error noise — same root cause + same fix as #7 above (single change covers both Ux entries plus §U30 going forward). The `'Codec failed to produce an image'` pattern was already in both `ignoredPatterns` lists; the forward-path bug was making it leak anyway. *Verification:* followup re-run of TEST `hardly_relevant_classes_4_test` (where §U29 fired in 2206) now shows **0 Codec lines, 0 EXCEPTION CAUGHT BY lines** in the log. **Cluster status: FIXED.**

### Phase 4 — Deferred items from `interpreter_unfixable.md`

One TODO per open Ux entry. Items #3, #4, #7, #8 above already address U17, U25, U28, U29 — those Ux entries remain "open" until their respective TODOs land verified fixes.

- [x] **9. — DEFERRED 20260529 (investigation done, no acute failure).** §U1 — Demo-scale renderings overload test-app transport (interpreter limitation). *Investigation:* (a) the 2 historically U1-affected scripts already have working script-side workarounds (per `interpreter_unfixable.md` §U1 affected-scripts table): `widgets/notificationlistener_test.dart` (C05 closed 2026‑05‑17) and `services/text_editing_delta_insertion_test.dart` (C51/C52 closed 2026‑05‑18). (b) The 2206 sweep logs (`grep 'Lost connection to device'` across all `*.log.txt`) contain zero hits → no currently-failing script triggers the U1 symptom. (c) The "real fix" paths per §U1 "What a real fix would look like" require substantial cross-cutting architecture work — pre-register adapter proxies in `DeclarationVisitor` pre-pass for top-level `const` interpreted subclasses of native abstract classes (sub-case 1, touches `tom_d4rt` + `tom_d4rt_ast` + abstract-class scanner + proxy wiring), and bridge-side `TextSpan` batching OR test-app transport-budget bump (sub-case 2). §U1 explicitly notes this is "not in scope for the C05 cluster." (d) The TODO body's suggested workaround-encoding ("encode 'split rendering across multiple builds, pump frames between' as bridge-level safety so scripts don't have to opt in manually") does not map cleanly to either U1 sub-case — that pattern matches the §U28 `requestRecycle` workaround family (per-test process recycle), not U1's per-build top-level-const or per-frame-TextSpan-count problems. Cluster status: **DEFERRED — no acute failure; working script-side workarounds for the 2 historical instances; real fix requires multi-package architectural changes explicitly out-of-scope per §U1; revisit if/when a new script that cannot be similarly rewritten triggers the symptom**.
- [x] **10. — DEFERRED 20260529 (investigation done, no acute failure).** §U2 — Non-wrappable arithmetic defaults on positional-only native constructors (generator limitation). *Investigation:* (a) the 1 historically U2-affected script already has a working script-side workaround per `interpreter_unfixable.md` §U2 affected-scripts table: `rendering/gradient_rendering_test.dart` (C09 closed 2026‑05‑17 on both drivers) — `Gradient.sweep(center, colors)` was expanded to 6 positionals (added `colorStops` 9-element stop list, `TileMode.clamp`, `0.0`, `math.pi * 2.0`). (b) The 2206 sweep logs (`grep 'non-wrappable default' / 'getRequiredArgTodoDefault'` across all `*.log.txt`) contain zero hits → no currently-failing script triggers the U2 symptom. (c) The "real fix" path per §U2 "What a real fix would look like" requires extending `BridgeGenerator._wrapDefaultValue` (`tom_d4rt_generator/lib/src/bridge_generator.dart`, around line 4613) with an arithmetic-default evaluator for compile-time-known expressions (`math.pi`, `math.e`, numeric literals, basic operators `+ - * /`). The generator change itself is small (~30-50 lines + test fixture), but it requires regenerating every bridge package in both projects (34 `.b.dart` files total) followed by full essential + important + secondary regression per rule (b). §U2 explicitly notes "fixing the generator and regenerating every bridge package would put hundreds of `.b.dart` files in the diff" is out-of-scope for the C09 cluster. (d) The TODO body's terse phrasing ("Generator-side fix: emit a `D4.coerceNum(arg, fallback: defaultValue)` adapter") is a slightly different approach than §U2's recommended generation-time evaluator — but both target the same generator code path and have the same regeneration + regression cost. Cluster status: **DEFERRED — no acute failure; working script-side workaround for the 1 historical instance; real fix requires generator change + cross-package bridge regeneration + rule (b) regression sweep that the current host-load conditions make unreliable; revisit when host is rebooted and a new `dart:ui` / `flutter/{painting,rendering}` API with an arithmetic positional default becomes blocking**.
- [x] **11. — DEFERRED 20260529 (investigation done, no acute failure).** §U3 — Interpreted subclass of native abstract `Curve` `transformInternal` override not routed through `Curve.transform` (interpreter / proxy-generator limitation). *Investigation:* (a) the 1 historically U3-affected script already has a working script-side workaround per `interpreter_unfixable.md` §U3 affected-scripts table: `animation/animation_misc_adv_test.dart` (C10 closed 2026‑05‑17 on both drivers) — the `_FlippedShim extends Curve` specimen was replaced with the framework's `FlippedCurve(Curves.easeInOut)` which is real native Dart; `_FlippedShim` kept as documentation with `// ignore: unused_element`. (b) The 2206 sweep logs (`grep "type 'Null' is not a subtype of type 'num' in type cast" / 'transformInternal'`) contain zero hits → no currently-failing script triggers the U3 symptom. (c) The "real fix" path per §U3 "What a real fix would look like" requires modifying `tom_d4rt_generator/lib/src/proxy_generator.dart` to emit native overrides on the `_InterpretedCurve` proxy for `transformInternal` that delegate to `interpretedInstance.invoke('transformInternal', [t])`. The general fix is to identify *every* template-method/abstract-hook pair in framework abstract classes the script can subclass (Curve → `transformInternal`, ScrollPhysics → `applyPhysicsToUserOffset`, …) and emit matching proxy overrides. §U3 explicitly notes "Neither variant is in scope for the C10 cluster — touching the proxy generator would put dozens of `.b.dart` files in the diff and risks regressing the existing `State` / `StatelessWidget` adapter-proxy paths." (d) The TODO body's terse phrasing ("Interpreter dispatch fix") matches the same generator code path. Cluster status: **DEFERRED — no acute failure; working script-side workaround for the 1 historical instance using a native framework class (`FlippedCurve`); real fix requires proxy generator change + cross-package bridge regeneration + rule (b) regression sweep + risk of regressing the existing State/StatelessWidget proxy paths; current host-load conditions make the regression sweep unreliable; revisit when host is rebooted and a new script requiring a framework abstract-class template-method override that has no native sibling becomes blocking**.
- [x] **12. — DEFERRED 20260529 (canonical guidance confirmed; truly unfixable).** §U4 — Standalone `'\n'` `TextSpan` between styled siblings crashes test-app transport. *Investigation:* (a) the 1 historically U4-affected script already has the mandatory script-side workaround applied per `interpreter_unfixable.md` §U4 affected-scripts table: `material/tooltip_feedback_test.dart` (C15 closed 2026‑05‑17 on both drivers). The workaround appends the `'\n'` to the preceding styled `TextSpan`'s `text` and drops the standalone newline child; alternatively a `WidgetSpan(child: SizedBox(width: double.infinity, height: 0))` sandwiched in place forces a line break without text content. (b) The 2206 sweep logs (`grep 'Lost connection to device'`) contain zero hits → no currently-failing script triggers the U4 symptom. (c) §U4 is explicitly categorised as **"truly unfixable"** in the doc: the crash terminates the Dart VM at a level the interpreter / test runner cannot intercept (no catchable `RuntimeD4rtException`; the test-app process dies mid-build). A hand-written `RichText` with the same shape rendered from native Dart works — so the fault lives in the d4rt bridged-render path, but the path of corruption is not localised to any interceptable boundary. *Canonical guidance (confirmed):* the script-side workaround (merge `'\n'` into preceding styled span, or substitute `WidgetSpan(SizedBox)`) is the only available remediation. (d) The TODO body itself ("Marked **truly unfixable** — confirm whether the per-script workaround … is still the canonical guidance") is a status check rather than an engineering ask; the answer is YES, the workaround is still canonical. Cluster status: **DEFERRED — no acute failure; mandatory script-side workaround applied to the 1 historical instance; §U4 is "truly unfixable" by design; canonical guidance unchanged; revisit only if (1) a new script with the same trigger pattern appears that cannot be similarly rewritten OR (2) someone produces a smaller native-Dart reproducer that localises the fault to an interceptable boundary**.
- [x] **13. — DEFERRED 20260529 (investigation done, no acute failure).** §U5 — Interpreted subclass of native abstract `NotchedShape` / `FloatingActionButtonLocation` rejected at the bridged-constructor boundary (interpreter / bridge-generator architectural limitation). *Investigation:* (a) the scripts referencing these patterns (`material/bottom_app_bar_test.dart`, `painting/notched_shape_test.dart`, `painting/notched_shapes_test.dart`, `material/standard_fab_location_test.dart`) already use the mandatory framework-alternative workarounds per `interpreter_unfixable.md` §U5 — `CircularNotchedRectangle()`, `FloatingActionButtonLocation.centerDocked`, `FloatingActionButtonLocation.endDocked`, etc. (verified via `grep` on the script sources). The historical `_TopRoundedNotchedShape` / `_CustomFabLocation` reproducers from the U5 doc are not present in the live corpus. (b) The 2206 sweep logs (`grep 'expected NotchedShape' / 'expected FloatingActionButtonLocation' / 'got InterpretedInstance'`) contain zero hits → no currently-failing script triggers the U5 symptom. (c) §U5 is explicitly the **same adapter-proxy delegation gap** as §U3 (see TODO #11): the bridge generator emits a `BridgedClass` for the abstract base but does not synthesise an adapter-proxy that wraps an `InterpretedInstance` in a native subclass implementing the abstract methods. The real fix is the **same generator change** as #11 — per-abstract-class native proxies in `proxy_generator.dart` — and applies to the broader family (`Curve`, `NotchedShape`, `FloatingActionButtonLocation`, `ShapeBorder`, `InputBorder`, `OutlinedBorder`, `BoxBorder`, `ScrollPhysics`, `InteractiveInkFeatureFactory`, `MaterialStateProperty<T>`, `PageTransitionsBuilder`, `RouteTransitionRecord`, `Decoration`, etc. — diagnostic list from §U5). (d) The TODO body's terse phrasing ("Generator + interpreter abstract-class subclass path") matches the same code path described in §U5 + §U3. Cluster status: **DEFERRED — no acute failure; mandatory framework-alternative workarounds applied to the affected scripts; real fix is the same multi-abstract-class proxy-generator extension as TODO #11 with identical bridge-regeneration + cross-package regression cost + risk of regressing existing State/StatelessWidget proxy paths; revisit when host is rebooted AND a new script requiring a framework abstract-class hookable-method override that has no native sibling alternative becomes blocking (same trigger as #11)**.
- [x] **14. — DEFERRED 20260529 (investigation done, no acute failure).** §U6 — Direct `import 'package:vector_math/vector_math_64.dart'` not resolvable in d4rt scripts (module-loader limitation). *Investigation:* (a) the historically U6-affected script(s) already have the mandatory script-side workaround applied per `interpreter_unfixable.md` §U6 — `grep "import 'package:vector_math"` across the entire `send_ast_via_http_scripts/` corpus returns **zero matches**. Scripts that previously imported `vector_math_64` for `Vector3` / `Matrix4.transform3` constructions have been rewritten to use inline column-major `Matrix4.storage` arithmetic OR `MatrixUtils.transformPoint(matrix, Offset)` (the bridged Flutter idiom for 2D affine transforms with perspective-divide). (b) The 2206 sweep logs (`grep 'Cannot resolve import.*vector_math' / 'Module source not preloaded.*vector_math' / 'package:vector_math/'`) contain zero hits → no currently-failing script triggers the U6 symptom. (c) The "real fix" path per §U6 "Constraints" requires either (i) generating a full `BridgedClass` set for the `vector_math` package's public API (~14 types: `Vector{2,3,4}`, `Quaternion`, `Matrix{2,3,4}`, `Aabb{2,3}`, `Frustum`, `Plane`, `Ray`, `Sphere`, `Triangle`, plus free functions) — significant generation churn for capability the Flutter painting/rendering bridges already cover via `Matrix4`'s consumer surface, OR (ii) registering it as an `explicitSources` entry — possible but requires the interpreter to handle the package's internal `Float64List`-backed math and FFI/typed-data paths. §U6 explicitly notes both routes are "out of scope for a single cluster-by-cluster pass." (d) The TODO body's terse phrasing ("Module-loader + bridge package work") matches the same two-route trade-off described in §U6. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied (Matrix4.storage inline arithmetic / MatrixUtils.transformPoint); real fix requires substantial bridge-package generation OR interpreter typed-data path validation; revisit when host is rebooted AND a new script appears with a `vector_math` use case that cannot be cleanly rewritten via the workaround table (`Matrix4.storage` for matrix-vector products, `MatrixUtils.transformPoint` for 2D affines, `Matrix4.rotationX/Y/Z` for rotations)**.
- [x] **15. — FIXED (already shipped in commit `f5ff30ee` prior to 2206 testlog).** §U7 — Dart-internal `_ConstMap` not in `Map` bridge's `nativeNames`. *Discovery:* the fix was already in place when the 2206 testlog was created — commit `f5ff30ee` (`fix(d4rt-interpreter): register _ConstMap in Map bridge nativeNames (C43)`) added `_ConstMap` to both `tom_d4rt_ast/lib/src/runtime/stdlib/core/map.dart` and `tom_d4rt/lib/src/stdlib/core/map.dart` `nativeNames` lists. The original TODO body ("Add `_ConstMap` … to the bridge's `nativeNames` allowlist") describes exactly the change that was already shipped. *Verification:* (a) the 2206 sweep logs contain zero "Cannot access property '…' on target of type `_ConstMap`" hits across all `*.log.txt` files. (b) The 5 historically U7-affected SemanticsEvent-family scripts (`announce_semantics_event_test.dart`, `tap_semantic_event_test.dart`, `semantics_events_test.dart`, `semantics_data_test.dart`, `semantics_event_test.dart`) all pass cleanly in the 2206 baseline. (c) `git log -- tom_d4rt_ast/lib/src/runtime/stdlib/core/map.dart` confirms `f5ff30ee` lands before the 20260528-2206 sweep timestamp. *Doc update:* §U7 in `interpreter_unfixable.md` now carries the `— **✅ FIXED in commit f5ff30ee**` header marker (matching the §U26 convention) plus a "2026-05-29 update — FIXED" appendix block noting the script-side workaround is now optional rather than mandatory. The broader architectural fix the doc previously proposed (teach the Map adapter to fall back to `target is Map` whenever the runtime type lookup misses) was NOT taken — the narrower `_ConstMap`-by-name fix matches the same naming-based approach the other `_CompactLinkedHashMap` / `_MapView` / `_UnmodifiableMapView` entries use. Cluster status: **FIXED**.
- [x] **16. — DEFERRED 20260529 (investigation done, no acute failure).** §U8 — Script-defined enum values are `InterpretedEnumValue`, not native `Enum`; plus `RestorableValue.value` asserts `isRegistered` (interpreter limitation + scripting trap). *Investigation:* (a) the 2 historically U8-affected scripts already have their mandatory script-side workarounds applied per `interpreter_unfixable.md` §U8 affected-scripts table. `widgets/restorable_values_test.dart` (C20 closed 2026‑05‑17) uses `RestorableEnum<Brightness>` (workaround 1 — framework-enum substitution from the §U8 table; `Brightness` chosen as the cleanest 2-state substitute for the original `_Mood` 4-state enum) plus shadow variables `_vInt`, `_vMood`, `_vDateTime`, `_vMoodCalm`, etc. (workaround 2 — shadow-variable pattern; the displays read the shadow rather than `restXxx.value`, exact whenever the demo never mutates `.value`). `foundation/diagnostics_serialization_delegate_test.dart` (Step 10 follow-up, 2026‑05‑19) uses `addProp('mode', '${c.mode}', 'StringProperty')` rather than `EnumProperty<_DemoMode>('mode', mode)` (workaround 3 — StringProperty substitution for diagnostics; description is byte-identical because `toString()` on `InterpretedEnumValue` returns `${parentEnum.name}.$name`). (b) The 2206 sweep logs contain zero `InterpretedEnumValue` / `expected Enum, got` / `isRegistered.*is not true` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U8 symptom. (c) The "real fix" path per §U8 "Constraints" requires `InterpretedEnumValue` to *implement* `Enum` (add `index`, `name`, and have the runtime type pass `is Enum`) — but `Enum` is a Dart-VM-special sealed type whose subtyping is constrained by the VM's reified-enum machinery, so a straight `implements Enum` would NOT satisfy the native `is Enum` check at the bridge boundary. §U8 explicitly notes "the fix is non-trivial and out of cluster scope" — a real fix would need either a parallel `EnumWrapper` synthesized at bridge-boundary crossings (touches `D4.getRequiredArg<Enum>` family + `extractBridgedArg<Enum?>` + every bridge call site that types arguments as `Enum`/`Enum?`/`T extends Enum`) or a VM-level enum bridging primitive that the analyzer-free `tom_d4rt_ast` runtime path cannot easily provide without depending on `dart:mirrors`. For the `RestorableValue.value` part (U8(2)), the real fix would require the script to wire a `RestorationMixin` host widget around every restorable demo — that refactors the entire script into a `StatefulWidget` and is invasive; for static teaching demos where values never mutate, the shadow-variable workaround is functionally exact and minimally disruptive. (d) The TODO body's terse phrasing ("§U8 — Script-defined enum values are `InterpretedEnumValue`, not native `Enum`") describes the same root cause and same scope as §U8 documents. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied (framework-enum substitution + shadow-variable pattern + StringProperty-for-diagnostics substitution); real fix requires either bridge-boundary `EnumWrapper` synthesis across the entire `Enum` parameter surface (large surgery touching `D4` helpers + every Enum-typed bridge call site + analyzer-free `tom_d4rt_ast` runtime) or a VM-level reified-enum bridging primitive that the sandboxed interpreter cannot easily provide; revisit when a new script appears that (1) requires a script-defined enum AT a native API boundary with NO suitable framework-enum substitute AND (2) cannot use the StringProperty-for-display fallback, OR when the analyzer-free `tom_d4rt_ast` runtime gains a usable enum-bridging primitive**.
- [x] **17. — DEFERRED 20260529 (investigation done, no acute failure).** §U9 — Script-defined `RouteAware` cannot be subscribed to a native `RouteObserver` (interpreter limitation). *Investigation:* (a) the 1 historically U9-affected script (`widgets/route_observer_test.dart`, C22 closed 2026‑05‑17) already has the mandatory script-side workaround applied per `interpreter_unfixable.md` §U9: defines `_DemoRouteObserver` (line 85 of the script) as a script-side stand-in that mirrors the native five-method contract (`subscribe`/`unsubscribe`/`didPush`/`didPop`/`didReplace`) over `Map<Route<dynamic>, List<_LoggingRouteAware>>` — the demo's call-order timeline is produced without crossing the d4rt → native boundary. The native `RouteObserver<PageRoute<dynamic>>()` is still constructed (line 363 — safe; the constructor itself has no script-defined arguments) to demonstrate the type exists; no `routeObserver.subscribe(...)` call is made on it. (b) The 2206 sweep logs contain zero `expected RouteAware` / `InterpretedInstance(_LoggingRouteAware` / `RouteObserver.*subscribe.*Argument Error` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U9 symptom. (c) The "real fix" path per §U9 "Constraints" requires the bridge generator to synthesise a native adapter that *implements* `RouteAware`, delegates each of the four lifecycle callbacks back to the interpreted instance via `InterpretedInstance.invoke`, and is automatically wrapped around any `InterpretedInstance` passed to a parameter typed `RouteAware`. This is the same multi-abstract-class proxy-generator extension as TODO #11 (§U3) and TODO #13 (§U5) — proxy_generator.dart changes + bridge regeneration across all `.b.dart` files + risk of regressing existing `State`/`StatelessWidget` adapter-proxy paths. §U9 explicitly notes "this is the same long-term proxy-synthesis sketch noted under U3 / U5 / U8 and is out of scope for a single cluster pass." Unlike the §U8 case which has framework-enum substitutes available (`Brightness`, `TextDirection`, etc.), §U9 has NO framework-provided `RouteAware` concrete subclass — `RouteAware` is designed to be mixed into application-side `State` subclasses, so every concrete implementation lives in user code. This means the script-side stand-in is the ONLY workaround route. (d) The TODO body's terse phrasing ("§U9 — Script-defined `RouteAware` cannot subscribe to native `RouteObserver`") matches the same root cause described in §U9. Cluster status: **DEFERRED — no acute failure; mandatory `_DemoRouteObserver` script-side stand-in applied to the 1 historical instance; real fix requires the same multi-abstract-class proxy-generator extension as TODOs #11/#13 with identical bridge-regeneration + cross-package regression cost; current host-load conditions (kernel-zombie port residue per TODO #2) make the regression sweep unreliable; revisit when host is rebooted AND a new script appears with a `RouteAware` subclass that cannot be similarly rewritten via the stand-in pattern**.
- [x] **18. — DEFERRED 20260529 (investigation done, no acute failure).** §U10 — Script-defined class `with DiagnosticableTreeMixin` / `Diagnosticable` cannot call inherited concrete methods (`toStringDeep`, `toString`, `toStringShallow`, `toDiagnosticsNode`); plus `super.debugFillProperties(...)` fails into bridged mixin (interpreter limitation). *Investigation:* (a) all 4 historically U10-affected scripts already have their mandatory script-side workarounds applied per `interpreter_unfixable.md` §U10 affected-scripts table. `foundation/class_test.dart` (C36 closed 2026‑05‑18) uses `_dumpNode` recursive helper. `foundation/diagnostics_serialization_delegate_test.dart` (C37 closed 2026‑05‑18) uses `_manualSerialize(config, delegate, depth)` for JSON shape. `foundation/object_flag_property_test.dart` (C38 closed 2026‑05‑18) uses `_diagnosticableDeepDump` + drops `super.debugFillProperties(...)` super call (also fixed an unrelated framework-assert bug by supplying empty-string text for `ifPresent`/`ifNull` slots). `foundation/text_tree_configuration_test.dart` (Cluster F #11 closed 2026‑05‑23) uses an in-script sparse-renderer fallback (lines 1772, 1788) — visually equivalent to the real `toStringDeep` output for this script's `_SampleScene extends DiagnosticableTree` specimen. (b) The 2206 sweep logs contain zero `expected DiagnosticableTreeMixin` / `expected Diagnosticable, got` / `does not have a standard or bridged superclass.*super` / `Cannot invoke method 'toStringDeep' on null` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U10 symptom. (c) The "real fix" path per §U10 requires a hand-written `_InterpretedDiagnosticableTreeMixin` proxy in `d4rt_runtime_registrations.dart` (analogous to `_InterpretedStatelessWidget`, `_InterpretedState`) that implements `DiagnosticableTreeMixin` natively, holds the `InterpretedInstance` + visitor, overrides each abstract callback (`debugFillProperties` / `debugDescribeChildren` / `toStringShort`) to invoke the interpreted method on the held instance, and is wired into the interpreter at mixin-resolution time so the `nativeProxy` chain produces this proxy whenever an interpreted class mixes in `DiagnosticableTreeMixin`. §U10 explicitly notes "this is feature-scale work (the existing 10+ `_Interpreted*` proxies are all roughly 100–200 lines apiece, and the wiring at mixin-resolution time is new infrastructure), deferred for this cluster pass." Same architectural family as §U3/§U5/§U8/§U9 plus the additional twist that the failure point is INSIDE the mixin's concrete methods (rather than at a separate bridged API boundary). U10 also requires solving the `super.debugFillProperties` interpreter-side `super`-into-bridged-mixin resolution gap — separate from the proxy-synthesis work above. (d) The TODO body's terse phrasing ("§U10 — Script-defined class `with DiagnosticableTreeMixin` cannot call inherited concrete methods") matches the same root cause described in §U10. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied to all 4 historical instances (`_dumpNode` helper, `_manualSerialize` for JSON shape, `_diagnosticableDeepDump` + drop super call, in-script sparse-renderer fallback); real fix requires new `_InterpretedDiagnosticableTreeMixin` proxy infrastructure + mixin-resolution-time wiring + a separate fix for `super` dispatch into bridged mixin — feature-scale work explicitly out-of-scope per §U10; revisit when host is rebooted AND a new script appears with a `DiagnosticableTreeMixin` use case that cannot be similarly rewritten via the helper-fn pattern (the 4 historical scripts all had a documentable manual-rendering path)**.
- [x] **19. — DEFERRED 20260529 (investigation done, no acute failure).** §U11 — Script-defined `HitTestTarget` rejected by `HitTestEntry(target)` constructor (interpreter limitation). *Investigation:* (a) the 1 historically U11-affected script (`gestures/hit_testable_test.dart`, C39 closed 2026‑05‑18) already has the mandatory script-side workaround applied per `interpreter_unfixable.md` §U11: `_FakeTarget implements HitTestTarget` (line 43) kept as a teaching reference but NOT instantiated for `HitTestEntry(target)`; pure script-side `_DemoHitEntry(label, runtimeTypeStr)` (line 58) substituted for the anatomy-panel display. Native `HitTestResult()` (line 78) and `BoxHitTestResult()` (line 79) constructors still execute successfully — only the `HitTestEntry(<script HitTestTarget>)` boundary crossing is skipped. The demo's actual functional need is purely visual (iterating `result.path` to display a stacked-card view of per-entry labels + `entry.runtimeType`) — it never calls `target.handleEvent(...)` or dispatches through `GestureBinding`, so the data-class substitution is functionally exact. (b) The 2206 sweep logs contain zero `expected HitTestTarget` / `InterpretedInstance(_FakeTarget` / `HitTestEntry.*Argument Error` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U11 symptom. (c) The "real fix" path per §U11 requires the same kind of `_InterpretedHitTestTarget` proxy that would resolve §U3/§U5/§U9/§U10 — a hand-written native adapter in `d4rt_runtime_registrations.dart` that implements `HitTestTarget` natively, holds the `InterpretedInstance` + visitor, and routes `handleEvent` back into the interpreter. §U11 explicitly notes "this is feature-scale work and deferred for this cluster pass." Same multi-abstract-class proxy-generator extension family as TODOs #11/#13/#17/#18. Unlike §U8 (framework-enum substitutes) and §U10 (manual rendering helpers), §U11 has NO framework-provided concrete `HitTestTarget` the script can substitute without standing up a full render tree — every concrete `HitTestTarget` in Flutter is a `RenderObject` subclass tied to the rendering pipeline, which is out of scope for a static teaching demo. The data-class substitution is therefore the only workaround route, and works only because this particular demo doesn't dispatch events through the target. (d) The TODO body's terse phrasing ("§U11 — Script-defined `HitTestTarget` rejected by `HitTestEntry(target)` constructor") matches the same root cause described in §U11. Cluster status: **DEFERRED — no acute failure; mandatory script-side workaround applied to the 1 historical instance (keep `_FakeTarget` as teaching reference, substitute `_DemoHitEntry` data-class for display); real fix requires the same proxy-generator extension family as TODOs #11/#13/#17/#18 — feature-scale work explicitly out-of-scope per §U11; revisit when host is rebooted AND a new script appears that needs to actually dispatch events through a script-defined `HitTestTarget` (which the data-class substitution cannot handle)**.
- [x] **20. — DEFERRED 20260529 (intentional generator policy; investigation done, no acute failure).** §U12 — `@Deprecated`-annotated SDK symbols are filtered out of the bridge surface by design (generator policy). *Investigation:* (a) all 6 historically U12-affected scripts already apply their mandatory script-side workarounds per `interpreter_unfixable.md` §U12 affected-scripts table. **Variant A — local stand-in** (used when no non-deprecated equivalent exists): C44 `services/key_data_transit_mode_test.dart` uses `_KeyDataTransitMode` enum stand-in; C45 `services/keyboard_side_test.dart` uses `_KeyboardSide` + `_ModifierKey` dual-enum stand-ins; C49 `services/raw_key_event_data_web_test.dart` uses `class _RawKeyEventDataWeb` with constructor params + accessor methods; C50 `services/raw_key_event_test.dart` uses the most elaborate coordinated stand-in set (`_ModifierKey`/`_KeyboardSide` enums + `_GLFWKeyHelper`/`_RawKeyEventDataLinux` classes + abstract `_RawKeyEvent` with `_RawKeyDownEvent`/`_RawKeyUpEvent` subclasses) — covering 7 deprecated SDK symbols. **Variant B — modern-name swap** (used when the deprecated symbol is a typedef-rename with identical surface): C46 `services/mouse_tracker_annotation_test.dart` uses `WidgetState` + `WidgetStateMouseCursor` in code positions, preserving the `MaterialState` alias in in-string/in-comment mentions only. Stand-in marker counts verified via `grep -c '^enum _\|^class _\|WidgetState\b'`: 9 / 5 / 11 / 1 / 6 hits across the 5 Variant-A scripts + the Variant-B C46 script. (b) The 2206 sweep logs contain zero `Undefined variable: KeyDataTransitMode|KeyboardSide|ModifierKey|MaterialState|RawKeyEvent|RawKeyDownEvent|RawKeyUpEvent|RawKeyEventDataLinux|RawKeyEventDataWeb|GLFWKeyHelper` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U12 symptom. (c) **§U12 is fundamentally different from §U3/§U5/§U8/§U9/§U10/§U11**: it is NOT an interpreter limitation that *could* be fixed with proxy synthesis. It is an **intentional generator policy** (`generateDeprecatedElements = false` in `tom_d4rt_generator/lib/src/element_mode_extractor.dart`) that keeps the bridge surface aligned with the framework's *non-deprecated* API so scripts stay aligned with what real Flutter apps can depend on going forward. §U12 explicitly states "this is the right interpreter / generator policy. Bridging a deprecated symbol invites scripts to depend on behaviour that the framework has already declared it intends to remove. … Flipping `generateDeprecatedElements = true` would temporarily resolve this symptom but would re-open the deprecated surface across the entire bridge corpus, which is contrary to the policy." The workarounds (local stand-in or modern-name swap) ARE the architecturally-correct path; there is no "real fix" to implement on the generator side. (d) The TODO body's terse phrasing ("§U12 — `@Deprecated`-annotated SDK symbols filtered out by generator policy") matches the policy description in §U12. Cluster status: **DEFERRED — intentional generator policy, not an interpreter limitation; mandatory script-side workarounds (Variant A local stand-in OR Variant B modern-name swap) applied to all 6 historical instances and ARE the architecturally-correct path; no acute failure; revisit only if a future framework deprecation introduces a symbol with NO non-deprecated equivalent AND NO meaningful stand-in shape (currently no such symbol is known)**.
- [x] **21. — DEFERRED 20260529 (investigation done, no acute failure).** §U13 — Native exceptions thrown across a bridged method are not catchable by their original type (interpreter limitation). *Investigation:* (a) both historically U13-affected scripts already apply their mandatory script-side workarounds per `interpreter_unfixable.md` §U13 affected-scripts table. `retest/services/method_codec_test.dart` (C53 AST / C55 TEST, both closed 2026‑05‑18): two `std.decodeEnvelope` / `json.decodeEnvelope` calls in the Section-6 error-envelope showcase now use broad `catch (e)` + string-parsing the wrapper's `'PlatformException(<code>, …)'` marker to recover the code, then re-flagging `thrownType = 'PlatformException'` (7 marker hits via `grep -c`). `services/codecs_test.dart` (Cluster E #10 of testlog_20260522-1328, closed 2026‑05‑23): the `_buildBinaryCodecsPage` error-envelope demo's `stdMethodCodec.decodeEnvelope(stdErrorBd)` call broadened to `catch (e)` and surfaces wrapped message as `'PlatformException-like: ${e.toString()}'` (3 marker hits via `grep -c`). The codec's intended contract (an exception is thrown for error envelopes) remains verified — only the type-based matching is replaced by string-parsed equivalent. (b) The 2206 sweep logs contain zero `Native error during bridged method call.*PlatformException` / `…FormatException` / `…MissingPluginException` / `…StateError` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U13 symptom. (c) The "real fix" path per §U13 "What a real fix would look like" requires in `tom_d4rt_ast/lib/src/runtime/bridged_class.dart` (and the mirror in `tom_d4rt`) propagating the original exception object on a side-channel of the `RuntimeError` (e.g. `RuntimeError.cause`); in the `InterpreterVisitor` try-catch matcher, when comparing an on-clause type against a `RuntimeError`, also test `exception.cause is <Type>`. Both pieces exist in isolation but are not currently wired together for bridge exception paths. §U13 explicitly notes "out of scope for a single cluster pass." Unlike §U3/§U5/§U9/§U10/§U11 this is NOT a proxy-synthesis gap — it's a boundary-translation issue requiring synchronized changes in both `bridged_class.dart` runtime AND `InterpreterVisitor.visitTryStatement` (which exist in both `tom_d4rt` and `tom_d4rt_ast`, doubling the surface area). Per the workspace rule "Keep tom_d4rt ↔ tom_d4rt_ast in sync" this would require coordinated edits across both interpreters + full essential/important/secondary regression sweep per rule (b) — host-load conditions and the kernel-zombie port residue from TODO #2 make that sweep unreliable currently. (d) The TODO body's terse phrasing ("§U13 — Native exceptions thrown across bridged method not catchable by original type") matches the same root cause described in §U13. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied to both historical instances (broad `catch (e)` + wrapper-text string-parse OR `catch (_)` boolean flag); real fix requires coordinated `RuntimeError.cause` side-channel + try-catch matcher changes across both `tom_d4rt` AND `tom_d4rt_ast` interpreters + full rule (b) regression sweep; revisit when host is rebooted AND a new script appears that requires a typed `on <Exception>` catch whose recovered field cannot be reliably extracted from the wrapper's stable `toString()` form**.
- [x] **22. — DEFERRED 20260529 (historical script FIXED via IntrinsicHeight; architectural gap open but no acute failure).** §U14 — `Center > ConstrainedBox(maxWidth)` inside `SingleChildScrollView`, or `Expanded` inside `Column(mainAxisSize.min)` inside `GridView.count` cell, leaks `maxHeight: infinity` down to `RenderConstrainedBox` (bridge/interpreter constraints-propagation gap). *Investigation:* (a) the 1 historically U14-affected script (`animation/cubic_test.dart`) was actually FIXED on 2026‑05‑23 per the §U14 "2026-05-23 update — FIXED (entry #19)" appendix — section-level bisection revealed the descriptions in this entry about `Center > ConstrainedBox(maxWidth)` and `GridView.count` were **red herrings**; the real source was two `Row(crossAxisAlignment: CrossAxisAlignment.stretch)` blocks (originally lines 1209 + 1219) inside `_PrivateConstructorCards`. A `Row(stretch)` requires bounded height from its parent; inside a `Column` that forwards `maxHeight: infinity` from the outer `SingleChildScrollView`, the stretch propagated infinite cross-axis into a synthetic `RenderConstrainedBox` inside each `_PrivateConstructorCard`'s 130-px plot Container. Fix: wrap each `Row(stretch)` in `IntrinsicHeight`, which resolves the Row's height to the intrinsic min height of the tallest child so the stretch has a finite cross-axis to work with. Verified in place: `grep` shows `IntrinsicHeight(` at lines 1219 + 1231 of the live script, with the entry-#19 comment-block (lines 1208-1213) documenting the root cause. Same family fix as the §U18 entry #10's `rendering/render_exclude_semantics_test.dart`. (b) The 2206 sweep logs contain zero `BoxConstraints forces an infinite height` / `RenderConstrainedBox's layout()` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U14 symptom. (c) The "real fix" path per §U14 "What a real fix would look like" requires either (minimal) making the bridged `RenderConstrainedBox.layout()` adapter clamp an incoming `maxHeight == double.infinity` to a finite fallback (e.g. `MediaQuery.of(context).size.height` or sentinel `9999.0`) instead of asserting, OR (more correct, larger) faithfully implementing `RenderPositionedBox.performLayout`'s shrink-wrap branch in the bridged `Center`/`Align` adapters plus `RenderSliverGrid`'s cell-height computation in the bridged `GridView.count` adapter, so no descendant ever sees an unbounded `maxHeight`. Both fixes would touch interpreter constraint-propagation semantics in both `tom_d4rt` AND `tom_d4rt_ast` per the "Keep in sync" workspace rule, plus require full rule (b) regression sweep — explicitly noted as "out of scope for a single script-rewrite pass." (d) The TODO body's terse phrasing ("§U14 — `maxHeight: infinity` leaks through `Center > ConstrainedBox > SingleChildScrollView`") matches the historical (but ultimately disproven) diagnosis path; the real cause turned out to be the `Row(stretch)` family pattern, fixable script-side via `IntrinsicHeight`. Cluster status: **DEFERRED — the 1 historically U14-affected script is FIXED via `IntrinsicHeight` wrap (the original diagnosis was wrong — `Center>ConstrainedBox>GridView` were red herrings; `Row(stretch)` was the real source); the architectural constraint-propagation gap described in §U14 remains an open concern for any future script that genuinely uses the `Center > ConstrainedBox > SCV` pattern that no `IntrinsicHeight` wrap can address; revisit when host is rebooted AND a new script appears with a genuine infinite-height-leak through the bridged Center/Align/GridView render adapters that the `IntrinsicHeight`-on-`Row(stretch)` fix cannot resolve**.
- [x] **23. — FIXED 20260529 (apparent self-resolution; verified in 2206 baseline).** §U15 — `RenderFlex overflowed by 2.0 pixels on the right` inside a bridged Cupertino layout the script cannot identify (bridge layout-rounding gap). *Investigation:* (a) the 1 historically U15-affected script (`cupertino/cupertino_nav_segmented_test.dart`) ran in the 2206 sweep with `frameworkErrors=0 status=success` on **both** projects. METRIC lines verified: AST `secondary_classes_test.log.txt` shows `frameworkErrors=0` for the cupertino_nav_segmented script (totalMs=2513); TEST `secondary_classes_test.log.txt` shows the same `frameworkErrors=0` (totalMs=2417). (b) Critically, the §U15 banner pattern `A RenderFlex overflowed by 2.0 pixels on the right` is NOT in either test_app's `ignoredPatterns` list (only the narrow subpixel pattern `'overflowed by 0.500 pixels'` is filtered, per `tom_d4rt_flutter_ast_app/lib/main.dart:336` comment block) — so the absence of hits in the 2206 logs is real, not suppression-driven. The 2206 sweep logs contain zero `A RenderFlex overflowed by 2.0 pixels on the right` hits across all 28 `*.log.txt` files (verified via `grep -c`). (c) Between testlog_20260519-1247 (where the banner fired at 2 per frame) and testlog_20260528-2206 (where it doesn't fire at all), 9 days of bridge regens + interpreter fixes have apparently closed the 2-pixel Cupertino layout-rounding gap that motivated §U15. Likely candidates: a Cupertino bridge regen that picked up framework-side intrinsic-width adjustments, an interpreter constraint-propagation fix, or a Flutter SDK version bump narrowing the rounding discrepancy. The exact closing change is not localised but the **outcome is verified clean** at 2206. (d) The TODO body's terse phrasing ("§U15 — `RenderFlex overflowed by 2.0 px` inside bridged Cupertino layout") matches the historical symptom — which is now resolved. Cluster status: **FIXED — apparent self-resolution between 0519-1247 and 0528-2206 sweeps; `cupertino/cupertino_nav_segmented_test.dart` now runs with `frameworkErrors=0 status=success` on both projects; absence of the 2.0px overflow pattern in the 2206 logs is real (pattern not in ignoredPatterns) rather than suppression-driven; the §U15 doc entry should be marked `— **✅ FIXED in 2206 baseline**` matching the §U7/§U26 convention; revisit only if the pattern re-emerges in a future sweep on a different Cupertino script — in which case a real bridge fix (`RenderFlex.layout()` adapter slack OR Cupertino intrinsic-width alignment) would be needed per the "What a real fix would look like" section of §U15**.
- [x] **24. — DEFERRED 20260529 (investigation done, no acute failure).** §U16 — `Text('')` (empty-string `Text` widget) triggers a NaN `Offset` assertion in `dart:ui` paragraph painting (bridge/interpreter text-layout gap). *Investigation:* (a) both historically U16-affected scripts already have their mandatory script-side workarounds applied per `interpreter_unfixable.md` §U16 affected-scripts table. `cupertino/restorable_cupertino_tab_controller_test.dart` (Item 5 of testlog_20260519-1247, fixed 2026‑05‑19) uses `composed.isEmpty ? ' ' : composed` guard in `_CodeBlock.build` so the composed text is never empty when fed to the bridged `Text` widget — `fwErr 1→0`. `gestures/velocity_test.dart` (Item 35 of testlog_20260519-1247, fixed 2026‑05‑19) uses `SizedBox(height: 14)` substitution for the blank `_CodeLine('')` separator (a variant U16 banner — under `IntrinsicHeight` ancestor the empty `Text` propagates unbounded intrinsic height instead of NaN paint origin, surfacing as `BoxConstraints forces an infinite height` rather than the NaN-Offset banner; both stem from the bridge's empty-paragraph metric path) — `fwErr 1→0`. Marker counts verified via `grep -c`: 1 hit (`composed.isEmpty`) in `restorable_cupertino_tab_controller_test.dart`, 8 hits (combined `composed.isEmpty\|SizedBox(height: 14`) in `velocity_test.dart`. (b) The 2206 sweep logs contain zero `Offset argument contained a NaN value` / `dart:ui/painting.dart.*Failed assertion: line 41` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U16 symptom. (c) The "real fix" path per §U16 "What a real fix would look like" requires short-circuiting `Text`/`RichText` paragraph layout when the resolved text is empty (zero `TextSpan` glyphs) so the painter never asks for baseline/line-width of an empty run — the native Flutter pipeline already does this implicitly; the bridge must replicate that fast-path. A larger fix audits every place inside the bridged paragraph painter where layout metrics can produce NaN for a zero-glyph run (baseline offset, alignment offset, line-width fitting) and short-circuits each. Both fixes touch interpreter text-painting pipeline in both `tom_d4rt` AND `tom_d4rt_ast` per the "Keep in sync" workspace rule, plus require full rule (b) regression sweep — explicitly noted as out of scope for a single script-rewrite pass. (d) The TODO body's terse phrasing ("§U16 — `Text('')` triggers `NaN Offset` assertion") matches the same root cause described in §U16. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied to both historical instances (composed-empty-guard substitution for the cupertino case, `SizedBox` substitution for the `IntrinsicHeight`-variant velocity case); real fix requires bridged paragraph-painter empty-run short-circuit in both `tom_d4rt` and `tom_d4rt_ast` — explicitly out-of-scope per §U16; revisit when a new script appears with composed empty-`Text` sites where the guard pattern is not practical OR an empty-`Text` site inside `IntrinsicHeight`/`IntrinsicWidth` ancestor where `SizedBox` substitution cannot preserve the demo's visual content**.
- [x] **25. — FIXED 20260529 (observable side fully resolved; architectural "by design" framing intact but produces no failure).** §U17 — `ConstraintsTransformBox` teaching script (`render_constraints_transform_box_test.dart`) is intrinsically incompatible with `frameworkErrors=0` (script design). *Investigation:* the §U17 doc frames this as "truly unfixable (script design)" because the script's teaching purpose is to feed pathological inputs to `ConstraintsTransformBox` and observe Flutter's debug-mode assertions. In practice however the observable side has been fully closed by two cooperating fixes: (1) `'A RenderConstraintsTransformBox overflowed by'` was already in both test_apps' `ignoredPatterns` lists (verified at `tom_d4rt_flutter_ast_app/lib/main.dart:382`, applied symmetrically in `tom_d4rt_flutter_test_app/lib/main.dart`) — this kept `_frameworkErrors == 0` for the script from day one. (2) TODO #7's `else if (!isIgnored)` guard in `_handleFlutterError` (commit landed 2026-05-29) closed the stdout/stderr leak via the unguarded `_originalFlutterErrorHandler?.call(details)` forward — that leak was the source of the "2 events captured this sweep (TEST side)" mentioned in the original TODO #25 body. The 2 events appeared in `timeout_tests_test.log.txt` baseline but are absent in all 7 post-TODO #7 followup directories on each project. *Verification (METRIC-line scan):* baseline 2206 AST `frameworkErrors=0` for the script, TEST `frameworkErrors=0`; post-TODO #7 followup AST `frameworkErrors=0`, TEST `frameworkErrors=0`. *Verification (log-noise scan):* baseline 2206 AST 0 hits, TEST 2 hits (the original event captures); post-TODO #7 followup all 7 dirs on AST (0/0/0/0/0/0/0) and all 8 dirs on TEST (0/0/0/0/0/0/0/0) → **0 hits everywhere** the `else if (!isIgnored)` guard is active. *Architectural status:* §U17's "truly unfixable by design" framing remains accurate — the script DOES intentionally feed pathological inputs to Flutter's debug-mode assertion machinery, and any "fix" (pre-normalize `kHalveMaxWidth`, resize Section 4/7/8 slots/children, wrap in `OverflowBox`, `try/catch` the layout pass) either erases the demo or exposes the next layer of cascade (cf §U17 2026-05-23 update). But because the suppression patterns silence the visible fallout cleanly, the architectural concern is invisible at the test outcome AND at the log level. The kHalveMaxWidth correctness fix (2026-05-23 entry #21, clamping `minWidth` to halved `maxWidth`) was retained because non-normalized BoxConstraints is undefined behavior — a real script-side bug, not just an error suppressor. Cluster status: **FIXED — observable side fully resolved by `ignoredPatterns` entry (pre-existing) + TODO #7 else-branch guard (2026-05-29); architectural "by design" framing remains accurate but produces neither `frameworkErrors` count NOR `.log.txt` noise in current state; revisit only if (a) someone removes the `'A RenderConstraintsTransformBox overflowed by'` entry from `ignoredPatterns`, OR (b) a new script appears that overflows `ConstraintsTransformBox` for non-teaching reasons (a real layout bug) — the suppression would then need narrowing or a real bridge fix per §U17's "What a real fix would look like" Option 3 (per-script exclude list)**. The §U17 doc entry can carry an `— **✅ FIXED in 2206 baseline (observable side; architectural framing intact)**` header marker matching the §U7/§U26 convention.
- [x] **26. — FIXED 20260529 (already shipped 2026-05-23 as entry #20; verified clean in 2206 baseline).** §U18 — `services/platform_test.dart` `_defaultVsThemeCard` Row(stretch)+Expanded(_twinCard): script-side P1 variants all destabilise the test-app transport (interpreter/bridge limitation). *Investigation:* the §U18 doc already carries a "2026-05-23 update — FIXED (entry #20)" appendix recording the combined fix: (i) `IntrinsicHeight` wrap on the `_defaultVsThemeCard` Row (same family as §U14 entry #19 / §U17 entry #19 / TODO #22's `IntrinsicHeight` family), AND (ii) wrap the page-level `Column(crossAxisAlignment.stretch)` body in a `SingleChildScrollView` (the `IntrinsicHeight` fix unmasked a 7257-px bottom `RenderFlex` overflow from the page's natural ~7000-px height exceeding the bounded ~800-px viewport). The 2026-05-20 reverted/deferred status was overwritten by the 2026-05-23 entry #20 work after the transport-cliff failed to reproduce on re-attempt — host/test-app stability had improved and/or intervening interpreter fixes removed the RenderFlex-construction trigger. *Verification in 2206 baseline:* (a) `grep` confirms `SingleChildScrollView` wrap at line 87 + `IntrinsicHeight(` wrap at line 565 of `services/platform_test.dart` are in place, with entry-#20 comment blocks at lines 76-86 (page-level rationale) and 555-564 (Row-level rationale). (b) METRIC for `services/platform_test.dart` reports `status=success httpStatus=200 outputLines=15 frameworkErrors=0` on both projects (verified via cross-project log scan with `grep -h "METRIC.*services/platform_test\.dart"`) → `fwErr 1→0` confirmed. (c) The 2206 sweep logs contain zero `Lost connection to device` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → the transport-cliff is no longer reproducing. (d) The TODO body's terse phrasing ("§U18 — `services/platform_test.dart` `Row(stretch)+Expanded` P1 variants destabilise transport") matches the pre-2026-05-23 symptom that has since been resolved. Cluster status: **FIXED — already shipped 2026-05-23 as entry #20 in testlog_20260523-1056-issue-analysis (`IntrinsicHeight` wrap on offending Row + `SingleChildScrollView` wrap on page body, both visible in live script); verified clean in 2206 baseline with `frameworkErrors=0 status=success` on both projects and zero `Lost connection to device` hits across all 28 log files; §U18 doc already documents the fix in its "2026-05-23 update" appendix + strikethrough on the affected-scripts row. The original transport-cliff diagnostic remains preserved in §U18 as a record of the 2026-05-20 investigation should it re-emerge under different conditions**.
- [x] **27. — DEFERRED 20260529 (investigation done, no acute failure).** §U19 — Per-character `TextSpan` stream of non-Latin glyphs triggers a NaN `Rect` assertion in `dart:ui` painting (bridge/interpreter text-layout gap; sibling of §U16). *Investigation:* (a) the 1 historically U19-affected script (`services/text_editing_delta_non_text_update_test.dart`, item 99 of testlog_20260519-1247, fixed 2026‑05‑20) already has its mandatory script-side workaround applied per `interpreter_unfixable.md` §U19 affected-scripts table. Verified via `grep`: `greet = 'aiueo'` (ASCII-only substitution) at line 733, with comment block at lines 720+ documenting the substitution rationale; the Japanese form `'こんにちは'` is retained ONLY in the surrounding example `story` prose at lines 802 + 821 (passed as a single `Text` argument, which is safe — the trigger is per-character span fragmentation, not the glyphs themselves). The substitution preserves the demo's *layout* (5-character composing range with offsets 0..5 identifying five distinct glyphs); only the cosmetic look of hiragana inside the per-character `_frozenFrame` visualisation is lost. (b) The 2206 sweep logs contain zero `Rect argument contained a NaN value` / `dart:ui/painting.dart.*Failed assertion: line 26` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U19 symptom. METRIC for `services/text_editing_delta_non_text_update_test.dart`: `status=success frameworkErrors=0` on both projects (was `frameworkErrors=4` before the 2026‑05‑20 fix). (c) The "real fix" path per §U19 "What a real fix would look like" requires routing per-`TextSpan` paint rect computation through the same cluster-geometry path the native Flutter pipeline uses, so single-character spans of non-Latin glyphs receive valid advance widths rather than NaN. A larger fix audits every site inside the bridged paragraph painter where per-span metrics are derived from a shortcut path (rather than from the cumulative cluster geometry) and replaces each with a cluster-aware computation. §U19 explicitly notes this shares the same root cause as §U16 (both surface NaN coordinates from the bridged text-painter for paragraphs whose glyph-cluster boundaries do not align with span boundaries) — both belong in the same bridged-paragraph-painter cluster-aware-metrics refactor. Touches both `tom_d4rt` and `tom_d4rt_ast` per the "Keep in sync" workspace rule, plus requires full rule (b) regression sweep — explicitly out of scope for a single script-rewrite pass. (d) The TODO body's terse phrasing ("§U19 — Per-character `TextSpan` stream of non-Latin glyphs triggers `NaN Rect` assertion") matches the same root cause described in §U19. Cluster status: **DEFERRED — no acute failure; mandatory script-side workaround applied to the 1 historical instance (ASCII-only `greet = 'aiueo'` substitution while retaining the Japanese form in surrounding `Text` prose where it is safe); real fix requires bridged paragraph-painter cluster-aware per-span metrics across both `tom_d4rt` and `tom_d4rt_ast` (same family as §U16) — explicitly out-of-scope per §U19; revisit when a new script appears with a semantically-meaningful non-Latin per-character `TextSpan` stream where ASCII substitution would erase the demo's teaching value**.
- [x] **28. — DEFERRED 20260529 (investigation done, no acute failure).** §U20 — `Table(border: TableBorder.all(...))` triggers a Flutter framework assertion in `table_border.dart` line 289 regardless of row count / column widths (bridge/framework interaction gap). *Investigation:* (a) the 1 historically U20-affected script (`widgets/editable_text_misc_test.dart`, item 107 of testlog_20260519-1247, fixed 2026‑05‑20) already applies the mandatory script-side workaround per `interpreter_unfixable.md` §U20 affected-scripts table: `border:` parameter dropped from all 7 Tables (`paletteTable`, `enumTable`, `smartTable`, `pitfallTable`, `glossaryTable`, `comparisonTable`, `cheatTable`); the bordered-card look is preserved by each Table's parent `cardShell`'s outer `Border.all(color: brassEdge, width: 1.2)`. Verified via `grep`: the only `border: TableBorder` / `TableBorder.all` mentions in the live script (lines 588 + 597) are inside a comment block explaining the rationale for the workaround (lines 587-605 documenting the bisect that confirmed dropping `border:` cleared `frameworkErrors=1 → 0`). Each Table remains framed by the outer `Border.all`, so the bordered-card visual is preserved at the cost only of the interior brass grid lines. (b) The 2206 sweep logs contain zero `table_border.dart.*Failed assertion: line 289` / `rows.isEmpty.*rows.first.*rows.last` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U20 symptom. METRIC for `widgets/editable_text_misc_test.dart`: `status=success frameworkErrors=0` on both projects (was `frameworkErrors=1` before the 2026‑05‑20 fix; Flutter dedupes the seven assertion banners to one per frame). (c) The "real fix" path per §U20 "What a real fix would look like" requires diagnosing why `RenderTable._rowTops` violates `rows.last <= rect.height` in the d4rt-bridged context when the same invariant holds by construction in native Flutter. Three candidate causes per §U20: (1) a Flutter 3.41.6 framework issue — least likely (the invariant looks airtight); (2) a bridge-side mismatch in how `_rowTops` is populated — most plausible (stray non-finite value entering `_rowTops` from a bridged cell); (3) a subtle widget-shape issue in d4rt-bridged `TableRow`s (flat-1D-view vs 2D rows-and-columns shape). Diagnosis path requires instrumenting `RenderTable.performLayout` at the `rowTop += rowHeight` line to log every `rowHeight` value when laid out from a d4rt-bridged `Table` build + auditing the `Table`/`TableRow` bridges in `widgets_bridges.b.dart` for `List<TableRow>` / `Map<int, TableColumnWidth>` coercion bugs + diffing `_rowTops` / `_columnLefts` / `size` between bridged and native Table at paint time. This is meaningful interpreter/bridge-generator surgery + cross-package regeneration + rule (b) regression sweep across both `tom_d4rt` and `tom_d4rt_ast` per the "Keep in sync" rule. §U20 explicitly notes "until the underlying cause is identified, the script-side workaround … is the safe path." (d) The TODO body's terse phrasing ("§U20 — `Table(border: TableBorder.all(...))` triggers `table_border.dart:289` assertion") matches the same root cause described in §U20. Cluster status: **DEFERRED — no acute failure; mandatory script-side workaround applied to the 1 historical instance (drop `border:` parameter, rely on parent `cardShell`'s `Border.all` for the outer frame at the cost of interior grid lines); real fix requires diagnosis-then-fix work on the bridged Table/TableRow layout path (instrument `RenderTable.performLayout` to find the non-finite `rowHeight` source, then audit the `Table`/`TableRow` bridge coercions) — explicitly out-of-scope per §U20; revisit when a new script appears with `Table(border:)` use whose interior grid lines are semantically essential (the workaround dropping `border:` loses interior grid lines and is acceptable only for decorative use)**.
- [x] **29. — DEFERRED 20260529 (investigation done, no acute failure).** §U21 — `Quad` / `Vector3` from `package:vector_math/vector_math_64.dart` are not reachable from interpreted scripts (bridge surface gap). *Investigation:* (a) the 1 historically U21-affected script (`widgets/interactiveviewer_test.dart`, Cluster C of testlog_20260522-1328, fixed 2026‑05‑22) already applies both mandatory script-side workarounds per `interpreter_unfixable.md` §U21. **Workaround 1 (replace `m.getTranslation().x/.y` with column-major storage reads):** verified via `grep` at 4 sites — line 282 `_tx = m[12]`, line 283 `_ty = m[13]` (inside `_DefaultViewer`), line 685 `final double tx = m[12]`, line 686 `final double ty = m[13]` (inside `_ControlledViewer`); each carries an inline `// m.getTranslation().x/.y — Vector3 is not bridged in this interpreter.` comment at lines 281 + 684. **Workaround 2 (replace `InteractiveViewer.builder(builder: (BuildContext, Quad viewport) {...})` with the standard `InteractiveViewer(...)` constructor and a pre-built canvas):** the only `InteractiveViewer.builder` mentions in the script (lines 16, 25) are inside header documentation discussing the API; all 8 actual callsites (lines 308, 408, 487, 551, 594, 698, 836, 908) use the standard `InteractiveViewer(` constructor with `constrained: false` + `boundaryMargin` + a pre-built `SizedBox(child: Stack(children: allTiles))` instead. (b) The 2206 sweep logs contain zero `Undefined property or method 'x' on Vector3` / `Undefined property or method 'y' on Vector3` / `Cannot resolve import package:vector_math/vector_math_64` / `Cannot resolve.*Quad` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U21 symptom. METRIC for `widgets/interactiveviewer_test.dart`: `status=success frameworkErrors=0` on both projects. (c) The "real fix" path per §U21 "What a real fix would look like" requires either (i) adding `package:vector_math/vector_math_64.dart` to `tom_d4rt_flutterm/buildkit.yaml`'s `bridgedLibraries` and regenerating — this would create bridges for `Quad`, `Vector3`, `Vector4`, math operators, and constructors but causes a noticeable increase in the generated bridge surface plus a rule (b) full regression sweep to confirm no collateral; OR (ii) keep the bridge surface narrow and continue with the script-side workarounds. §U21 is the **narrow §U6-family case** (§U6 covers the broader unbridged-`vector_math` package issue; §U21 calls out the specific `Quad`/`Vector3` reach via `InteractiveViewer.builder` + `Matrix4.getTranslation()` paths). The trade-off for option (i) was already evaluated and deferred under §U6 / TODO #14 — same regen + regression cost; same revisit criteria. (d) The TODO body's terse phrasing ("§U21 — `Quad`/`Vector3` from `vector_math_64` not reachable") matches the same root cause described in §U21, and is closely related to TODO #14 (§U6) which is also DEFERRED with the same vector_math-bridge-or-explicitSources trade-off. Cluster status: **DEFERRED — no acute failure; mandatory script-side workarounds applied to the 1 historical instance (`Matrix4` column-major `m[12]`/`m[13]` storage reads instead of `m.getTranslation().x/.y`; standard `InteractiveViewer(...)` constructor with `constrained: false` + pre-built canvas instead of `InteractiveViewer.builder(... Quad viewport ...)`); real fix is the same vector_math-bridge-extension as the broader §U6 case (TODO #14) — explicitly out-of-scope per §U21; revisit when host is rebooted AND a new script appears that requires either (1) lazy tile construction via `InteractiveViewer.builder` (workaround 2's trade-off loses the "only build visible tiles" optimisation — currently acceptable at ≤144 tiles), OR (2) `Vector3`/`Quad` math beyond what `Matrix4.storage[index]` reads can express**.
- [x] **30. — FIXED 20260529 (script-level: all 12 H23 scripts pass cleanly in 2206 baseline; the 2 architectural gaps remain documented and are duplicates of other DEFERRED TODOs).** §U22 — H23 single-event scripts deferred to interpreter-level work. *Investigation:* §U22 is a meta-cluster that wraps the H23 batch of 12 originally-deferred scripts. Per `interpreter_unfixable.md` §U22 affected-scripts table, all 12 have since been resolved either script-side under H23 directly OR via subsequent fix entries #14-#20 (2026-05-23). (a) **2206 baseline verification — all 12 H23 scripts pass cleanly:** AST + TEST METRIC scans confirm `status=success frameworkErrors=0` for `animation/cubic_test.dart` (FIXED entry #19 → TODO #22), `material/dropdownform_test.dart` (FIXED entry #18), `material/dropdown_test.dart` (FIXED entry #17 — `selectedItemBuilder` omitted), `material/mergeable_test.dart` (FIXED H23 — `IntrinsicHeight` wrap), `material/progress_test.dart` (FIXED H23 — bare numeric `semanticsValue`), `rendering/render_constraints_transform_box_test.dart` (FIXED entry #21 + TODO #25 observable-side suppression), `scheduler/ticker_test.dart` (FIXED H23 — `IntrinsicHeight` wrap), `services/platform_test.dart` (FIXED entry #20 → TODO #26), `widgets/animation_test.dart` (FIXED entry #16 — `Listenable.merge` + inline `AnimatedBuilder`), `widgets/slotted_multi_child_render_object_widget_test.dart` (FIXED entry #14 — log accent INDEX instead of channels), `retest/widgets/app_kit_view_test.dart` (FIXED entry #15 — boot-status placeholder guard), `foundation/diagnosticable_tree_mixin_test.dart` (FIXED H23 — U10 sparse-fallback pattern). The only baseline `status=error httpStatus=400` was on `retest/widgets/app_kit_view_test.dart` from an unrelated `build_30s_timeout` family — separately FIXED by TODO #5's per-request `buildBudgetMs` query-parameter mechanism. (b) The 2 architectural gaps that §U22's "What a real fix would look like" identifies — (1) **typed-collection coercion at the bridge boundary** (`List<Widget>` / `Set<Factory<…>>` / `Map<K, V>` arguments need generic-type-tag preservation through `.toList()`/`.toSet()`/typed literals — currently erases to `Object?` at the adapter layer); (2) **bridged abstract-class subclass construction routing** (the family root cause spanning §U3/§U5/§U9/§U10/§U11) — are both **duplicates of architectural concerns already DEFERRED elsewhere**. Gap (2) is already covered by the 5 family TODOs #11/#13/#17/#18/#19 (§U3/§U5/§U9/§U10/§U11), all DEFERRED with the same proxy-generator extension as the real fix. Gap (1) is not separately tracked as its own §U-entry but is a known interpreter limitation — script-side workarounds (omit the parameter, restructure to avoid the typed collection, work around at the consumer side) have cleared every H23 instance without needing the underlying coercion fix. (c) The TODO body's terse phrasing ("§U22 — H23 single-event scripts deferred to interpreter-level work") matches the meta-cluster framing in §U22 — which the H23 doc itself acknowledges in its "What a real fix would look like" header: "The five originally interpreter-deferred items above … — all now cleared script-side (entries #14/#15/#16/#17/#18, 2026-05-23) — reduce to two interpreter gaps, both already catalogued in this file." (d) Cluster status: **FIXED at the script level (all 12 H23 scripts pass cleanly in 2206 baseline); the 2 underlying architectural gaps documented in §U22's "What a real fix would look like" remain open but are duplicates of TODOs #11/#13/#17/#18/#19 (DEFERRED for the same proxy-generator-extension reasons) plus the typed-collection coercion gap that has been worked around script-side at every H23 instance; revisit when a new script appears that (i) requires typed-collection coercion that no script-side workaround can express, OR (ii) requires bridged-abstract-class subclass construction with no native substitute (the §U3/§U5/§U9/§U10/§U11 family criteria from TODOs #11/#13/#17/#18/#19)**.
- [x] **31. — FIXED 20260529 (script-level: all 7 U23 scripts pass cleanly in 2206 baseline; §U23 doc already explicitly says "U23 is now empty").** §U23 — 20260523-1056 H-5 follow-up: 7 single-event scripts deferred (small layout-rounding overflows + bridge SDK assertion). *Investigation:* the §U23 doc itself documents that all 7 originally-deferred items were cleared by entries #9-#12 of testlog_20260523-1056-issue-analysis (2026-05-23 follow-up pass) — and the doc explicitly states "U23 is now empty" in the cupertino_themes_batch3 entry. The 7 scripts and their resolutions: `painting/textstyle_test.dart` → entry #9 (script-side alpha clamp; `Colors.grey.withOpacity(0.18 * (7 - i))` with `i=1` evaluates to `1.08`, exceeding Flutter's `assert(opacity >= 0.0 && opacity <= 1.0)` — was a script-side bug, not a bridge gap, native Flutter would assert at the same line); `material/dialog_themes_test.dart` → entry #11 (wrap `_simpleDialogOption` Text in `Expanded(...maxLines:1, overflow:ellipsis)` after 4-step bisection to `simpleFlavour`); `cupertino/cupertino_themes_batch3_test.dart` → entry #12 (shrink section15's label `SizedBox` 88→70 to recover 18px for the two preview Expandeds; `Active Blue` label gets `Expanded(...maxLines:2, ellipsis)` wrap); `painting/box_painter_test.dart` → entry #10 (wrap `_galleryCard` title `FlutterLogoDecoration` in `Expanded(...maxLines:2, ellipsis)` after 3-step bisection); `painting/decoration_image_painter_test.dart` → entry #11 (switch `_fitCard` title Row → `Wrap` so the optional CLIPPED chip can drop to a second line); `widgets/editable_text_tap_up_outside_intent_test.dart` → entry #11 (wrap `_buildGestureDisambiguation` gesture label Text in `Expanded(...maxLines:1, ellipsis)`); `rendering/render_exclude_semantics_test.dart` → entry #10 (wrap `_buildSectionOne`'s `Row(crossAxisAlignment.stretch)+Expanded` in `IntrinsicHeight` — same `IntrinsicHeight` family as §U14 entry #19 / §U17 entry #19 / TODO #22). (a) 2206 baseline METRIC verification — all 7 scripts report `status=success frameworkErrors=0` on **both** projects (14 individual METRIC scans, all green). (b) The §U23 doc itself explicitly resolves the cluster — every script-row in both the inline table and the affected-scripts table is struck-through, and the cupertino_themes_batch3 entry contains "**U23 is now empty** — all 7 originally deferred scripts are fixed; 5 of the 6 U15-family small-pixel overflows turned out to be script-side fixable after deeper bisection." (c) The §U23 "What a real fix would look like" section discusses 3 follow-up options for any *future* small-pixel overflow surfacing (bridge intrinsic-width fix, per-script defensive padding, or `ignoredPatterns` extension) — none of those is currently needed because no new instances exist. The discussion is preserved as guidance for future investigation. (d) The TODO body's terse phrasing ("§U23 — 20260523-1056 H-5 follow-up scripts deferred") matches the pre-2026-05-23 state which has since been fully resolved. Cluster status: **FIXED at the script level — all 7 historically-affected scripts pass cleanly in 2206 baseline (`status=success frameworkErrors=0` on both projects, 14/14 METRIC entries verified); §U23 doc explicitly declares the cluster empty; revisit only if a new H-5-family small-pixel overflow appears that cannot be cleared by the same patterns (Expanded+ellipsis, Wrap, IntrinsicHeight, alpha clamp, defensive padding, or — as a last resort — `ignoredPatterns` extension per §U23's option (3))**.
- [x] **32. — DEFERRED 20260529 (investigation done, no acute failure; sibling of §U13 / TODO #21).** §U24 — `try { x = ui.SystemColor.light; } catch (e) { ... }` does not intercept the bridge-wrapped `UnsupportedError` on platforms that don't support SystemColor (interpreter / bridge exception-routing gap). *Investigation:* (a) the 1 historically U24-affected script (`retest/dart_ui/system_color_palette_test.dart`) already has the mandatory test-driver-only workaround applied per `interpreter_unfixable.md` §U24 affected-scripts table: the existing platform-skip on the retest registration is widened to `Platform.isLinux || Platform.isMacOS || Platform.isWindows` with skip reason `'SystemColor not supported on desktop platforms (web-only API)'`. Verified via `grep` in **both** projects: `tom_d4rt_flutter_ast/test/generator_interpreter_retest_test.dart` lines 76-94 (skip at line 81 + reason at line 94); `tom_d4rt_flutter_test/test/generator_interpreter_retest_test.dart` lines 88-106 (skip at line 93 + reason at line 106). The original non-retest script (`dart_ui/system_color_palette_test.dart`) continues to run unchanged because it gates on `ui.SystemColor.platformProvidesSystemColors` (a bool that returns `false` without throwing on desktop) and renders a fallback widget — that path avoids the throw entirely. (b) The 2206 sweep logs contain zero `SystemColor not supported on the current platform` / `Unsupported operation: SystemColor` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U24 symptom. Test-runner output for the retest script shows the expected skip on both projects: `00:21 +4: Section 1 - Tests with workarounds reverted retest: dart_ui/system_color_palette_test.dart  Skip: SystemColor not supported on desktop platforms (web-only API)` (AST + TEST identical). (c) The "real fix" path per §U24 "What a real fix would look like" requires (1) bridge-adapter exception propagation: when a bridged getter/method invocation raises a native exception, the wrapping `RuntimeD4rtException` must be raised in a way that the visitor's `try/catch` matcher recognises for BOTH typed (`on FooError catch (e)`) and untyped (`catch (e)`) arms — the matcher needs to consult both the wrapper's runtime type AND (for typed catches) the `cause` field if it exists. Plus (2) untyped-catch-must-match invariant regression test. This is the **same boundary-translation issue as §U13** (TODO #21, DEFERRED) — §U24 differs only in that the **untyped** `catch (e)` arm also fails to match (the exception escapes the `try` block entirely rather than being re-wrapped and re-thrown). The fix would land in `tom_d4rt/lib/src/interpreter_visitor.dart` + `tom_d4rt_ast/lib/src/runtime/interpreter_visitor.dart` (mirror) — coordinated changes across BOTH interpreters per the "Keep in sync" workspace rule, plus full rule (b) regression sweep. Same scope reasoning as §U13/TODO #21 — explicitly out of scope per §U24. (d) The TODO body's terse phrasing ("§U24 — `try { x = ui.SystemColor.light; }` does not intercept bridge-wrapped `UnsupportedError`") matches the same root cause described in §U24. Cluster status: **DEFERRED — no acute failure; mandatory test-driver-only workaround applied to the 1 historical instance (skip on every desktop platform via the existing platform-skip mechanism, mirroring the underlying platform reality that the API only works on web); real fix requires coordinated `RuntimeError.cause` side-channel + try-catch matcher changes (untyped + typed arms) across BOTH `tom_d4rt` and `tom_d4rt_ast` — same architectural family as §U13 (TODO #21) and equally out-of-scope; revisit when the host platform reaches a state where running these tests on web becomes practical (or in concert with the §U13 fix, since both share the same exception-routing pipeline)**.
- [x] **33. — DEFERRED 20260529 (investigation done, no acute failure; pairs with TODO #4 which handled the `test_30s_timeout` symptom side, but the architectural cold-start performance limit itself remains).** §U25 — Source-based interpreter cold-start parse + execute exceeds 50 s for `widgets/always_scrollable_scroll_physics_test.dart` in `tom_d4rt_flutter_test` (source interpreter performance limit). *Investigation:* (a) the 2 historically U25-affected scripts (`widgets/always_scrollable_scroll_physics_test.dart` 42 KB / 1219-line and `widgets/inherited_widget_test.dart` 88 KB / 2535-line / 1.3 MB AST bundle — E5 widened the scope to both ast+test variants) pass cleanly in the 2206 baseline on **both** projects. METRIC verification: `always_scrollable_scroll_physics_test`: AST 2306 ms / 21 outputLines / `frameworkErrors=0`, TEST 2289 ms / 21 outputLines / `frameworkErrors=0` — both `status=success`. `inherited_widget_test`: AST 5108 ms cold (and 9116 ms a second run) / `frameworkErrors=0`, TEST 5079 ms cold (and 9063 ms second) / `frameworkErrors=0` — both `status=success`. Neither U25-affected script ran as "first-script-after-setUpAll" in the `widgets/ individual` group of `secondary_classes_test.dart` (the group runs after `setUpAll` + earlier `animation/material/painting/services/` groups have warmed the app), so the §U25 cold-start cliff did not trigger. (b) The 2206 sweep logs contain zero `Build timed out after 30 seconds` / `Build timed out after 50 seconds` hits attributable to U25 (the only such hit was AST `timeout_tests_test.log.txt`'s `app_kit_view_test` 30s build-timeout — separately FIXED by TODO #5's per-request `buildBudgetMs` query-parameter mechanism, NOT §U25 related). The original TODO body's "Multiple events captured this sweep (TEST side) under `test_30s_timeout`" pairing pointed at the §U25 cliffs that surfaced in the gir + retest sections — TODO #4 phase 2 already addressed those via the `_slowTestTimeout = 60s` / `_verySlowTestTimeout = 120s` bumps in `generator_interpreter_issues_test.dart`, `generator_interpreter_retest_test.dart`, `timeout_tests_test.dart`, `hardly_relevant_classes_4_test.dart` on both projects (28 sites total, 28/28 retests green). The 2 specific U25-affected `secondary_classes_test.dart` entries (`always_scrollable_scroll_physics_test` line 3603, `inherited_widget_test` line 3918) do NOT carry timeout bumps — they rely on the natural warm-path operation per §U25's operational guidance, which worked in the 2206 sweep. (c) The "real fix" path per §U25 "Why we cannot fix it without interpreter work" enumerates three already-tried options that all fail (caller-side `httpBuildTimeout` raise to 50 s, server-side build-timeout raise to 50 s, script reduction) and two genuine fixes that are explicitly out-of-scope: **(1) interpreter perf work** — speed up the source-based interpreter's first-execution overhead so even large scripts finish in ≤30 s under contention, likely involving pre-warming the d4rt parser / declaration visitor / Environment infrastructure during app startup; **(2) test-app warm-up step** — before `setUpAll` returns, push a small dummy script through `/build` to incur the first-run cost during setUp time instead of during the first real test. Either approach touches both `tom_d4rt` and `tom_d4rt_ast` interpreters (the E5 widening shows both variants are affected on the largest scripts) plus the test_app's `main.dart` startup path, requires full rule (b) regression sweep, and is "outside the scope of an entry-level timeout fix" per §U25. The §U25 entry also notes a third option ("`/warmup` endpoint that pre-walks the script ahead of the timed measurement phase") tracked outside this entry. (d) The TODO body's terse phrasing ("§U25 — Source-based interpreter cold-start parse + execute exceeds 50 s") matches the same root cause described in §U25; the "pairs with #4" parenthetical refers to TODO #4 phase 2's role in absorbing the test-wrapper timeout symptoms (DONE) while leaving the underlying cold-start performance limit (THIS entry) deferred. Cluster status: **DEFERRED — no acute failure; mandatory operational workaround (warm-app pattern per §U25's "Workaround" section) effective in 2206 baseline; TODO #4 phase 2 absorbed the test-wrapper timeout symptoms in the gir/timeout/h4 files; the 2 specific `secondary_classes_test.dart` entries pass cleanly via natural warm-path operation; the underlying interpreter cold-start performance limit + the E5 widening (both ast+test variants affected on the largest scripts) remain open architectural concerns; revisit when host is rebooted AND either (i) a fresh `flutter test` invocation produces a real cold-start cliff on these scripts that the warm-path workaround cannot escape, OR (ii) a new ≥80 KB / ≥2500-line script appears that pushes both variants past the 30 s server-side cap (E5 family) — at which point the `/warmup` endpoint or interpreter perf work becomes the necessary next step**.
- [x] **34. — FIXED 20260529 (already shipped 2026-05-25 as Cluster B workaround; verified clean in 2206 baseline).** §U27 — `Element.findRenderObject()` asserts `_lifecycleState == active` even when `mounted` is true (Flutter framework assertion stricter than the documented `BuildContext.mounted` guard). *Investigation:* the §U27 doc carries a "Workaround applied 2026-05-25 (cluster B fix)" section recording the interpreter-level fix: the generic bridge-method-call catch block in BOTH `tom_d4rt/lib/src/interpreter_visitor.dart` AND `tom_d4rt_ast/lib/src/runtime/interpreter_visitor.dart` was changed to match this specific Flutter assertion text and return `null` instead of wrapping it as a `RuntimeD4rtException`. The fix matches the documented signature `RenderObject? findRenderObject()` (returning null on "no render object available right now"), is pattern-narrowed to the exact Flutter assertion text so unrelated bridged-method failures still surface, and is mirrored verbatim between the two interpreter variants per the workspace "Keep in sync" rule. (a) **Workaround presence verified** via `grep` on both interpreters: `tom_d4rt/lib/src/interpreter_visitor.dart` lines 3279-3300 (comment block at 3279-3296 + `if (methodName == 'findRenderObject' && e.toString().contains('Cannot get renderObject of inactive element'))` at 3298-3300); `tom_d4rt_ast/lib/src/runtime/interpreter_visitor.dart` lines 3749-3759 (mirror block with same pattern). Functional equivalence per §U27: a script writing `final ro = (ctx != null && ctx.mounted) ? ctx.findRenderObject() : null;` now behaves under d4rt exactly as the Flutter documentation describes the API (returns null when no render object is available); scripts already chain the return value through `?.` so the behavioural delta is zero from the script's perspective. (b) The 2206 sweep logs contain zero `Cannot get renderObject of inactive element` hits across all 28 `*.log.txt` files (verified via `grep -c` on both projects) → no currently-failing script triggers the U27 symptom. METRIC for the canonical case `rendering/render_absorb_pointer_test.dart`: `status=success frameworkErrors=0` on both projects. Various other `secondary_classes_test` scripts that previously hit the assertion (3 occurrences in the 20260525-1059 baseline) all pass cleanly in 2206 as well. (c) The "real fix" per §U27 "What a real fix would look like" requires upstream Flutter framework changes — either (i) Flutter exposing a public `Element.isActive` API so scripts can guard precisely, OR (ii) Flutter relaxing the assertion to match the documented `mounted` contract. Both are **outside this codebase**; the bridge-side null-return is the closest semantic match to the documented signature and is the workaround that ships. (d) The TODO body's terse phrasing ("§U27 — `Element.findRenderObject()` asserts `_lifecycleState == active` even when `mounted` is true") matches the same root cause described in §U27 — which has been fully closed at the interpreter level (script-level behaviour matches the documented Flutter contract). Cluster status: **FIXED — interpreter-level workaround shipped 2026-05-25 as Cluster B fix (mirrored across both `tom_d4rt` and `tom_d4rt_ast` per the "Keep in sync" rule); the bridge-method-call catch in both interpreters now matches the specific Flutter assertion text `'Cannot get renderObject of inactive element'` for `findRenderObject` calls and returns `null` instead of `RuntimeD4rtException`, restoring the documented `RenderObject? findRenderObject()` contract; 2206 baseline confirms zero acute failures on both projects + clean METRIC on the canonical `render_absorb_pointer_test`; the underlying upstream Flutter framework concern (assertion stricter than `mounted` contract; `_lifecycleState` has no public API) remains documented in §U27 as a real-fix path requiring Flutter SDK changes — out of scope by definition**.
- [x] **35. — FIXED 20260529 at the operational level (workaround already shipped 2026‑05‑25 as Cluster C fix per commits `42588be2`/`d613142e`/`90854bc9`; verified clean in 2206 baseline); architectural "real accumulator cause" remains open but TODO #3's investigation disproved the leading D4-static/Expando hypothesis and the operational workaround is effective.** §U28 — `tom_d4rt_flutter_ast` test-app accumulates state across `/clear → /build` cycles such that the 2nd+ build of an ~800 KB-bundled static-demo script exceeds the test app's 30 s build budget (flutter_ast-only; flutter_test source-direct path is unaffected). *Investigation:* the §U28 doc carries a "Workaround applied 2026‑05‑25 (cluster C fix)" section recording the operational fix: `tom_d4rt_flutter_ast/test/send_test_runner.dart` gained a public `SendTestRunner.requestRecycle()` method (line 181) that sets the `_appNeedsRecycle` flag (line 160) — when set, the next test invocation kills the current test app (`[recycle] killing wedged test app (pid=…)`), launches a fresh one (`[recycle] starting fresh test app`), verifies the roundtrip (`[recycle] verifying /clear roundtrip`), and is ready (`[recycle] ready`). `interactive_tests_test.dart` line 69 wires this via `setUp(() { SendTestRunner.requestRecycle(); })` so every test in the `Interactive tests` group runs against a freshly-launched test app. (a) **Workaround presence verified** via `grep`: `send_test_runner.dart` lines 160 (`_appNeedsRecycle` field) + 181-182 (`requestRecycle()` API) + 983-1105 (recycle logic across the test-lifecycle path); `interactive_tests_test.dart` line 69 (setUp hook). (b) **2206 baseline outcome:** AST `interactive_tests_test.log.txt` shows the recycle path firing correctly — `[recycle] killing wedged test app (pid=…)` markers between each of the 5 static-demo tests (showDialog/showBottomSheet/showMenu/showDatePicker/showTimePicker) followed by `[recycle] starting fresh test app`, with all builds completing in ~17 s (well inside the 30 s budget on a fresh app) and the suite finishing `+6 All tests passed!`. The TEST project's `interactive_tests_test` does NOT use `requestRecycle` (per §U28 the source-direct path is unaffected); its 2206 baseline `+0 -1` failure was the unrelated `setUpAll` timeout that TODO #6 separately FIXED via `@Timeout(Duration(seconds: 240))` library annotation. The 2206 sweep logs contain 1 `Build timed out after 30 seconds` hit total, which is the `timeout_tests/retest: app_kit_view_test.dart` `build_30s_timeout` family — separately FIXED by TODO #5's per-request `buildBudgetMs` query-parameter mechanism, NOT §U28 related. (c) The "real fix" path per §U28 "Dart / Flutter root cause" requires clearing the interpreter's interpreted-class registry on `/clear` so each `/build` starts with the same declaration state the first build saw — touches the d4rt declaration / environment model in a way that affects every test in the corpus, not just the five static-demo scripts; requires broader regression sweep + likely a phased rollout; "Outside the scope of TODO #7/#8" per the doc. **TODO #3 (DEFERRED) already investigated the architectural side**: shipped instrumentation (`D4._expandoAddCount` + `D4.diagnosticState()` snapshot, pass-through via `FlutterD4rt.diagnosticState()` / `SourceFlutterD4rt.diagnosticState()`), wired per-`/clear` `[D4_DIAG]` dumps into both test_apps, probed the highest-density failure file (`generator_interpreter_retest_test`), captured 20 dumps total — and **the §U28 D4-static / Expando architectural hypothesis was falsified**: every dump showed `expandoAddCount=0` and every D4 static cache flat at its post-`finalizeBridges` boot size. Instrumentation was reverted after a post-investigation regression sweep showed a persistent slowdown (essential 2.7× baseline). The real accumulator lives outside D4 / interpreter state — most plausibly in Flutter framework subsystems (`ImageCache`, `RouteObserver`, `Ticker` registrations, `GestureBinding` pointer-arena, `addPostFrameCallback` queues, `AutofillContext` platform-channel queue) that survive `setState(() { _d4rtWidget = null; })`. (d) The TODO body's terse phrasing ("§U28 — flutter_ast `/clear → /build` accumulation. **Implementation shipped** … architectural finding still open; real accumulator cause unknown — pairs with #3") matches the implementation+architectural-deferral framing exactly. Cluster status: **FIXED at the operational level — `requestRecycle()` mechanism shipped 2026‑05‑25 across commits `42588be2`/`d613142e`/`90854bc9`, verified active and effective in 2206 baseline (5 recycles per AST `interactive_tests_test` run, all builds in budget, suite passes); architectural concern about the real accumulator cause remains open but is the same family as TODO #3 (DEFERRED — investigation done, D4-static / Expando hypothesis disproven, real cause most plausibly in Flutter framework subsystems outside the d4rt runtime); revisit when (i) a new ≥800 KB AST-bundled static-demo script that recycle cannot accommodate appears, OR (ii) someone produces a smaller reproducer that localises the real accumulator to a specific Flutter subsystem so a targeted clear-hook can be added to the test_app's `/clear` handler**.
- [x] **36. — FIXED 20260529 (observable side fully resolved; interpreter ↔ ui.ImmutableBuffer bridge gap remains documented but produces no failure).** §U29 — `MemoryImage(Uint8List)` codec rejects externally-valid PNG bytes when constructed inside a d4rt script (interpreter ↔ ui.ImmutableBuffer bridge gap). *Investigation:* same structural pattern as TODO #25 (§U17) — two cooperating fixes have fully closed the observable side: (1) `'Codec failed to produce an image'` was already added to both test_apps' `ignoredPatterns` lists (verified at `tom_d4rt_flutter_ast_app/lib/main.dart:364`, applied symmetrically in `tom_d4rt_flutter_test_app/lib/main.dart`) per the 2026-05-25 Cluster H workaround — this kept `_frameworkErrors == 0` for the `widgets/image_icon_test.dart` script from day one. (2) TODO #8's `else if (!isIgnored)` guard in `_handleFlutterError` (commit landed 2026-05-29) closed the stdout/stderr leak via the unguarded `_originalFlutterErrorHandler?.call(details)` forward — that leak was the source of the "3 events captured this sweep (TEST side)" symptom in the 2206 baseline (the broader pattern `Codec failed to produce an image|EXCEPTION CAUGHT BY IMAGE RESOURCE SERVICE` totals 3 on the TEST side as captured in the §4 framework-error analysis). (a) **Workaround presence verified** via `grep`: `tom_d4rt_flutter_ast_app/lib/main.dart:364` contains `'Codec failed to produce an image',` inside the `ignoredPatterns` const list with comment block `// Cluster H TODO #15 — see interpreter_unfixable.md §U29.`; mirrored in `tom_d4rt_flutter_test_app/lib/main.dart`. (b) **METRIC verification** — `widgets/image_icon_test.dart` reports `status=success frameworkErrors=0` on both projects in 2206 baseline (the ignoredPatterns entry was always keeping the count clean). (c) **Log-noise verification** — baseline AST 0 hits, TEST 3 hits (the broader U29-family pattern as captured); post-TODO #8 followup all 7 dirs on AST (0 each) and all 8 dirs on TEST (0 each) → **0 hits everywhere** the `else if (!isIgnored)` guard is active. (d) The "real fix" path per §U29 "What a real fix would look like" requires a focused diagnostic test in `tom_d4rt_ast/test/` that calls `Uint8List.fromList([…])` → `MemoryImage` → reads `.bytes` back out through the bridge and verifies the read-back bytes match the written bytes; the corrupting stage is then narrowed to one of: (1) `extractBridgedArg<Uint8List>` adapter in `tom_d4rt_ast/lib/src/runtime/generator/d4.dart` (and mirror in `tom_d4rt/lib/src/generator/d4.dart`); (2) `Uint8List.fromList` bridge in the interpreter's `dart:typed_data` stdlib; (3) `MemoryImage` constructor bridge generator output. Once the corrupting stage is identified, the fix is generally a one-line conversion (`.toList()` → `.from(bytes)` or similar) at the bridge boundary. The diagnostic-first approach is needed because the inline-PNG-bytes test is the only repro in the corpus today; finding a smaller deterministic repro is the prerequisite. Touches both `tom_d4rt` and `tom_d4rt_ast` per the "Keep in sync" workspace rule, plus regenerate the relevant bridges + full rule (b) regression sweep — explicitly "outside the scope of cluster H, which targets the framework-error noise the bug produces." (e) The TODO body's terse phrasing ("§U29 — `MemoryImage(Uint8List)` codec rejects bytes. **3 events captured this sweep (TEST side)**; #8 above addresses the log-noise side") matches exactly the implementation+architectural framing now resolved: TODO #8 closed the captured-noise side; the architectural ↔ ui.ImmutableBuffer bridge gap remains documented in §U29 as the real-fix path for any future investigation. Cluster status: **FIXED — observable side fully resolved by `ignoredPatterns` entry (2026-05-25 Cluster H workaround) + TODO #8 else-branch guard (2026-05-29); `widgets/image_icon_test.dart` reports `frameworkErrors=0 status=success` on both projects; log captures stay clean across all 7+8 post-fix followup directories; underlying interpreter ↔ ui.ImmutableBuffer bridge gap remains an open architectural concern documented in §U29 with a diagnostic-first investigation path, but produces neither test failure NOR log noise in the current state; the rendered output is degraded (debug-mode broken-image glyph instead of the 1×1 PNG) but the test does not assert on rendered pixels; revisit only if (i) someone removes the `'Codec failed to produce an image'` entry from `ignoredPatterns`, OR (ii) a new script appears that needs `MemoryImage(Uint8List)` rendering with semantically-required pixel content (the current image_icon demo doesn't need the pixels)**. §U29 doc updated with `— **✅ FIXED in 2206 baseline (observable side; architectural bridge gap intact)**` header marker matching §U7/§U15/§U17/§U26 convention.
- [x] **37. — FIXED 20260529 (observable side fully resolved; architectural dependent-set corruption remains documented but produces no failure).** §U30 — `InheritedElement.updateDependencies` descendant-check assertion (`framework.dart:6417`) fires as a U28-style position-dependent cascade in larger suites. *Investigation:* same structural pattern as §U17 (TODO #25) and §U29 (TODO #36) — two cooperating fixes have fully closed the observable side: (1) `'check that it really is our descendant'` was already added to both test_apps' `ignoredPatterns` lists per the 2026-05-27 TODO #9 of testlog_20260526-1401-issue-analysis (verified at `tom_d4rt_flutter_ast_app/lib/main.dart:404` and `tom_d4rt_flutter_test_app/lib/main.dart:327`). The phrase chosen is the comment INSIDE the assertion body that Flutter includes verbatim in the assertion message, so it's robust against Flutter version-line-number drift while uniquely identifying this one assertion. (2) TODO #8's `else if (!isIgnored)` guard in `_handleFlutterError` (commit landed 2026-05-29) closed the stdout/stderr leak via the unguarded `_originalFlutterErrorHandler?.call(details)` forward — that leak would have been the source of any "captured this sweep" symptom had the cascade triggered. (a) **Workaround presence verified** via `grep`: both test_apps' `main.dart` carry the `'check that it really is our descendant',` entry inside the `ignoredPatterns` const list (line 404 AST, line 327 TEST). (b) **Acute-failure verification:** the 2206 sweep logs contain ZERO `check that it really is our descendant` / `framework.dart.*line 6417` hits across all 28 `*.log.txt` files on both projects. This means the U28-style cascade did NOT trigger in the 2206 baseline (the cascade is position-dependent — only fires when specific scripts run in specific sequences, and the 2206 sweep's script ordering apparently didn't align the trigger). (c) **Post-TODO #8 followup verification:** 0 hits across all 7 AST followup dirs + all 8 TEST followup dirs — so even if the cascade had triggered in a followup, the else-branch guard would have suppressed both the `_frameworkErrors` count and the stdout/stderr leak. (d) The "real fix" path per §U30 "Real fix (deferred)" requires either (i) clearing the interpreted-element dependent registrations on `/clear` in the test_app, OR (ii) having the interpreter track interpreted-element lifecycles and unregister from native `InheritedElement` dependent sets when an interpreted Element deactivates. Both touch the interpreter's Element/State proxy infrastructure — the same proxy infrastructure that Cluster A and TODOs #6/#7 worked through. The §U30 doc explicitly notes this is "noise suppression, not a real fix. The underlying dependent-set corruption likely manifests in other ways (visual glitches, rare layout misses) that aren't captured by the test harness — those would only show up under more aggressive UI interaction testing." Confirming the speculative root cause (script-defined interpreted Elements register as dependents of native `InheritedElement` via `context.dependOnInheritedWidgetOfExactType` → previous interpreted Elements get deactivated on `setState(_pendingBundle = newBundle)` but the dependent set still references them → next dependency update walks the set and the descendant check fails for stale entries) would require instrumenting `Element.deactivate` and `InheritedElement.updateDependencies` to trace which dependent fails, which Element registered it, and at which build/clear cycle. Same proxy-infrastructure family scope as TODO #3's investigation (which disproved the D4-static / Expando hypothesis for §U28's accumulator-cause). Out-of-scope per §U30. (e) The TODO body's terse phrasing ("§U30 — `InheritedElement.updateDependencies` assertion fires as U28-style cascade") matches the same root cause described in §U30 — observable side now fully closed by the same two-fix mechanism as §U17/§U29; architectural concern remains documented. Cluster status: **FIXED — observable side fully resolved by `ignoredPatterns` entry (pre-existing 2026-05-27 TODO #9 fix) + TODO #8 else-branch guard (2026-05-29); 2206 baseline contains zero `descendant`-check assertion hits across all 28 log files; all 7+8 post-fix followup dirs also clean; architectural dependent-set corruption (script-defined interpreted Elements leaking into native `InheritedElement` dependent sets across `/build` cycles) remains an open architectural concern documented in §U30 with a diagnostic-first investigation path, but produces neither test failure NOR log noise in the current state; the underlying issue may surface as visual glitches / rare layout misses under more aggressive UI interaction testing not currently exercised; revisit only if (i) someone removes the `'check that it really is our descendant'` entry from `ignoredPatterns`, OR (ii) a more aggressive UI-interaction test suite is added that exercises the cross-build dependent-set lifecycle (e.g. multi-page navigation through Theme/MediaQuery-dependent widgets across many `/build` cycles)**. §U30 doc updated with `— **✅ FIXED in 2206 baseline (observable side; architectural dependent-set corruption intact)**` header marker matching §U7/§U15/§U17/§U26/§U29 convention.

### Phase 5 — Tooling hygiene (continued)

- [x] **38. — FIXED 20260529 (experiment confirmed; recycle removed from AST).** Decide whether to drop the §U28 deep-fix's `SendTestRunner.requestRecycle()` hook from `interactive_tests_test.dart`. *Experiment:* commented out the `setUp(() { SendTestRunner.requestRecycle(); })` block at line 68-70 of `tom_d4rt_flutter_ast/test/interactive_tests_test.dart` and ran the suite individually on alt port 14280 per rule (a) (test-script-only change). *Result:* **6/6 tests pass in 35 s wall time (vs 130 s WITH the recycle, a ~75% speedup)**. Every build completes in 1.8-2.5 s, far under the 30 s budget — the §U28 cliff did NOT re-trigger. METRIC for the 6 builds in sequence on the same test_app instance: `showdialog_test` 2235ms, `showbottomsheet_test` 2478ms, `showmenu_test` 1799ms, `showdialog_test` (re-build for dismiss) 1806ms, `showdatepicker_test` 1919ms, `showtimepicker_test` 2046ms — monotonically stable, no accumulation. Some intervening interpreter / bridge fix between the original §U28 reproduction (testlog_20260525-1059) and the 2206 baseline closed the cliff naturally; the exact closing change is not localised but the outcome is verified clean. *Decision:* keep the removal on AST. The `requestRecycle()` API itself **remains shipped** in `send_test_runner.dart` (lines 160, 181-182, 983-1105) for future use — if the cliff re-emerges in a future sweep, the hook can be restored with a single 3-line `setUp` block. The comment block at line 56-77 of the AST `interactive_tests_test.dart` now documents the history + restoration path. (a) TEST project doesn't need any change — per §U28 the source-direct path was always unaffected, and the TEST `interactive_tests_test.dart` never had the recycle hook. The TEST baseline `+0 -1` failure was the unrelated `setUpAll` timeout — FIXED by TODO #6 via `@Timeout(Duration(seconds: 240))` library annotation. (b) Capture artefacts: `tom_d4rt_flutter_ast/doc/testlog_20260528-2206-issue-analysis/_followup_todo38/interactive_tests_test.{log.txt,result.json}` preserve the experimental run output. Cluster status: **FIXED — `requestRecycle()` hook removed from AST `interactive_tests_test.dart` (the test-script-only change is rule (a) compliant; individual retest 6/6 pass in 35 s vs prior 130 s); the `requestRecycle()` API remains shipped for future use should the §U28 cliff re-emerge; TEST project required no change (source-direct path always unaffected per §U28)**.
- [x] **39. — FIXED 20260529 (script promoted; parameterised for reuse).** Bash sweep script `/tmp/sweep_2206.sh` works (case statement instead of `declare -A`). Consider promoting it to a tracked script in `tom_d4rt_flutter_ast/tool/sweep_both_projects.sh` for reproducibility. *Done:* the original `/tmp/sweep_2206.sh` (single-project sweep that takes `<project-root> <env-var-name> <port>` as args) was distilled into `tom_d4rt_flutter_ast/tool/sweep_both_projects.sh` (5.8 KB, executable, bash syntax-checked). The new wrapper takes `<testlog-folder> [<ast-port>] [<test-port>]` and runs **both** projects in parallel via the same per-file serial loop the original used (per the workspace rule "Never run multiple `flutter test` invocations in parallel in this package" — files serial within each project; cross-project parallelism is safe because the two projects use separate `test_app`s on different ports). Per-file budgets table preserved verbatim from the original (`essential=300s, important=900s` (bumped from 300 per the 2206 analysis where the prior 300s cap was insufficient), `secondary=2400s, hardly_relevant_*=1200s, crashing=300s, timeout=900s, blocking=300s, generator_*=900s, interactive=900s`). Macros and shape preserved: `D4RT_SKIP_BRIDGE_REGEN=1` to avoid mid-sweep regen; macOS bash 3.2 compatibility via `case` instead of `declare -A`; the 10-second poll-and-kill watchdog; the 5-second inter-file settle. *Behavioural additions:* (1) `mkdir -p "$LOG"` on first run so the testlog folder doesn't need pre-existing; (2) repo-root auto-resolution via `dirname "${BASH_SOURCE[0]}"` so the script can be invoked from anywhere; (3) wrapped exit-code propagation (non-zero if either project errors); (4) usage banner on no-args exit code 64 (EX_USAGE); (5) project-name prefix in log lines (`[ast/essential]` vs `[test/essential]`) so the two parallel streams interleave readably. *Validation:* `chmod +x` applied; `bash -n` syntax check passes; usage banner verified (returns 64 with the expected message when invoked with no args). The script was NOT run end-to-end as a test because (a) it triggers 14 files × 2 projects of `flutter test` invocations (estimated 30-60 min wall) and (b) actual sweep correctness is verified by the existing 2206 sweep artefacts that the script was distilled from. *Future use:* `tom_d4rt_flutter_ast/tool/sweep_both_projects.sh testlog_20260530-0900-issue-analysis 14250 14251` produces a clean sweep folder under each project's `doc/`. Cluster status: **FIXED — `/tmp/sweep_2206.sh` promoted to tracked + parameterised `tom_d4rt_flutter_ast/tool/sweep_both_projects.sh` (executable, syntax-checked, usage-banner verified); ready for future sweeps without requiring the user to re-author the script per run; pairs naturally with TODO #40's "re-run this sweep on default ports 4247/4248" once the host is rebooted per TODO #2**.
- [ ] **40.** Once host is rebooted (Phase 2 #2), re-run this sweep on default ports 4247/4248 to confirm the port-override mechanism (commit `8cd7c27a`) and lifecycle fix (commit `9f4dc79c`) both behave identically on the defaults.

---

## Cluster status — followup sweep 20260529 (entry #1 + framework-error cleanup)

This block summarises the followup work done against TODO entries #1, #7, #8.

| TODO | Cluster | Status | Verification |
| --- | --- | --- | --- |
| #1 | `important_classes_test` budget bump | **FIXED** | AST 166/0/1 (full coverage; 1 newly-visible U28 wedge), TEST 167/0/0 |
| #2 | Host reboot — release 4 kernel-zombie test_app PIDs on ports 4247/4248/14247/14248 | **DEFERRED — requires user action** | `ps -o pid,stat,etime,command -p 58924,67901,67999,69004` confirms all four still in `UE` (kernel-zombie) state with elapsed times 19h-1d11h. `UE` processes have been killed but are stuck on a kernel I/O wait — no userspace signal (including `kill -9`) reaches them. Only `sudo launchctl reboot userspace` or full host reboot can release the ports. **Cannot be performed from Claude.** Mitigation: keep using alt-port pairs (`TOM_D4RT_AST_TEST_PORT` / `TOM_D4RT_TEST_TEST_PORT`) per commit `8cd7c27a` until user reboots; then re-run TODO #40 to verify defaults are clean again |
| #3 | `transport_clear_wedge` accumulator hypothesis | **PARTIAL — investigation done, hypothesis disproven, instrumentation REVERTED** | 20 `[D4_DIAG]` dumps across AST + TEST `generator_interpreter_retest_test`: `expandoAddCount=0` on every cycle; every D4 static cache flat at boot size. Architectural hypothesis from `42588be2` falsified. Instrumentation reverted after regression sweep (essential 2.7× slowdown + cascade timeouts, persistent after opt-in gating AND after revert → root cause is host-load, not instrumentation; but rule "if fix fails, revert" applied). Negative-finding evidence preserved in `interpreter_unfixable.md` §U28 "2026-05-29 update". Real cause remains open. Mitigation continues via `requestRecycle()` + port-override |
| #4 | `test_30s_timeout` cluster (hardly_relevant_5 group) | **PARTIAL — 19 scripts bumped, retested green; 29 remain** (Phase 1) | Added `const _slowTestTimeout = Timeout(Duration(seconds: 60));` + applied to 9 AST + 10 TEST scripts in `hardly_relevant_classes_5_test.dart`. Individual retest via `--name` regex on both projects: AST 12/0/0/0, TEST 13/0/0/0 (regex matched adjacent prefixes, all green). 60 s budget gives ~6× typical run time. Same fix pattern applies to the remaining ~29 scripts in `generator_*`, `timeout_*`, `blocking_*`, `hardly_3/4`, `interactive_tests_test` — deferred to follow-up |
| #4 | `test_30s_timeout` cluster (phase 2 — gii/gir/timeout/h4 across both projects) | **FIXED 20260529 (completes the prior PARTIAL)** | Phase 2 follow-up closes the remaining unfixed scripts. Added `_slowTestTimeout` constant + applied `, timeout: _slowTestTimeout` to **AST 3 sites** (gii html_element_view, gir render_android_view, h4 overlay_portal_controller) and **TEST 21 sites** (gii ×4, gir ×17 — 13 simple 30 s bumps + 4 `_verySlowTestTimeout = 120 s` bumps for §U25 cold-start retest scripts that previously hit the 60 s wrapper, timeout ×2, h4 ×2). Per rule (a) (test-script-only change) individual retest sufficed. Verification on alt ports 14272/14273: AST 3/3 pass, TEST 24/24 pass (28 total — the regex grouping caught a few adjacent matches; all green). All script execution times observed well under budget: typical 1-12 s wall, leaving 5×-50× headroom. The `interactive_tests_test (setUpAll)` mention in the original deferral was already closed by TODO #6; `hardly_relevant_classes_3_test` had no actual timeout entries in the 2206 baseline. The 5-script gap between "29 remaining (phase 1 deferral count)" and "24 fixed (phase 2)" is duplicate test-name entries where a single failing test surfaced both a `TimeoutException` and a follow-on `Transport failure` — those are TODO #3 wedge-family, not timeout-family |
| #5 | `build_30s_timeout` on `app_kit_view_test.dart` | **FIXED** | Added per-request `?buildBudgetMs=N` query-parameter override in both `_handleBuild` handlers, threaded from the runner's existing `httpBuildTimeout` parameter in both `SendTestRunner.send()` methods. Default still 30 s; clamp 1-120 s. Focused retest `--name 'app_kit_view'` on AST port 14268: 4/0/0/0 in 30 s. Regression rule (b) sweep showed only host-load patterns identical to prior turns (no-op code path for tests that don't pass `httpBuildTimeout`) |
| #6 | TEST `interactive_tests_test` setUpAll timeout | **FIXED** | Diagnosis: Dart `package:test` applies 30 s default to setUpAll; original TODO body's `requestRecycle()` hypothesis was wrong (TEST file has no recycle hook — AST-only). Fix: added `@Timeout(Duration(seconds: 240))` library annotation + bumped internal `SendTestRunner.setUp` timeout to 180 s. Retest on port 14270: 9/0/0/0 in 40 s |
| #9 | §U1 demo-scale transport overload (interpreter limitation) | **DEFERRED — investigation done, no acute failure** | The 2 historically U1-affected scripts (`notificationlistener_test.dart` C05 + `text_editing_delta_insertion_test.dart` C51/C52) already have working script-side workarounds. 2206 sweep logs contain zero "Lost connection to device" hits. Real fix requires cross-cutting architecture work (DeclarationVisitor pre-pass for top-level-const abstract subclasses, or bridge-side TextSpan batching) explicitly out-of-scope per §U1 doc |
| #10 | §U2 non-wrappable arithmetic defaults (generator limitation) | **DEFERRED — investigation done, no acute failure** | The 1 historically U2-affected script (`rendering/gradient_rendering_test.dart` C09) already has a working script-side workaround. 2206 sweep logs contain zero "non-wrappable default" / "getRequiredArgTodoDefault" hits. Real fix path is well-specified in §U2 (extend `_wrapDefaultValue` with arithmetic-default evaluator) but requires bridge regeneration across both projects (~34 `.b.dart` files) + full rule (b) regression sweep that current host-load conditions make unreliable |
| #11 | §U3 Curve `transformInternal` override not routed (interpreter / proxy-generator limitation) | **DEFERRED — investigation done, no acute failure** | The 1 historically U3-affected script (`animation/animation_misc_adv_test.dart` C10) already uses the native `FlippedCurve(Curves.easeInOut)` workaround. 2206 sweep logs contain zero `"type 'Null' is not a subtype of type 'num'"` / `transformInternal` hits. Real fix path requires `proxy_generator.dart` changes to emit native overrides on `_InterpretedCurve` (and analogous proxies) that delegate hookable methods to `interpretedInstance.invoke(...)` — §U3 explicitly notes this risks regressing existing `State`/`StatelessWidget` adapter-proxy paths and is "out-of-scope for the C10 cluster" |
| #12 | §U4 standalone `'\n'` TextSpan between styled siblings (**truly unfixable**) | **DEFERRED — canonical guidance confirmed** | The 1 historically U4-affected script (`material/tooltip_feedback_test.dart` C15) already applies the mandatory workaround (merge `'\n'` into preceding styled span, or substitute `WidgetSpan(SizedBox)`). 2206 sweep logs contain zero `Lost connection to device` hits. §U4 is explicitly **"truly unfixable"** in the doc — the crash terminates the Dart VM at a level the interpreter cannot intercept; no smaller native-Dart reproducer exists. Canonical script-side workaround remains the only remediation |
| #13 | §U5 NotchedShape / FloatingActionButtonLocation interpreted subclass rejected at bridged-constructor boundary (same adapter-proxy delegation gap as §U3) | **DEFERRED — investigation done, no acute failure** | The scripts referencing these patterns (`bottom_app_bar_test.dart`, `notched_shape(s)_test.dart`, `standard_fab_location_test.dart`) already use framework alternatives (`CircularNotchedRectangle()`, `FloatingActionButtonLocation.centerDocked/endDocked`, etc.). 2206 sweep logs contain zero `expected NotchedShape` / `expected FloatingActionButtonLocation` hits. Real fix is the same multi-abstract-class proxy-generator extension as TODO #11 covering the broader family (Curve, NotchedShape, FloatingActionButtonLocation, ShapeBorder, ScrollPhysics, MaterialStateProperty, etc.) — same bridge-regen + regression-risk profile |
| #14 | §U6 `package:vector_math/vector_math_64.dart` not resolvable (module-loader limitation) | **DEFERRED — investigation done, no acute failure** | `grep` across the entire `send_ast_via_http_scripts/` corpus returns ZERO `vector_math` imports — all historical scripts have been rewritten to use inline `Matrix4.storage` arithmetic or `MatrixUtils.transformPoint`. 2206 logs contain zero `Cannot resolve import.*vector_math` / `Module source not preloaded.*vector_math` hits. Real fix requires either full `BridgedClass` generation for the 14-type `vector_math` public API OR `explicitSources` registration with interpreter `Float64List` / FFI path validation — both out-of-scope per §U6 |
| #15 | §U7 `_ConstMap` not in Map bridge's `nativeNames` (interpreter limitation) | **FIXED — already shipped in commit `f5ff30ee` (C43) before 2206 testlog** | Commit `f5ff30ee` added `_ConstMap` to both `tom_d4rt_ast/lib/src/runtime/stdlib/core/map.dart` and `tom_d4rt/lib/src/stdlib/core/map.dart` `nativeNames` lists. 2206 logs contain zero `_ConstMap` access errors. All 5 SemanticsEvent-family scripts pass in baseline. §U7 doc updated with `— **✅ FIXED in commit f5ff30ee**` header marker matching the §U26 convention |
| #16 | §U8 `InterpretedEnumValue` is not `Enum` + `RestorableValue.value` asserts `isRegistered` (interpreter limitation + scripting trap) | **DEFERRED — investigation done, no acute failure** | The 2 historically U8-affected scripts already use the mandatory workarounds: `widgets/restorable_values_test.dart` (C20) uses `RestorableEnum<Brightness>` + `_vMood`/`_vInt`/`_vDateTime` shadow variables (workarounds 1+2 from the §U8 table); `foundation/diagnostics_serialization_delegate_test.dart` (Step 10) uses `addProp('mode', '${c.mode}', 'StringProperty')` instead of `EnumProperty<_DemoMode>` (workaround 3). 2206 sweep logs contain zero `InterpretedEnumValue` / `expected Enum, got` / `isRegistered.*is not true` hits across all 28 log files. Real fix requires `InterpretedEnumValue` to satisfy the native `is Enum` check at the bridge boundary, but `Enum` is a Dart-VM-special sealed type — a real fix would need either parallel `EnumWrapper` synthesis at every bridge-boundary crossing (touches `D4.getRequiredArg<Enum>` family + `extractBridgedArg<Enum?>` + every Enum-typed bridge call site + analyzer-free `tom_d4rt_ast` runtime) or a VM-level reified-enum bridging primitive the sandboxed interpreter cannot easily provide; §U8 explicitly classifies this as "non-trivial and out of cluster scope" |
| #17 | §U9 script-defined `RouteAware` cannot be subscribed to native `RouteObserver` (interpreter / proxy-generator limitation; same family as §U3/§U5/§U8) | **DEFERRED — investigation done, no acute failure** | The 1 historically U9-affected script (`widgets/route_observer_test.dart`, C22) already applies the mandatory `_DemoRouteObserver` script-side stand-in (line 85) that mirrors the native five-method contract over `Map<Route, List<_LoggingRouteAware>>`; native `RouteObserver<PageRoute<dynamic>>()` is still constructed (line 363) but no `subscribe(...)` call crosses the boundary. 2206 sweep logs contain zero `expected RouteAware` / `InterpretedInstance(_LoggingRouteAware` / `RouteObserver.*subscribe.*Argument Error` hits. Real fix is the same multi-abstract-class proxy-generator extension as TODOs #11/#13 — synthesise a native adapter that implements `RouteAware` and auto-wraps `InterpretedInstance` arguments at the bridge boundary. Unlike §U8 there is NO framework-provided `RouteAware` concrete subclass to substitute (it's designed to be mixed into application-side `State` subclasses) — the script-side stand-in is the only available workaround route |
| #18 | §U10 script-defined class `with DiagnosticableTreeMixin` / `Diagnosticable` cannot call inherited concrete methods (`toStringDeep`/`toString`/`toDiagnosticsNode`); plus `super.debugFillProperties` fails into bridged mixin (interpreter limitation; same family as §U3/§U5/§U8/§U9 with the twist that the failure point is INSIDE the mixin's concrete methods) | **DEFERRED — investigation done, no acute failure** | All 4 historically U10-affected scripts already apply their mandatory script-side workarounds: `foundation/class_test.dart` (C36) uses `_dumpNode` recursive helper; `foundation/diagnostics_serialization_delegate_test.dart` (C37) uses `_manualSerialize` for JSON shape; `foundation/object_flag_property_test.dart` (C38) uses `_diagnosticableDeepDump` + drops `super.debugFillProperties` super call; `foundation/text_tree_configuration_test.dart` (Cluster F #11) uses in-script sparse-renderer fallback (lines 1772/1788) visually equivalent to real `toStringDeep`. 2206 sweep logs contain zero `expected DiagnosticableTreeMixin` / `expected Diagnosticable, got` / `does not have a standard or bridged superclass.*super` / `Cannot invoke method 'toStringDeep' on null` hits across all 28 log files. Real fix requires hand-written `_InterpretedDiagnosticableTreeMixin` proxy (analogous to `_InterpretedStatelessWidget`/`_InterpretedState`, ~100-200 lines) + new mixin-resolution-time wiring + a separate fix for `super`-into-bridged-mixin dispatch — feature-scale work explicitly out-of-scope per §U10 |
| #19 | §U11 script-defined `HitTestTarget` rejected by `HitTestEntry(target)` constructor (interpreter limitation; same family as §U3/§U5/§U8/§U9/§U10) | **DEFERRED — investigation done, no acute failure** | The 1 historically U11-affected script (`gestures/hit_testable_test.dart`, C39) already applies the mandatory workaround: `_FakeTarget implements HitTestTarget` (line 43) kept as a teaching reference but NOT instantiated for `HitTestEntry`; pure script-side `_DemoHitEntry(label, runtimeTypeStr)` (line 58) substituted for anatomy-panel display. Native `HitTestResult()` + `BoxHitTestResult()` constructors still execute (no script-defined target argument). 2206 sweep logs contain zero `expected HitTestTarget` / `InterpretedInstance(_FakeTarget` / `HitTestEntry.*Argument Error` hits across all 28 log files. Real fix is the same `_InterpretedHitTestTarget` proxy as the §U3/§U5/§U9/§U10 family — feature-scale, explicitly out-of-scope per §U11. Unlike §U8/§U10, §U11 has NO framework-provided concrete substitute (every native `HitTestTarget` is a `RenderObject` subclass tied to the rendering pipeline) — the data-class substitution works only because this demo doesn't dispatch events |
| #20 | §U12 `@Deprecated`-annotated SDK symbols filtered out of the bridge surface (intentional generator policy — NOT an interpreter limitation) | **DEFERRED — intentional policy; workarounds ARE the architecturally-correct path; no acute failure** | All 6 historically U12-affected scripts apply the documented workarounds: **Variant A (local stand-in)** for C44 `key_data_transit_mode`, C45 `keyboard_side`, C49 `raw_key_event_data_web`, C50 `raw_key_event` (most elaborate — covers 7 deprecated SDK symbols via coordinated `_ModifierKey`/`_KeyboardSide` enums + `_GLFWKeyHelper`/`_RawKeyEventDataLinux` classes + abstract `_RawKeyEvent` + `_RawKeyDownEvent`/`_RawKeyUpEvent` subclasses); **Variant B (modern-name swap)** for C46 `mouse_tracker_annotation` (`MaterialState` → `WidgetState`). 2206 sweep logs contain zero `Undefined variable: KeyDataTransitMode\|…` hits across all 28 log files. §U12 explicitly states "this is the right interpreter / generator policy. Bridging a deprecated symbol invites scripts to depend on behaviour the framework has declared it intends to remove. Flipping `generateDeprecatedElements = true` would temporarily resolve this symptom but would re-open the deprecated surface across the entire bridge corpus, contrary to the policy." There is no "real fix" to implement on the generator side — the workarounds are the policy-aligned path |
| #21 | §U13 native exceptions thrown across bridged method not catchable by original type — only `RuntimeError` wrapper escapes; original type discarded (interpreter limitation; boundary-translation issue, NOT proxy-synthesis like §U3/§U5/§U9/§U10/§U11) | **DEFERRED — investigation done, no acute failure** | Both historically U13-affected scripts apply the mandatory workarounds: `retest/services/method_codec_test.dart` (C53 AST / C55 TEST) uses broad `catch (e)` + string-parse of wrapper's `'PlatformException(<code>, …)'` marker (7 marker hits via `grep -c`); `services/codecs_test.dart` (Cluster E #10) uses broad `catch (e)` + surfaces wrapped message as `'PlatformException-like: ${e.toString()}'` (3 hits). Codec's intended exception-thrown contract remains verified — only type-based matching replaced by string-parse equivalent. 2206 sweep logs contain zero `Native error during bridged method call.*PlatformException\|…FormatException\|…MissingPluginException\|…StateError` hits across all 28 log files. Real fix requires coordinated `RuntimeError.cause` side-channel in `bridged_class.dart` + try-catch matcher changes in `InterpreterVisitor.visitTryStatement` across BOTH `tom_d4rt` and `tom_d4rt_ast` (workspace "Keep in sync" rule doubles surface area) + full rule (b) regression sweep — explicitly out-of-scope per §U13 |
| #22 | §U14 `maxHeight: infinity` leaks through `Center > ConstrainedBox > SingleChildScrollView` (bridge/interpreter constraints-propagation gap; original diagnosis turned out to be a red herring per §U14 2026‑05‑23 update) | **DEFERRED — historical script FIXED via `IntrinsicHeight`; architectural gap remains open but no acute failure** | The 1 historically U14-affected script (`animation/cubic_test.dart`) was FIXED on 2026‑05‑23 via section-level bisection: the real source was two `Row(crossAxisAlignment: CrossAxisAlignment.stretch)` blocks in `_PrivateConstructorCards` (NOT `Center>ConstrainedBox>GridView` as originally diagnosed). Fix: wrap each `Row(stretch)` in `IntrinsicHeight` (lines 1219, 1231 of live script; entry-#19 comment-block at 1208-1213 documents root cause). `fwErr 1→0` on both projects. 2206 sweep logs contain zero `BoxConstraints forces an infinite height` / `RenderConstrainedBox's layout()` hits across all 28 log files. Real fix (for any future script with genuine `Center>ConstrainedBox>SCV` infinite-height leak that `IntrinsicHeight`-on-`Row(stretch)` cannot resolve) requires clamping incoming `maxHeight==infinity` in the bridged `RenderConstrainedBox.layout()` adapter OR faithfully implementing `RenderPositionedBox.performLayout`'s shrink-wrap branch in bridged `Center`/`Align` + `RenderSliverGrid`'s cell-height computation in `GridView.count` — touches both `tom_d4rt` and `tom_d4rt_ast`, explicitly out-of-scope per §U14 |
| #23 | §U15 `RenderFlex overflowed by 2.0 pixels on the right` inside bridged Cupertino layout (bridge layout-rounding gap) | **FIXED 20260529 — apparent self-resolution; verified in 2206 baseline** | The 1 historically U15-affected script (`cupertino/cupertino_nav_segmented_test.dart`) ran in the 2206 sweep with `frameworkErrors=0 status=success` on both projects. METRIC lines from secondary_classes_test.log.txt confirm: AST `frameworkErrors=0 totalMs=2513`, TEST `frameworkErrors=0 totalMs=2417`. The §U15 banner pattern is NOT in either test_app's `ignoredPatterns` list (only `'overflowed by 0.500 pixels'` subpixel pattern is filtered, per main.dart:336 comment block) — so the absence is real, not suppression. 2206 logs contain zero `A RenderFlex overflowed by 2.0 pixels on the right` hits across all 28 files. Between 0519-1247 (banner firing) and 0528-2206 (clean), 9 days of bridge regens + interpreter fixes apparently closed the 2-pixel Cupertino rounding gap. §U15 doc updated with `— **✅ FIXED in 2206 baseline (apparent self-resolution)**` header marker matching §U7/§U26 convention plus retained original analysis as cautionary tale |
| #24 | §U16 `Text('')` triggers NaN `Offset` assertion in `dart:ui` paragraph painting (bridge/interpreter text-layout gap; also surfaces as `BoxConstraints forces an infinite height` under `IntrinsicHeight` ancestor — same empty-paragraph metric path) | **DEFERRED — investigation done, no acute failure** | Both historically U16-affected scripts apply mandatory script-side workarounds: `cupertino/restorable_cupertino_tab_controller_test.dart` (Item 5, fixed 2026‑05‑19) uses `composed.isEmpty ? ' ' : composed` guard in `_CodeBlock.build` (1 marker hit); `gestures/velocity_test.dart` (Item 35, fixed 2026‑05‑19) uses `SizedBox(height: 14)` substitution for blank `_CodeLine('')` separators under `IntrinsicHeight > Row(stretch)` chrome — variant U16 banner where empty `Text` propagates unbounded intrinsic height instead of NaN paint origin (8 combined marker hits). Both `fwErr 1→0`. 2206 sweep logs contain zero `Offset argument contained a NaN value` / `dart:ui/painting.dart.*Failed assertion: line 41` hits across all 28 log files. Real fix requires bridged `Text`/`RichText` paragraph layout to short-circuit on zero `TextSpan` glyphs (the native Flutter pipeline does this implicitly; the bridge must replicate the fast-path) — touches both `tom_d4rt` and `tom_d4rt_ast`, explicitly out-of-scope per §U16 |
| #25 | §U17 `ConstraintsTransformBox` teaching script intrinsically incompatible with `frameworkErrors=0` (script design — "truly unfixable by design", but observable side fully suppressed) | **FIXED 20260529 — observable side fully resolved by `ignoredPatterns` (pre-existing) + TODO #7 else-branch guard; architectural "by design" framing remains accurate but produces no failure** | The §U17 entry's "2 events captured this sweep (TEST side)" symptom was stdout/stderr leak via the unguarded `_originalFlutterErrorHandler?.call(details)` forward in `_handleFlutterError` — TODO #7's `else if (!isIgnored)` guard (2026‑05‑29) closed that path. The `ignoredPatterns` entry `'A RenderConstraintsTransformBox overflowed by'` was already in place (verified at `main.dart:382` in both test_apps), keeping `_frameworkErrors == 0` for the script from day one. METRIC verification: baseline 2206 AST `frameworkErrors=0`, TEST `frameworkErrors=0`; post-TODO #7 followup same. Log-noise verification: baseline AST 0 hits / TEST 2 hits → post-fix 0 hits across all 7 AST followup dirs + all 8 TEST followup dirs. §U17 doc updated with `— **✅ FIXED in 2206 baseline (observable side; architectural framing intact)**` header marker matching §U7/§U15/§U26 convention plus 2026-05-29 update appendix. The kHalveMaxWidth correctness fix (2026-05-23 entry #21) is retained — that was a real script-side bug (non-normalized BoxConstraints is undefined behavior), not just an error suppressor |
| #26 | §U18 `services/platform_test.dart` `_defaultVsThemeCard` Row(stretch)+Expanded(_twinCard) P1 variants destabilise test-app transport (interpreter/bridge limitation; original 2026-05-20 diagnosis was a transport-cliff that no longer reproduces) | **FIXED — already shipped 2026-05-23 as entry #20; verified clean in 2206 baseline** | The §U18 doc already carries a "2026-05-23 update — FIXED (entry #20)" appendix recording the combined fix: (i) `IntrinsicHeight` wrap on `_defaultVsThemeCard` Row (line 565; same `IntrinsicHeight` family as §U14/§U17 entry #19); (ii) `SingleChildScrollView` wrap on the page-level `Column(stretch)` body (line 87) to clear the 7257-px overflow the `IntrinsicHeight` fix unmasked. Both wraps confirmed in live script via `grep` with entry-#20 comment blocks at lines 76-86 and 555-564. METRIC for `services/platform_test.dart` reports `status=success httpStatus=200 outputLines=15 frameworkErrors=0` on both projects. 2206 sweep logs contain zero `Lost connection to device` hits across all 28 log files → the transport-cliff is no longer reproducing. The §U18 affected-scripts row is already struck-through in the doc. Original transport-cliff diagnostic remains preserved in §U18 should it re-emerge under different conditions |
| #27 | §U19 per-character `TextSpan` stream of non-Latin glyphs triggers NaN `Rect` assertion in `dart:ui` painting (bridge/interpreter text-layout gap; sibling of §U16 — same bridged-paragraph-painter cluster-geometry shortfall) | **DEFERRED — investigation done, no acute failure** | The 1 historically U19-affected script (`services/text_editing_delta_non_text_update_test.dart`, item 99 of testlog_20260519-1247, fixed 2026‑05‑20) already applies the mandatory ASCII-substitution workaround: `greet = 'aiueo'` at line 733 (was `'こんにちは'`) with comment block at lines 720+; Japanese form retained in safe `Text` prose at lines 802 + 821. METRIC reports `status=success frameworkErrors=0` on both projects (was `frameworkErrors=4` before fix). 2206 sweep logs contain zero `Rect argument contained a NaN value` / `dart:ui/painting.dart.*Failed assertion: line 26` hits across all 28 log files. Real fix requires bridged paragraph-painter cluster-aware per-span metrics (same family as §U16 — both stem from the bridged text-painter producing NaN coordinates for paragraphs whose glyph-cluster boundaries do not align with span boundaries) — touches both `tom_d4rt` and `tom_d4rt_ast`, explicitly out-of-scope per §U19 |
| #28 | §U20 `Table(border: TableBorder.all(...))` triggers Flutter framework assertion in `table_border.dart` line 289 regardless of row count / column widths (bridge/framework interaction gap — `RenderTable._rowTops` ends up violating `rows.last <= rect.height` in the d4rt-bridged context though the invariant holds by construction in native Flutter) | **DEFERRED — investigation done, no acute failure** | The 1 historically U20-affected script (`widgets/editable_text_misc_test.dart`, item 107 of testlog_20260519-1247, fixed 2026‑05‑20) already applies the mandatory workaround: `border:` parameter dropped from all 7 Tables (paletteTable / enumTable / smartTable / pitfallTable / glossaryTable / comparisonTable / cheatTable); the bordered-card look is preserved by each Table's parent `cardShell`'s outer `Border.all(color: brassEdge, width: 1.2)` at the cost only of the interior brass grid lines. Verified via `grep`: the only `border: TableBorder` mentions in the live script (lines 588 + 597) are inside a comment block explaining the rationale. METRIC: `status=success frameworkErrors=0` on both projects (was `frameworkErrors=1` before fix; Flutter dedupes 7 banners to 1 per frame). 2206 logs contain zero `table_border.dart.*Failed assertion: line 289` / `rows.isEmpty.*rows.first.*rows.last` hits. Real fix requires diagnosing why `RenderTable._rowTops` violates the invariant in the bridged context (most plausibly a non-finite `rowHeight` value entering from a bridged cell, OR a TableRow shape coercion bug in the bridges) + instrumenting `RenderTable.performLayout` + auditing `Table`/`TableRow` bridges in `widgets_bridges.b.dart` — meaningful surgery touching both `tom_d4rt` and `tom_d4rt_ast`, explicitly out-of-scope per §U20 |
| #29 | §U21 `Quad`/`Vector3` from `package:vector_math/vector_math_64.dart` not reachable from interpreted scripts (bridge surface gap; narrow §U6-family case calling out the specific `InteractiveViewer.builder` + `Matrix4.getTranslation()` paths) | **DEFERRED — investigation done, no acute failure** | The 1 historically U21-affected script (`widgets/interactiveviewer_test.dart`, Cluster C of testlog_20260522-1328, fixed 2026‑05‑22) already applies both mandatory script-side workarounds. **Workaround 1:** `m[12]`/`m[13]` column-major Matrix4 storage reads instead of `m.getTranslation().x/.y` (verified at 4 sites: lines 282, 283, 685, 686 with inline `// Vector3 is not bridged` comments at 281 + 684). **Workaround 2:** standard `InteractiveViewer(...)` constructor with `constrained: false` + `boundaryMargin` + pre-built `SizedBox(child: Stack(children: allTiles))` at all 8 callsites (308, 408, 487, 551, 594, 698, 836, 908) instead of `InteractiveViewer.builder` (only mentioned in header documentation at lines 16, 25). METRIC: `status=success frameworkErrors=0` on both projects. 2206 logs contain zero `Undefined property or method 'x' on Vector3` / `Cannot resolve import package:vector_math/vector_math_64` / `Cannot resolve.*Quad` hits. Real fix is the same vector_math-bridge-extension as the broader §U6 case (TODO #14) — adding the package to `tom_d4rt_flutterm/buildkit.yaml`'s `bridgedLibraries` causes a noticeable bridge-surface increase + rule (b) regression sweep, explicitly out-of-scope per §U21 |
| #30 | §U22 H23 single-event scripts (meta-cluster of 12 originally-interpreter-deferred scripts that were ultimately resolved script-side via H23 directly OR via subsequent entries #14-#20 on 2026-05-23) | **FIXED at the script level — all 12 H23 scripts pass cleanly in 2206 baseline; the 2 underlying architectural gaps documented in §U22 are duplicates of TODOs #11/#13/#17/#18/#19 (DEFERRED)** | METRIC scan confirms `status=success frameworkErrors=0` on both projects for: `animation/cubic_test` (entry #19/TODO #22), `material/dropdownform_test` (entry #18), `material/dropdown_test` (entry #17 — `selectedItemBuilder` omitted), `material/mergeable_test` (H23 — `IntrinsicHeight`), `material/progress_test` (H23 — bare numeric `semanticsValue`), `rendering/render_constraints_transform_box_test` (entry #21 + TODO #25), `scheduler/ticker_test` (H23 — `IntrinsicHeight`), `services/platform_test` (entry #20/TODO #26), `widgets/animation_test` (entry #16 — `Listenable.merge` + inline `AnimatedBuilder`), `widgets/slotted_multi_child_render_object_widget_test` (entry #14 — log accent INDEX), `retest/widgets/app_kit_view_test` (entry #15 — boot-status placeholder guard), `foundation/diagnosticable_tree_mixin_test` (H23 — U10 sparse-fallback). The 2 architectural gaps in §U22's "What a real fix would look like" (1: typed-collection coercion at bridge boundary; 2: bridged abstract-class subclass construction routing) are duplicates of TODOs #11/#13/#17/#18/#19 already DEFERRED for the same proxy-generator-extension reasons. The 1 baseline `status=error` on `retest/widgets/app_kit_view_test` was unrelated `build_30s_timeout` family — separately FIXED by TODO #5 |
| #31 | §U23 20260523-1056 H-5 follow-up — 7 single-event scripts (small layout-rounding overflows + 1 alpha-clamp script-side bug; §U23 doc explicitly says "U23 is now empty") | **FIXED at the script level — all 7 U23 scripts pass cleanly in 2206 baseline; §U23 doc explicitly resolved (every script-row struck-through, "U23 is now empty" statement embedded)** | METRIC scan confirms `status=success frameworkErrors=0` on both projects for all 7: `painting/textstyle_test` (entry #9 — alpha clamp; was a script-side bug, not bridge gap), `material/dialog_themes_test` (entry #11 — Expanded label in `_simpleDialogOption`), `cupertino/cupertino_themes_batch3_test` (entry #12 — shrink section15 label SizedBox 88→70), `painting/box_painter_test` (entry #10 — Expanded title in `_galleryCard`), `painting/decoration_image_painter_test` (entry #11 — Row→Wrap in `_fitCard`), `widgets/editable_text_tap_up_outside_intent_test` (entry #11 — Expanded gesture label), `rendering/render_exclude_semantics_test` (entry #10 — `IntrinsicHeight` wrap, same `IntrinsicHeight` family as §U14/§U17 entry #19 / §U18 entry #20). §U23 "What a real fix would look like" preserves 3 future-option guidance (bridge intrinsic-width fix, defensive padding, or `ignoredPatterns` extension) for any future H-5-family overflow that may arise |
| #32 | §U24 `try { x = ui.SystemColor.light; } catch (e) {...}` does not intercept bridge-wrapped `UnsupportedError` on platforms that don't support SystemColor (interpreter/bridge exception-routing gap; sibling of §U13/TODO #21 — but stronger because the untyped `catch (e)` arm also fails to match) | **DEFERRED — investigation done, no acute failure** | The 1 historically U24-affected script (`retest/dart_ui/system_color_palette_test.dart`) already has the mandatory test-driver-only workaround applied: skip widened to `Platform.isLinux \|\| Platform.isMacOS \|\| Platform.isWindows` with reason `'SystemColor not supported on desktop platforms (web-only API)'`. Verified in both projects via `grep`: AST retest_test lines 76-94, TEST retest_test lines 88-106. Original non-retest script continues to run unchanged via `ui.SystemColor.platformProvidesSystemColors` gate. 2206 sweep logs contain zero `SystemColor not supported on the current platform` / `Unsupported operation: SystemColor` hits. Test-runner output shows expected `Skip: SystemColor not supported on desktop platforms` on both projects. Real fix is the same boundary-translation issue as §U13/TODO #21 — coordinated `RuntimeError.cause` side-channel + try-catch matcher changes (untyped + typed arms) across BOTH `tom_d4rt` and `tom_d4rt_ast`; explicitly out-of-scope per §U24 |
| #33 | §U25 source-based interpreter cold-start parse + execute exceeds 50 s on 1219-line / 42 KB `widgets/always_scrollable_scroll_physics_test.dart` in `tom_d4rt_flutter_test`; E5 widening shows the cold-start ceiling also affects the AST variant on ≥80 KB / ≥2500-line `widgets/inherited_widget_test.dart` (1.3 MB bundle); pairs with TODO #4 which absorbed the test-wrapper-timeout symptom side | **DEFERRED — investigation done, no acute failure; operational warm-app workaround effective in 2206** | Both U25-affected scripts pass cleanly in 2206 baseline on **both** projects via the operational warm-path workaround (neither runs as "first-script-after-setUpAll" — by the time the `widgets/ individual` group runs in `secondary_classes_test.dart`, earlier groups have warmed the app). METRIC verification: `always_scrollable_scroll_physics_test` AST 2306ms / TEST 2289ms — both `status=success frameworkErrors=0`; `inherited_widget_test` AST 5108ms cold / 9116ms second; TEST 5079ms cold / 9063ms second — both `status=success frameworkErrors=0`. 2206 logs contain zero U25-attributable `Build timed out after 30/50 seconds` hits (the 1 hit in AST `timeout_tests_test` was `app_kit_view_test` — separately FIXED by TODO #5's `buildBudgetMs` mechanism). TODO #4 phase 2 already absorbed the test-wrapper-timeout symptoms in the gir/timeout/h4 files via `_slowTestTimeout=60s` / `_verySlowTestTimeout=120s` bumps (28 sites, 28/28 retests green). Real fix requires either interpreter perf work (pre-warm parser/declaration visitor/Environment) OR test-app `/warmup` endpoint that pre-walks a dummy script during `setUpAll` — both touch both `tom_d4rt` and `tom_d4rt_ast` + test_app `main.dart` startup path + full rule (b) regression sweep, explicitly out-of-scope per §U25 |
| #34 | §U27 `Element.findRenderObject()` asserts `_lifecycleState == active` even when `mounted` is true (Flutter framework assertion stricter than documented `BuildContext.mounted` guard; the script-wanted "is Element still active" check has no public Dart API — `_lifecycleState`/`debugIsActive` are private/debug-only) | **FIXED — interpreter-level workaround already shipped 2026-05-25 as Cluster B fix; verified clean in 2206 baseline** | Bridge-method-call catch in BOTH `tom_d4rt/lib/src/interpreter_visitor.dart` (lines 3279-3300) AND `tom_d4rt_ast/lib/src/runtime/interpreter_visitor.dart` (lines 3749-3759) was changed to match the specific Flutter assertion text `'Cannot get renderObject of inactive element'` for `findRenderObject` calls and return `null` instead of wrapping as `RuntimeD4rtException` — restores the documented `RenderObject? findRenderObject()` contract. Pattern-narrowed to the exact assertion text so unrelated bridged-method failures still surface; mirrored verbatim across both interpreters per the "Keep in sync" rule. METRIC for canonical `rendering/render_absorb_pointer_test.dart`: `status=success frameworkErrors=0` on both projects. 2206 logs contain zero `Cannot get renderObject of inactive element` hits across all 28 log files. The "real fix" per §U27 requires upstream Flutter SDK changes (either expose public `Element.isActive` API or relax the assertion to match the `mounted` contract) — outside this codebase by definition |
| #35 | §U28 `tom_d4rt_flutter_ast` test-app accumulates state across `/clear → /build` cycles such that the 2nd+ build of an ~800 KB-bundled static-demo script exceeds the 30 s build budget (flutter_ast-only; flutter_test source-direct path unaffected) | **FIXED at the operational level — `requestRecycle()` mechanism shipped 2026‑05‑25 as Cluster C fix (commits `42588be2`/`d613142e`/`90854bc9`); architectural "real accumulator cause" still open but TODO #3 disproved the leading D4-static/Expando hypothesis** | `SendTestRunner.requestRecycle()` (lines 160, 181-182, 983-1105 of `send_test_runner.dart`) sets `_appNeedsRecycle` which kills+relaunches the test_app on the next call. `interactive_tests_test.dart` line 69 wires it as a setUp hook so every interactive test runs against a fresh app. 2206 AST `interactive_tests_test.log.txt` confirms the path firing: `[recycle] killing wedged test app (pid=…)` + `[recycle] starting fresh test app` markers between each of 5 static-demo tests; all builds complete in ~17 s well inside the 30 s budget; suite finishes `+6 All tests passed!`. TEST project doesn't use requestRecycle (source-direct path unaffected per §U28); its baseline `+0 -1` was the unrelated `setUpAll` timeout — FIXED by TODO #6. The 1 baseline `Build timed out after 30s` hit was `app_kit_view_test` — FIXED by TODO #5, not §U28. The architectural concern (real accumulator cause) remains in the same DEFERRED bucket as TODO #3 (investigation done, D4-static/Expando hypothesis disproven, real cause most plausibly in Flutter framework subsystems outside the d4rt runtime) |
| #36 | §U29 `MemoryImage(Uint8List)` codec rejects externally-valid PNG bytes when constructed inside d4rt script (interpreter ↔ ui.ImmutableBuffer bridge gap; observable side fully suppressed) | **FIXED 20260529 — observable side fully resolved by `ignoredPatterns` (pre-existing Cluster H workaround 2026-05-25) + TODO #8 else-branch guard; architectural bridge gap remains documented but produces no failure** | The §U29 entry's "3 events captured this sweep (TEST side)" symptom was stdout/stderr leak via the unguarded `_originalFlutterErrorHandler?.call(details)` forward in `_handleFlutterError` — TODO #8's `else if (!isIgnored)` guard (2026‑05‑29) closed that path. The `ignoredPatterns` entry `'Codec failed to produce an image'` was already in place (verified at `main.dart:364` in both test_apps with the Cluster H comment marker), keeping `_frameworkErrors == 0` for `widgets/image_icon_test.dart` from day one. METRIC verification: baseline 2206 AST `frameworkErrors=0`, TEST `frameworkErrors=0`. Log-noise verification: baseline AST 0 hits / TEST 3 hits (broader `Codec failed to produce an image|EXCEPTION CAUGHT BY IMAGE RESOURCE SERVICE` pattern) → post-fix 0 hits across all 7 AST followup dirs + all 8 TEST followup dirs. §U29 doc updated with `— **✅ FIXED in 2206 baseline (observable side; architectural bridge gap intact)**` header marker matching §U7/§U15/§U17/§U26 convention plus 2026-05-29 update appendix. Architectural ↔ ui.ImmutableBuffer bridge gap (corruption between `Uint8List.fromList(<int>[…])` → `MemoryImage._bytes` → `ImmutableBuffer.fromUint8List`) remains documented as real-fix path requiring focused diagnostic test + bridge adapter fix; rendered output degrades to debug-mode broken-image glyph but test doesn't assert on pixels |
| #37 | §U30 `InheritedElement.updateDependencies` descendant-check assertion (`framework.dart:6417`) fires as U28-style position-dependent cascade in larger suites; observable side fully suppressed via same two-fix mechanism as §U17/§U29 | **FIXED 20260529 — observable side fully resolved by `ignoredPatterns` (pre-existing 2026-05-27 TODO #9 workaround) + TODO #8 else-branch guard; architectural dependent-set corruption remains documented but produces no failure** | `'check that it really is our descendant'` was already added to both test_apps' `ignoredPatterns` lists per 2026-05-27 TODO #9 fix (verified at `tom_d4rt_flutter_ast_app/lib/main.dart:404` and `tom_d4rt_flutter_test_app/lib/main.dart:327`). The phrase is the comment INSIDE the assertion body that Flutter includes verbatim, robust against version-line-number drift. TODO #8's `else if (!isIgnored)` guard (2026-05-29) closed the stdout/stderr leak path. 2206 baseline contains zero `check that it really is our descendant` / `framework.dart.*line 6417` hits across all 28 log files on both projects (U28-style cascade is position-dependent and did NOT trigger in 2206 script ordering). Post-TODO #8 followup: 0 hits across all 7 AST + 8 TEST followup dirs. §U30 doc updated with `— **✅ FIXED in 2206 baseline (observable side; architectural dependent-set corruption intact)**` header marker matching §U7/§U15/§U17/§U26/§U29 convention plus 2026-05-29 update appendix. Architectural concern (script-defined interpreted Elements leak into native `InheritedElement` dependent sets across `/build` cycles → stale entries fail the descendant check on next dependency update) remains documented as real-fix path requiring either `/clear`-time interpreted-element dependent unregistration in the test_app OR interpreter-side lifecycle tracking that unregisters on `Element.deactivate`. §U30 explicitly notes the suppression "is noise suppression, not a real fix" — underlying corruption may manifest as visual glitches / rare layout misses under more aggressive UI testing not currently exercised |
| #38 | Tooling hygiene — drop §U28 deep-fix's `SendTestRunner.requestRecycle()` hook from AST `interactive_tests_test.dart` (the recycle was the operational workaround for the now-self-resolved §U28 cliff per the 2206 baseline evidence) | **FIXED — recycle hook removed from AST; experiment 6/6 pass in 35 s vs prior 130 s (~75% speedup)** | Commented out `setUp(() { SendTestRunner.requestRecycle(); })` at line 68-70 of `tom_d4rt_flutter_ast/test/interactive_tests_test.dart` (test-script-only change → rule (a)). Individual retest on alt port 14280: 6/6 pass in 35 s wall; all builds 1.8-2.5 s (far under 30 s budget). The §U28 cliff did NOT re-trigger — some intervening interpreter/bridge fix closed it naturally between the original 2026-05-25 reproduction and the 2206 baseline. The `requestRecycle()` API itself **remains shipped** in `send_test_runner.dart` (lines 160, 181-182, 983-1105) for future use should the cliff re-emerge. TEST project required no change (source-direct path always unaffected per §U28; never had the recycle hook). Capture: `_followup_todo38/interactive_tests_test.{log.txt,result.json}` |
| #39 | Tooling hygiene — promote `/tmp/sweep_2206.sh` to tracked + reusable `tom_d4rt_flutter_ast/tool/sweep_both_projects.sh` for reproducibility | **FIXED — script promoted, parameterised, executable, syntax-checked, usage-banner verified** | Distilled the original single-project `/tmp/sweep_2206.sh` (3-arg `<project> <env-var> <port>`) into a both-projects wrapper that takes `<testlog-folder> [<ast-port>] [<test-port>]` (defaults: 14250/14251). Runs files SERIAL within each project (per workspace rule) but cross-project parallel (safe — separate `test_app`s on different ports). Per-file budgets table preserved verbatim from origin (`essential=300s, important=900s` (bumped per 2206 analysis), `secondary=2400s, hardly_relevant_*=1200s, crashing=300s, timeout=900s, blocking=300s, generator_*=900s, interactive=900s`). macOS bash 3.2 compatible (`case` instead of `declare -A`); `D4RT_SKIP_BRIDGE_REGEN=1` set; 10s poll-and-kill watchdog; 5s inter-file settle. Additions: `mkdir -p` on the testlog folder; auto repo-root resolution via `dirname "${BASH_SOURCE[0]}"`; wrapped exit-code propagation; usage banner on no-args (exit 64); project-name prefix in log lines for readable interleaving. `chmod +x` applied; `bash -n` clean; usage banner verified. Script NOT run end-to-end (would be 30-60 min wall + 2206 artefacts already validate the underlying loop). Pairs with TODO #40 (re-run sweep on default ports after host reboot) |
| #7 | §U17 framework-error log noise | **FIXED** | TEST `timeout_tests_test` followup: 0 EXCEPTION CAUGHT BY / 0 overflow / 0 RCTB / 0 framework_error events |
| #8 | §U29 framework-error log noise | **FIXED** | TEST `hardly_relevant_classes_4_test` followup: 0 Codec / 0 EXCEPTION CAUGHT BY events |

**What was done:**

1. **Diagnosed the framework-error leak.** Initial hypothesis (#7/#8 in original list) was that the `ignoredPatterns` lists didn't include §U17 / §U29 patterns. Investigation showed both patterns WERE already in the lists — the leak was elsewhere. Root cause: the inner `if (_capturingFrameworkErrors) { ... }` block correctly skipped adding ignored patterns to `_frameworkErrors`, but the outer `else { _originalFlutterErrorHandler?.call(details); }` ran unconditionally for every non-silenced error, forwarding ignored patterns to Flutter's default handler and printing the red `EXCEPTION CAUGHT BY ...` block to stdout.

2. **Fixed both test_apps' `_handleFlutterError`** (tom_d4rt_flutter_ast_app/lib/main.dart + tom_d4rt_flutter_test_app/lib/main.dart):
   - Hoisted `isIgnored` declaration to outer scope (`var isIgnored = false;` before the inner block).
   - Inside the `if (_capturingFrameworkErrors)` block, changed `final isIgnored = ...` to `isIgnored = ...` (assignment to outer var).
   - Changed outer `} else { ... }` → `} else if (!isIgnored) { ... }` so ignored patterns now skip BOTH the `_frameworkErrors` add AND the red-screen forward.

3. **Re-ran the regression sweep per rule (b)** since the test_apps are host-side `tom_d4rt_flutterm` code (not test scripts in the `test/` subfolder): essential + important + secondary on both projects, plus the two cleanup-verification files (TEST timeout_tests_test, TEST hardly_relevant_classes_4_test) at fresh ports 14252/14253.

**Followup sweep results (07:51 → 08:50 PDT, ~59 min wall):**

| File | AST followup | TEST followup |
| --- | --- | --- |
| `essential_classes_test` | 111 / 0 / 0 / 0 | 111 / 0 / 0 / 0 |
| `important_classes_test` | 166 / 0 / 1 / 0 (entry #1 ✅) | 167 / 0 / 0 / 0 (entry #1 ✅) |
| `secondary_classes_test` | 656 / 0 / 0 / 1 | 656 / 0 / 0 / 1 |
| `timeout_tests_test` (TEST verify) | — | 54 / 0 / 0 / 0 (was 52 / 0 / 2 / 0) |
| `hardly_relevant_classes_4_test` (TEST verify) | — | 230 / 0 / 0 / 0 (was 225 / 0 / 5 / 0) |
| **Followup totals** | **933 / 0 / 1 / 1** | **1218 / 0 / 0 / 1** |
| Regression vs. 2206 | clean (no new failures introduced by the change) | clean — and 7 prior errors disappeared (likely host-load variance in the U28-wedge family) |

**Log-noise verification (the actual user-requested check):**

| Pattern | AST followup logs | TEST followup logs |
| --- | ---: | ---: |
| `EXCEPTION CAUGHT BY` | **0** | **0** |
| `overflowed by` | **0** | **0** |
| `Codec failed to produce an image` | **0** | **0** |
| `RenderConstraintsTransformBox overflowed` | **0** | **0** |
| `[framework] FlutterError fired` (trace lines) | **0** | **0** |

Both test_apps now correctly suppress the entire ignored-pattern set — `_frameworkErrors` stays clean (build assertions still see `frameworkErrors == 0` for these scripts) AND log captures stay clean (no stderr noise from Flutter's default handler). The change is fully symmetric across both test_apps.

**Regression rule (b) compliance.** The change touches host-side test_app `lib/main.dart` files — classified as "tom_d4rt_flutterm code" (not test scripts in the test/ scripts subfolder). Rule (b) requires essential + important + secondary + modified-component verification. All five files re-ran clean above; the 1 newly-visible `bottomappbar_test` error in AST important is a pre-existing U28-wedge symptom that was previously masked by the 300 s budget cap (it was never reached in the 2206 sweep), NOT a regression introduced by this change.

**Followup captures location.** Raw `.result.json` + `.log.txt` files for the 8 followup runs live in `doc/testlog_20260528-2206-issue-analysis/_followup/` in each project. The `_followup/` subfolder is gitignored per the `testlog_*/**` convention; only this analysis file documents the outcome.

---

**End of analysis.** The combination of the §U28 deep-fix (`42588be2`/`d613142e`/`90854bc9`), the port-override (`8cd7c27a`), the lifecycle fix (`9f4dc79c`), and the 20260529 framework-error-suppression fix yielded a sweep with **4309 passing tests + 933+1218 followup passes, 1 failure, 79 errors (now 1 in followup), 8 skipped** — clean test outcomes and clean per-file log captures. All non-pass outcomes classify into known U28-wedge-family causes or pre-documented `interpreter_unfixable.md` items; zero novel infrastructure regressions.

Phase 1 (#1) ✅ closed. Phase 3 #7/#8 ✅ closed. Phase 2 (#2, host reboot) deferrable. Phase 3 #3/#4/#5 (real U28 wedge accumulator + §U25 cold-start) remain the highest-leverage open work.
