name: tom_d4rt_flutter_ast_app
description: "Test app for tom_d4rt_flutter_ast — Flutter Material D4rt bridge."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  tom_d4rt_flutter_ast:
    path: ../..  # Go up from test/tom_d4rt_flutter_ast_app to tom_d4rt_flutter_ast
  vector_math: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Placeholder assets for d4rt scripts that demonstrate AssetImage / DecorationImage
  # API surface. The script `painting/decoration_test.dart` documents these as
  # "AssetImage placeholder strings — the painting recipe is still valid even when
  # the asset cannot resolve at frozen-frame time"; bundling tiny 1×1 placeholder
  # PNGs makes the demos render without the `Unable to load asset` framework error.
  # See `doc/testlog_20260526-1401-issue-analysis/error_analysis.md` TODO #1 (F1/F7).
  assets:
    - plaster.png
    - cartoon.png
    - assets/checker.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
