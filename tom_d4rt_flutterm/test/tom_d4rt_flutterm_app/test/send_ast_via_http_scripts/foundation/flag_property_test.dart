// D4rt test script: Tests FlagProperty from foundation
import 'package:flutter/widgets.dart';

dynamic build(BuildContext context) {
  print('FlagProperty test executing');

  // Test FlagProperty - FlagProperty
  print('FlagProperty is available in the foundation package');
  print('FlagProperty: FlagProperty');

  print('FlagProperty test completed');
  return Column(
    mainAxisSize: MainAxisSize.min,
    children: [
      Text('FlagProperty Tests'),
      Text('FlagProperty'),
    ],
  );
}
