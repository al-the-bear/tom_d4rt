// ignore_for_file: avoid_print, deprecated_member_use, sort_child_properties_last
// D4rt test script: Tests SelectionRegistrar from rendering
import 'package:flutter/widgets.dart';

dynamic build(BuildContext context) {
  print('SelectionRegistrar test executing');

  // Test SelectionRegistrar - Selection registrar
  print('SelectionRegistrar is available in the rendering package');
  print('SelectionRegistrar: Selection registrar');

  print('SelectionRegistrar test completed');
  return Column(
    mainAxisSize: MainAxisSize.min,
    children: [
      Text('SelectionRegistrar Tests'),
      Text('Selection registrar'),
    ],
  );
}
