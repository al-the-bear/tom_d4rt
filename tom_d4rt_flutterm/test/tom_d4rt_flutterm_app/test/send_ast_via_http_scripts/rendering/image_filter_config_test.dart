// D4rt test script: Tests ImageFilterConfig from rendering
import 'package:flutter/widgets.dart';

dynamic build(BuildContext context) {
  print('ImageFilterConfig test executing');

  // Test ImageFilterConfig - Filter config
  print('ImageFilterConfig is available in the rendering package');
  print('ImageFilterConfig: Filter config');

  print('ImageFilterConfig test completed');
  return Column(
    mainAxisSize: MainAxisSize.min,
    children: [
      Text('ImageFilterConfig Tests'),
      Text('Filter config'),
    ],
  );
}
