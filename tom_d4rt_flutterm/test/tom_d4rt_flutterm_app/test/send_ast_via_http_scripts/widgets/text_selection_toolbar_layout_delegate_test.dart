// D4rt test script: Tests TextSelectionToolbarLayoutDelegate from widgets
import 'package:flutter/widgets.dart';

dynamic build(BuildContext context) {
  print('TextSelectionToolbarLayoutDelegate test executing');

  // Test TextSelectionToolbarLayoutDelegate - TextSelectionToolbarLayoutDelegate
  print('TextSelectionToolbarLayoutDelegate is available in the widgets package');
  print('TextSelectionToolbarLayoutDelegate runtimeType accessible');

  print('TextSelectionToolbarLayoutDelegate test completed');
  return Column(
    mainAxisSize: MainAxisSize.min,
    children: [
      Text('TextSelectionToolbarLayoutDelegate Tests'),
      Text('TextSelectionToolbarLayoutDelegate'),
    ],
  );
}
