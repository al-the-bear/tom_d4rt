// Entry point for the slide_puzzle sample (example #24).
//
// The in-tester harness (`SourceFlutterD4rt.buildMultiFile`) invokes
// a top-level `Widget build(BuildContext)`; this file exposes that
// contract.
import 'package:flutter/material.dart';

import 'home.dart';

Widget build(BuildContext context) {
  return const SlidePuzzleApp();
}
