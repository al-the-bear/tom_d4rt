dependency_overrides:
  runtime_native_semaphores:
    git:
      url: https://github.com/al-the-bear/native_semaphores.git
      ref: fork-fix
  tom_d4rt:
    path: ../tom_d4rt
  tom_d4rt_ast:
    path: ../tom_d4rt_ast
  tom_d4rt_exec:
    path: ../tom_d4rt_exec
  tom_ast_generator:
    path: ../tom_ast_generator
  tom_ast_model:
    path: ../tom_ast_model
  # tom_d4rt_generator + tom_analyzer_shared overrides removed for analyzer-10
  # migration (both stay on analyzer ^8.4.1; only reached via the generator,
  # which is deferred to the cli_tools-quest migration).
  tom_build_base:
    path: ../../basics/tom_build_base
